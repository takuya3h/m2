# 完了報告の構造化された対。散文（RESULT.md）とは別に、実行者が事実だけを書く。
result_version: 2
task_id: "T-2026-08-18-report-back-to-ledger"
status: pass
host: "lecun"
branch: "feat/report-back"
pr: 75
merged: false

gates:
  - id: G1
    verdict: pass
    note: "壊れやすい要素を含む 8653 バイトの本文を送って読み戻し、要約値が完全に一致した。末尾 1 文字を変えた本文では要約値が異なることも確かめ、照合が空振りしていないことを示した。確認用の行は superseded にし削除していない"
  - id: G2
    verdict: pass
    note: "実際の環境の資格情報を含む本文は拒まれ、外部への送信は 0 回だった。一致の内訳は環境の値そのものと既知の接頭辞の 2 件で、値は出力に含まれない。含まない本文は誤検出なく通った"

tests:
  before_failed: 5
  after_failed: 5
  after_passed: 359

deviations:
  - type: judgement
    note: "取り込み側の _scan_children に読み分けを足した。契約は送り返しの経路だけを求めているが、目印が無いと本文で配布された行で契約本文と報告が連結され、取り込みが壊れるためである"
  - type: judgement
    note: "往復の確認用に台帳へ新しい行を作った。契約は確認用の本文で測れとだけ書いており行の作成には触れていないが、既存の契約行で試すと本番の行を汚すため別の行を用意した"

issuer_defects:
  - type: check_does_not_check
    note: "台帳の列を 6 つ挙げる一方で、報告の本文をどのブロックへ置くと契約の取り込みと混ざるかを問うていない。指示どおり code ブロックへ置くだけでは、本文で配布された行で _scan_children が契約本文と報告を連結してしまい、以後その契約を取り込めなくなる"

followups:
  - "報告の目印 #!TASK-REPORT v1 は取り込み側と送り返し側の両方が知っている。形式を変えるときは両方を同時に直すこと。片方だけ変えると、契約本文と報告が静かに混ざる"
  - "秘匿の検査が見るのは NOTION_API_KEY と WANDB_API_KEY の 2 つと、既知の接頭辞である。資格情報を増やしたら SECRET_ENV_KEYS へ足すこと。足し忘れても検査は通るため気付けない"
  - "送信の時点で壁時計を使っている（completed_at）。生成物ではないため冪等の検査には影響しないが、投影に壁時計を入れない方針とは別の判断である"

unknowns:
  - "台帳の他の行が変わっていないことは、触れた行を限定した事実からしか言えていない。全行の内容を送信前後で突き合わせてはいない"
  - "他ホストでは本 task の変更を実行していない。lecun 上でのみ実測した"

commits:
  - "c43fb11"
