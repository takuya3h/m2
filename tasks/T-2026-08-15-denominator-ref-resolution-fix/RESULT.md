# RESULT — T-2026-08-15-denominator-ref-resolution-fix

**実行者:** andrew / feat/denominator-ref-resolution-fix / 2e5239f
**実行日時:** 2026-08-15T07:07Z 開始
**判定:** PASS

## 1. 解決された参照

| 項目 | spec の記載 | 解決結果 |
|---|---|---|
| conventions_rev | `d422b08` | 現在値も `d422b08`。差分なし。置換不要 |
| created_from.runindex_commit | `44697d9` | 現在値も `44697d9`。索引は動いていない |
| created_from.counts | index 751 / experiments 207 / verdicts 1038 | 実測も 751 / 207 / 1038。一致 |
| contract.inject_verbatim | `conventions#prohibitions` | 錨は実在（錨は 7 個）。原文を下に転記 |
| denominator | 本契約は `kind: impl` のため無し | 解決対象なし |

`context/conventions.md#prohibitions` の原文（要約せず転記）。

    | id | 禁止事項 |
    |---|---|
    | `no_split_redefine` | split を再定義しない |
    | `no_raw_write` | `data/raw` `data/external` に書き込まない |
    | `no_frozen_change` | 凍結源を変更しない |
    | `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
    | `no_runindex_hand_edit` | `runindex/` を手で編集しない |

契約の `contract.prohibitions` はこの 5 個の id をすべて列挙しており、原文と一致する。

## 2. ゲートの通過状況

| gate | 判定 | 実測 |
|---|---|---|
| G1 | pass | 二区画は第一層を通り第二層で落ちた。完全形は第一層で落ちた。**双方を実際に走らせた**（`audit/before_exclusivity.json`） |
| G2 | pass | 新しい試験は直した後で 5/5 通り、直す前の実装に対して 4/5 落ちた（`audit/g2_tests.txt`） |

## 3. 成果物

| 種別 | パス | 件数 |
|---|---|---|
| 実装 | `tools/validate_task.py` | 2 箇所（L1-4 の文法と文言、L2-2 の照合と文言） |
| 文法 | `tasks/_schema/spec.schema.json` | 1 箇所 |
| 試験 | `tests/test_validate_task.py` | 新規 5 件、既存の更新 2 件 |
| 測定器 | `audit/phase_a.py` `audit/exclusivity.py` `audit/grammar.py` | 3 件 |
| 記録 | `audit/*.json` `audit/*.txt` `audit/symbol_collision.md` | 9 件 |

## 4. 受入基準の充足

| acceptance | 結果 |
|---|---|
| 実在する識別子が直した後の文法をすべて通る | **207 / 207**（`audit/grammar_after.json`、実装から読み出した文法で測定） |
| 実在する識別子で第二層が通り、実在しない識別子と旧書式では落ちる | 通る／落ちる／落ちる をいずれも実測 |
| 新しい試験が直す前の実装に対して落ちる | **4 / 5 が落ちた**（残る 1 件の理由は §6） |
| 収穫器と索引を変更していない | `git diff --name-only` に `harvest_runindex.py` と `runindex/` は現れない |

---

## 直す前の実測（排他の証拠）

**「直す前の文法」は 1 つではなく 2 つあった。** 実装から機械的に読み出した値。

    schema    : ^exp:[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+(?![\s\S])
    validator : exp:[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+     （re.fullmatch で使用）

| 測るもの | 期待 | 実測 |
|---|---|---|
| 識別子の総数 | 207 | **207** |
| 区切りが二個のもの | 総数と一致 | **207** |
| 識別子が段と等しいもの | 0 | **0** |
| 直す前の文法を通るもの | 0 | **0**（schema・validator の**双方**で 0） |
| 群と段の組が一意でない組 | 6 件 | **6 件** |
| その行数 | 41 行 | **41 行** |
| 本件の群と段の候補 | 10 件 | **10 件** |

排他の実証（`validate_l1` と `validate_l2` に spec を与えた実挙動）。

| 与えた参照 | 第一層 | 第二層 |
|---|---|---|
| `exp:phase1/s4_phase_baseline`（二区画） | **通る** | **落ちる**（L2-2） |
| `exp:phase1/…@val~relation_detr_seed42`（完全形） | **落ちる**（L1-1 と L1-4 の両方） | 落ちる |

第一層を通る形は第二層で必ず落ちる。**排他が成立していた。**

原因は、L2-2 が参照から群を剥ぎ取ってから `experiment_id` 列と比較していたことである。
同列は群を先頭に含むため、剥ぎ取った文字列とは決して一致しない。落ちたときの文言は
`f"{group}/{experiment_id}"` で表示用に再結合しており、**照合した文字列と表示した文字列が
食い違っていた。** 前の契約 `T-2026-08-11-phase-baseline-power` の取り込み失敗が読み解き
にくかったのはこのためである。文言も完全形を出す形へ直した。

## 直した後の被覆

    schema    : ^exp:[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+@[A-Za-z0-9_.-]+(?:~[A-Za-z0-9_.-]+|#[A-Za-z0-9_.-]+)?(?![\s\S])
    validator : exp:[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+@[A-Za-z0-9_.-]+(?:~[A-Za-z0-9_.-]+|#[A-Za-z0-9_.-]+)?

**被覆 207 / 207。** 文法は本文から写さず、候補を実物に当てて確定してから実装へ入れ、
実装へ入れた後に**実装から読み直して**同じ測定を繰り返した（`audit/grammar_candidate.json`
と `audit/grammar_after.json` が同じ被覆を示す）。

末尾の扱いは既存の作法を踏襲した。schema 側は `$` ではなく `(?![\s\S])` を使う
（`$` は末尾改行を通すため）。実装側は `re.fullmatch` が全体一致を要求するため
そのままで改行を通さない。**同じ目的を二つの作法で担保している。**

## 広げすぎていないことの実測

5 種すべてが schema と実装の**双方**で落ちた（`audit/grammar_after.json`）。

| 与えたもの | 結果 |
|---|---|
| 二区画だけの旧い書式 | 落ちる |
| 区画が一つ欠けたもの | 落ちる |
| 分離の記号が無いもの | 落ちる |
| 許されない記号を含むもの（空白） | 落ちる |
| 末尾に改行が付いたもの | 落ちる |

## 新しい試験が直す前の実装に対して落ちること

復元の方法は、`origin/phase0` を指す **detached worktree を別の場所に建てた**もので、
作業ツリーには一切触れていない。直す前の実装であること（旧 regex が 1 箇所ある）と、
索引が同一であること（`diff -q` で一致）を確認してから測った。測定後に worktree は撤去した。

| 対象 | 結果 |
|---|---|
| 直した後の実装 | **5 / 5 通る** |
| 直す前の実装 | **4 / 5 落ちる** |

## 実際の分母の参照で第一層と第二層が通ること

起票者が候補 10 件から選んだ参照を最小の契約に置き、検査だけのために走らせた。

    exp:phase1/s4_phase_baseline/frozen_tecno_phase_baseline@val~relation_detr_seed42

| 検査 | 結果 |
|---|---|
| `make task-validate LEVEL=l1` | **exit 0** |
| `make task-validate LEVEL=l2` | **exit 0** |

検査用の契約は確認後に削除した。`tasks/` に残っていない。

## 5. deviations（指示書どおりにしなかった箇所）

**空にしない。** 6 件ある。

1. **指示:** Phase C Step 1 で開始前の失敗件数を実装より先に測る。
   **実際:** Phase B を終えた後に測った。
   **理由:** 測り忘れた。`origin/phase0` の worktree を建てて直す前の状態を復元し、
   そこで基準線を取得することで事後に回復した。**回復の経路を含めて記録する。**
   **分類:** 判断が必要だった

2. **指示:** 群の列との照合は冗長な検査として残してよい。残すか落とすかを記録する。
   **実際:** **残した。**
   **理由:** 識別子の先頭区画は実測で 207/207 が群の列と一致するため情報を失わない。
   将来ここが食い違ったときに、黙って解決するより落ちるほうがよい。
   **分類:** 判断が必要だった

3. **指示:** Phase C Step 2 の試験 3 は「旧い二区画の書式で第二層が落ちる」。
   **実際:** 第二層に加えて**第一層でも落ちること**を主張させた。
   **理由:** 指示どおり第二層だけを見る形では、直す前の実装でも通ってしまい回帰を検出しない
   （§6）。第一層の判定を加えて初めて前後で挙動が分かれた。
   **分類:** SPEC の欠陥

4. **指示:** Phase C Step 5 で失敗の識別子の集合を比べる。
   **実際:** 比べた。ただし基準線は worktree で測ったため、skip の件数が 14 と 0 で食い違う。
   **理由:** worktree には `data/` 配下の重み・注釈・manifest が無く、それに依存する 14 件が
   skip された。**私の変更とは無関係である。** 判定に使った失敗識別子の集合は前後で完全一致
   したため、比較の妥当性は保たれている。
   **分類:** 環境差

5. **指示:** `tests/` へ新規の試験を置く。
   **実際:** 既存の試験 2 件（`test_pipe_outside_table_fields_is_warning_only`、
   `test_abs_notation_decision_rule_passes`）の分母の参照も更新した。
   **理由:** 両者が旧二区画の書式 `exp:transfer/s4_base_tecno` を使っており、文法を狭めた
   結果として落ちた。Phase C の Files が Modify を許している。
   **分類:** 判断が必要だった

6. **指示:** （明示なし）
   **実際:** `tests/test_validate_task.py:252` の `exp:transfer/example` は旧書式のまま
   据え置いた。
   **理由:** 当該箇所は `_warn_population_drift` の試験で、分母の**存在**しか見ておらず
   文法検査を経ない。落ちていない箇所を触らない。
   **分類:** 判断が必要だった

## 6. 起票者の推測のうち、実測で裏づけられたもの／否定されたもの

**裏づけられたもの**（契約の冒頭の表は 6 項目すべて実測と一致）。

| 契約の記載 | 実測 |
|---|---|
| 索引の識別子は完全形のみ、二百七行すべてが区切り二個 | 一致 |
| 段だけの形は零件 | 一致 |
| 第一層の文法は完全形を通さない（通過零件） | 一致。**しかも二箇所とも零** |
| 導入時から壊れている | 一致。`0928ae4` 時点で既に完全形 |
| 第二層への言及が試験に零件 | 一致 |
| 分母の参照を持つ既存契約は零件 | 一致 |
| 一意でない組が六件・四十一行、本件の候補は十件 | 一致 |
| 十件の主指標の幅は計画の揺らぎのおよそ三十倍 | 幅 0.1439。σ ≈ 0.003–0.005 に対し **約 29–48 倍**。妥当 |

**否定された・補正されたもの。**

1. **契約は文法を単数として語るが、実装には独立した二箇所があった。**
   `spec.schema.json` の JSON Schema と `validate_task.py:132` の `re.fullmatch` である。
   Files には両方が挙がっていたため修正範囲は足りていたが、**片方だけ直すと検査がすり抜ける。**
   両方を直し、測定も両方に対して行った。

2. **試験 3 は「今回の回帰そのもの」ではなかった。** 契約の指示どおり第二層だけを見る形で
   置いたところ、**直す前の実装でも通った。** 直す前は「どんな参照でも第二層で落ちる」ため、
   第二層の指摘だけでは前後を区別できない。区別が付くのは第一層の側である（直す前はこの
   書式が第一層を**通っていた**）。第一層の判定を加えて回帰検出器になった。
   なお `test_l2_rejects_nonexistent_experiment_id` は強化後も前後どちらでも通る。これは
   回帰検出器ではなく**広げすぎ防止の見張り**であり、この 1 件が落ちないことは正しい。

3. **識別子の区画が列の値に対応するという前提は、179/207 でしか成り立たない。**
   28 件で不一致がある。内訳は split が空値のため `@None` になっているもの 13 件、
   step 列の先頭下線が脱落しているもの 8 件、step 列の `_p123` が脱落しているもの 7 件。
   本修正は完全形を丸ごと照合するため影響を受けないが、L1-4 の文言で区画に名前を与える
   のは正確でないため、文言は「`experiments.csv` の `experiment_id` 列の完全形」と書いた。

4. **空値の混入は 2 件ではなく 15 件だった。** Phase A で見つけた `#None` 2 件のほかに、
   `@None` が 13 件ある。受け皿へ起票した。

## 7. 判断が要る事項・申し送り

1. **区切りの方言を収穫器側で一つに寄せるか。** 文法で両方を受けたことで方言が固定化する。
   寄せると索引の再生成に波及し `BL-harvester-scan-is-host-dependent` と混ざる。受け皿へ起票済み。
2. **識別子への空値の混入（15 件）。** 収穫器側の別の欠陥。受け皿へ起票済み。
3. **`#` の解釈を種別ごとに分ける必要は現時点では無い**（`audit/symbol_collision.md`）。
   参照を種別によらず解く共通関数が将来導入された時点で表面化する。受け皿へ起票済み。
4. **`frozen_source.ref` も同型の二区画の文法である**（`run:<group>/<run_name>`）。
   本契約の範囲外のため触れていない。**同じ欠陥があるかは未測定（UNKNOWN）。**
5. **未追跡のセッション抽出物が 3 件ある**（`docs/sessions/digest/` の 2026-08-02 /
   2026-08-12 / 2026-08-15）。前セッション由来で本契約の範囲外のため触れていない。
   `tasks/README.md` は未追跡のまま放置すると自動同期の `merge --ff-only` が止まると
   記している（B-30）。**記録するかは伏せ字の確認を伴うため、起票者の判断を仰ぐ。**

## 8. 数値の出所

すべて実測である。出所は `audit/` の記録に対応する。

| 数値 | 出所 |
|---|---|
| 識別子の形の 7 項目、方言の内訳 | `audit/before.json`（`audit/phase_a.py` が生成） |
| 排他の実挙動（前・後） | `audit/before_exclusivity.json` / `audit/after_exclusivity.json` |
| 被覆 207/207、広げすぎの 5 種 | `audit/grammar_candidate.json` / `audit/grammar_after.json` |
| 試験の件数と G2 | `audit/pytest_before.txt` / `audit/pytest_after.txt` / `audit/g2_tests.txt` |
| 実契約の検査 | `audit/real_contract_check.txt` |
| 記号の衝突の判断 | `audit/symbol_collision.md` |

未測定の項目は §7-4 の 1 件で、`UNKNOWN` と明記した。σ の値（0.003–0.005）は本契約で
測ったものではなく既存の記録から引いた概算であり、倍率の幅（29–48 倍）はその概算に由来する。
