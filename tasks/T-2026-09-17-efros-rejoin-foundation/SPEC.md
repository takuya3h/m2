# 同期の群れへ戻すための基盤を作る（efros）

**task_id:** `T-2026-09-17-efros-rejoin-foundation`  **kind:** `impl`
**実行ホスト:** `efros`  **repo:** **`~/slocal2/m2`**

## Goal

**本ホストは八月の再構築の対象外だった。** 到達性が失われていたためである。
**復旧したので、同期の群れへ戻す。**

**本契約は基盤を作り、鍵と識別子を版管理へ公開する。**
**中心への登録と、中継・起動・疎通は後続の契約で行う。**

### 本ホストの現状（実測済み。再測定は不要）

| 項目 | 実測 |
|---|---|
| repo | **`~/slocal2/m2`**（`~/slocal/m2` は存在しない） |
| 版管理 | `phase0` に追従済み |
| 合言葉 | **配置済み** |
| 送出の経路 | **HTTPS へ変更済み**。`gh` の認証は通る |
| **`.venv`** | **Python 3.12。十一ギガ。torch 2.13.0。mmcv と mmdet は無い** |
| uv | **`~/.local/bin/uv` に在る** |
| `~/bin/` と同期処理の設定 | 無い |
| 中心への到達 | **確認済み**（中心から測定） |
| ディスクの空き | 三テラバイト |

**`.venv` が他台と揃っていない。** 他台は Python 3.11.16 / torch 2.1.2+cu118 である。
**本契約で作り直す。既存は退避する。消さない。**

### 起票者の記述に誤りがある

**`context/env-facts.md` に三件の誤りがある。本契約で訂正する。**

| 誤り | 実測 |
|---|---|
| 「lecun / efros は `~/slocal/m2`」 | **efros は `~/slocal2/m2`** |
| 「efros / he は `REFUSED`」 | **efros は復旧した** |
| 「uv の実体は `~/.local/share/uv/python/...`」 | **本ホストには存在しない** |

**いずれも五台の実測を本ホストへ引き写したものである。**

## 0. 前提と禁止事項

    cd ~/slocal2/m2
    git --no-pager status --porcelain | grep -c ''
    source .venv/bin/activate 2>/dev/null || echo "仮想環境が使えない"
    make task-start TASK=T-2026-09-17-efros-rejoin-foundation

**仮想環境が壊れているため、取り込みが失敗しうる。**
**その場合は Task 1 を先に済ませてから取り込む。** 手で分岐を切ってもよい。

**常駐処理は動いていない**（`~/bin/` が無い）。**抑止は要らない。** その旨を記録する。

| # | 禁止 |
|---|---|
| 1 | **既存の `.venv` を削除する。** `uv venv --clear` を使う |
| 2 | **退避したものを削除する**（`stash` に一件ある） |
| 3 | 中継の目印を作る。中継を張る |
| 4 | **同期処理を起動する**（識別子を公開するだけ） |
| 5 | 中心と他ホストへ接続する。他ホストの状態を変更する |
| 6 | 受け入れ一覧を変更する |
| 7 | **生成物を再生成する** |
| 8 | 未追跡の成果物を削除する |
| 9 | 秘匿の値を出力・記録する。**検査そのものが値を出力してはならない** |
| 10 | 装置を使う |
| 11 | 未測定の値を書く（未測定は `UNKNOWN`） |
| 12 | `experiments/**` `transfer/**` `data/**` を変更・削除する |
| 13 | 学習・評価コードを変更する |

**禁止 1 の理由。** 既存の `.venv` は十一ギガある。**作り直すが、古い方は退避する。**
**空きは三テラバイトあり、両方を置ける。**

**禁止事項は実行者の操作に対するものであり、同期処理による配布を含まない。**

### 起票者が把握していない規約

**`context/conventions.md` に `proposal_gate` と `folds` という節が増えている。**
**起票者はこれらを読んでいない。**

**本契約に適用されるかを確かめ、適用されるなら従う。**
**適用されないなら、その旨を記録する。**

### 申し送り

**`conventions#issuer_cautions` を参照する**（`inject_verbatim` で注入される）。

本契約に固有の注意。

- **本ホストは五台と経緯が違う。** 五台の実測を前提として信用しない
- **`.venv` の大きさを測ってから触る。** 丸めた表示を実数として扱わない
- **README の手順が `uv venv --clear` を含む場合がある。使わない**

`conventions_rev` は**実行者が実測して置換する。逸脱ではなく手順である。**

**`inputs.data` は雛形の必須項目であり、本契約は参照しない。参照しなかったことを記録する。**

---

## Task 1 (Phase A): 実行環境を作り直す

**Files:** Create: `tasks/T-2026-09-17-efros-rejoin-foundation/audit.md`

- [ ] **Step 1: 既存を測り、退避する**

**大きさ・件数・Python の版・主要な依存の版**を記録する。

**退避は移動で行う。削除しない。** 退避先は repo の外にする
（repo は後で同期対象になるため、中に置くと全台へ配られる）。

**退避後に大きさが一致することを確かめる。**

- [ ] **Step 2: 他台と同じ構成で作る**

**`README.md` の「推奨セットアップ」に従う。**

| 項目 | 他台の実測 |
|---|---|
| Python | `3.11.16` |
| torch | `2.1.2+cu118` |
| torchvision | `0.16.2` |
| mmcv | `2.1.0` |

**`uv venv --clear` を使わない**（禁止 1）。既存は Step 1 で退避済みである。
**uv は `~/.local/bin/uv` に在る。**

- [ ] **Step 3: 動くことを確かめる**

**仮想環境の Python の版、torch の版と装置の可否、mmcv の版**がすべて通ること。

**`mmcv` が `libGL.so.1` で落ちる場合、五台と同じ対処が要る**（`libgl1` の導入）。
**`sudo` はパスワードを要求する。回避せず、利用者へ提示して許諾を得る。**

- [ ] **Step 4: 検証に要るものを補う**

**`jsonschema` が無いと契約の検証が通らない**（五台での実測）。

**素の `pip` は別の環境へ入る事故が記録されている。** 仮想環境の中のものを明示して使う。

| # | 完了判定 |
|---|---|
| A | 既存の `.venv` を測り、退避した（大きさが一致。**削除していない**） |
| B | 他台と同じ版で作り直した（Python・torch・torchvision） |
| C | 装置が使え、`mmcv` が読み込める（落ちた場合は原因と対処） |
| D | 検証に要るものが揃った |

---

## Task 2 (Phase A): 論理名を設定する

**Files:** Modify: `~/.zshenv`, `~/.profile`

- [ ] **Step 1: 現状を確かめる**

**`SERVERNAME` が設定されているか。** `hostname` が何を返すか。

- [ ] **Step 2: 両方へ置く**

**`~/.zshenv` と `~/.profile` の両方に置く。**
前者は zsh の全形態、後者は bash のログイン時に読まれる。

**`scripts/sync/setup_host_servername.sh` がある。** 読んでから使うか、手で追記する。
**どちらにせよ追記内容を記録する。**

- [ ] **Step 3: 新しいシェルで解決されることを確かめる**

**zsh と bash の両方で `efros` が出ること。**

| # | 完了判定 |
|---|---|
| E | 設定前の状態を記録した（`hostname` の値を含む） |
| F | 追記内容を記録した |
| G | 両方の形態で論理名が解決される |

---

## Task 3 (Phase B): 中心宛の鍵を作り、公開鍵を公開する

**Files:** Create: `~/.ssh/id_ed25519_efrostophilip`, `scripts/sync/hub_keys/efros.pub`

**秘密鍵はこのホストから出さない。公開鍵だけを版管理へ置く。**

- [ ] **Step 1: 既存の鍵を確かめる**

**中心宛の鍵が既に在れば作らない。** 記録して次へ進む。

**`~/.ssh/` に `id_ed25519_github` が在る**（実測）。**これは別用途である。触らない。**

- [ ] **Step 2: 鍵を作る**

**合言葉は付けない。** 常駐処理が対話なしで使うためである。
**指紋を記録する。** これが中心の受け入れ一覧に入る値である。

- [ ] **Step 3: 権限を確かめる**

**秘密鍵は所有者だけが読める形、`~/.ssh` も同様であること。**

- [ ] **Step 4: 公開鍵を版管理へ置く**

`scripts/sync/hub_keys/efros.pub` へ置く。**指紋が Step 2 と一致すること。**

**置いたものが公開鍵だけであることを三つで確かめる。**

| 検査 | 期待 |
|---|---|
| 先頭 | `ssh-` で始まる |
| 秘密鍵の書き出し | **零件** |
| 行数 | **一行** |

**陽性対照**: 秘密鍵の書き出しを模した囮に同じ検査をかけ、**一以上を返すこと**を確かめる。
**囮は版管理へ入れない。**

| # | 完了判定 |
|---|---|
| H | 既存の鍵を確かめた（在れば作っていない） |
| I | 中心宛の鍵を作り、指紋を記録した（秘密鍵の中身なし） |
| J | 権限が期待どおり |
| K | 公開鍵を版管理へ置き、指紋が一致し、三つの検査と陽性対照を通った |

---

## Task 4 (Phase B): 同期処理を導入し、識別子を公開する

**Files:** Create: `~/bin/syncthing`, `scripts/sync/device_ids/efros.txt`

- [ ] **Step 1: 中心と同じ版を取得する**

**中心は `v2.1.3`。実行ファイルの要約値は
`e8a08fdd8b25340aae0c0a00ab131b293830e4ea47504d4b83a82f31b52b96c4`。**

**配布物の中に同名の別物が三件ある**（五台での実測）。**大きさで特定する。**

**取得できない、または要約値が中心と違う場合は停止して報告する。**

- [ ] **Step 2: 配置する**

**実行権は落としたままにする。まだ起動させない**（禁止 4）。

**起動の引き金は実行権だけである**（`keeper.sh` が見る）。ただし本ホストには
`~/bin/keeper.sh` が無いため、**いま実行権を戻しても起動はしない。**
それでも**後続の契約の順序に合わせて落としておく。**

**要約値が中心と一致すること。**

- [ ] **Step 3: 識別子を発行する**

**`generate` は設定と鍵を作るが常駐しない。**
**既に設定が在れば上書きしない。** 記録して次へ進む。

- [ ] **Step 4: 識別子を読み取り、公開する**

**取り方は `serve --home ... --device-id`。`device-id` という下位命令は無い**（五台での実測）。

`scripts/sync/device_ids/efros.txt` へ**一行で**置く。**識別子は秘匿ではない。**

- [ ] **Step 5: 起動していないことを確かめる**

**`22000` と `8384` が待ち受けていないこと。**

**プロセスの計数は `/proc/PID/exe` で絞る。** 部分一致は実行基盤を拾う。
**両方向の対照を取る。**

| # | 完了判定 |
|---|---|
| L | 配布物の要約値が中心と一致した |
| M | 配置物の要約値が中心と一致し、実行権が落ちている |
| N | 識別子を発行した（既存があれば上書きしていない） |
| O | 識別子を一行で公開した |
| P | **同期処理が起動していない**（両方向の対照つき） |

---

## Task 5 (Phase C): 記述を訂正し、報告する

**Files:** Modify: `context/env-facts.md`
Create: `tasks/T-2026-09-17-efros-rejoin-foundation/RESULT.md`, `result.yaml`,
`tasks/inbox.d/T-2026-09-17-efros-rejoin-foundation.md`

- [ ] **Step 1: `env-facts.md` を訂正する**

| 箇所 | 訂正 |
|---|---|
| repo の位置 | **lecun のみ `~/slocal/m2`。efros は `~/slocal2/m2`** |
| 到達性 | **efros は復旧した。he は未確認** |
| uv の実体 | **ホストによる。本ホストには五台と同じ場所に無い** |

**他の記述で本ホストと食い違うものがあれば、それも直す。** 直した箇所を記録する。
**`conventions.md` は触らない。**

- [ ] **Step 2: 報告を書く**

**`RESULT.md` は起票者が判断に使う事実だけ。目安 150 行以内。**
**手続きの証跡は `audit.md` へ置き、行番号で指す。**

| 節 | 内容 |
|---|---|
| 判定 | `verdict` と各 Gate の結果 |
| 完了判定 | 表。各項目に**実測値** |
| **後続で使う値** | **鍵の指紋、識別子、同期処理の版** |
| **`proposal_gate` と `folds` の適用** | 本契約に適用されるか |
| 訂正した記述 | `env-facts.md` のどこを直したか |
| 起票者の誤り | 型と内容。一件 3 行以内 |
| 逸脱・想定外・UNKNOWN | — |
| 送出 | PR の番号と終了コードだけ |

- [ ] **Step 3: 検証を通し、秘匿を検査し、送出する**

`conventions_rev` を実測して置換し、検証を通す。**生成物を再生成しない**（禁止 7）。

**秘匿検査は形で判定し、検査そのものが値を出力しない。陽性対照を取る。**

**変更が契約のディレクトリ、公開鍵、識別子、`env-facts.md` に限られること。**

commit・push・PR。**台帳へ返す。**

| # | 完了判定 |
|---|---|
| Q | `env-facts.md` を訂正した（直した箇所） |
| R | 報告が指定の構成で、目安の分量に収まっている |
| S | **`proposal_gate` と `folds` の適用可否を記録した** |
| T | 秘匿検査を自分で行った（検査が値を出力していない。陽性対照つき） |
| U | 変更が契約の範囲に限られ、分岐が送出され PR が存在する |
| V | 報告が台帳へ返っている |

---

## 想定外が起きたときの扱い

| 事象 | 対応 |
|---|---|
| **既存の `.venv` を消してしまった** | **直ちに報告。** 十一ギガである |
| 仮想環境を作れない | **停止して報告。** README の手順と出力を貼る |
| `mmcv` が落ちる | 原因を記録。**管理者権限が要るなら利用者へ提示して許諾を得る** |
| 配布物の要約値が中心と違う | **停止して報告。** 版が揃わない |
| 識別子を読み取れない | 設定の中を探す。**何を見たかを記録する** |
| **同期処理が起動した** | **記録して報告** |
| `proposal_gate` が本契約に適用される | **従う。** 従った内容を記録する |
| `env-facts.md` の他の記述も食い違う | **直して記録する** |
| 退避したもの（`stash`）を戻したくなった | **戻さない。** 本契約の範囲外 |

**言い訳をしない。事実と、測れなかったことを書く。**
