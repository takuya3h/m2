# 事実の記録 — T-2026-08-26-oracle-ceiling-and-tool-drop

**status: partial** — Phase A / B / C は完了。**Phase D は入っていない**（事前登録 §4 の停止条件 5 に該当）。
host `lecun` / branch `feat/oracle-ceiling-and-tool-drop` / 証跡は `EVIDENCE.md`（行番号で指す）。

## 1. 判定

**本契約は判定を行っていない。** 出したのは効果量と、改善側へ倒れた個数の 2 つだけである。
判定則は同時に走る別契約が確定させる。

### 1.1 上限（Phase B）— 予測に対するオラクルの追加利得は**小さくなかった**

本番の時系列モデル（causal TeCNO・2063-d 入力）で、正しい術具存在を与えた腕を新規に測った。
lecun・決定化あり・seed 42・50 epoch・所要 25 秒。証跡 `experiments/transfer/b2a_det2phase_oracletool_009_b2a_det2phase_oracletool_seed42`。

| 指標 | オラクル（本契約の実測） | 予測（既存記録 n=9 の平均） | Δ |
|---|---:|---:|---:|
| accuracy | 0.955776 | 0.936927 (pstd 0.001870) | +0.018849 |
| **macro-F1（主終点）** | **0.823675** | **0.790542** (pstd 0.004257) | **+0.033133** |
| edit score | 52.634921 | 43.312808 (pstd 5.612936) | +9.322112 |
| jaccard | 0.794676 | 0.744649 (pstd 0.005595) | +0.050027 |
| seg-F1@10 / @25 / @50 | 0.594407 / 0.594407 / 0.543764 | 0.503592 / 0.489440 / 0.461353 | +0.090815 / +0.104966 / +0.082412 |

**改善側へ倒れた個数**: 既存記録を種ごとに対にすると（5 種）、accuracy **5/5**（平均 Δ +0.02130）、
macro-F1 **5/5**（平均 Δ +0.03399）。同一ホスト（lecun）だけで成立する対は seed 789 と 1000 の **2 組**で、**2/2** が改善側。
→ 効果量と対の内訳は `EVIDENCE.md` 328-359 行、工程ごとの内訳は 361-393 行。

**契約の背景の前提と実測が食い違っている。** 契約 §1 は代理モデルの所見として
「完璧な術具存在を与えても追加的な分類の利得はほとんど無い」と述べているが、
本番の時系列モデルでは分類・分節ともに一貫した差が出た。**この事実をそのまま報告する。**
なお `intent.hypothesis` も「オラクルの追加利得は小さいままである」と予測しており、**予測は外れている。**

**この腕は上限の測定専用である。手法の成果ではない。**
印は `config.yaml` の `oracle_upper_bound_only_do_not_report: true`（`EVIDENCE.md` 280 行）。

### 1.2 除去（Phase D）— **測っていない。UNKNOWN。**

工程を弁別しない術具を落とす腕は**一本も走らせていない**。
効果量も、改善側へ倒れた個数も UNKNOWN である。**足りない本数から結論を作っていない。**

理由は時間ではない。**時間は制約にならなかった**（残り 15.64 時間、1 本 25〜34 秒、対 828 組が入る計算。`EVIDENCE.md` 438-453 行）。
理由は事前登録 §4 の停止条件 5「落とす集合を求める仕組みが本番側に実装されていない場合」に該当したことである。
異質な 3 方法（内容検索 / ファイル名走査 / 版管理の一覧、先頭ドット含む）が一致して、
集合計算 `H(phase|tool)/log2(9)` は `docs/analysis_scripts/` の代理側 3 本にのみ存在し、
`src/` と `scripts/` には無いことを示した（`EVIDENCE.md` 154-178 行）。
この事実と、代理スクリプトが読む入力は本番キャッシュそのものである点を併せて起票者へ提示し、
**「契約どおり止める」との回答を得た**（`tasks/inbox.d/`）。

## 2. 完了判定の表

| # | 判定 | 結果 | 空振りでないことの確認（実測） |
|---|---|---|---|
| a | 入力の実在を一つずつ確かめた | **達成** | 存在しない経路 `experiments/transfer/zzz_no_such_run/config.yaml` を同じ関数に投げると `task_id=''` を返した（E9 / 489 行） |
| b | 上限の評価が完走した | **達成** | 分類と分節の双方を `metrics.json` に記録（E8.1 / 291-299 行）。入力を欠くと `load_clips` が `KeyError` で止まる設計 |
| c | 学習一本の所要時間を実測した | **達成** | 25 / 26 / 27 / 34 秒。零も負も無い（E8.6 / 440-441 行） |
| d | 決定化が効いている | **達成** | 両方向。同種 2 本は 50 epoch 全行一致（差分 0 行）、種違いは 102 行差。さらに**決定化を外すと同種でも 96 行差**で best acc が 0.9564 と 0.9545 に分かれた（E8.5 / 395-436 行） |
| e | 落とす集合が正しく求まる | **未達（UNKNOWN）** | Phase D 未実施。集合計算が本番側に無い（E5.3） |
| f | 落とした本数を集合の差で求めた | **未達（UNKNOWN）** | 同上 |
| g | 対の両側が同数そろっている | **該当なし** | 片側だけの腕は 0 本。そもそも腕を 1 本も作っていない |
| h | 実行の順序を記録した | **達成** | Phase B → C1a → C1b → C2 → N1 → N2 の順（E8.1 / E8.5）。各命令に開始 epoch 秒を残した |
| i | 効果量と倒れた個数の双方を出した | **上限のみ達成** | 上限は Δ と 5/5 の双方（1.1）。除去は双方 UNKNOWN（1.2） |
| j | 判定を行っていない | **達成** | 本書に「有意」「非有意」の語は現れない。σ は記録のみで、abs(Δ)/σ を計算していない |
| k | 上限の腕に測定専用の印がある | **達成** | `config.yaml:49` に `oracle_upper_bound_only_do_not_report: true`。印の無い記録は 0 件 |
| l | 変更範囲の一覧を出した | **達成（検査は fail）** | 12 件中 7 件が違反で、7 件すべて本契約の run の内側。5 件を違反としていないため「何でも違反にする」壊れ方ではない（E9 / 455-490 行） |
| m | PR が存在する | **§7 参照** | — |
| n | 分岐が規定の接頭辞で始まる | **達成** | `feat/oracle-ceiling-and-tool-drop` は `feat/` で始まる |
| o | 報告が二つに分かれている | **達成** | 本書（事実）と `EVIDENCE.md`（証跡）。本書は証跡を行番号で指し、同じ内容を二度書いていない |
| p | 締切に対する経過が記録されている | **達成** | 各 Phase の入口の経過を表にした（E1 / 18-25 行） |

## 3. 次の契約で使う値

- 本番の上限（オラクル術具存在・val・凍結源 relation_detr_seed42・決定化あり・lecun）:
  `accuracy 0.955776 / macro-F1 0.823675 / edit 52.634921 / jaccard 0.794676 / seg-F1@50 0.543764`
- 予測側の分母（既存記録 n=9、**うち 7 件は efros で測られたもの**）:
  `accuracy 0.936927 ± 0.001870 (pstd) / macro-F1 0.790542 ± 0.004257`
- **学習 1 本の所要時間（この入口で初めて実測）**: 決定化あり 25〜34 秒（n=4, 平均 28.0）、決定化なし 11〜19 秒（n=2, 平均 15.0）。
- **決定化の減速倍率**: 平均比 **1.87 倍**。ただし範囲は 1.32〜3.09 倍で n が小さい。申し送り 4 の「二倍強」は本入口では確認できていない。
- **macro-F1 の分母は 7**（9 工程のうち val に支持の無い `disinfection` と `irrigation` は除外される）。
  `dressing` は支持 24 frame がありながら F1=0.0 で、除外されず分母に入っている（E8.4 / 361-393 行）。
- 実測で差し替えた契約の 2 欄: `runindex_commit = 7918b5dd9aab3d15b3c459f87aebdd9eb1653116` /
  `conventions_rev = a8c07e813696d3720ceee648e8aa202224285955`（E4 / 104-118 行）。

## 4. 起票者の誤り

4 件。詳細は `result.yaml` の `issuer_defects`。要点のみ。

1. **`self_contradiction`** — `outputs.expected_runs: 0` / `resources: {runs: 0, est_hours: 0}` と
   Phase C（最低 2 本）・Phase D（対）の要求が矛盾する。宣言 0 本のまま run が生まれるため、禁止領域検査が必ず失敗する。
2. **`asserted_without_measuring`** — §2.1「再学習を要しない。既存の学習済みの重みを使って評価するだけである」。
   入口に評価専用の経路が無く、指示どおりでは Phase B に着手できない。学習 1 本（25 秒）で達成した。
3. **`asserted_without_measuring`** — Task 4 Step 1「決定化を有効にした状態で」。
   入口に決定化の切り替えが無く、配線を足すまで有効にする手段が存在しない。
4. **`asserted_without_measuring`** — 申し送り 4「決定化を有効にすると二倍強遅くなる」。
   本入口の実測は平均比 1.87 倍で、二倍強と断定できる測定になっていない。

**正しかった前提**: 申し送り 1（入口の名が変わっている可能性）は杞憂で `scripts/train_b2a.py` のままだった。
申し送り 3（凍結源の重みの有無）は在り、要約値も規約の正本と一致した。
申し送り 5（分母の出所を確かめよ）は有効で、予測側 9 件のうち 7 件が別ホスト由来と判明した。

## 5. 逸脱

8 件。詳細は `result.yaml` の `deviations`。**逸脱は無しではない。** 要点のみ。

1. Phase B を「評価だけ」では実行できず、学習 1 本を走らせた（誤り 2 への対処）。
2. `scripts/train_b2a.py` へ既存の本番ユーティリティ `enable_determinism` を配線した（誤り 3 への対処）。学習の数式には触れていない。
3. 同ファイルへ `--task-id` と上限測定専用の印の書き出しを足した。契約の `outputs.stamp` と §2.1 Step 3 を満たすため。
4. Phase D へ入らなかった（1.2）。
5. **禁止 5 に従い `make taskindex` と `make inbox` を回していない。** skill の手順は再生成を求めるが、契約の禁止を優先した。
   したがって `context/auto/` の投影と `tasks/inbox.md` は本報告を含まない。**全 PR 統合後に一台で回す必要がある。**
6. Phase B の run は W&B と Notion の追跡が no-op のまま走った。実行前に `scripts/load_env.sh` を読み込んでいなかったためで、
   資格情報が読めないからではない（当初そう記録したが**誤りだった**。E13 / 532-559 行）。指標そのものは `metrics.json` に残っている。
7. 決定化の対照 3 本と非決定の 2 本は `--no-evidence` で走らせ、`experiments/` を汚していない。証跡はログとして残した。
8. 申し送り 4 が「Phase C で実測すること」と求めた減速倍率のため、決定化なしの run を 2 本走らせた。
   契約の腕ではなく道具の測定であり、証跡を残していない。**「別の腕の追加」に当たるかは起票者の判断を仰ぐ。**

## 6. 想定外

- **検証の WARN**: L2-8 が 2 件（`index.csv` 751→1177 / `experiments.csv` 207→213）。
  既知の教訓 `BL-harvester-scan-is-host-dependent` のとおりホスト依存の現象で、
  **参照先の分母そのものは移動していない**（E3.1 / 47-68 行）。提示のうえ続行の承認を得た。
- **プリフライトの SKIP 2 件は合格ではない**。P2 `cuda_ext_loaded` は契約に記載が無く未実行。
  P3 `deterministic_flags` は判定基準が未確定（backlog B-20）で、検査器では決定化を確かめられない。
  **契約が Phase C の対照で測れと定めていたのは、この空白を埋めるためである**（E6 / 197-231 行）。
- **シェルの前提**: `${PIPESTATUS[0]}` が zsh で空になり、最初の終了コード検査が空振りした。
  以後すべて `; echo "EXIT_CODE=$?"` に統一した（E2 / 39-43 行）。SPEC §6 の予告どおりの事故である。
- **シェルの前提（2 件目）**: `source scripts/load_env.sh 2>&1 | tail -3` と書いたため
  **パイプが副シェルを起こし、`export` が呼び出し元に残らなかった。**
  読み込みを同じ命令に入れても、**パイプに繋いだ時点で無効になる。**
  結果として「資格情報が読めない」と一度誤って記録した。パイプを外したところ `make task-report` は exit 0 で通った。
  SPEC §6 の「読み込みは同じ命令の中に入れる」は**必要条件であって十分条件ではない**（E13 / 532-559 行）。

## 7. 送出と PR

- **PR: https://github.com/takuya3h/m2/pull/152**（`feat/oracle-ceiling-and-tool-drop` → `master`）。送り出しただけの状態ではなく、PR が存在する状態で終えている。分岐は規定の接頭辞 `feat/` で始まる。
- commit: `1f4dd9c7e886aad2db9c4650b31a32d364f1f95a`（事前登録の記録）/
  `a4a95cf2768b89f7b2ca221dc069b3761daaf890`（コードの配線・run の証跡・本報告）。
- 台帳への送出: 下記「送出の結果」を参照（証跡は E12 / 517-530 行）。
- **本契約では触れていない未追跡ファイル**: `experiments/analysis/lovo_decision_rule/dump_folds.py` は
  同時に走っている別契約（判定則を確定させる契約）の生成物であり、commit に含めていない。

### 送出の結果

`make task-report` は **exit 0 で送出に成功した。** 台帳の応答:

    {"task_id": "T-2026-08-26-oracle-ceiling-and-tool-drop", "verdict": "partial",
     "n_issuer_defects": 4,
     "report_sha256": "f736d4c56fc6075dad0c1e52afb43b23aff1ae1411f1d5c23731d96d92a29d25",
     "report_bytes": 12440, "replaced_blocks": 0}

秘匿の検査は無効にしていない。送出は `make task-report` の 1 経路のみで行った。
