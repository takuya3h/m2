result_version: 3
task_id: "T-2026-08-22-lecun-node-foundation"
status: pass
host: "lecun"
branch: "feat/lecun-node-foundation"
pr: 122
merged: false

gates:
  - id: G1
    verdict: pass
    note: >-
      HEAD と origin/phase0 がともに 8eec82ec、rev-list --left-right --count が 0 0。
      .venv/bin/python は uv 管理の cpython-3.11.16 へ解決でき Python 3.11.16 を返した。
      du -sh .venv は前 6.2G / 後 6.2G。uv venv --clear は実行していない。
      which python は /home/ubuntu/slocal/m2/.venv/bin/python。
      zsh -c と bash -lc がともに SERVERNAME=lecun を返した（設定前は unset）。
      git config --local に user.name=takuya3h / user.email=daky.o7600@gmail.com を設定。
      git remote -v は fetch/push とも https で、初めから git@ ではなかった。
  - id: G2
    verdict: pass
    note: >-
      鍵の指紋は SHA256:g5TwfvgDPsNhiSd9OXDZoWDj99au1y8yEnW8hmNyqHI (ED25519)。
      scripts/sync/hub_keys/lecun.pub の指紋が生成時と一致。
      三つの検査は head -c 30 が ssh-ed25519 AAAAC3NzaC1lZDI1NT、PRIVATE が 0 件、行数が 1。
      囮に同じ三つをかけると PRIVATE が 2 件、行数 3、先頭は鍵の書き出しの標識行で
      三つとも外れた。囮は版管理へ入れていない。
      配布物 sha256 は c04ffbdedcd1d18ccb4a34a341a6a2b2461082f7a6f43537eb0bba860975fd60、
      配置物 sha256 は 32ab747eb18ff3a01423f9719c5b8a8165da63e60ee9c3f733887464c70ca1dd で
      いずれも中心の値と一致。syncthing v1.27.10 が表示できた。
      scripts/sync/device_ids/lecun.txt は grep -c '' が 1 で
      OOOTQMG-2WT55EF-YGX55VM-YWFWVRT-XUSDUUB-3AXCYV4-OVY2X3H-KRFOWA3。
      port_22000 と port_8384 はいずれも非待ち受け（port_22 は LISTEN）。pgrep -x syncthing は該当なし。

tests:
  before_failed: 5
  after_failed: 5
  after_passed: 462

deviations:
  - type: spec_defect
    note: >-
      SPEC の repo 位置 ~/slocal2/m2 が存在しないため ~/slocal/m2 で作業した。
      ls で ~/slocal2 の不在を確かめてから判断した。
  - type: judgement
    note: >-
      SPEC は git checkout -b feat/lecun-node-foundation origin/phase0 を指示するが、
      分岐は既に存在し origin/phase0 と同一の先頭を指していた（rev-list が 0 0）。
      切り直すと未追跡の扱いに影響が出るため既存の分岐をそのまま使った。
  - type: environment
    note: >-
      契約の取得を手で行っていない。spec.yaml と SPEC.md はセッション開始時点で
      tasks/T-2026-08-22-lecun-node-foundation/ に未追跡で置かれていた。再取得していない。
  - type: environment
    note: >-
      SPEC Task 1 Step 2 の主眼である .venv の貼り直しは、実測の結果壊れていなかったため
      実施対象が無かった。jsonschema の導入（Step 3、既に 4.26.0）と
      git remote set-url --push（Step 5、既に https）も同じ理由で実行していない。
  - type: judgement
    note: >-
      契約は論理名を ~/.zshenv と ~/.profile の 2 つへ置くよう求めるが、道具
      scripts/sync/setup_host_servername.sh は ~/.bashrc にも同じ 3 行を置く。
      冪等性と戻し方が保証されるため道具をそのまま使った（契約の要求の上位集合）。
  - type: judgement
    note: >-
      陽性対照の生出力に含まれる囮の値（語=値 と 語: 値 の 2 行）と鍵の書き出しの標識行は、
      囮であって実在の資格情報ではないが形が該当するため、SPEC の「削る」指示に従い
      字面を記述へ置き換えた。件数による証拠は残している。

issuer_defects:
  - type: asserted_without_measuring
    note: >-
      SPEC 冒頭が repo を ~/slocal2/m2 と書き、前提でも cd ~/slocal2/m2 を指示するが、
      /home/ubuntu/slocal2 は存在しない（ls: cannot access）。実体は /home/ubuntu/slocal/m2 である。
      指示どおり実行すると最初の cd が失敗し、以降の git fetch や git checkout -b が
      別のディレクトリで走るか set -e 下では即座に止まる。
  - type: shell_assumption
    note: >-
      Task 3 Step 1 の for f in ~/.ssh/id_* は、bash ではマッチが無いとき未展開の文字列で
      1 回まわり case で落ちて無害に終わるが、ログインシェルの zsh は既定でマッチが無いと
      no matches found を出してその場でコマンドを失敗させる。本契約は全て失われた新規構築を
      前提としており鍵が零件なのは正常な状態である。実際に
      (eval):1: no matches found: /home/ubuntu/.ssh/id_* が出て既存確認で止まった。
      ls -la ~/.ssh/ の一覧で代替して確かめた。
  - type: asserted_without_measuring
    note: >-
      Task 2 Step 2 が scripts/sync/setup_host_servername.sh --help を指示し「道具を読んでから
      使う」と続けるが、この道具は --help を受け付けず
      ERROR: 不明なオプション '--help'（--dry-run / --verify のみ） を返す。
      指示どおりでは使い方が得られないため、道具の先頭 60 行を直接読んで用法を得た。

positive_controls:
  - judgement: "版管理へ置いたものが公開鍵だけであることの三つの検査"
    breaking_input: "秘密鍵の書き出しを模した囮ファイル（版管理の外に置いた）"
    observed: "三つすべてで外れた。先頭は鍵の書き出しの標識行、PRIVATE が 2 件、行数が 3"
  - judgement: "送信前の秘匿検査（grep による走査）"
    breaking_input: "語+区切り+値 の形を 2 行含む囮ファイル"
    observed: "2 件を検出した（一以上）。囮は版管理へ入れていない"
  - judgement: "同期処理が起動していないこと（待ち受けの不在）"
    breaking_input: "実際に待ち受けている口"
    observed: "同じ検査器が port_22=LISTEN を返した。検出能力そのものは働いている"
  - judgement: "論理名が新しいシェルの両方の形態で解決されること"
    breaking_input: "設定前の状態"
    observed: "設定前は SERVERNAME=unset、設定後は zsh -c と bash -lc がともに lecun。同じ命令が前後で違う値を返した"
  - judgement: "配布物の要約値が中心と一致すること"
    breaking_input: "別の版の配布物を落とす"
    observed: "UNKNOWN。中心と版を揃える要求のため意図的に測っていない"
  - judgement: "make forbidden-check が禁止領域の変更を捕まえること"
    breaking_input: "禁止領域下のファイルを変更する"
    observed: "UNKNOWN。禁止領域を意図的に汚す検査は行っていない"

followups:
  - >-
    前契約（philip）の実測 10 件のうち 4 件が lecun では当てはまらず、1 件が半分だけ当たった。
    非該当は #1（.venv が壊れた繋がり）、#4（pushurl が SSH）、#5（jsonschema の導入が要る）、
    #10（libGL.so.1 が無く mmcv/mmdet を読み込めない。lecun では mmcv 2.1.0 / mmdet 3.3.0 とも
    import 成功）。半分は #3（~/.gitconfig は在るが user.name/user.email だけが無い）。
    他台へ同じ契約を配るときは、前契約の実測を前提ではなく仮説として扱うべきである。
  - >-
    P9 spec_lint の separated_source 3 件（SPEC.md:396/399/402）は検査器の側の誤検知である。
    SPEC の当該箇所は \ による行継続で source ... && ... を 1 つの命令として書いており
    読み込みは引き継がれる。tools/check_spec.py の rule_separated_source が行を \ で結合せず
    次行を「次の命令」と見なすため該当が出る。契約の誤りではない。
    検査器が行継続を結合するよう直せば、この誤検知は消える。
  - >-
    tests の失敗 5 件は本契約以前から在るもので内容も無関係である。追跡ファイルを一つも
    変更していないため試験対象の木は origin/phase0 と同一であり、前後の実測は同じ値になる。
    test_research_logger の 4 件は Notion への記録が None を返すことによる
    （assert None == 'page-abc'）。scripts/load_env.sh が使えず資格情報が入らない状況と整合する。
    test_engines の 1 件は未追跡の理由で落ちており本契約では触れていない。
  - >-
    次の契約（登録と起動）で使う値。識別子は
    OOOTQMG-2WT55EF-YGX55VM-YWFWVRT-XUSDUUB-3AXCYV4-OVY2X3H-KRFOWA3
    （scripts/sync/device_ids/lecun.txt）。中心の受け入れ一覧へ入れる指紋は
    SHA256:g5TwfvgDPsNhiSd9OXDZoWDj99au1y8yEnW8hmNyqHI
    （公開鍵は scripts/sync/hub_keys/lecun.pub）。秘密鍵は
    ~/.ssh/id_ed25519_lecuntophilip にあり当ホストから出していない。

unknowns:
  - >-
    配布物の要約値の照合が空振りでないこと。別版を落として不一致になることは確かめていない。
    中心と版を揃える要求と禁止事項のため意図的に測っていない。
  - >-
    make forbidden-check が禁止領域の変更を実際に捕まえるかどうか。
    禁止領域を意図的に汚す検査は行っていない。
  - >-
    test_engines.py::test_mmdet_trainer_eval_recipe_in_metrics が失敗する理由。
    本契約の範囲外のため追っていない。

commits:
  - "9c1a8b3d"
