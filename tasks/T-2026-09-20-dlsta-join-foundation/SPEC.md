# 同期の群れへ加わるための基盤を作る（dlsta）

**task_id:** `T-2026-09-20-dlsta-join-foundation`  **kind:** `impl`
**depends_on:** なし
**実行ホスト:** `dlsta`  **repo:** **`~/local/m2`**

## Goal

**本ホストは同期の群れに入っていない。** 六台（philip / lecun / bengio / andrew / ilya / efros）が
中心 philip を介して繋がっている。**七台目として加わる。**

**本契約は基盤を作り、鍵と識別子を版管理へ公開する。**
**中心への登録と、中継・起動・疎通は後続の契約で行う。**

### 本ホストの現状（実測済み。再測定は不要）

| 項目 | 実測 |
|---|---|
| repo | **`~/local/m2`**（`~/slocal/m2` も `~/slocal2/m2` も存在しない） |
| 版管理 | `phase0` に追従済み。作業ツリーは空 |
| 合言葉 | **配置済み** |
| 送出の経路 | **既に HTTPS** |
| **`.venv`** | **存在しない**（ディレクトリごと無い） |
| uv | **`~/.local/bin/uv` に在る** |
| **nvcc** | **12.9**（`setup_env.sh` が 11.8 を要求して止まりうる） |
| `~/bin/` と同期処理の設定 | 無い |
| `~/.ssh/` | `authorized_keys`・`config`・`config.d`・`id_ed25519_github`・`known_hosts` |
| **`hostname`** | **`4f3861ae8d3b`**（容器の識別子。論理名と無関係） |
| ディスクの空き | 七五八ギガ |
| 版管理の識別 | **未設定** |

### 他の六台と違う点

| # | 違い |
|---|---|
| 1 | **repo の位置が三種目である**（`~/local/m2`） |
| 2 | **`.venv` が無い。** 他台は壊れた状態で存在していた |
| 3 | **`hostname` が容器の識別子。** 論理名を明示しなければ何も解決しない |
| 4 | **版管理の識別が未設定。** commit の前に要る |

**前ホストの実測を定数として引き継がない。** 本ホストで測る。

## 0. 前提と禁止事項

    cd ~/local/m2
    git --no-pager status --porcelain | grep -c ''
    git fetch origin
    git checkout -b feat/dlsta-join-foundation origin/phase0

**`.venv` が無いため、取り込みの仕組みは使えない。** 手で分岐を切り、配られたものを置く。
**Task 1 が済めば仕組みが使えるようになる。**

**常駐処理は動いていない**（`~/bin/` が無い）。**抑止は要らない。** その旨を記録する。

| # | 禁止 |
|---|---|
| 1 | 中継の目印を作る。中継を張る |
| 2 | **同期処理を起動する**（識別子を公開するだけ） |
| 3 | 中心と他ホストへ接続する。他ホストの状態を変更する |
| 4 | 受け入れ一覧を変更する |
| 5 | **生成物を再生成する** |
| 6 | 未追跡の成果物を削除する |
| 7 | 秘匿の値を出力・記録する。**検査そのものが値を出力してはならない** |
| 8 | 装置を使う |
| 9 | 未測定の値を書く（未測定は `UNKNOWN`） |
| 10 | `experiments/**` `transfer/**` `data/**` を変更・削除する |
| 11 | 学習・評価コードを変更する |

**禁止事項は実行者の操作に対するものであり、同期処理による配布を含まない。**

### 起票者が把握していない規約

**`context/conventions.md` に `proposal_gate` `folds` `symmetry` の節がある。**
**起票者は読んでいない。**

**`proposal_gate` の禁止語は契約の本文にも適用される**（前契約の実測）。
**`tools/check_proposal.py --only forbidden` を送出前に当てる。**

**他の二つが本契約に適用されるかを確かめ、適用されるなら従う。**
**学習も評価も行わない契約であるため、適用されない見込みである。確かめて記録する。**

### 申し送り

**`conventions#issuer_cautions` を参照する**（注入される）。

本契約に固有の注意。

- **`hostname` は容器の識別子を返す。** 論理名の解決に使わない
- **自分の命令行が計数に一致しうる。** 自分の識別子と子を除く
- **属性名や下位命令を推測しない。** 実在するものを読んでから使う

`conventions_rev` は**実行者が実測して置換する。逸脱ではなく手順である。**

**`inputs.data` は雛形の必須項目であり、本契約は参照しない。参照しなかったことを記録する。**

---

## Task 1 (Phase A): 実行環境を作る

**Files:** Create: `tasks/T-2026-09-20-dlsta-join-foundation/audit.md`

**`.venv` が無いため、退避は要らない。新規に作る。**

- [ ] **Step 1: 何が無いかを記録する**

**`.venv` が存在しないこと、uv の位置、`nvcc` の版、装置の状態**を記録する。

- [ ] **Step 2: 他台と同じ構成で作る**

**`README.md` の「推奨セットアップ」に従う。**

| 項目 | 他台の実測 |
|---|---|
| Python | `3.11.16` |
| torch | `2.1.2+cu118` |
| torchvision | `0.16.2` |
| mmcv | `2.1.0` |

**`scripts/setup_env.sh` が `nvcc != 11.8` で止まる**（前ホストの実測）。
**本ホストの `nvcc` は 12.9 である。** 検査を飛ばす手段が用意されているなら使う。

**その検査自体が誤りである**という指摘が前ホストの報告にある。
**prebuilt wheel を使う経路に対して `nvcc` を要求しているためである。**
**飛ばした場合、その旨を記録する。**

- [ ] **Step 3: 動くことを確かめる**

**仮想環境の Python の版、torch の版と装置の可否、mmcv の版**がすべて通ること。

**`mmcv` が `libGL.so.1` で落ちる場合、他台と同じ対処が要る**（`libgl1` の導入）。
**管理者権限はパスワードを要求する。回避せず、利用者へ提示して許諾を得る。**

- [ ] **Step 4: 検証に要るものを補う**

**`jsonschema` が無いと契約の検証が通らない**（他台での実測）。

**素の `pip` は別の環境へ入る事故が記録されている。** 仮想環境の中のものを明示して使う。

| # | 完了判定 |
|---|---|
| A | 開始時に何が無いかを記録した（`.venv`・uv・`nvcc`・装置） |
| B | 他台と同じ版で作った（Python・torch・torchvision） |
| C | 装置が使え、`mmcv` が読み込める（落ちた場合は原因と対処） |
| D | 検証に要るものが揃った |

---

## Task 2 (Phase A): 論理名と版管理の識別を設定する

**Files:** Modify: `~/.zshenv`, `~/.profile`

- [ ] **Step 1: 現状を確かめる**

**`SERVERNAME` が設定されているか。** `hostname` が何を返すか。

**本ホストの `hostname` は容器の識別子である。** 論理名を明示しなければ解決しない。

- [ ] **Step 2: 両方へ置く**

**`~/.zshenv` と `~/.profile` の両方に置く。**
前者は zsh の全形態、後者は bash のログイン時に読まれる。

**`scripts/sync/setup_host_servername.sh` がある。** 読んでから使うか、手で追記する。
**どちらにせよ追記内容を記録する。**

**論理名は `dlsta` である**（版管理の `scripts/sync/hosts/` の記載と、住所の対応による）。

- [ ] **Step 3: 新しいシェルで解決されることを確かめる**

**zsh と bash の両方で `dlsta` が出ること。**

- [ ] **Step 4: 版管理の識別を設定する**

**未設定である。commit の前に要る。**

**他台と同じ値を使う。** 版管理の記録から読むか、利用者へ確かめる。
**設定内容を記録する。範囲は repo の中でよい。**

| # | 完了判定 |
|---|---|
| E | 設定前の状態を記録した（`hostname` の値を含む） |
| F | 追記内容を記録した |
| G | 両方の形態で論理名が解決される |
| H | 版管理の識別を設定した（値の出所） |

---

## Task 3 (Phase B): 中心宛の鍵を作り、公開鍵を公開する

**Files:** Create: `~/.ssh/id_ed25519_dlstatophilip`, `scripts/sync/hub_keys/dlsta.pub`

**秘密鍵はこのホストから出さない。公開鍵だけを版管理へ置く。**

- [ ] **Step 1: 既存の鍵を確かめる**

**中心宛の鍵が既に在れば作らない。** 記録して次へ進む。

**`~/.ssh/` に `id_ed25519_github` が在る**（実測）。**これは別用途である。触らない。**

- [ ] **Step 2: 鍵を作る**

**合言葉は付けない。** 常駐処理が対話なしで使うためである。
**指紋を記録する。** これが中心の受け入れ一覧に入る値である。

- [ ] **Step 3: 権限を確かめる**

**秘密鍵は所有者だけが読める形、`~/.ssh` も同様であること。**

- [ ] **Step 4: 公開鍵を版管理へ置く**

`scripts/sync/hub_keys/dlsta.pub` へ置く。**指紋が Step 2 と一致すること。**

**置いたものが公開鍵だけであることを三つで確かめる。**

| 検査 | 期待 |
|---|---|
| 先頭 | `ssh-` で始まる |
| 秘密鍵の書き出し | **零件** |
| 行数 | **一行** |

**陽性対照**: 秘密鍵の書き出しを模した囮に同じ検査をかけ、**一以上を返すこと**を確かめる。
**囮は版管理へ入れない。**

| # | 完了判定 |
|---|---|
| I | 既存の鍵を確かめた（在れば作っていない） |
| J | 中心宛の鍵を作り、指紋を記録した（秘密鍵の中身なし） |
| K | 権限が期待どおり |
| L | 公開鍵を版管理へ置き、指紋が一致し、三つの検査と陽性対照を通った |

---

## Task 4 (Phase B): 同期処理を導入し、識別子を公開する

**Files:** Create: `~/bin/syncthing`, `scripts/sync/device_ids/dlsta.txt`

- [ ] **Step 1: 中心と同じ版を取得する**

**中心は `v2.1.3`。実行ファイルの要約値は
`e8a08fdd8b25340aae0c0a00ab131b293830e4ea47504d4b83a82f31b52b96c4`。**

**配布物の中に同名の別物が三件ある**（他台での実測）。**大きさで特定する。**

**取得できない、または要約値が中心と違う場合は停止して報告する。**

- [ ] **Step 2: 配置する**

**実行権は落としたままにする。まだ起動させない**（禁止 2）。

**起動の引き金は実行権だけである**（常駐処理が見る）。本ホストには常駐処理が無いため、
**いま実行権を戻しても起動はしない。** それでも**後続の契約の順序に合わせて落としておく。**

**要約値が中心と一致すること。**

- [ ] **Step 3: 識別子を発行する**

**設定と鍵を作る命令があるが、常駐はしない。**
**既に設定が在れば上書きしない。** 記録して次へ進む。

- [ ] **Step 4: 識別子を読み取り、公開する**

**取り方は版による**（前ホストの実測）。
**`v2.1.3` では下位命令として用意されている。** 実在するものを確かめてから使う。

`scripts/sync/device_ids/dlsta.txt` へ**一行で**置く。**識別子は秘匿ではない。**

- [ ] **Step 5: 起動していないことを確かめる**

**`22000` と `8384` が待ち受けていないこと。**

**待ち受けの一覧を取る道具が無い場合がある**（他台の実測）。**`/proc/net/tcp` から復号する。**

**プロセスの計数は自分と子を除く。** 部分一致は実行基盤を拾う。
**両方向の対照を取る。**

| # | 完了判定 |
|---|---|
| M | 配布物の要約値が中心と一致した |
| N | 配置物の要約値が中心と一致し、実行権が落ちている |
| O | 識別子を発行した（既存があれば上書きしていない） |
| P | 識別子を一行で公開した |
| Q | **同期処理が起動していない**（両方向の対照つき） |

---

## Task 5 (Phase C): 記述を訂正し、報告する

**Files:** Modify: `context/env-facts.md`
Create: `tasks/T-2026-09-20-dlsta-join-foundation/RESULT.md`, `result.yaml`,
`tasks/inbox.d/T-2026-09-20-dlsta-join-foundation.md`

- [ ] **Step 1: `env-facts.md` を訂正する**

**repo の位置に三種目が加わった。** 本ホストは `~/local/m2` である。

**参加するホストの一覧も変わる。** efros が加わり、本ホストも加わる見込みである。

**他の記述で本ホストと食い違うものがあれば、それも直す。** 直した箇所を記録する。

**`conventions.md` は触らない。**

- [ ] **Step 2: 報告を書く**

**`RESULT.md` は起票者が判断に使う事実だけ。目安 150 行以内。**
**手続きの証跡は `audit.md` へ置き、行番号で指す。**

| 節 | 内容 |
|---|---|
| 判定 | `verdict` と各 Gate の結果 |
| 完了判定 | 表。各項目に**実測値** |
| **後続で使う値** | **鍵の指紋、識別子、同期処理の版** |
| **規約の適用判定** | `proposal_gate` `folds` `symmetry` |
| 訂正した記述 | `env-facts.md` のどこを直したか |
| 起票者の誤り | 型と内容。一件 3 行以内 |
| 逸脱・想定外・UNKNOWN | — |
| 送出 | PR の番号と終了コードだけ |

- [ ] **Step 3: 検証を通し、秘匿を検査し、送出する**

`conventions_rev` を実測して置換し、検証を通す。**生成物を再生成しない**（禁止 5）。

**禁止語の検査を送出物に当てる。** 陽性対照を取る。

**秘匿検査は形で判定し、検査そのものが値を出力しない。陽性対照を取る。**

**変更が契約のディレクトリ、公開鍵、識別子、`env-facts.md` に限られること。**

commit・push・PR。**台帳へ返す。**

| # | 完了判定 |
|---|---|
| R | `env-facts.md` を訂正した（直した箇所） |
| S | 報告が指定の構成で、目安の分量に収まっている |
| T | **三つの規約の適用可否を記録した** |
| U | 禁止語の検査を送出物に当てた（陽性対照つき） |
| V | 秘匿検査を自分で行った（検査が値を出力していない。陽性対照つき） |
| W | 変更が契約の範囲に限られ、分岐が送出され PR が存在する |
| X | 報告が台帳へ返っている |

---

## 想定外が起きたときの扱い

| 事象 | 対応 |
|---|---|
| 仮想環境を作れない | **停止して報告。** 手順と出力を貼る |
| `mmcv` が落ちる | 原因を記録。**管理者権限が要るなら利用者へ提示して許諾を得る** |
| `nvcc` の検査で止まる | **飛ばす手段を使い、その旨を記録する。** 無ければ停止して報告 |
| 配布物の要約値が中心と違う | **停止して報告。** 版が揃わない |
| 識別子を読み取れない | **実在する下位命令を確かめる。** 何を見たかを記録する |
| **同期処理が起動した** | **記録して報告** |
| 版管理の識別の値が分からない | **利用者へ確かめる。推測で設定しない** |
| 論理名が両形態で解決されない | 置き場所を見直す |
| 規約が本契約に適用される | **従う。従った内容を記録する** |
| `env-facts.md` の他の記述も食い違う | **直して記録する** |

**言い訳をしない。事実と、測れなかったことを書く。**
