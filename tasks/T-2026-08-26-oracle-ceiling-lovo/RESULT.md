# 事実の記録 — T-2026-08-26-oracle-ceiling-lovo

**status: pass** — Phase A / B / C / D をすべて実行。**15 組の対 30 本すべてが完走し、漏れは 0 であった。**
host `lecun` / branch `feat/oracle-ceiling-lovo` / 起点 `phase0` / 証跡は `EVIDENCE.md`（行番号で指す）。

## 1. 判定

**本契約は判定を行っていない。** 出したのは効果量と、改善側へ倒れた個数と、先行契約との並びの 3 つである。

### 1.1 主たる成果 — 先行契約の値との並び（主終点 = accuracy）

| 何を測ったか | 基準 | Δaccuracy | 改善側へ倒れた個数 |
|---|---|---:|---|
| **一つ抜き検証（本契約）** | lecun・決定化あり・seed42・1 本ずつ・15 組の対 | **+0.008282**（中央値 +0.005267） | **11/15**（同値 2・悪化 2） |
| 検証側の分割（先行契約）基準A | 新規 oracle 1 本 − pred 既存平均 n=9 | +0.018849 | — |
| 検証側の分割（先行契約）基準B | 既存記録の種ごとの対 5 種の平均 | +0.021298 | 5/5 |
| 検証側の分割（先行契約）基準C | 既存記録の seed42 だけの対 | +0.016722 | — |

**縮んだ。** 一つ抜き検証の平均Δは、検証側の分割で得た値の **39〜50%**（基準B で 39%、A で 44%、C で 50%）である。
**改善側へ倒れた個数も 5/5 から 11/15 へ落ちた。**

事前登録が引用した代理モデルの一つ抜き検証は **10/15** であった。**本番の時系列モデルで 11/15 は、これとほぼ同じ水準である。**

→ 分け方ごとの内訳は `EVIDENCE.md` 238-262 行、基準の内訳は 24-48 行と 302-304 行。

**したがって、先行契約が検証側の分割で見た大きな追加利得は、分割に固有の現象である。**
既存の方針（工程認識のために検出器の強化へ投資しない）を覆す根拠は、本契約の実測からは得られていない。
**有意・非有意の判定は行っていない。**

### 1.2 なぜ分割で変わるのかの直接の実測

**予測側と正しい側の術具存在が食い違う割合は、split によって大きく違う。**

| split | 二値化して違う (frame,dim) | 割合 |
|---|---:|---:|
| train | 749 / 144855 | 0.52% |
| **val（＝検証側の分割）** | 657 / 22725 | **2.89%** |
| **test** | 5347 / 63975 | **8.36%** |

**val は test の 2.9 分の 1 しか食い違わない。** 凍結検出器は train で学習されているため train が最小なのは当然だが、
val と test の開きは説明を要する。SPEC §1.3 の「検証側の分割は術具と工程の対応が異常にきれい」を、
**検出器の側から裏づける実測である。**（`EVIDENCE.md` 134-147 行）

### 1.3 特定の動画が支配していないか

**主終点の accuracy は支配されていない。** どの 1 組を除いても平均Δは正のままで、最大の変化は 20% である。

    動画 07 を除く +0.006619（-20%） / 動画 12 を除く +0.006879（-17%） / 動画 10 を除く +0.007369（-11%）

**一方 macro-F1 は動画 12 に支配されている。** 動画 12（ΔmF1=+0.138447）を除くと平均が
+0.014511 から +0.005658 へ **61% 縮む。**（`EVIDENCE.md` 264-279 行）

### 1.4 工程の支持

**評価側に現れない工程は分け方ごとに違う。** 抜いた動画に現れる工程数は **4 から 7** まで幅がある。
macro-F1 は支持のある工程だけで平均されるため、**分母が分け方ごとに違う。**
分け方をまたいだ macro-F1 の平均は同じ土俵の平均ではない。
`disinfection` はどの分け方でも評価側に現れない。（`EVIDENCE.md` 281-303 行）

**主終点を accuracy に固定した事前登録の判断は、この点で整合している。**

## 2. 完了判定の表

| # | 判定 | 結果 | 空振りでないことの確認（実測） |
|---|---|---|---|
| a | 経路の有無を実測で確かめた | **無い**と確定 | 実在しない語 `zqx_absent_mechanism` で同じ 3 方法を回すと grep 0 / find 0 / git ls-files 0（E4 / 76-98 行） |
| b | 配線した内容を記録した | **達成** | 6 項目を列挙。差分は +62/−11 行（E5 / 100-120 行） |
| c | 評価側が学習側に含まれていない | **達成（30/30）** | 意図的に漏らすと検査が `['09']` を返す（E6 / 122-132 行） |
| d | 漏れの検査が働いている | **達成** | 陽性で検出・陰性で空集合。片方向では「常に空集合を返す壊れ方」と区別できない |
| e | 二つの腕で入力が違う | **達成** | 全体 2.92% の要素が違う。同じ腕を二度読むと完全一致（E7 / 134-147 行） |
| f | 所要時間を実測した | **達成** | 34〜49 秒（n=30、平均 38.0）。零も負も無い（E9 / 205-213 行） |
| g | 対の両側が同数そろっている | **達成** | 15 組 30 本。片側だけの腕を数えると 0 本（E10 / 215-236 行） |
| h | 実行の順序を記録した | **達成** | 30 行の表。対ごとに隣接して交互（pred→oracle）（E9 / 168-202 行） |
| i | 効果量と倒れた個数の双方を出した | **達成** | 7 指標すべてで平均Δ・中央値・pstd・改善側の個数（E11 / 246-262 行） |
| j | 先行契約の値と並べた | **達成** | 同じ表に 4 行（1.1）。基準の違いを明示した |
| k | 分け方ごとの内訳を出した | **達成** | 15 行の表と、1 組を除いたときの平均Δ（E11 / 238-279 行） |
| l | 判定を行っていない | **達成** | 本書に「有意」「非有意」の語は現れない。abs(Δ)/σ を計算していない |
| m | 上限の腕に測定専用の印がある | **達成** | 印の無い oracle run は 0 本（E10 / 219 行） |
| n | 既存の分割が書き換えられていない | **達成** | 1 行足すと diff が 0→8 行に増え、戻すと 0（E13 / 305-315 行） |
| o | 陽性対照の証跡を残していない | **達成** | CLI に漏れを起こす経路を足さず、関数を直接呼んだ。run は生成していない |
| p | 変更範囲の一覧を出した | **達成（検査は fail）** | 231 件中 5 件を違反としていない（E14 / 317-349 行） |
| q | PR が存在し base が正しい | **§7 参照** | 起点は `phase0`。既定の `master` ではない（E17 / 373-385 行） |
| r | 分岐が規定の接頭辞で始まる | **達成** | `feat/oracle-ceiling-lovo` は `feat/` で始まる |
| s | 報告が二つに分かれている | **達成** | 本書（事実）と `EVIDENCE.md`（証跡）。行番号で指し、同じ内容を二度書いていない |
| t | 締切に対する経過が記録されている | **達成** | 各 Phase の入口の経過を表にした（E1 / 8-22 行） |

## 3. 次の契約で使う値

- **一つ抜き検証の Δaccuracy**: 平均 +0.008282 / 中央値 +0.005267 / pstd 0.010832 / 範囲 −0.007182〜+0.031560 / 改善側 11/15。
- **一つ抜き検証の ΔmacroF1**: 平均 +0.014511（動画 12 を除くと +0.005658）/ 改善側 11/15。**動画 12 に支配されている。**
- Δjaccard +0.020975（13/15）/ Δedit +3.905476（8/15）/ Δseg-F1@10 +0.032564（9/15）/ @25 +0.057288（12/15）/ @50 +0.045762（9/15）。
- **学習 1 本の所要時間（LOVO の経路）**: 34〜49 秒、平均 38.0（n=30）。
  **先行契約の標準 split での 25〜34 秒より長い。** 学習側の clip が 13 から 21 へ増えるためである。
- **予測側と正しい側の食い違い**: train 0.52% / val 2.89% / test 8.36%。
- 分け方ごとの macro-F1 の分母は 4〜7 で一定でない。`disinfection` はどの分け方でも評価側に現れない。
- **PR の base は `phase0`。既定の `master` ではない。**

## 4. 起票者の誤り

3 件。詳細は `result.yaml` の `issuer_defects`。要点のみ。

1. **`self_contradiction`** — `outputs.expected_runs: 32` / `resources.runs: 32` と、データが決める分け方の数（動画 15 本 → 15 組 30 本）が合わない。
2. **`check_does_not_check`** — 契約が定める漏れの検査は**動画の集合しか見ない。**
   最良 epoch は**抜いた動画の正解**で選ばれており、その経路は検査されない。
   指示どおり実行すると「漏れ 0」と報告しながら、抜いた動画のラベルが epoch 選択に使われる。
   **両腕に等しくかかるため対の差は受けにくいが、絶対値は楽観側に寄る。**
3. **`self_contradiction`** — §1.2 で「比較できない二つを並べていた」ことを起票者の誤りとしながら、
   Step 2 で並べよと求める先行契約の値が、**同じ基準で作られていない。**
   指示どおり並べると基準の違う二つを並べることになる。**3 通りの基準を明示して併記することで緩和した。**

**正しかった前提**: 申し送り 1（本番側に経路が在るか未確認）は的中し、実際に無かった。
申し送り 2（代理側の仕組みが読む入力は本番の蓄えそのもの）も的中し、配線は軽く済んだ。
申し送り 3（所要時間が同じとは限らない）も的中し、25〜34 秒ではなく 34〜49 秒だった。
SPEC §7 の「検証の警告が母集団の件数の食い違いを報告する」も的中した。

## 5. 逸脱

7 件。詳細は `result.yaml` の `deviations`。**逸脱は無しではない。** 要点のみ。

1. 本番側に経路が無かったため、SPEC Task 2 Step 3 が許可する範囲だけを配線した（+62/−11 行）。
   **学習の数式・損失・最適化、既存の分割ファイル、凍結源には触れていない。**
2. `expected_runs` を 32 から 30 へ直した（起票者の回答に従う）。
3. 漏れの検査の陽性対照は、CLI に漏れを起こす経路を足さず `check_no_leak` を直接呼んだ（禁止 9 のため）。
4. **禁止 5 に従い `make taskindex` と `make inbox` を回していない。** 全 PR 統合後に一台で回すこと。
5. **同じ作業ツリーで別契約が同時に走っており、その未追跡ファイル 16 件に触れていない。**
   SPEC Task 1 Step 2 は「汚れていれば退避」と述べるが、退避すると実行中の作業を壊す。mtime で活動中を確かめた。
6. 検証側分割との並びは、同じ基準の対が存在しないため 3 通りの基準を併記した。**新しい run は足していない。**
7. 決定化の対照 3 本は `--no-evidence` で走らせ、`experiments/` を汚していない。

## 6. 想定外

- **検証の WARN**: L2-8 が 2 件（先行契約と同一）。SPEC §7 の指示どおり参照先を確かめ、
  `n_runs=17 / accuracy_mean=0.8973014948553679` と**先行契約時と完全に同一**で移動していないことを実測した（E2 / 24-48 行）。
- **プロセスの絞り込みが両方向とも空振りした**。`pgrep -f` は陰性対照 `zzz_no_such_token_xyz` に 2 を返し、
  `/proc` の cmdline 走査も陰性対照 `qqq_absent_marker` に 1 を返した。**自分の命令行に語が含まれるためである。**
  `conventions#issuer_cautions` 注意 6 と同型で、**判断は mtime に拠った**（E14 / 337-347 行）。
- **プリフライトの SKIP 2 件は合格ではない。** P3 は検査器では決定化を確かめられないことを意味し、
  事前登録 §4 の対照で埋めた（E8 / 149-162 行）。
- **先行契約の報告の誤りを 1 件見つけた。** PR #152 の base は `master` ではなく `phase0` で、
  すでに MERGED であった。**先行契約の報告で「master 宛」と書いたのは誤りである**（E17 / 373-385 行）。

## 7. 送出と PR

- **PR: https://github.com/takuya3h/m2/pull/156**（`feat/oracle-ceiling-lovo` → **`phase0`**）。送り出しただけの状態ではなく PR が存在する状態で終えている。base は起点と同じ分岐であり、既定の `master` ではない。分岐は規定の接頭辞 `feat/` で始まる（E17 / 373-385 行）。
- commit: `9563ce5169bf305a245d6fef007369c2ed5c7ebf`（事前登録の記録）/ `ee93885e329f2566c094d74185f53c97eb81aa3b`（配線・30 run の証跡・本報告）。
- **本契約では触れていない未追跡ファイル**: `experiments/analysis/error_shape_selectivity/` 7 件と `experiments/analysis/lovo_decision_rule/` 9 件は同時に走る別契約の生成物であり、commit に含めていない。
- 台帳への送出: 下記「送出の結果」を参照。

### 送出の結果

`make task-report` は **exit 0 で送出に成功した。** 台帳の応答:

    {"task_id": "T-2026-08-26-oracle-ceiling-lovo", "verdict": "pass",
     "n_issuer_defects": 3,
     "report_sha256": "38630f280780957954f8b2a3e33068656dfda7d56cd527338b37c4a9d88a0fab",
     "report_bytes": 12830, "replaced_blocks": 0}

`source scripts/load_env.sh` はパイプに繋がず同じ命令の中で実行した。
**先行契約でパイプが副シェルを起こして export が消えた事故を繰り返さないためである。**
秘匿の検査は無効にしていない。外部への送信は `make task-report` の 1 経路のみで行った。
