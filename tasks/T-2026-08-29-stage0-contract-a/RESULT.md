# RESULT — T-2026-08-29-stage0-contract-a

**kind:** `analysis` / ホスト `philip` / 分岐 `feat/stage0-contract-a` / **GPU 不使用**

証跡は `audit.md`、全表は `docs/stage0/`。**同じ内容を二度書かない。**

**`inputs.data.split_files` は様式を満たすための記載であり、本契約は分割を参照していない**
（A4 は既存の分割ファイルの train 内で測り、**再定義していない**）。

---

## 判定

**verdict: pass**（八項目すべて実測済み、UNKNOWN は A3 のみ理由つき）

| Gate | 結果 | 根拠 |
|---|---|---|
| G1 | **pass** | 各項目の入力の所在を実測し、実施可否を確定（`audit.md:87-111`、全表 `docs/stage0/A0_data_inventory.md`） |

---

## 完了判定

| # | 判定 | 実測 | 空振りでないことの確認 |
|---|---|---|---|
| a | 網羅 | **八項目すべてが埋まっている**。A3 のみ理由つき UNKNOWN（属性情報が存在しない） | 下の項目別表に空欄なし。A3 の理由は Step A-2 の実測（`surgeon_folds.json` が 3 バイトの `{}`）に対応 |
| b | A7 の結論 | **「実在の run に遡れない」**（三値のうち）。根拠 run と metrics の有無は `docs/stage0/A7_k1_provenance.md` | 陽性対照 **1 件一致**（`0.8973014948553679`）／陰性対照 `-0.0202`・`-0.0200` とも **0 件**（`audit.md:148-161`） |
| c | 出所 | 無作為の三数値すべてが出所へ遡り、**独立に再計算して一致** | 1352（A1）・7.2284（A5）は再計算で一致、248（A6）は基準を成果物に明記（`audit.md:198-206`） |
| d | 変更範囲 | `docs/stage0/` と契約ディレクトリのみ。**対象外 0 件** | 全量を `audit.md:219-225` に記載 |
| e | 読み取り専用 | `experiments/` `data/` `runindex/` の変更 **すべて 0 件** | 同じ検査を `docs/stage0`・契約ディレクトリへ当てると **各 1 件**出る（`audit.md:208-217`） |
| f | 退避の処分 | drop 前 **3 件** → 後 **0 件** | 前後の一覧を `audit.md:20-47` に記載 |

---

## 項目別の要点

| 項目 | 要点 | 成果物 |
|---|---|---|
| **A1** 折りの材料 | 15 動画 / 23 クリップ / 工程 **9** 種 / 術具 **15** クラス。総フレーム **17,233**、術具アノテ **49,652**。**HTS 3 系統は全 15 動画を被覆** | `docs/stage0/A1_fold_material.md` |
| **A2** 動画ID重複 | 追加 6 動画（**17-22**）と公式 test（04,05,07）の重複 **0 件**。train・val とも 0 件。ID 空間が互いに素 | `docs/stage0/A2_id_overlap.md` |
| **A3** 術者・症例重複 | **UNKNOWN（属性情報が存在しない）**。`data/splits/surgeon_folds.json` は scaffold 以来 **3 バイトの `{}`**、追加動画側も該当キー 0 件 | `docs/stage0/A3_attribute_overlap.md` |
| **A4** クリップID識別 | region-token（3840 次元）**0.9712**／在否（15 次元）**0.6319**。偶然水準 0.0769（13 クラス）。**判定に変換していない** | `docs/stage0/A4_clipid_probe.md` |
| **A5** H(tool\|phase) 上界 | **6.3425 ビット**（底 2・平滑化なし・実測）。無条件は 8.6570、差 **2.3145**。欠損 0 件 | `docs/stage0/A5_entropy_bound.md` |
| **A6** 検出誤りの分解 | 誤り **424 件**（GT の 9.01%）。内訳 **分類誤り 58.5% / 局在誤り 29.7% / 見落とし 11.8%** | `docs/stage0/A6_error_decomposition.md` |
| **A7** K1 出所照合 | **実在の run に遡れない**（下記） | `docs/stage0/A7_k1_provenance.md` |
| **A8** Cholec80 | **philip に存在しない**。L0 代理は未実施 | `docs/stage0/A8_cholec80.md` |

---

## A7 の結論

**三値のうち「実在の run に遡れない」。**

| 系統 | 実測 | 一致 |
|---|---|---|
| 1 無効判定の aligndetr 群 | `n_runs=3` / **`n_runs_excluded=3`（全件除外）**。Δ = **−0.050877**（台帳 −0.0201 と 0.0308 乖離）。hemostasis F1 は 0.0 / 0.1667 / 0.0（全て `excluded=True`） | ✗ |
| 2 作り直し版 | 索引に `aligndetr` を含む experiments 行は **2 件のみ**。**区別できる別行が存在しない**。`baselines/s0/aligndetr_bbox@val` は `accuracy_mean` が空 | ✗ |
| 3 それ以外 | hemostasis F1 = 0.179 の一致 **1 件**。ただし `transfer/b2a_ro_oracle_noise000` seed456 の値で **AlignDETR と無関係**。対になる 0.801 も伴わない | 偶然の一致 |

**AlignDETR の検出性能 0.686 に対応する値は索引から特定できない。**

### K1 を実測扱いにできるか

**できない。** 許容差（Δ は ±5e-5、F1 は ±5e-4。いずれも表示桁の丸め幅）を明示した照合で、
**索引 4 種のどの run にも遡れなかった。**

**複数系統が同じ数値を持つ状況ではないため、escalate の条件には当たらない。**

---

## 実測（次の契約で使う値）

| 値 | 実測 |
|---|---|
| `runindex_commit` / `conventions_rev` | `7918b5dd` / `a8c07e81`（事前記入と一致・置換不要） |
| 索引の件数 | index 1177 / experiments 213 / verdicts 1038 / **per_class 8370** |
| 追加動画 | **17, 18, 19, 20, 21, 22 の 6 動画 / 23 クリップ**（16 は phase CSV が無い） |
| 術者・症例の属性 | **存在しない**（`surgeon_folds.json` は 3 バイトの `{}`） |
| 方針文書 v2 | **リポジトリに未配置**。README の相対リンク 36 件はすべて実在 |
| Cholec80 | **philip に無い** |
| A6 の基準 | IoU **0.5**（既存実装 `detection.py:184-226` 由来）。IoU 0.1 と score 0.05 は**実行者が置いた値** |
| 退避 | **0 件**（3 件とも drop 済み） |

---

## 起票者の誤り

1. **`asserted_without_measuring`** — SPEC §1「philip には退避が**二件**残っている」。
   実測は**三件**だった。三件目は本契約の取り込み直前に実行者が作ったもので、
   起票時点では存在し得ない。**「二件」を前提に drop すると一件残る。**
2. **`asserted_without_measuring`** — SPEC §0 は方針文書を「統合済みの README から相対リンクで
   辿れる位置に**あるかは未確定**」とするが、実測では**リポジトリに一切存在しない**
   （`research_policy_v2*` のヒット 0 件）。「未確定」ではなく「未配置」が正しい。

---

## 逸脱・想定外・UNKNOWN・判断待ち

### 逸脱

1. **開始前から在った汚れ（`.sync-pause.released` 1 件）を退避してから進めた。** `mv` は使っていない。
2. **`decisions_required` 2 件を利用者へ提示して回答を得た。** ただし**両方とも実測が出てから
   でないと答えられない**性質だったため、Phase A/B を先に進めてから提示した。自分では決めていない。
3. **A6 で既存実装に無い値を二つ置いた**（IoU 下限 0.1、score しきい値 0.05）。
   SPEC は「新しい基準を発明しない」と定めるが、既存実装が持つのは IoU 0.5 のマッチ基準のみで、
   **「見落とし」と「局在誤り」を分ける下限が存在しなかった**。実行者の判断として成果物に明記した。

### 想定外

- **A7 で数値が一件も一致しなかった。** SPEC は三系統を試すよう定めており全て試したが、
  結論は「実在の run に遡れない」となった。**照合器の対照は両方向とも働いている。**
- **`b2a_ro_oracle_noise000` の hemostasis F1 が偶然 0.179 に一致した。**
  AlignDETR とは無関係で、対になる 0.801 も伴わない。**偶然の一致として退けた。**

### UNKNOWN

| 項目 | 理由 |
|---|---|
| **A3** 術者・症例の重複 | **属性情報が存在しない**（`surgeon_folds.json` が空、追加動画側もキー 0 件）。存在しないこと自体を実測として報告 |
| AlignDETR の検出性能 0.686 の所在 | `baselines/s0/aligndetr_bbox@val` の `accuracy_mean` が空で、索引から特定できない |

### 判断待ち

| 対象 | 状態 |
|---|---|
| Cholec80 の取得 | **取得しない**と回答済み（2026-08-29）。A8 は実体確認のみで閉じた |
| A7 の結論の方針文書への反映 | **本契約では反映しない**と回答済み。材料のみ提供。方針文書自体がリポジトリに未配置 |
| A9（工程塔候補の文献値） | **起票者が行う**（SPEC §0）。実行者は触れていない |

---

## 送出

（この節は commit・PR の後に埋める）
