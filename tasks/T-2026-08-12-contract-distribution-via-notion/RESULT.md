# RESULT — T-2026-08-12-contract-distribution-via-notion

**実行者:** `lecun` / `feat/notion-distribution` / `origin/phase0` の `dc13196` から分岐
**実行日時:** 2026-08-10T13:49Z 〜 2026-08-10T14:33Z（1 回目・G1 で停止）／
2026-08-10T14:33Z 〜 2026-08-10T15:00Z（2 回目・完走）
**判定:** **PASS** — **G1 / G2 / G3 すべて通過。**識別子だけで契約を取り込めるようになった。

| 受入基準 | 結果 |
|---|---|
| 実行ホストの資格情報で台帳を照会できる | ✅ HTTP 200 |
| 書き込みと読み戻しで内容が変わらないことが要約値で確認されている | ✅ **sha256 が完全一致** |
| 識別子だけを与えて契約を取り込める | ✅ `make task-notion TASK=…` が exit 0 |
| 取り込みに失敗した場合に作業領域へ痕跡が残らない | ✅ 陰性 3 件とも件数不変・差分 0 |
| 手元に契約が無いとき自動で取得する経路が手順書に書かれている | ✅ `SKILL.md` と `tasks/README.md` |
| 資格情報が出力にも記録にも含まれない | ✅ 実際の値で照合して 0 件 |
| `make task-validate` が exit 0 | ✅ |
| `make task-preflight` が exit 0 | ✅ 4 PASS / 4 SKIP / 0 FAIL |

---

## 1. 解決された参照

| 項目 | spec の記載 | 解決結果 |
|---|---|---|
| `inputs.denominator.ref` | **記載なし** | 対象外 |
| `inputs.sigma_policy` | **記載なし** | 対象外 |
| `inputs.frozen_source.ref` | **記載なし** | 対象外。preflight の `P5` も `kind=impl` のため SKIP |
| `contract.conventions_rev` | `1201f4f` | **`d422b08` へ実測置換**（SPEC Task 5 Step 1 の手順に従う） |
| `contract.inject_verbatim` | `conventions#prohibitions` | 下記に原文を転記 |

### `conventions#prohibitions`（原文）

```
<a id="prohibitions"></a>
## prohibitions

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |
```

### `conventions_rev` の差分

`1201f4f` → `d422b08` は **+10 / −0**。差分ハンクは L56（`frozen_source` 節）と L143（変更履歴）の 2 箇所。
**原文注入する `prohibitions`（L98–108）は無変更。**

---

## 2. ゲートの通過状況

| gate | 判定 | 実測 |
|---|---|---|
| **G1**（after A） | **PASS**（2 回目） | `databases/{DB}` と `query` がともに HTTP 200 |
| **G2**（after B） | **PASS** | 5364 バイトの本文が往復で **sha256 完全一致**。陽性対照も働いた |
| **G3**（after C） | **PASS** | 正常系が exit 0、陰性 3 件が非ゼロ、件数不変・差分 0 |

---

## 3. Phase A — 到達性

### 3-1. 既存実装から取った値（推測していない）

| 項目 | 値 | 出所 |
|---|---|---|
| API の版 | `2022-06-28` | `src/egosurgery/utils/notion_logger.py:35` の `NOTION_VERSION` |
| 基底 URL | `https://api.notion.com/v1` | 同 `:36` の `_API_BASE` |
| rich_text の上限 | **2000 文字 / 1 要素** | 同 `:337` の `_RICH_TEXT_LIMIT` |

SPEC が「推定であり、食い違えば実装に従う」とした版は、**実装と一致していた。**

### 3-2. 到達性 — **共有操作が必要だった**

**1 回目の実行（13:49Z）では到達できず、G1 で停止した。**

| 照会 | 1 回目（13:55Z） | 2 回目（14:35Z） |
|---|---|---|
| `databases/{database id}` | **404 `object_not_found`** | **200** |
| `databases/{DB}/query` | **404** | **200** |
| `databases/{data source id}` | **404** | — |
| `data_sources/{DS}`（版 2025-09-03） | **404** | — |
| `/v1/search` の database 件数 | **0** | 100 件中に「TASK配布」あり |

1 回目の停止時に**利用者へ共有操作を依頼**し、その後に統合へ台帳が接続された。
2 回目の実行では `databases/{DB}` が HTTP 200 を返し、名前「TASK配布」と
SPEC 記載どおりの列（`task_id(title)` / `status` / `kind` / `issued_at` /
`sha256` / `bytes` / `target_host` / `note`）を確認した。

**この経路は共有設定に依存する。** 実行側からは復旧できない。

---

## 4. Phase B — 往復の忠実性（本 task の要）

### 4-1. 検査用の本文

壊れやすい要素を意図的に含めた。

| 含めた要素 | 実際に入れたもの |
|---|---|
| 行末の空白 | `行末に空白がある行   `（3 個） |
| 連続する空行 | 2 行 |
| 引用符とバッククォート | `"` `'` `` ` `` |
| markdown の記号 | `# - \| * _ ~ >` |
| 日本語と英数字の混在 | `日本語と ASCII が mixed な行 12345` |
| 2000 字を超える連続した本文 | `A` × 2500、数字列 2500 |
| 長い区切り文字 | `PROBEDELIM` + 40 桁 |

| 項目 | 値 |
|---|---|
| バイト数 | **5364** |
| 文字数 | 5288 |
| 行数 | 11 |
| sha256 | `ac941ae3ad753decce903adb14cc94ebd8deb7596d576075295ea18343bddfdb` |

### 4-2. 格納形式と分割・再結合の方法

**符号化していない。本文をそのまま格納した。** base64 等へ切り替える必要はなかった。

| 項目 | 方法 |
|---|---|
| 格納先 | ページ本体の **`code` ブロック 1 個**（`language: "plain text"`） |
| 分割 | 本文を **2000 文字**ごとに切り、`rich_text` の要素として並べる（既存実装の `_RICH_TEXT_LIMIT` に合わせた） |
| 送信時の分割数 | **3**（2000 / 2000 / 1288 文字） |
| 再結合 | 全 `rich_text` 要素の `plain_text` を**単純連結**する |
| ブロックの頁送り | `has_more` / `next_cursor` に対応（試験で検査） |

**再結合が単純連結で足りる根拠は実測にある。** 送信時は 3 要素に分けたが、
**読み戻すと 1 要素（5288 文字）に結合されて返った**（`rich_text 要素数: 1`）。
分割は送信側の制約に過ぎず、境界位置を覚える必要はない。

### 4-3. G2 — 照合の結果

| 項目 | 元 | 読み戻し |
|---|---|---|
| バイト数 | **5364** | **5364** |
| sha256 | `ac941ae3ad753dec…` | **`ac941ae3ad753dec…`** |
| **一致** | — | **True** |

**行末の空白・連続空行・引用符・バッククォート・markdown 記号・2500 字の行・
日英混在のいずれも改変されなかった。** `roundtrip_mismatch` は発生していない。

### 4-4. 陽性対照

同じ長さのまま 1 文字だけ変えた本文（`12345` → `12346`）で要約値が変わることを確認した。

| 検査 | 結果 |
|---|---|
| 改変を検出できるか | **True** |
| 元と改変後の長さ | 5364 / 5364（**長さでは区別できない**） |

**長さが同じでも検出できる。** この照合は空振りしていない。

---

## 5. Phase C — 取り込み経路

### 5-1. 既存経路の再利用（要件 6）

`read_source()` の docstring が拡張点を明示していた。

> 入力の取得方法だけがここで分かれる。取り込みの流れ（一時ディレクトリへの展開・
> 設置・検証・巻き戻し）は取得方法によらず共通であり、複製しない。

したがって **`read_source()` に取得元を 1 つ足すだけ**とし、`fetch()` には触れていない。
一時展開・アトミックな名前の確保・`make task-validate` の実行・`BaseException` 捕捉での
巻き戻し・SIGTERM 処理は**すべて既存の実装がそのまま動く。**

    make task-notion TASK=<task_id>
      → fetch("notion:<task_id>")
      → read_source() が配布台帳から取得（ここだけが新規）
      → 以降は --src と完全に同じ経路

### 5-2. 実装した要件

| # | 要件 | 実装 |
|---|---|---|
| 1 | 登録簿から配布先を読む | `_notion_database_id()` が `configs/notion.yaml` の `databases.task_distribution` を読む。**コードへ直書きしない** |
| 2 | 資格情報が無ければ明確な理由で失敗 | `NOTION_API_KEY` 未設定なら `BundleError`。黙って進まない |
| 3 | 識別子で行を照会し本文を取得 | title 列 `task_id` の完全一致で照会 |
| 4 | 要約値を計算し列と照合 | 取得した本文の sha256 と `sha256` 列を比較 |
| 5 | 一致しなければ受け付けずに失敗 | 両方の値を出して `BundleError` |
| 6 | 既存の取り込み経路へ渡す | §5-1 のとおり。**複製していない** |
| 7 | 資格情報を出力しない | 見出しにのみ載せ、例外文にも含めない（試験で検査） |
| 8 | 頁送りに対応 | `has_more` / `next_cursor` を辿る（試験で検査） |

### 5-3. 自分で決めた 2 点（SPEC が定めていない）

| 状況 | 決めた振る舞い | 理由 |
|---|---|---|
| 同じ識別子の行が複数ある | `status = superseded` を除外し、**なお複数なら失敗**（件数と対処を提示） | 黙って 1 件目を選ぶと取り違える |
| `sha256` の列が空 | **拒否する** | 「照合できない」を「照合に通った」と扱うと、改変が素通りする |

### 5-4. 試験（先に書いて失敗を確認した）

外部サービスへは接続しない。HTTP を担う `_notion_call` だけを差し替えている。

実装前: **7 件すべて ERROR**（`AttributeError`）。実装後: **7 件すべて通過**。

| 試験 | 検査する内容 |
|---|---|
| `test_task_id_locates_the_row` | 識別子で行を特定できる |
| `test_unknown_task_id_fails_with_reason` | 見つからない識別子は理由付きで失敗 |
| `test_sha256_mismatch_is_rejected` | 要約値が合わない本文を受け付けない |
| `test_missing_credential_fails_with_reason` | 資格情報が無ければ理由付きで失敗 |
| `test_paginated_blocks_are_concatenated` | 頁送りされた本文を正しく再結合する |
| `test_read_source_routes_to_the_shared_intake` | 取得方法だけが分かれ、取り込みは共通経路 |
| `test_credential_is_not_included_in_the_error` | 失敗の理由に資格情報を混ぜない |

### 5-5. G3 — 実地の確認

**正常系と陰性 3 件の両方を測った。**

| 検査 | 期待 | 実測 |
|---|---|---|
| **正常系**（`make task-notion TASK=T-2026-08-12-probe-intake`） | 取り込める | **exit 0**。設置され `make task-validate` も通過。件数 22 → 23 |
| 陰性1: 存在しない識別子 | 非ゼロ | **exit 2**「配布台帳に契約が見つかりません: T-2099-01-01-no-such-task」 |
| 陰性2: 資格情報を外す | 非ゼロ | **exit 2**「NOTION_API_KEY が未設定です。source scripts/load_env.sh を先に実行してください」 |
| 陰性3: 要約値を誤った値へ | 非ゼロ | **exit 2**。台帳の記載と取得した本文の**両方**を提示して拒否 |
| 件数 | 不変 | **22 → 22** |
| 作業領域 | 清潔 | `git status --porcelain tasks/` が **0 件** |

陰性3 では台帳の `sha256` を一時的に `0` × 64 へ書き換え、`try/finally` で**必ず復元**した
（復元後に読み直して一致を確認済み）。

`make` はレシピ失敗時に自身の終了コード 2 を返すため、非ゼロは 2 として現れる。

---

## 6. Phase D — 手順書と検査用の行

| ファイル | 変更 |
|---|---|
| `.claude/skills/task/SKILL.md` | 手順 1「読む」へ、契約が手元に無い場合の取得を追加 |
| `tasks/README.md` | 「契約の受け取り」へ 3 つ目の経路を追加。往復の実測値も併記 |

`.codex/skills/task` は `.claude/skills/task` への symlink であり、**1 箇所の更新で両方に反映**される
（`.codex` 側でも `task-notion` が 1 件見つかることを実測）。

検査用の 2 行は **`status = superseded` にして残した。削除していない。**

| 台帳の行 | status |
|---|---|
| `T-2026-08-12-probe-roundtrip` | superseded（往復の忠実性の証拠） |
| `T-2026-08-12-probe-intake` | superseded（取り込み経路の証拠） |
| （無題・起票者が作成済み） | 変更していない |

---

## 7. 貼り付けによる配布を残すかどうかの所見

**残すべきである。ただし主経路は台帳へ移してよい。**

台帳経由が使えることは実測で確認できた（G2 で 1 バイトの改変も無し）。
一方で **この経路は共有設定に依存し、実行側からは復旧できない。**
実際、本 task の 1 回目は 404 で停止し、利用者の操作を経て 2 回目で通った。

| 経路 | 強み | 弱み |
|---|---|---|
| 配布台帳（`make task-notion`） | 端末が要らない。要約値で改変を検出できる | 共有設定に依存。外部サービスの状態で止まる |
| 貼り付け（`make task-paste`） | 外部サービスに依存しない | 端末の操作が要る |
| ファイル / URL（`make task-fetch`） | 同上 | 同上 |

**3 経路とも取り込み・検証・巻き戻しは共通の実装を通る。** 経路を残す費用は
`read_source()` の分岐 1 つ分でしかない。台帳が使えない日に配布が止まることの方が高くつく。

---

## 8. 完了判定

| # | 判定 | 期待 | 実測 |
|---|---|---|---|
| 1 | 台帳へ到達できる | HTTP 200 | ✅ 200（**共有操作の後**） |
| 2 | 往復で内容が変わらない | 要約値が一致 | ✅ `ac941ae3…` が完全一致 |
| 3 | 照合が空振りでない | 陽性対照が検出する | ✅ 同じ長さの 1 文字改変を検出 |
| 4 | 識別子だけで取り込める | 成功する | ✅ exit 0 |
| 5 | 存在しない識別子を拒む | 非ゼロ | ✅ exit 2 |
| 6 | 資格情報が無いとき拒む | 非ゼロ | ✅ exit 2 |
| 7 | 要約値が合わないとき拒む | 非ゼロ | ✅ exit 2 |
| 8 | 失敗時に痕跡が残らない | 件数不変・清潔 | ✅ 22 で不変 / 差分 0 |
| 9 | 巻き戻しを複製していない | 既存経路を呼んでいる | ✅ `fetch()` 無変更。`read_source()` に分岐 1 つ |
| 10 | 資格情報が出力に無い | 検査で 0 件 | ✅ **実際の値**で追跡 11 ファイルを照合し 0 件 |
| 11 | 手順書が新しい経路を指す | 該当あり | ✅ `README.md:67` / `SKILL.md:32`（`.codex` 側にも反映） |
| 12 | 登録簿に配布先がある | 1 件 | ✅ `configs/notion.yaml:20` |
| 13 | 契約検証が通る | exit 0 | ✅ |
| 14 | 実行前検査が通る | exit 0 | ✅ 4 PASS / 4 SKIP / 0 FAIL |
| 15 | 試験が不変 | 開始前と比較 | ✅ **前 5 failed, 264 passed → 後 5 failed, 271 passed**。失敗テスト名も同一 |
| 16 | 禁止領域が無変更 | 出力なし | ✅ 出力なし |

**判定15 の基準点（やり直し開始前・2026-08-10 14:34 実測）**

```
FAILED tests/test_engines.py::test_mmdet_trainer_eval_recipe_in_metrics
FAILED tests/test_research_logger.py::test_log_run_idempotent
FAILED tests/test_research_logger.py::test_run_logging_invokes_log_run_on_finally
FAILED tests/test_research_logger.py::test_run_logging_no_double_post_on_normal_exit
FAILED tests/test_research_logger.py::test_run_logging_swallows_exception_in_user_block
5 failed, 264 passed, 22 warnings in 24.16s
```

**失敗は 5 件のまま増えていない。** 通過が 264 → 271 に増えたのは、本 task が追加した 7 試験である。

**判定10 の検査方法**: 語で探すのではなく、**実際の資格情報の値そのもの**を検索語にして、
本 task が変更した追跡ファイル 11 件を照合した。過去に無関係な語への一致で偽陽性が出ているため、
値そのものを使う方が確実である。

### preflight で SKIP された項目（合格ではない）

| 項目 | 理由 |
|---|---|
| `P2 cuda_ext_loaded` | `plan.env.preflight` に記載なし → **未実施** |
| `P3 deterministic_flags` | `plan.env.preflight` に記載なし → 未実施 |
| `P4 prereg_committed` | `kind=impl` のため対象外 |
| `P5 frozen_source_hash` | `kind=impl` のため対象外 |

---

## 9. deviations（指示書どおりにしなかった箇所）

### D-1. 資格情報の長さを出力しなかった

- **指示:** Phase A Step 2 は `f"(長さ {len(k)})"` を出す
- **実際:** **有無のみを出した。**
- **理由:** 禁止事項 1 は「資格情報を出力・記録する（**存在と有無のみ**扱う）」と定める。
  長さは有無を超える情報であり、禁止事項の方が具体的である。
- **分類:** **SPEC の欠陥**（本文と検査コマンドが食い違っている）

### D-2. SPEC の到達性の探査が database id しか試していなかった

- **指示:** Phase A Step 3 の探査は `databases/{DB}` と `databases/{DB}/query` のみ
- **実際:** SPEC は **data source id も明示している**のに探査が使っていない。
  1 回目の停止時に、data source id と新しい API 版（`2025-09-03`）も試した。
  いずれも 404 で結論は変わらなかったが、試さなければ
  「識別子の指定が違うだけかもしれない」という可能性を潰せなかった。
- **分類:** **SPEC の欠陥**

### D-3. G3 の正常系を確かめるため、検査用の契約を作った

- **指示:** Phase C Step 5 のコマンドは**陰性対照 3 件のみ**を示す
- **実際:** G3 の check は「識別子だけを与えて契約を**取り込め**、失敗時に痕跡が残らないこと」で
  あり、**取り込めることの確認が含まれる。** 陰性だけでは充足しない。
  最小の有効な契約（`T-2026-08-12-probe-intake`）を組み立てて台帳へ置き、
  実際に取り込めることを確認した。確認後に設置物を消し、台帳の行は `superseded` にした。
- **分類:** **SPEC の欠陥**（check と検査コマンドが食い違っている）

### D-4. SPEC が定めていない 2 つの振る舞いを決めた

- **実際:** §5-3 のとおり、「同じ識別子の行が複数」「要約値の列が空」の扱いを決めた。
  どちらも**失敗させる**側に倒した。
- **理由:** 黙って 1 件目を選ぶ・照合を省くのは、いずれも壊れた契約を取り込む経路になる。
- **分類:** **判断が必要だった**

### D-5. 試験結果を書き込み完了前に読んだ

- **実際:** 判定15 の比較を、背景で走らせた `pytest` の出力が書き終わる前に実行し、
  `FAILED` が 0 行に見えた。ファイルの状態を確認して測り直し、
  **`5 failed, 271 passed`** を得た。
- **注記:** 直前の契約でも同じ誤りをしている。**背景の処理は完了を確かめてから読む。**
- **分類:** **判断が必要だった**（自分で気付いて正した）

### D-6. 1 回目の停止記録を破棄して書き直した

- **実際:** 1 回目の実行は G1（HTTP 404）で停止し、その記録を PR #63 として起票していた。
  利用者の共有操作の後、**「やり直し。既存の result はすべて捨てて良い」**との指示により、
  同じ分岐で作業を続け、`RESULT.md` を本内容へ書き直した。
  1 回目の commit（`28d4065`）は履歴に残っている。
- **分類:** **判断が必要だった**（利用者の指示による）

### D-7. 利用者による `.env.gpg` の変更を本 PR に含めた

- **実際:** 作業ツリーに利用者が更新した `.env.gpg`（1608 → 1607 バイト）があった。
  **私の変更ではない**ため扱いを確認し、本 PR に含める判断を得た。
  台帳への到達はこの暗号文に依存するため、配らなければ他ホストで経路が機能しない。
  形式が `GPG symmetrically encrypted data (AES256 cipher)` であることは確認したが、
  **復号していないため中身は検証していない。**
- **分類:** **判断が必要だった**

### D-8. `conventions_rev` を実測値へ置換した

- **指示:** SPEC Task 5 Step 1 が「実行者が実測して置換する。**これは逸脱ではなく手順である**」と明記
- **実際:** `1201f4f` → `d422b08` に更新した
- **分類:** 手順どおり（記録のため列挙）

---

## 10. 未解決・申し送り

### 10-1. この経路は共有設定に依存する

§3-2 のとおり、1 回目は 404 で停止した。**共有が外れれば経路は即座に使えなくなり、
実行側からは復旧できない。** 貼り付けを残す理由でもある（§7）。

### 10-2. 他ホストでの動作は未検証

本 task が確認したのは lecun のみである。他ホストで `make task-notion` が動くには、

1. 本 PR が統合され、各ホストが取り込む
2. 各ホストで `source scripts/load_env.sh` が成立する
3. 更新後の `.env.gpg` が各ホストへ届く

の 3 つが揃う必要がある。**いずれも未検証である。**

### 10-3. 起票側の書き込み手順は用意していない

本 task が作ったのは**取り込み側**だけである。起票者が台帳へ行を作る手順
（本文を code ブロックへ入れ、`sha256` と `bytes` を埋める）は、
本 RESULT の §4-2 に実測として記してあるが、**道具にはしていない。**
`tools/fetch_task.py --pack` で組み立てたバンドルを手で貼る運用になる。

### 10-4. 台帳の既存の無題行は素性が不明

台帳には起票者が作成した無題・`status` 無しの行が 1 件ある。
禁止事項 2 に従い**触れていない**。用途は `UNKNOWN`。

---

## 11. 数値の出所

**すべての数値は本ホスト（lecun）での実測である。**
測定できなかった項目（他ホストでの動作、`.env.gpg` の中身、無題行の用途）は
**未検証または `UNKNOWN` と明記**しており、推測で補っていない。
**資格情報は値も長さも出力していない。** 扱ったのは有無だけである。
