# RESULT — 設定同期の停止に関する実測（lecun）

**task_id:** `T-2026-08-12-sync-audit-lecun`  **kind:** `analysis`
**実行ホスト:** `lecun`  **分岐:** `feat/sync-audit-lecun`  **status:** pass

**読み取りのみ。復旧操作は一切行っていない。方針の判断はユーザーが行う。**
生の出力は `audit.md`（521 行）。一覧は `inventory.tsv`（2531 行）。
機械可読の対は `result.yaml`。**散文から値を抜いて対を作っていない。**

同じ内容の契約を複数ホストで並行実行している。**他ホストの結果は見えない前提で書いた。
自ホストで測れないものは `UNKNOWN` としてある。**

---

## 1. 解決された参照

### `contract.inject_verbatim: [conventions#prohibitions]`

`context/conventions.md`（版 `d422b08`）の該当アンカーの**原文**。要約していない。

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |

`spec.yaml` の `contract.prohibitions` が挙げる 5 件はすべてこの表に存在する。

### `inputs.sigma_policy`（省略）

`context/conventions.md#sigma` の既定値を継承した。原文。

    series: pstd
    sigma_source: paired_delta
    delta_sigma_source: paired

本契約は数値の比較を行わないため**この継承は使っていない。使わなかったことを記録する。**

### `contract.conventions_rev`

実測して `d422b08`。`spec.yaml` の記載と一致したため置換していない
（Task 6 Step 2 は実行者が実測して置換する手順であり、逸脱ではない）。

    git --no-pager log -1 --format=%h -- context/conventions.md
    d422b08

### 解決しなかった参照

`inputs.denominator.ref` と `inputs.frozen_source.ref` は `spec.yaml` に無い。
`kind: analysis` のため `prereg.md` も無い。

### L3 で実行されなかった検査

`make task-preflight` は **5 PASS / 0 WARN / 4 SKIP / 0 FAIL**。
**`SKIP` は合格ではない。** 実行されなかった 4 件は
P2 `cuda_ext_loaded`（契約に未記載）、P3 `deterministic_flags`（契約に未記載）、
P4 `prereg_committed`（`kind=analysis` のため対象外）、
P5 `frozen_source_hash`（同）。

---

## 2. 結論

**見立ては実測で裏づけられた。中心の一台だけが到達不能であり、代替の経路は構成上存在しない。**

| 問い | 実測による答え |
|---|---|
| 星型の中心はどこか | **philip**（`192.168.196.150`）。実装に埋め込まれている（`keeper.sh` 13-19 行） |
| 中心は停止しているか | **到達不能**。3 ポート（22000 / 22001 / 50072）すべて `No route to host` |
| 他ホストへは届くか | **届く。** 9 台すべて `50072` が `OPEN`、`22000` と `22001` は `REFUSED`（機器までは到達） |
| 外へは届くか | **届く。** `git ls-remote` が参照を返し `exit=0` |
| いつ止まったか | **2026-08-06 20:24:02**（接続喪失の瞬間が記録に残っている）。約 5 日前 |
| 代替の経路はあるか | **構成上無い。** 大域探索と中継が設定で無効。他 10 台とは一度も直接接続していない |
| 復旧で消えうるものは | 停止後に自ホストで更新された **172 件・約 2.0 MB**。版の退避は設定で無効 |

### 非対称は「構内 vs 外」ではない。「philip vs それ以外」である

| 相手 | 結果 | 意味 |
|---|---|---|
| 外（`github.com`） | 参照が返る | 外向き通信は生きている |
| 同一構内の 9 台 | `50072` が `OPEN`、`22000`/`22001` が `REFUSED` | **機器までは届いている。** 口が閉じているだけ |
| 同一構内の philip | 3 ポートすべて `No route to host` | **機器まで届かない** |

**構内全体の障害では説明できない。** `22000` が 9 台すべてで `REFUSED` であることは、
`keeper.sh` 14 行のコメント「コンテナ間は SSH(50072) しか通らない」と整合する。

### 星型であることは実測で確定した

    Established secure connection の相手: {'GO2U7PF': 21}

**接続を確立した相手は 21 件すべて `GO2U7PF`（philip）である。**
他 10 台の識別子はログに各 6 件前後現れるが、いずれも設定読み込みに関する記録で、
**一度も接続していない。** すべての同期は philip 経由でのみ行われていた。

さらに `globalAnnounceEnabled=false` と `relaysEnabled=false` であるため、
接続経路は「設定に書かれた直結」か「局所探索」のみである。
**中心が落ちたときの代替が構成上存在しない。** これが「一台の停止で全ホストへ
渡らなくなる」ことの構造的な説明である。

### 停止時期は四つの情報が整合した

| # | 情報 | 推定 |
|---|---|---|
| A | 最新の衝突ファイルの名前に埋め込まれた時刻 | `2026-08-06 16:22:24` |
| B | `sync-alerts.log` の他ホスト最終行（hinton） | `2026-08-06 20:20:11` |
| C | `~/.tunnel.log` の連続失敗 263 行 × 1800 秒からの導出 | `2026-08-06 21:05:26` |
| D | `~/.syncthing.log` の最後の同期 | `2026-08-06 20:20:23` |

**幅は 4 時間 43 分。整合する。** 順序も意味を持つ（A ≤ B・D ≤ C）。
B と D は同一事象の両面である（届いた行そのものが D の同期対象）。

**接続喪失の瞬間が直接記録されている。**

    2026-08-06 20:20:23 INF Synced file (folder.id=claude-sync ... file.name=sync-alerts.log ...)
    2026-08-06 20:24:02 INF Lost device connection (kind=primary device=GO2U7PF
                            connection=127.0.0.1:22000-127.0.0.1:22001/tcp-client/TLS1.3-...)

**以後 2026-08-07 から 08-11 まで、記録が一行も無い。**

---

## 3. 完了判定 20 項目

**「実施した」ではなく「何が出たか」を書いた。空欄は無い。**

| # | 判定 | 実測値 |
|---|---|---|
| 1 | プローブが三通りを出し分けた | `A_open=OPEN` / `B_closed=REFUSED` / `C_noroute=TIMEOUT`。期待と全一致 |
| 2 | 版管理側の経路が生きている | 参照 1 行、`exit=0`（`refs/heads/phase0` = `a85cf78`） |
| 3 | 常駐処理の稼働数を二通りで数えた | 角括弧法 **keeper 2 / ssh -L 1**（**誤り**）、`/proc` 法 **keeper 1 / ssh -L 0**（正）。食い違いの原因を特定 |
| 4 | 中心ホストの決め方を実装から読んだ | `keeper.sh` 13-19 行を引用。中心 `philip` = `192.168.196.150`、条件 `~/.tunnel_to_philip` の存在、経路 SSH `50072`。稼働中の版は正本とバイト一致（差 0 行） |
| 5 | 中継の目印を集合として列挙した | **2 件**（`.tunnel.log` / `.tunnel_to_philip`）。`home_total=69`。`find` でも 2 件 |
| 6 | 同期処理と中継の稼働状況 | syncthing **2 プロセス**（`ppid` 連鎖 1071→1079→1395414 の監視親と実働子。**二重起動ではない**）。`ssh -L` **0 件** |
| 7 | 中継の入口への接続結果 | `127.0.0.1:22001` = **REFUSED**（待ち受け一覧に 22001 が無く、入口が存在しない） |
| 8 | 共有相手と共有フォルダの件数 | device **11 台** / folder **2 件**（`claude-sync` と `m2`、いずれも 11 台全部と共有、`paused` なし） |
| 9 | 記録に秘匿の値が含まれない | 設定の解析結果に対する検査 **0** |
| 10 | 対象一覧を三つの出所から集めた | `ssh_count=4` / `hosts_count=7`（遠隔 **0**）/ `stcfg_count=11` → 和集合 **遠隔 10 台**。既知 11 台 = 遠隔 10 + 自ホスト。**縮んでいない** |
| 11 | 全対象を測り合計が一致した | **30 = 30 = 30**（分類の合計 / 行数 / 対象数 10 台 × 3 ポート） |
| 12 | 拒否と経路なしを区別した | `OPEN` **9** / `REFUSED` **18** / `TIMEOUT` **0** / `No_route_to_host` **3**（philip のみ） |
| 13 | 設定共有の件数 | `EXISTS`。ファイル **2532**（symlink 1・ディレクトリ 900）、一覧 **2531**（`.stfolder` の 1 件を除外） |
| 14 | 一覧に内容が含まれない | 語の検査 **4**（0 ではない）だが**全件が第 1 列の名前の語**。列の型検査で 2531 行すべて 4 列・整数・時刻・16 桁 hex を確認 |
| 15 | 退避と衝突の痕跡 | `.stversions` **0**（両フォルダの `versioning.type` が空 = **設定で無効**）／衝突ファイル **10**（最新 `20260806-162224`） |
| 16 | 停止時期を二つ以上の独立した情報から推定 | **四つ**（A `16:22:24` / B `20:20:11` / C `21:05:26` / D `20:20:23`、すべて 2026-08-06）。**整合**（幅 4 時間 43 分）。接続喪失の瞬間 **2026-08-06 20:24:02** |
| 17 | 16 項目すべてに実測値または UNKNOWN | **空欄なし。** UNKNOWN は `result.yaml` の `unknowns` に 4 件を明示 |
| 18 | 作業ツリーの変更が契約の範囲に限られる | **2 行**。`tasks/T-2026-08-12-sync-audit-lecun/` と `tasks/inbox.d/T-2026-08-12-sync-audit-lecun.md`。`make forbidden-check` は `pass`（changed 8 / checked 8 / violations 0）。unmerged **0** |
| 19 | 抑止が repo 直下から消えている | Step 7 の実測を後述 |
| 20 | 報告が台帳へ返っている | Step 8 の実測を後述 |

### 復旧時に消えうるもの（判断の材料）

接続喪失の時刻（`2026-08-06 20:24:02`）を境に、自ホストで更新されたファイルを数えた。

| 項目 | 実測 |
|---|---|
| 一覧の総件数 | 2531 |
| 停止前に最終更新 | 2359 |
| **停止後に最終更新** | **172** |
| 停止後の合計サイズ | **2,059,010 バイト（約 2.0 MB）** |

内訳は `codex/` **170 件**、直下 **2 件**。人が触る設定に当たるのは
`settings.json`（14419 バイト、`2026-08-11T05:49:59`）と
`codex/config.toml`（901 バイト、`2026-08-11T21:59:17`）の 2 件で、
残る 170 件は `codex/plugins/cache/` 配下の自動生成物である。

**版の退避は設定で無効**（両フォルダの `versioning.type` が空）であり、
上書きされた旧版は残らない。**衝突ファイルの機構は働いている**（過去 10 件の実績）。

**他ホスト側で停止期間に何が変更されたかは自ホストからは測れない（UNKNOWN）。**

---

## 4. 起票者の誤り（4 件）

型と本文は `result.yaml` の `issuer_defects` にある。**本文は要約していない方を読むこと。**

1. **`check_does_not_check`** — Task 5 Step 5 の `find ~ -maxdepth 3 -name "syncthing*.log"` は
   **0 件**を返すが、`~/.syncthing.log` は **31137 行で実在する**（`-name` が先頭の `.` に
   一致しない）。このファイルは**接続喪失の瞬間とその後 5 日間の空白を直接記録した唯一の
   情報源**であり、停止時期の最も強い証拠である。指示どおり件数だけを見ると
   「別の場所にログは無い」と誤読し、最も強い証拠を落としたまま推定することになる。
   `keeper.sh` 24 行の追記先を読んでいたため気付けた。

2. **`check_does_not_check`** — Task 2 Step 1 と Task 3 Step 1 の角括弧法
   （`ps -eo args | grep -c "[k]eeper.sh"` / `grep -c "[s]sh .*-L"`）は、この実行環境では
   **測定命令の全文が別プロセスの `args` に埋め込まれるため自己一致する。**
   実測では keeper を **2 件**（実際 1 件）、`ssh -L` を **1 件**（実際 0 件）と数えた。
   SPEC は `pgrep -f` を過去 2 回の発生を理由に禁じたが、**その回避策である角括弧法も
   同じ理由で無効である。** 角括弧は `grep` 自身を除くだけで、コマンド全文を `args` に
   持つ別プロセスは除けない。指示どおり数えると、常駐処理が二重起動しており中継も
   動いていると誤読する。

3. **`asserted_without_measuring`** — Task 3 Step 2 は `ss` か `netstat` があることを前提に
   「零行なら手段が無かったということ」と書くが、この環境には `ss` も `netstat` も `lsof` も
   **無い**（`command -v` で 3 件すべて未導入を確認）。指示どおり実行すると `手段なし` の
   1 行が返るだけで、待ち受けの一覧が一切得られない。代替の指示は無い。

4. **`self_contradiction`** — Task 5 Step 3 は `Expected: 0` と断定しながら、同じ Step の中で
   「ファイル名に該当語がある場合はその行を残しつつ…明記する」と 0 でない場合の扱いを
   書いている。実測は **4 件**で、全件がファイル名の語だった。`Expected 0` を判定基準として
   素直に読むと、**正常な状態で不合格になる。** 判定は語の件数ではなく列の構造で行うべきである。

### 申し送りは効いた

SPEC の注意 1〜8 と zsh に関する申し送りは実際に役立った。具体的には、
注意 1（零件を別の探し方で確かめる）で `TIMEOUT=0` の検査を 3 通り試し、
**うち 1 つ（`awk`）が引用の誤りで走っていないこと**を見つけた。
注意 2（実装を読んでから信じる）で `~/.syncthing.log` の場所を特定できた。
注意 5・7（一覧そのものを疑う・名前を決め打ちしない）で、`sync-alerts.log` の
ホスト名に**大文字の変種があり最初の正規表現が 108 行を落としていた**ことに気付いた。

---

## 5. 逸脱（9 件）

`result.yaml` の `deviations` に全件。**空にしていない。** 主なものは次のとおり。

- 待ち受けの一覧を `/proc/net/tcp` と `/proc/net/tcp6` から復号した（`ss`/`netstat`/`lsof` が無い）
- 稼働数を `/proc/*/cmdline` から**自分と祖先を除いて**数え直した（角括弧法が自己一致）
- 停止時期の情報源を**四つ**に増やした（SPEC は二つ以上。四つ目は実装から場所を特定）
- 測るポートを 2 つから **3 つ**へ増やした（実装が使う `50072` を追加）
- **復旧時に衝突しうるファイル数を追加で測った**（分布だけでは決定事項の材料にならない）
- `versioning` の設定を追加で読み、`.stversions` が 0 件である理由を確定させた
- `sync-alerts.log` の大文字のホスト名で **108 行を落としていた**ことを記録（結論は不変）
- **プローブが観測対象のログに 1 行を残した**（読み取りのみだが痕跡は残る）
- 本分岐に契約の範囲外の commit が 1 件ある（前契約の digest。**作業開始前の操作**）

### 観測が対象に痕跡を残したこと

`~/.syncthing.log` の 2026-08-12 の唯一の行は本契約のプローブによるものである。

    2026-08-12 08:17:25 WRN Failed TLS handshake (address=127.0.0.1:55292 error=EOF log.pkg=connections)

`127.0.0.1:22000` へ TCP 接続して即座に閉じたため、syncthing が TLS の失敗として記録した。
**状態は変えていないが、読み取りのみの契約でも観測は対象のログを 1 行伸ばす。**

---

## 6. 未解決（4 件）

`result.yaml` の `unknowns` に全件。要点は 2 つである。

**他ホストの状態は測れない。** 並行実行の前提により、停止期間に他ホスト側で何がどれだけ
変更されたかは自ホストからは分からない。**復旧時に衝突する相手側の件数は UNKNOWN である。**

**philip の停止の原因と復旧時期も測れない。** 本契約が確定できたのは
「lecun から philip の 3 ポートすべてへ経路が無い」ことだけである。
記録にある「ハード側の理由」「復旧は 2 週間以降」は本契約では検証していない。

---

## 7. 禁止事項の遵守

| # | 禁止 | 遵守 |
|---|---|---|
| 1 | 同期処理の設定変更・起動・停止・再起動、中継の張り切り | 行っていない |
| 2 | `~/claude-sync/**` `~/bin/**` の変更 | 読み取りのみ |
| 3 | 手による複製で同期を代替 | 行っていない |
| 4 | 他ホストへの書き込み・読み取り以外の命令 | TCP 接続の可否のみ測定。命令は実行していない |
| 5 | 資格情報・API キーの値の出力・記録 | 識別子は先頭 7 文字のみ。`~/.tunnel_to_philip` の中身は読んでいない |
| 6 | 装置の使用・統合・自動統合の有効化 | 行っていない。抑止の目印を置いた状態で作業した |
| 7 | `make task-report` 以外の経路での外部送信 | 他の経路は使っていない |
| 8 | 生成物の再生成（`make context` / `taskindex` / `inbox`） | **実行していない** |
| 9 | 未測定の値を書く | UNKNOWN として明示（4 件） |
| 10 | `runindex/**` `context/auto/**` の手編集 | 触っていない（`forbidden-check` が `pass`） |
| 11 | `experiments/**` `transfer/**` `data/splits/**` の変更・削除 | 触っていない |
