# T-2026-09-21-philip-sync-outbound-off — 判断

- **外向きを決める設定は三つで、どれか一つで STUN が止まる**（`stunKeepaliveMinS` / `stunKeepaliveStartS` / `natEnabled`）。実体の逆アセンブルと Go の型情報で確定し、`natEnabled` だけを落とした。変える項目は最小にする。
- **「止まった」は不在で示さない。** 同期処理は無効化の瞬間に `STUN disabled` を通常の水準で記録する。変更前に「記録では確かめられない」と見立てたのは誤りで、実測が覆した。**見立てを実測より先に信じない。**
- **使用状況の日次送信が、前契約の最中に `urAccepted` 0→3 で有効化されていた。** 誰が変えたかは UNKNOWN。**設定は変えた覚えが無くても変わる。契約のたびに `options` 全体を控えて差分を見る。**
- **秘匿の検査は形だけでは足りない。** 32 文字の画面の鍵は「40 文字以上の塊」という形の規則をすり抜け、実在値との照合だけが捕らえた。**形と照合の両方を当てる。**
- `P9 spec_lint` の `host_mismatch` は OS 名を見ているため、この repo の「ホスト」（同期処理上の名前）と食い違う。`separated_source` は行継続 `\` を繋げずに見ている。**どちらも検査側の誤りで、契約の誤りではない。**
