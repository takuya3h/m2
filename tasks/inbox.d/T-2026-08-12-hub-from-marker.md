- 正本は `.tunnel_to_*` を辞書順で一つ選び、ファイル名から中心、1 行目から鍵パス、任意の 2 行目から住所を導出する。目印無しは張らず、旧 1 行形式も維持する。
- 偽 HOME の四対照は全件期待どおりで、変更前後とも `ssh -N -L=0`、`keeper.sh=1`、陰性対照 `0`。稼働版と本物の目印は size・mtime とも不変。
- 次契約では非中心ノードの `.tunnel_to_lecun` に鍵パスと到達住所を置き、lecun 自身は目印を置かない。配置・再起動・目印変更は本契約では未実施。
