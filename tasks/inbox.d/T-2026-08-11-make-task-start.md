# T-2026-08-11-make-task-start — 判断の受け皿

- **分岐名の導出規則は既存の実例と一致しない。** 契約 30 件のうち規則に従うのは 13 件で、分岐が現存する 23 件のうち **10 件は人が短縮**していた（`feat/report-back` ← `T-2026-08-18-report-back-to-ledger` 等）。規則は `T-YYYY-MM-DD-<slug>` → `feat/<slug>` と定め、**既存分岐は改名しない**（統合済み・PR 進行中があるため）。今後作る分岐が従う。
- **データに触れない契約が意味のないプレースホルダを書き続けている。判断が要る。** 既存 30 契約のうち **29 本**が `split_files: [data/splits/ego_val.txt]` を検証を通すためだけに書いている。`entrypoints` にデータ関連を含むのは 1 本のみ。三択 —（1）スキーマを緩める（2）`kind` で必須を分ける（3）慣行として残す。**本 task では直していない**（スキーマ変更は既存 30 契約の検証結果を変えうるため、性質の異なる変更を同じ PR に混ぜない）。
- **起票側の自己検査にスキーマ検証が含まれていない。** 本契約は初回配布時にそれで落ちた。落ちる欄は取り込み時まで分からない。起票側で `jsonschema` を回すか `--pack` して検証するかを決める必要がある。
- 台帳の probe 行 4 件（`probe-intake` `probe-roundtrip` `probe-attachment` `probe-report`）はすべて `superseded` で、取り込み検査に使えない。**検査用の行を用意したつもりが使えない状態**になっている。
- `make task-start` は `.sync-pause` を置く側だけを担う。**消す側は実行者が行う**（報告後の `rm -f .sync-pause`）。
- 統合されるまでは、`task-start` が作った分岐に `task_start.sh` が存在しない（起点が `origin/phase0` のため）。統合後に解消する。
