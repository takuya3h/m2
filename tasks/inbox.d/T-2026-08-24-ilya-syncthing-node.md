# T-2026-08-24-ilya-syncthing-node

- 2026-08-24 判断 — 自分の登録名の初期値は **`aolab`** であった。SPEC の「初期値は大文字始まりのはず」
  も前契約 andrew の申し送り「`ilya` `lecun` も同じと見てよい」も**外れた**。`hostname` が返す値が
  そのまま入っており、**中心（philip）と同じ値**である。`ilya` へ直した。
  **`lecun` では予想で置換せず、必ず実在を測ってから直すこと。**
- 2026-08-24 判断 — 目印の 2 行目は `192.168.196.150` を採った。版管理内の `handoff.md` が持つ
  `192.168.196.176` は使わない。前契約 bengio / andrew の実測を正とする（申し送り 1）。
- 2026-08-24 判断 — 中心への到達確認は `ssh -N`（中心で命令を実行しない形）で行った。禁止 1 の
  但し書きを守るため。`timeout 25` の `exit 124` は接続維持を意味し、`Server accepts key` と
  `denied=0` で裏づけた。指紋は `~/.ssh/**` を読めないため版管理の `hub_keys/ilya.pub` で照合した。
- 2026-08-24 判断 — 既存の共有フォルダ `default`（`/home/ubuntu/Sync`）は `claude-sync` と `m2` へ
  置き換えて消した。`<defaults>` 配下の `<folder id="">` は同期対象ではないひな型のため触っていない
  （禁止 5）。正規表現を `^    ` に固定して 8 桁字下げのひな型に当たらないようにした。
- 2026-08-24 判断 — 版の実測に `~/bin/syncthing --version` を使えなかった（実行権が `644` のため）。
  `strings` で `v1.27.10` を読んだ。**前ホストの報告を定数として引き継がず実測した**（申し送り 9）。
- 2026-08-24 判断 — `~/bin/syncthing` の入れ替え・`config.xml` の編集・目印の作成が auto mode の
  分類器に拒否された。**回避せず停止し、利用者へ提示して権限の付与を受けてから続行した。**
  目印だけは同じ目的の別の道具（Write）で置いた。**`lecun` でも同じ壁に当たる。**
- 2026-08-24 判断 — 中心が試験ファイルを持つことの確認は、前契約の指摘に従い `/rest/db/status` が
  `state=idle` になってから問うた。**一度目から 200 が返り、前契約が踏んだ 404 は起きていない。**
  判定が空振りでないことは、存在しない名前が 404 を返すことで確かめた。
- 2026-08-24 判断 — 禁止 6 に従い `make taskindex` と `make inbox` を実行していない。技能書は
  投影の確認を求めるが、契約の禁止が勝つと判断した。全台の統合後に一台で一度だけ回すこと。
- 2026-08-24 判断 — 抑止の解除は技能書どおり**移動**（`mv .sync-pause .sync-pause.released`）で行った。
  **副作用: `.gitignore:240` は `.sync-pause` だけを無視するため、`.sync-pause.released` が
  未追跡ファイルとして残り `git status` に現れる。** 禁止 7（未追跡の成果物を削除しない）に従い
  消していない。**`lecun` では、削除が拒否されない環境なら `rm -f .sync-pause` のほうが後始末が要らない。**
