# T-2026-08-11-split-and-recipe-audit — 判断の受け皿

- 分割の実体は健全だった（9657/1515/4265、split 跨ぎ 0 件、陽性対照つき）。**分割由来の基準点汚染は無い。**
- **S0 の検出器比較表が 6 種類の `eval_recipe_id` に跨っている。** `score_thr` が `1e-08` と `0.0` の 2 系統。9 検出器の mAP の幅（0.6973〜0.7268）を検出器の優劣として報告できない。統一条件での再評価は 9 検出器 × 3 seed = 27 run。**判断が要る（再評価するか / 論文から落とすか / 差を明記して載せるか）。**
- `experiments/baselines/_legacy_score_thr_0` の 33 run が `excluded=False` のまま解析対象に含まれている。ただし同一 `experiment_id` の新旧混在は 0 件で、問題は混入ではなく「9 検出器の唯一の記録がそこにある」こと。除外すると記録が索引から消える。**判断が要る。**
- 工程 503 run の `split_train_images = 9657` は定数 `PAPER_SPLIT_SIZES` の転記であり実測ではない。実測経路の `configs/stage/s4_phase_baseline.yaml` の `ann_file` は実体の無いプレースホルダ。**値は正しいが、記録の一致を分割の一致の証拠に使ってはならない。**
- `recipes_match` は注入対照ペアに対して構造的に必ず `False` を返す（独立変数であるモデル構造が `test_cfg` に同居しているため）。対照ペア 136 件で split と GPU 構成の食い違いは 0 件だった。**Δ の妥当性は崩れていない。**
- 実運用の Δ 集計（`experiments.csv` の `delta_*`）は照合を通っていない。`recipes_match` の呼び出し元は学習時経路のみ。
- ゼロ同士の照合は `True` を返す（起票者の疑いは仕組みとして実在）。ただし該当は codetr の 3 run のみで工程側は 0 件。
