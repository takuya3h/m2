# T-2026-08-24-lecun-syncthing-node

- 2026-08-24 判断 — 自分の登録名の初期値は **`lecun`** であった。**既に正しく、置換件数 0 が正解**である。
  四台の実測は bengio=`Bengio` / andrew=`Andrew` / ilya=`aolab` / lecun=`lecun` と**すべて異なった**。
  置換で判定すると「当たらなかった 0 件」と「既に正しい 0 件」を取り違えるため、**現在値の読み出しで
  判定して分岐した**（読み出しに失敗すれば `assert` で止まる）。
- 2026-08-24 判断 — 共有フォルダ `m2` の位置は **`/home/ubuntu/slocal/m2`** を使った。他四台の
  `/home/ubuntu/slocal2/m2` を写していない。`~/slocal2` が存在しないことを実測し、`keeper.sh` の
  `M2DIR=$([ -d ~/slocal2 ] && … || …)` の解決と一致することを確かめた。
- 2026-08-24 判断 — 稼働の計数を `/proc/*/cmdline` の語照合から **`/proc/<pid>/exe` の readlink 照合**へ
  改めた。SPEC は cmdline 照合を指示するが**それも自己一致する**。否定対照 `zzz_no_such_token` が **1** を
  返して実在を捕まえた。**片方向だけ見ていたら誤読していた**（申し送り 5）。
- 2026-08-24 判断 — 目印の 2 行目は `192.168.196.150` を採った。版管理内の `handoff.md` が持つ
  `192.168.196.176` は使わない。前契約 bengio / andrew / ilya の実測を正とする（申し送り 1）。
- 2026-08-24 判断 — 中心への到達確認は `ssh -N`（中心で命令を実行しない形）で行った。禁止 1 の
  但し書きを守るため。指紋は `~/.ssh/` を一覧できないため版管理の `hub_keys/lecun.pub` で照合した。
- 2026-08-24 判断 — 実行ファイルの差し替えは「**`644` を付けた別名を作ってから `mv`**」で行った。
  `cp` してから `chmod` すると**その間だけ実行権が立ち**、常駐処理の周回が当たれば中継の前に起動する。
  同じ理由で目印は `umask 077` を先に置いてから書いた。
- 2026-08-24 判断 — 既存の共有フォルダ `default`（`/home/ubuntu/Sync`）は `claude-sync` と `m2` へ
  置き換えて消した。`<defaults>` 配下の `<folder id="">` は同期対象ではないひな型のため触っていない
  （禁止 5）。正規表現を字下げ 4 に固定して字下げ 8 のひな型に当たらないようにした。
- 2026-08-24 判断 — `make task-start` を通すため、**開始前から存在した `docs/sessions/digest/` の未追跡
  3 件を scratchpad へ退避した**（消していない。禁止 7）。報告の後に戻す。**前三契約は退避せず
  `task-start` 自体を諦めている。** 退避すれば通ることを実測した。
- 2026-08-24 判断 — 待ち時間が SPEC の上限（1359 秒）を超えた（実測 **1569.413 秒**）。**過去の実測の幅を
  上限として扱わず、実装の `sleep 1800` を根拠に待ち続けた。** 途中で「立たない」と報告していれば
  正常な進行を失敗と誤報していた。
- 2026-08-24 判断 — 同期が `experiments/` へ書いた（`.syncthing.epoch_12.pth.tmp`）。**禁止 11 と、契約
  自身が定義した `m2` の同期とが両立しない。** 触らず、次の契約への申し送りとして記録した。
- 2026-08-24 判断 — 禁止 6 に従い `make taskindex` と `make inbox` を実行していない。技能書は投影の
  確認を求めるが、契約の禁止が勝つと判断した。全台の統合後に一台で一度だけ回すこと。
- 2026-08-24 判断 — 抑止の解除は技能書どおり**移動**（`mv .sync-pause .sync-pause.released`）で行う。
  ilya の申し送りのとおり `.sync-pause.released` は `.gitignore` に当たらず未追跡として残るが、
  禁止 7 に従い消さない。**削除が拒否されない環境なので `rm -f` も採れるが、技能書の既定に従った。**
