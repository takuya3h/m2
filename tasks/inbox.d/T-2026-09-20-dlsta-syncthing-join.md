- [ ] 2026-09-20 [cc] 起動前に .stignore が無いことを見つけ、利用者の許諾を得て keeper.sh:48-49 と同じ処理を正しい位置へ手で行った。無ければ .venv 6.46GB と .git が群れ全体へ流れた。常駐処理が repo の位置を二択でしか解決せず本ホストでは自動反映が働かないことが根因である（scripts/sync/keeper.sh:28）
- [ ] 2026-09-20 [cc] 禁止 5 相当の「生成物を再生成しない」と手順書の make taskindex / make inbox の衝突は前契約で決着済みのため本契約では諮らず、投影と集約は対象外の読みをそのまま踏襲した（tasks/T-2026-09-20-dlsta-join-foundation/result.yaml）
- [ ] 2026-09-20 [cc] 常駐処理の周期が 1800 秒で、実行権を戻した後の同期処理の起動を待つと約 26 分かかるため kill して起こし直した。中継は別プロセスで維持された。SPEC の順序（中継→実行権→同期処理）を守ったまま待ちを詰める方法として、周回の頭で実行権を見に行く形にするかの判断が要る（scripts/sync/keeper.sh:51）
- [ ] 2026-09-20 [cc] 告知と外部中継を無効にしても natEnabled が既定 true のままで公開 STUN へ出た。外部へ出ないことを目的とするなら無効化の対象に含める必要がある。七台すべてが同じ状態である（~/.syncthing.log の Resolved external address）
- [ ] 2026-09-20 [cc] 試験が 6 件失敗から 7 件失敗へ増えたが、コードは無変更で原因は同期の途中経過である。飛ばし条件がディレクトリの存在だけを見るため、同期がディレクトリを先に作り中身を後から運ぶ間だけ落ちる。同期を使う全ホストで転送中に再現する（tests/test_loss_mask.py:70）
- [ ] 2026-09-20 [cc] 中心の側から見た本ホストの住所は本契約でも確定できなかった。中継の足元は 172.17.0.12、STUN 由来は 131.113.39.33 で、登録簿の 192.168.196.54 とどちらも異なる。中心の sshd の記録を読むには中心で命令を実行する必要があり禁止 1 に当たるため、中心を実行ホストとする契約で確かめるのが筋である（scripts/sync/hosts/ssh_config.d.snapshot.conf）
