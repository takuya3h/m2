- [ ] 2026-08-30 [cc] **許可の宣言は `contract.allow_write`（接頭辞の配列）。検査は `make forbidden-check TASK=<task_id>`。** 上限は `data/` 配下（常に不可）と、`experiments/` `transfer/` のうち起点に既に存在する経路（既存 run 配下）。**収穫を伴う契約は `allow_write: [runindex/]` を置けば道具を迂回せずに通せる**（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] **収穫の検証は `make harvest-verify`**（`BASE=<commit>` で起点を変える。既定 `HEAD`）。run 単位の `index.csv` は追加のみ、集約表は**既存の群の判定列が不変**。判定列は `same_sign`/`verdict`/`agree`/`reason`/`n_seeds` を名前に含む列で見分ける（`experiments.csv` では 631 列中 106 件）。**「既存行の変更零」を集約表に課すのはやめる**（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] **置換前提の参照は `ref: "unresolved:<何を索引で引くか>"` + `resolve_by_executor: true` で宣言する。** 実行者は `tasks/<task_id>/resolved.yaml` に `resolved_to` と `how` の対を書き、書くまで `P12 refs_resolved` が止める。形式検査は緩めていないので**宣言の無い `TBD` は従来どおり落ちる**（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] **preflight に書いてよい名前は `venv_active`/`cuda_ext_loaded`/`deterministic_flags`/`gpu_free` の 4 つだけ。未知名は FAIL になる。** schema の `enum` と `KNOWN_PREFLIGHT_NAMES` が同じ集合であることは試験で縛った。宣言は契約が実際に使う経路に限る（工程側だけを回す契約は検出器の拡張を宣言しない）（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] **`P11 gpu_free` は「GPU を占有する compute プロセスが 0 件」で判定する。閾値は置いていない。** nvidia-smi が無い・失敗する・タイムアウトする場合は FAIL。共有ホストで他者の学習中に契約が止まる頻度は未測定（本契約は GPU を使わないため）。閾値が要るなら別契約で（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] 🔴 **SPEC §5-g「全テストが通る」は未充足のまま終えた。** 起票時点で既に 6 件が落ちており本契約では動かせない（`test_engines` 1・`test_fetch_task` 1・`test_research_logger` 4）。**今後の契約は「実行前の失敗件数を分母として記録する」形にすべき**。件数の増加だけを見ると既存の失敗が残っていても気付けない（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] `test_fetch_task.py::test_rejects_unknown_file_name` は誤り文言と試験の期待がずれているだけに見える（期待「受け取れないファイル」／実際「経路として受け取れない名前です」）。`tools/fetch_task.py` は本契約の entrypoints に入っているが F1–F5 のいずれでもないため触っていない。直すなら別契約で（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] 🔴 **自分の修正に穴が 2 つあり、いずれも対照で見つけて塞いだ。** (1) F1 の上限を宣言の文字列で判定していたため `allow_write: ["d"]` が `data/` 配下を通した → 経路ごとの判定に変更し 5 通りを試験で固定。(2) F5 の伏せ字の目印を `xxxx`/`**` まで広げたため合成鍵を誤認し既存試験を 1 件壊した → 省略記号と明示語だけに絞込み。**目印や接頭辞は狭く取る**（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] 🔴 **対照の途中で実在ファイルを削除した。** F1 の検査に使った `touch`/`unlink` が `experiments/audit/l0_hts_acceptance/acceptance_report.json` を消した。復元し要約値の一致と差分零を確認済み。**対照でファイルを触るときは控えを取り `finally` で必ず戻す**（T-2026-08-30-tooling-fixes-five）
- [ ] 2026-08-30 [cc] **手順書と契約が再び衝突した。** 手順書は `make taskindex`/`make inbox` を求めるが契約 §4-3 が再生成を禁止。契約を優先したため本報告は `context/auto/` の投影にまだ現れない。**前契約（hts-candidate-acceptance）でも同じ衝突が起きており、手順書側に「契約が禁じている場合は契約を優先する」を明記すべき**（T-2026-08-30-tooling-fixes-five）
