- [ ] 2026-08-16 [cc] **信号を工程分類へ渡す設計は捨てる。** 正解を渡した腕（上限）は 60 種の対の差で平均 +0.0016832・標準誤差 0.0011428・比 1.4729 で事前登録の判定（比>=2）を満たさなかった。実際に使える形（推論した値）は −0.0003190 で実質零。**ただし「効果が無い」と証明したのではなく「上限が 60 種で見える大きさより小さい」を示した**（平均 0.0016832 < 達成 2SEM 0.0022856）。この区別を次の契約へ必ず引き継ぐこと（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] **規模をフレームで語ると判断が早い。** val は 1515 フレームなので accuracy の量子化幅は 1/1515 = 0.00066。上限の腕の平均 +0.0016832 は **2.55 フレーム**、達成された検出限界は **3.46 フレーム**。比で見ると「惜しい」に見えるが、フレームで見ると追う価値の無い規模だと即座に分かる。今後の工程認識の Δ 議論では**フレーム換算を併記する**ことを提案する（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] 事前登録の検出力の見込みが外れた。前契約が**別の腕**で測った対の差の母標準偏差 0.0054519 を主たる腕へ流用したため、見込み 2SEM 0.0014 に対し実測 0.0088520 → 達成 0.0022856（1.63 倍悪い）。**今後 exp 契約で検出力を見込むときは「どの対の差で測った散らばりか」を明示し、腕が違えば流用しない**（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] **決定化はホストを跨いでもビット単位で成立する。** andrew が記録した best checkpoint sha256 `e909ab8d3296481f`（無情報の腕・seed 42・全 50 世代）を lecun で再現した。さらに装置二枚の並行実行・2.09 時間を通して抜き取り四本（腕を分散）が全て sha256 一致。**実行ホストの違いは決定化すれば結果に効かない**ため、今後の exp 契約はホスト指定を絶対条件にせず、空いている装置で走らせてよい（記録には host を残す）（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] `ExperimentManager` の採番に競合の窓がある（`next_sequence` で走査してから `mkdir(exist_ok=False)`、experiment_manager.py:140-144）。並行実行では片方が FileExistsError で落ちうる。本契約は起動間隔と再試行を実行側に置いて回避し再試行 0 件だったが、**並行実行が常態化するなら採番を原子的にする impl 契約を立てるべき**である（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] 決定性が主題の契約なのに `plan.env.preflight` に `deterministic_flags` が無く、プリフライトの P3 が SKIP になった。**検査器は決定化が効いていなくても同じ結果を返す。** exp 契約の起票時は、契約の主題に対応する preflight 項目を `plan.env.preflight` に必ず並べること（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] 工程別の分解で **dressing は両腕とも F1 = 0**（支持数 24・モデルが一度も当てていない）、**disinfection と irrigation は val に支持が無い**。9 工程のうち 3 工程がこの状態で macro F1 を語っている。**工程認識の評価を macro F1 で述べるときはこの 3 工程の扱いを明示する**必要がある（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] 把持の推論の質を割合で見ると誤る。`left_hand` は正しさの割合 0.9848 に対し曲線下面積 0.7040（陽性率 0.985）、`two_hands_tool` は割合 0.8845 に対し平均適合率 0.5587（陽性率 0.116）。**run の記録には割合しか残らない**ため、重みから事後に測る必要があった。把持系を今後も測るなら AUC/AP を metrics に残す配線を検討すべきである（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] **`make forbidden-check` は `kind: exp` の契約で必ず fail する。** 生成物として除外するのが `context/auto/` と `tasks/inbox.md` だけで、`outputs.destination: "experiments/"` の契約が新規 run を作ることも `make runindex` が `runindex/` を作り直すことも知らないため。本契約では違反 2932 件の内訳を手で確かめ、既存 run の変更・削除が 0 件（`git diff` に M も D も無し）であること、`runindex/` は再生成で同一になることを示して契約違反でないと判定した。**道具を exp 契約に対応させる impl 契約が要る**（T-2026-08-15-injection-sweep-deterministic）
- [ ] 2026-08-16 [cc] **索引の集約が決定化の有無を混ぜている。** `experiment_id` に `task_id` が入らないため、本契約の 360 本（決定化あり）が前の契約の 66 本（決定化なし）と同じ 6 実験行へ集約された（ctrl/inj は 73 本、他は 70 本）。解析は `task_id` で絞ったので主たる差に影響は無いが、**`experiments.csv` のこの 6 行を条件照合に使うと決定化の有無が混在した集約を見ることになる**。集約の鍵に含めるか、混在を検出して警告すべきである（T-2026-08-15-injection-sweep-deterministic）
