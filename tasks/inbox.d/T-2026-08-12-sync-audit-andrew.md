# T-2026-08-12-sync-audit-andrew

- 2026-08-12 / andrew / 同期停止の実測。**中心 `philip`（192.168.196.150）だけが両ポートとも `No route to host`。他の 9 台は SSH `50072` が `OPEN` で機器は生きて届き、外向き（github）も生きている。落ちているのは単一ホストへの経路のみ。**
- 2026-08-12 / andrew / `~/.syncthing.log` の接続確立 21 件は**すべて `philip` 宛**で、他 9 台へ直接つながった記録は 0 件。星型は設定だけでなく接続履歴でもそうであり、中心が落ちると他が生きていても全対向が同時に切れる。
- 2026-08-12 / andrew / **停止時刻 `2026-08-06 20:24:02`（UTC、接続喪失）。** 独立な第二の情報（設定共有の更新時刻分布の最終 `2026-08-06T17:05:14`）と整合。第三の裏取りとして中継の連続失敗 261 行 ≒ 261.6 個の 30 分周期。
- 2026-08-12 / andrew / **本ホストの分岐は 1 ファイルのみ**（`sync-alerts.log`。`m2-sync.sh` が 30 分ごとに追記する自ホスト由来の記録）。復旧で失われる固有の成果は無い。設定共有は 2496 件で、一覧と要約値を `inventory.tsv` に残した（他ホストとの突合用）。
- 2026-08-12 / andrew / **`.stversions` は 0 件**（4 通りの探し方で一致）。版の退避が働いておらず、復旧時に衝突として残らない形で上書きされた場合は同期処理からは復元できない。既存の衝突ファイル 10 件はすべて `sync-alerts.*.log`（08-03〜08-06、由来は he・adam・dlsta・ilya）。
- 2026-08-12 / andrew / 9 台の `22000` はすべて `REFUSED`＝同期処理の待受が外側の住所に出ていない。加えて `globalAnnounceEnabled=false` / `relaysEnabled=false` で発見サーバも中継サーバも使わない設定。**経路を変える案はいずれも SSH `50072` を通す前提になる。**
- 2026-08-12 / andrew / 起票者の誤り 4 件。うち 2 件は結論を取り違えさせる種類である。(1) `ps -eo args | grep -c "[s]sh .*-L"` が検索命令自身に一致し偽陽性 `1` を返す（真値 0）。(2) `find ~ -maxdepth 3 -name "syncthing*.log"` が先頭ドットの `~/.syncthing.log` に一致せず 0 件を返し、停止時刻を示す唯一の時刻付き記録を見落とさせる。
- 2026-08-12 / andrew / 測れなかったもの: 待受ポート一覧（`ss`・`netstat` とも不在）、他ホスト側の分岐量（読み取り専用契約のため）、`philip` の停止理由と復旧見込み、9 台の `22000` が閉じている理由。
