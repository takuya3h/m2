# T-2026-09-17-efros-syncthing-join

- 2026-09-17 efros: `~/.zshrc` の keeper 起動行が**既に在り、指す先の脚本だけが無い**状態であった。SPEC の「既に在れば追記しない」に従って追記せず、配置だけを行った。
- 2026-09-17 efros: `~/claude-sync/.stfolder` を先回りして作らず実挙動を測った。**syncthing が起動時に自分で作る**ため、新しいホストを入れるとき人の操作は要らない。
- 2026-09-17 efros: 中継の待ち時間は SPEC の 413〜1569 秒ではなく**約 4 秒**であった（自分で keeper を起こす場合は最初の周回の先頭で張られる）。
- 2026-09-17 efros: 起動で設定が書き戻されるという前提が成り立たず、`config.xml` の sha256 は**不変**であった。起動の判定に要約値の変化を使わないこと。
