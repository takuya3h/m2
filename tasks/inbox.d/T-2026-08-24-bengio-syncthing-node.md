# T-2026-08-24-bengio-syncthing-node

- 2026-08-24 判断（利用者）— 共有フォルダの型は `claude-sync` `m2` とも **`sendreceive`**。`handoff.md` §2.3 が
  `UNKNOWN` として残した未決の判断を、bengio の実測（`~/claude-sync/` = 4031 B / 1 件、削除の履歴なし。
  中心も 1 件）を示したうえで利用者が決めた。中心側の選択と揃う。結果は上書きではなく衝突ファイルで、
  消えたものは無い。
- 2026-08-24 判断（利用者）— Task 1 Step 6 の認証確認は SPEC の `ssh … 'echo REACHABLE'` ではなく
  `ssh -N`（中心で命令を実行しない形）で行う。禁止 1 の但し書きを守るため。
- 2026-08-24 実施（利用者）— Task 4 Step 4 の実行権の復帰と、`<defaults>` 配下の `<folder id="" path="~">` の
  削除は利用者が行った。前者は中継の成立より後で順序は守られている。後者は同期対象ではないひな型であり、
  最上位の共有フォルダは前後とも 2 件のまま。「ホーム全体が共有される状態だった」という事実は無い。
