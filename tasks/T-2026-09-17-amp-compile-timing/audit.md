# audit — T-2026-09-17-amp-compile-timing

実施ホスト `efros`（RTX A6000 × 2 / driver 595.84 / nvcc 12.9 / torch 2.1.2+cu118）。JST 2026-09-17。
**数値はすべて本ホストの実測である。未測定は UNKNOWN と書く。**

## Task A — 開始状態と基準

### A-1 作業ツリー

| 項目 | 実測 |
|---|---|
| 開始時の HEAD | `4624b002c65acf263e8821dac943de23950c6f71`。`origin/phase0` と同一点（ahead/behind 0/0） |
| 分岐 | `feat/amp-compile-timing`。`make task-start` が `origin/phase0` を起点に作成 |
| 開始時の未追跡 | **0 件**（`git status --porcelain` が空）。契約の取り込み後は `tasks/T-2026-09-17-amp-compile-timing/` の 1 件 |
| 開始前から在った未追跡 | **0 件**。退避は不要だった |
| `git stash list` の件数 | 2（`pre-T-2026-09-17-frozen-feature-cache-timing` と `efros-pre-rejoin-2026`。いずれも本契約より前から在り、触れていない） |

🔴 **前契約の道具が本分岐に無かった。** `make task-start` は `origin/phase0` を起点に分岐を作るが、
前契約の PR #181 は未併合（`state: OPEN` / `mergedAt: null`）であるため、SPEC §2 が「本契約でも使える」と
書く `scripts/profile_t1b_step.py` が存在しなかった。基準の step 時間を**同じ道具で**取り直す要件を
満たすため、`git checkout feat/frozen-feature-cache-timing -- scripts/profile_t1b_step.py` で
その 1 ファイルだけを取り寄せた（分岐の統合はしていない。取り寄せ後の差分は 0 行）。

### A-2 装置

開始前:

    index, memory.used [MiB], utilization.gpu [%]
    0, 40361 MiB, 100 %
    1, 40381 MiB, 100 %
    pid, used_gpu_memory [MiB]
    129999, 40338 MiB
    130000, 40338 MiB

`/proc/PID/exe` で解決した実体は 2 件とも
`/home/ubuntu/.local/share/uv/python/cpython-3.11.16-linux-x86_64-gnu/bin/python3.11`。
これは**前契約の終了時に利用者の指示で立て直した仮占有プロセス**であり、他利用者のものではない。
利用者の常設の指示「GPU 使用時はダミープロセスを kill する。利用終了したら再度立ち上げる」に従って停止した。

停止後:

    0, 15 MiB, 0 %
    1, 35 MiB, 0 %
    compute プロセス件数: 0

**他利用者の処理は 0 件**（`nvidia-smi --query-compute-apps=pid --format=csv,noheader` の行数を
`grep -c` で数えた。終了コードを件数として使っていない）。

### A-3 現行の数値精度（実装の行を指す）

`scripts/train_t1b.py` を語で走査した結果。

| 探した語 | 件数 | 備考 |
|---|---:|---|
| `autocast` | **0** | 自動混合精度を張る箇所が無い |
| `GradScaler` | **0** | 勾配の尺度調整が無い |
| `float16` / `bfloat16` / `half` / `fp16` / `bf16` | **各 0** | 半精度の指定が無い |
| `torch.compile` | **0** | 計算グラフの最適化が無い |
| `channels_last` | **0** | 記憶配置の変更が無い |
| `amp` | 3 | いずれも `GroupedBatchSampler` / `DataLoader` / `RandomSampler` の語中一致で、混合精度とは無関係 |
| `dtype` | 3 | 96・135・199 行。いずれも `np.zeros(NUM_PHASES, dtype=np.float32)`（phase context を単精度で作る） |

学習ループ（`scripts/train_t1b.py` の 364〜378 行付近）は
`loss_dict = model(images, targets)` → `loss.backward()` → `opt.step()` を素で呼ぶだけで、
autocast も scaler も挟まない。`import` も `torch` のみ（52 行）で accelerate を経由しない。

上流の `third_party/Relation-DETR/main.py` は `Accelerator(mixed_precision=..., dynamo_backend=...)`
を持つ（107〜115 行）が、**`train_t1b.py` はその経路を通らない**。`--mixed-precision` の既定も
`None`（28〜31 行）である。

**結論: 現行は単精度（fp32）で、混合精度もコンパイルも入っていない。**

補足の実測: torch の既定では `torch.backends.cuda.matmul.allow_tf32` が False、
`torch.backends.cudnn.allow_tf32` が True である。すなわち**畳み込みは TF32 を使うが行列積は使っていない**。
これも数値精度の設定であるため、条件表へ `tf32` を加えた（SPEC §3 の表には無い条件。追加した理由を逸脱に記す）。

### A-4 基準の step 時間の取り直し

前契約と**同じ道具**（`scripts/profile_t1b_step.py`、warmup 10・60 step・eval 60 枚）で取り直した。

| 量 | 前契約（2026-09-17 早朝） | 本契約（取り直し） | 差 |
|---|---:|---:|---:|
| 1 step | 0.4937 s | **0.4946 s** | +0.0009 s（+0.18%） |
| データ供給 | 0.51% | 0.51% | — |
| 順伝播 | 62.52% | 62.32% | — |
| backbone（hook 帰属） | 14.74% | 15.34% | — |
| 逆伝播 | 36.45% | 36.68% | — |
| 最適化 | 0.52% | 0.49% | — |
| eval 1 枚 | 0.0877 s | 0.0893 s | +0.0016 s |

**前契約の値をそのまま引き継いでいない。** 差は 0.18% で、同一ホスト・同一処方の再現とみなせる。

### A-5 前契約が残した一時領域

| 項目 | 実測 |
|---|---:|
| 前契約の終了時点の大きさ | 6,827,503,447 bytes（`third_party/Relation-DETR/data/processed/t1b_backbone_cache`） |
| 本契約の開始時点 | **0 bytes**。ディレクトリ自体が存在しない |
| `third_party/Relation-DETR/data/processed` 全体 | 0 bytes |

前契約の報告時に大きさを実測して利用者へ提示し、**利用者の指示により削除済み**である。
本契約の `decisions_required` の 2 件目はこの経緯をもって回答とした（`meta.amendments`）。

### A-6 占位の差し替え

| 項目 | 実測値 | 出所 |
|---|---|---|
| `contract.conventions_rev` | `e7a5100597a79b3b9c60935bf38d232f8ae96822` | `git log -1 -- context/conventions.md` |
| `meta.created_from.runindex_commit` | `96eb3a1ca20ab7eb4b373d93304c9e6b33b733d5` | `git log -1 -- runindex/` |
| `counts` | index 1266 / experiments 285 / verdicts 1506 | 各 CSV の行数からヘッダ 1 行を引いた値 |

## 逐語で受け取った規約

`contract.inject_verbatim` は `conventions#prohibitions` / `conventions#issuer_cautions` /
`conventions#frozen_source` の 3 件。原文は `context/conventions.md`（rev
`e7a5100597a79b3b9c60935bf38d232f8ae96822`）の該当アンカーである。要約していない。

`inputs.sigma_policy` は省略のため `conventions#sigma` の既定値を継承する
（`series: pstd` / `sigma_source: paired_delta` / `delta_sigma_source: paired`）。
**本契約は seed 間の散らばりを主指標の比較に使うため、この既定を実際に適用する**
（`series: pstd` に従い σ は母集団標準偏差を採る）。

凍結源（`conventions#frozen_source`）は `meta.kind: impl` のため preflight では対象外（P5 SKIP）だが、
実装の確認の過程で実測した。sha256 `03936318f9d45ac956fa928278cff9a869d3c2583e86b3af3ac1bbd27675e824`、
195,421,066 bytes。**規約の記載と完全一致**。凍結源は変更していない。

## Task D-3 — 前契約の「153.1〜208.7 日」の食い違い

SPEC §3 Task D-3 は「前契約の報告にある『縮退なしで 153.1〜208.7 日』がどの前提で出た数字かを
特定し、計算器が同じ前提（K=3、2 枚、24h）で返す 84.1〜111.9 日との食い違いの原因を記録する」と求める。

**原因は前提の取り違えであり、計算器は正しい。取り違えたのは前契約の実行者（すなわち私）である。**

計算器の 2 つの節は、**前提と対象範囲の両方が違う**。各節は自分の前提を冒頭に書いている。

| 節 | 1 日あたり | 対象範囲 | 縮退なしの日数 |
|---|---:|---|---:|
| `degrade` | **24.0 時間** | **Stage 1 + Tier 1 + Tier 2 + Tier 3 の全体** | 84.1〜111.9 |
| `deadline_sensitivity` | **12.0 時間** | **Stage 1 + Tier 1**（Tier 2・3 は投稿の必須ではない） | 153.1〜208.7 |

機械可読出力で裏が取れる。

    stage1_plus_tier1.wall_low = 1836.7375 h  → / 12 h/日 = 153.06 日
    all.wall_low               = 2017.5292 h  → / 24 h/日 =  84.06 日

前契約の `docs/stage0/C1_frozen_feature_cache_timing.md` §7 と `RESULT.md` は
「**縮退なし（K=3・装置 2・24h/日）: 153.1〜208.7 日**」と書いた。
**この括弧の前提が誤りである。** 153.1〜208.7 は 12h/日・Stage 1 + Tier 1 の値であり、
24h/日・全体なら 84.1〜111.9 が正しい。同じ報告の中で degrade 節の 84.1〜111.9 は
正しい前提で併記していたため、**一つの報告に両方の数字が別前提で並んでいた**。

数値そのものは計算器の出力のままで、捏造ではない。誤っているのは**添えた前提の記述**である。
前契約の成果物は PR #181（未併合）に在るため、本契約の分岐からは直せない。
訂正の申し送りを `followups` に置き、利用者へ提示する。

## Task B — 条件ごとの step 時間

測り方は次のとおり。**処方は変えない**（同じ config・同じ loader・同じ最適化器群・同じ seed・同じバッチ）。

- バッチは最初に一度だけ取り出して実装のメモリに保持し、**全条件へ同じ列を与える**。
  形の列から要約値を作って記録した。同一の走行の中では全条件が同じ要約値を持つ。
  seed を固定しているため**別の走行でも同じ列**になる（40 step の走行 3 本が
  いずれも `17bef89b47dc69e7…` を返した。うち 1 本は W2、1 本は compile 用の別過程である）。
- 暖機 10 step を捨て、**初回の 1 step は別に計る**（compile の構築時間はここに入る）。
- **装置の同期を取ってから計時する。** 取らない計測も併記した（下の表の「同期なし」）。
- 条件ごとに凍結源から重みを読み直し、最適化器を作り直す。

**W1（入力適合層のみ）・短い測定 40 step**（バッチ列の要約値 `17bef89b47dc69e7…` / 保持 90 バッチ / 学習対象 266,880 param）

| 条件 | matmul TF32 | 1 step（同期あり） | 1 step（同期なし） | 倍率 | 記憶領域の山 | 初回 step | 非有限 | 跳ね |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| fp32 | False | 0.4571 s | 0.4276 s | **1.000×** | 8,376,721,408 B | 0.85 s | 0 | 0 |
| tf32 | True | 0.3825 s | 0.3781 s | **1.195×** | 8,375,474,176 B | 0.35 s | 0 | 0 |
| bf16 | False | 0.3798 s | 0.3418 s | **1.204×** | 6,876,328,448 B | 0.46 s | 0 | 0 |
| fp16 | False | 0.3887 s | 0.3315 s | **1.176×** | 6,877,685,248 B | 0.44 s | 0 | 0 |

**W1（入力適合層のみ）・長い測定 200 step**（バッチ列の要約値 `7355892ca3024a09…` / 保持 410 バッチ / 学習対象 266,880 param）

| 条件 | matmul TF32 | 1 step（同期あり） | 1 step（同期なし） | 倍率 | 記憶領域の山 | 初回 step | 非有限 | 跳ね |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| fp32 | False | 0.4611 s | 0.4379 s | **1.000×** | 14,590,299,648 B | 1.01 s | 0 | 0 |
| tf32 | True | 0.3902 s | 0.3855 s | **1.182×** | 14,589,924,864 B | 0.35 s | 0 | 0 |
| bf16 | False | 0.3927 s | 0.3443 s | **1.174×** | 12,726,704,640 B | 0.50 s | 0 | 0 |
| fp16 | False | 0.3945 s | 0.3634 s | **1.169×** | 12,726,155,776 B | 0.47 s | 0 | 0 |

**W1・compile（dynamo の eager 退避あり）**（バッチ列の要約値 `17bef89b47dc69e7…` / 保持 90 バッチ / 学習対象 266,880 param）

| 条件 | matmul TF32 | 1 step（同期あり） | 1 step（同期なし） | 倍率 | 記憶領域の山 | 初回 step | 非有限 | 跳ね |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| fp32 | False | 0.4626 s | 0.4327 s | **1.000×** | 8,376,721,408 B | 0.92 s | 0 | 0 |
| compile | — | **失敗** | — | — | — | — | — | — |
| compile_tf32 | — | **失敗** | — | — | — | — | — | — |
| compile_bf16 | — | **失敗** | — | — | — | — | — | — |

**W2（末端まで学習）・短い測定 40 step**（バッチ列の要約値 `17bef89b47dc69e7…` / 保持 90 バッチ / 学習対象 25,505,568 param）

| 条件 | matmul TF32 | 1 step（同期あり） | 1 step（同期なし） | 倍率 | 記憶領域の山 | 初回 step | 非有限 | 跳ね |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| fp32 | False | 0.5652 s | 0.5032 s | **1.000×** | 13,619,660,288 B | 0.95 s | 0 | 0 |
| tf32 | True | 0.4396 s | 0.4572 s | **1.286×** | 13,613,707,264 B | 0.38 s | 0 | 0 |
| bf16 | False | 0.4503 s | 0.3880 s | **1.255×** | 9,929,365,504 B | 0.51 s | 0 | 0 |
| fp16 | False | 0.4613 s | 0.3863 s | **1.225×** | 9,920,399,360 B | 0.58 s | 0 | 0 |

### 短い測定と長い測定の一致（計時が壊れていないことの確認）

| 条件 | 短(40 step) | 長(200 step) | 差 |
|---|---:|---:|---:|
| tf32 | 1.195× | 1.182× | 1.1% |
| bf16 | 1.204× | 1.174× | 2.5% |
| fp16 | 1.176× | 1.169× | 0.6% |

**一致する。** 倍率は測定の長さに依らない。

### 同期を取らないと速く見えること

同じ条件で、同期を取った計測と取らない計測を並べた（W1 長）。

| 条件 | 同期あり | 同期なし | 見かけの短縮 |
|---|---:|---:|---:|
| fp32 | 0.4611 s | 0.4379 s | 5.0% |
| tf32 | 0.3902 s | 0.3855 s | 1.2% |
| bf16 | 0.3927 s | 0.3443 s | 12.3% |
| fp16 | 0.3945 s | 0.3634 s | 7.9% |

**同期を取らないと速く見える。** 見かけの短縮は条件によって 1.2〜12.3% と異なるため、
同期なしの値で条件を比べると**倍率そのものが歪む**（bf16 が fp32 より過大に見える）。
本報告の倍率はすべて同期ありの値である。

### 記憶領域

| 条件 | W1 長(200) の山 | fp32 比 | W2 短(40) の山 | fp32 比 |
|---|---:|---:|---:|---:|
| fp32 | 14,590,299,648 B | — | 13,619,660,288 B | — |
| tf32 | 14,589,924,864 B | −0.003% | 13,613,707,264 B | −0.04% |
| bf16 | 12,726,704,640 B | **−12.8%** | 9,929,365,504 B | **−27.1%** |
| fp16 | 12,726,155,776 B | −12.8% | 9,920,399,360 B | −27.1% |

TF32 は記憶の表現を変えないため山も変わらない。半精度は W1 で 12.8%、W2 で 27.1% 減る。
**将来バッチを増やす余地になるのは半精度だけである。**
（山の絶対値は保持したバッチ列の分を含むため、走行の長さが違う表の間では比べられない。
条件の間の差だけが意味を持つ。）

### 数値の異常

**W1・W2 のすべての条件で、非有限の損失は 0 件、中央値の 5 倍を超える跳ねも 0 件であった**
（各条件 90〜410 step、暖機と同期なしの計測を含む全 step を数えた）。
fp16 は GradScaler による尺度調整を伴い、勾配の刈り込みは尺度を戻してから当てている。

### コンパイルが完走しないこと

3 条件（compile / compile_tf32 / compile_bf16）とも**完走しない**。

1. 素の `torch.compile`: `TorchRuntimeError: Failed running call_function <built-in function one_hot>`
   `(*(FakeTensor(..., device='cuda:0', size=(2, s4), dtype=torch.int64), 16), **{})`:
   `Cannot call sizes() on tensor with symbolic sizes/strides`。
   出所は `third_party/Relation-DETR/models/bricks/set_criterion.py:199` の
   `F.one_hot(target_classes, self.num_classes + 1)[..., :-1]`。
   画像ごとに物体数が変わるため形が記号のままになり、torch 2.1 の dynamo が扱えない。
2. **実装を改変しない回避**（`torch._dynamo.config.suppress_errors = True`）: 例外は警告に変わり
   eager へ退避して続行するが、**`ValueError: matrix contains invalid numeric entries` で失敗**する。
   Hungarian matcher の費用行列に非数が入る。
3. 学習の前処理が解像度を乱択するため**再コンパイルが多発**する（graph 59・再コンパイル 17 回を観測）。
   暖機だけで数分かかり、その間 GPU の使用率は 0% である。

利用者の判断（2026-09-17）により、**実装を改変せず条件から除外して記録する**。
compile の上乗せ分は UNKNOWN のままとする。

### 最良条件

**tf32**（行列積の TF32 を許す）。長い測定で 1.182×、W2 で 1.286× と全条件の最大であり、
記憶の表現を変えないため半精度より変更が小さい。bf16 は倍率がほぼ同じ（1.174×）で
記憶領域を 12.8〜27.1% 減らすため、**バッチを増やす余地が要る場面では bf16 が優る**。

**W2 のほうが効く**（1.286× 対 1.182×）。学習対象が 95 倍（266,880 → 25,505,568 param）で
逆伝播の行列積が増えるためである。Tier 1 には W2 の行が 19.8% 含まれるため、
下の日数の差し戻しで W1 の倍率を全行に当てるのは**控えめな見積もり**になる。

## Task D — 計算器への差し戻し

### D-2 W2 の代理値の置き換え

`tools/estimate_tier_cost.py` の `det_iface_w2` を **4.00 h → 4.82 h** に直した
（前契約の実測比 1.205 × W1 の 4.00 h）。`measured` は **False のまま**とした。
値は実測の比から導いたが、**W2 の run そのものは依然として repo に一件も無い**ためである。
`docs/stage0/B1_tier1_cost_estimate.md` の 8 区画を計算器の出力で再生成し、
`--check-doc` が **差 0 件**であることを確かめた。

置き換えの手順そのものが正しいことは、**表を変える前に一度回して 8 区画すべてが
無変更になること**で確かめてある（陽性対照の逆向き）。

なお `det_iface_w1` は 4.00 h のまま据え置いた。本ホストの実測は 4.18 h
（41.8 分/epoch × 6）であるが、出所の「1 run 約 4 時間」の精度の内側であり、
1 ホストの計時で正本を動かすのは過剰と判断した。差は逸脱に記す。

### D-1 日数（単精度と最良条件）

最良条件の日数は `--duration-scale 0.846226`（= 1 / 1.182）で出した。
**検出側の行が GPU 時間の 99.9% を占める**ため（実測。下表）、全行へ当てる近似の誤差は 0.2% 未満である。

| run 型 | GPU 時間（低〜高） | 全体に占める割合 |
|---|---:|---:|
| `det_iface_w1` | 2,764.0〜3,764.0 h | 68.5% |
| `det_iface_w2` | 800.0〜1,120.0 h | 19.8% |
| `det_tower_train` | 336.0〜336.0 h | 8.3% |
| `det_tower_w3` | 96.0〜96.0 h | 2.4% |
| `det_eval` | 33.3〜46.7 h | 0.8% |
| 工程側 + probe の合計 | 5.7〜6.9 h | 0.1% |

（割合は `det_iface_w2` を 4.00 h としたときの値。置き換え後も検出側の占有率は変わらない。）

| 前提 | 単精度 | 最良条件（tf32・1.182×） |
|---|---:|---:|
| 24h/日・Stage 1 + Tier 1〜3 の全体・縮退なし | **87.5〜116.6 日** | **74.0〜98.7 日** |
| 12h/日・Stage 1 + Tier 1・縮退なし | **159.9〜218.2 日** | **135.3〜184.7 日** |
| 12h/日・Stage 1 + Tier 1・縮退段 3（seed 5 → 3） | 96.2〜131.2 日 | **81.4〜111.0 日** |

締切の判定が変わる箇所が一つある。

| 段 | 締切 | 単精度 | 最良条件 |
|---|---|---|---|
| 縮退なし | MICCAI 2027（残 152 日） | 収まらない | **読みにより分かれる** |
| 縮退段 3 | IPCAI long abstract（残 121 日） | 読みにより分かれる | **収まる** |

IPCAI 2027 intention（残 39 日）はどちらでも収まらない。

**この差し戻しは W1 の倍率を全行に当てた控えめな値である。** W2 の行（19.8%）は実測で
1.286× 効くため、実際にはもう少し短くなる。

## Task C — 最良条件での折り A 一本

### 起動

    python scripts/train_t1b.py --inject film --trainable film --phase-source real --zero-ctx \
        --seed 42 --epochs 6 --tf32 \
        --task-id T-2026-09-17-amp-compile-timing --run-name pd_refin_empty_seed42_tf32

**基準との差は `--tf32` の 1 つだけ**である。`config.yaml` で照合した。

| 項目 | 基準 `pd_refin_empty_seed42` | tf32 `pd_refin_empty_seed42_tf32` |
|---|---|---|
| epochs / seed / inject / trainable | 6 / 42 / film / film | **同一** |
| lr / film_lr / phase_source / zero_ctx | 1e-4 / 5e-4 / real / true | **同一** |
| model_cfg | `relation_detr_resnet50_egosurgery_t1b.py` | **同一** |
| warm_start_ckpt | `checkpoints/incoming/seed42/best_ap.pth` | **同一** |
| `tf32` / `matmul_allow_tf32` | （項目なし。既定 False） | true / true |
| `amp` | （項目なし。既定 no） | `no` |

**処方の差分は 0 項目**である（数値精度を除く）。

🔴 `--tf32` は過程全体の設定であるため、**学習だけでなく評価にも効く**。
学習だけに限る実装も可能だったが、採用の判断材料としては run 全体で一貫させるほうが正しいと判断した。
`--amp`（autocast）のほうは学習の順伝播にのみ張り、評価は常に単精度である。

### 所要時間

| 量 | 実測 |
|---|---:|
| 壁時計 | 開始 2026-09-17 11:29:54 UTC → 完了 15:14:20 UTC = **13,466 s = 3.7406 h** |
| GPU 時間 | **3.7406 GPU 時間**。GPU 0 を占有し、走行中 GPU 1 に compute プロセスは 0 件であった（排他） |
| 1 step（run 内の実測） | **0.4225 s**（各 epoch の 4800 step 時点の平均 it/s は 2.4/2.4/2.4/2.4/2.3/2.3、平均 2.367 it/s） |
| fp32 の 1 step（同一ホスト・Task A-4） | 0.4946 s（2.022 it/s） |
| **run 内での実測倍率** | **1.171×** |
| Phase B の予測（W1 長 200 step） | 1.182× |

**Phase B の予測（1.182×）と実 run の実測（1.171×）は 1% 以内で一致する。**
バッチを保持した計測が実際の学習を代表していたことの確認になる。

基準 run の壁時計と GPU 時間は **UNKNOWN** である。証跡に時間の列が無く、
本ホストでは fp32 の 6 epoch を回していない。参考として、Task A-4 の step 実測から積むと
6 × (4,809 × 0.4946 + 1,515 × 0.0893) + 1,515 × 0.0893 = 15,218 s = 4.23 h になるが、
**これは実測ではなく導出値である**。run 単位の倍率 1.13× も同じく導出値である。

### 主指標の比較

seed 間の散らばりの出所: `t1b_filmonly_seed{42,123,456}`（`runindex/index.csv`、step=`t1b_filmonly`）。
mAP は 0.736802 / 0.731410 / 0.725682 で、**σ = 0.004540**（pstd。`conventions#sigma` の既定 `series: pstd` に従う）。
sstd は 0.005561。この 3 本は基準と同じ inject=film・trainable=film の W1 界面 run である。

| 指標 | 基準 fp32 | tf32 | 差 | abs(差)/σ |
|---|---:|---:|---:|---:|
| init（warm-start・恒等点） | 0.7302938995 | 0.7302930990 | −0.0000008004 | **0.0002** |
| best | 0.7335755045（ep2） | 0.7337119334（ep2） | +0.0001364288 | **0.0301** |
| final（ep5） | 0.7313710102 | 0.7313652919 | −0.0000057183 | **0.0013** |

epoch ごとの突き合わせ。

| epoch | 基準 fp32 | tf32 | 差 | abs(差)/σ |
|---|---:|---:|---:|---:|
| −1（init） | 0.730294 | 0.730293 | −0.000001 | 0.000 |
| 0 | 0.732763 | 0.733391 | +0.000628 | 0.138 |
| 1 | 0.732751 | 0.732127 | −0.000624 | 0.137 |
| 2 | 0.733576 | 0.733712 | +0.000136 | 0.030 |
| 3 | 0.731134 | 0.731360 | +0.000227 | 0.050 |
| 4 | 0.730722 | 0.730224 | −0.000498 | 0.110 |
| 5 | 0.731371 | 0.731365 | −0.000006 | 0.001 |

**最大の差は 0.138σ**（ep0）であり、**すべての epoch で seed 間の散らばりの内側**に収まる。
最良 epoch も ep2 で一致する。escalate_if の「差が散らばりと同程度以上」には**該当しない**。

ビット一致ではない。init（学習前・FiLM は zero-init で恒等）の時点で
−0.0000008 の差が出ており、**TF32 は評価にも効くため恒等点でも完全一致はしない**。
差は σ の 1/5000 である。

### 数値の異常

| 量 | 実測 |
|---|---:|
| 損失の記録点数 | 150（200 step ごと + 各 epoch の末尾） |
| 非有限（nan / inf） | **0 件** |
| 中央値 | 14.9115 |
| 最小 / 最大 | 4.3380 / 37.1440 |
| 中央の 5 倍超 | **0 件** |
| 中央の 3 倍超 | **0 件** |
| 中央の 2 倍超 | 2 件 |

epoch 別の中央値は 14.640 / 14.306 / 17.147 / 14.097 / 14.594 / 16.550 で、
**発散も単調な増大も無い**。凍結検出器からの warm-start で注入層だけを学習する構成では
損失が大きく下がらないのは基準でも同じである（基準は val mAP が init 0.7303 から best 0.7336 までしか動かない）。

🔴 **基準 run の損失の推移は記録に存在しない。** `experiments/transfer/pd_refin_empty_seed42/logs/` に
在るのは `eval_meta_val.json` と `val_metrics_by_epoch.json` だけで、step 別の損失を残していない。
SPEC §3 Task C-4 の「学習の経過（損失の推移）を基準と並べる」は、**epoch 別の val mAP では並べたが、
step 別の損失は基準側が UNKNOWN** である。
