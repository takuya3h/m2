result_version: 3
task_id: "T-2026-09-17-amp-compile-timing"
status: pass
host: "efros"
branch: "feat/amp-compile-timing"
pr: 182
merged: false

gates:
  - id: G1
    verdict: pass
    note: "scripts/train_t1b.py に autocast と GradScaler と float16 と bfloat16 と half と fp16 と bf16 と torch.compile と channels_last が各 0 件であることを語の走査で示した。基準の step 時間を同じ道具で取り直して 0.4946 s（前契約 0.4937 s、差 +0.18%）"
  - id: G2
    verdict: pass
    note: "fp32 と tf32 と bf16 と fp16 と compile 系 3 条件を同一バッチ列で測った。バッチ列の要約値は 40 step の 3 走行がいずれも 17bef89b…。短い測定と長い測定で倍率が一致し、最良条件は tf32（W1 1.182 倍 / W2 1.286 倍）"
  - id: G3
    verdict: pass
    note: "主指標の差は best で +0.0001364288 = 0.030 σ、全 epoch を通じて最大 0.138 σ（σ=0.004540）。最良 epoch も基準と同じ ep2。escalate_if の「散らばりと同程度以上」には該当しない"

tests:
  before_failed: 6
  after_failed: 6
  after_passed: 550

deviations:
  - type: judgement
    note: "前契約の scripts/profile_t1b_step.py が本分岐に無かったため git checkout feat/frozen-feature-cache-timing -- でその 1 ファイルだけを取り寄せた（取り寄せ後の差分 0 行）。分岐の統合はしていない。PR #181 が先に併合されても同一内容のため衝突しない"
  - type: judgement
    note: "条件表に tf32 を足した。SPEC §3 の表には無い条件だが、Task A-3 で行列積の TF32 が無効（matmul の既定 False / cudnn の既定 True）と分かったため加えた。結果として最良条件だった"
  - type: spec_defect
    note: "scripts/bench_t1b_precision.py を新設し、scripts/train_t1b.py に --tf32 と --amp を足した。既定は従来と同一の経路で、--tf32 なしでは設定に触れず --amp no では autocast も GradScaler も作らない。§3 はコマンドを書かないとするが scripts/ への変更は明示の対象外である"
  - type: environment
    note: "pkill -f bench_t1b_precision が自分の命令行に一致して自滅した（exit 144）。conventions#issuer_cautions の 6 番「プロセスは /proc/PID/exe で絞る。部分一致は実行基盤の包み込みを拾う」の実例である。以後は部分一致を使っていない"
  - type: environment
    note: "main() 内の import torch._dynamo が torch を局所名にして module 側の torch を隠し、UnboundLocalError で tf32 の測定が 1 度落ちた。from torch import _dynamo へ直して再測定した"
  - type: judgement
    note: "--tf32 は過程全体の設定のため学習だけでなく評価にも効く。学習だけに限る実装も可能だったが、採用の判断材料としては run 全体で一貫させるほうが正しいと判断した。--amp のほうは学習の順伝播にのみ張り評価は単精度である"
  - type: judgement
    note: "det_iface_w1 を 4.00 h のまま据え置いた。本ホストの実測は 4.18 h だが出所の「1 run 約 4 時間」の精度の内側であり、1 ホストの計時で正本を動かすのは過剰と判断した"
  - type: judgement
    note: "Task C は GPU 0 のみを使い GPU 1 は空けたままにした。train_t1b.py は単一プロセス実装で DDP 起動の記述がリポジトリ全域に無く、1 run を 2 枚へ分散できないためである"
  - type: spec_defect
    note: "契約が contract.allow_write を宣言していないため make forbidden-check が Task C の成果物 9 経路を禁止領域として弾いた。Task C が生成する 1 run に限って allow_write を追補し、宣言後に status pass / violations 0 / permitted 9 / rejected 0 になることを確かめた"

issuer_defects:
  - type: asserted_without_measuring
    note: "SPEC §2 は「前契約は scripts/profile_t1b_step.py を新設した。本契約でも使える」と書くが、make task-start は origin/phase0 を起点に分岐を作り前契約の PR #181 は未併合（mergedAt が null）のため本分岐に存在しなかった。指示どおり進めると Task A-4 の「同じ道具で取り直す」が実行できない"
  - type: check_does_not_check
    note: "plan.env.preflight が venv_active のみのため、GPU を使う契約でありながら P2 cuda_ext_loaded と P11 gpu_free が SKIP になる。指示どおり実行すると CUDA 拡張が読めるかも装置が空いているかも検査されないまま実行に入る。実際に開始時の装置は 2 枚とも占有されていた"
  - type: asserted_without_measuring
    note: "SPEC §2 は torch.compile について「2.1 系では動くが DETR 系の動的な形に弱い場合がある」と書くが、実測では 3 条件とも TorchRuntimeError で動かない。実装を改変しない回避でも ValueError で非数が出る。「弱い場合がある」ではなく完走しない"
  - type: asserted_without_measuring
    note: "SPEC §3 Task B の条件表は数値精度の手当てを半精度 2 種だけに限るが、実装は行列積の TF32 を無効にしたままであり（allow_tf32 の既定は matmul が False で cudnn が True）、そこが最良条件だった。表のとおりに測ると最良条件を取り逃す"
  - type: check_does_not_check
    note: "契約は Task C で experiments/transfer 配下へ書く run を必ず伴うのに contract.allow_write を宣言していないため、Task E-1 が求める make forbidden-check が必ず失敗する。実測で 9 経路が禁止領域の内側として弾かれ、許可を追補するまで通らなかった"

positive_controls:
  - judgement: "計時が装置の同期を取っている（非同期で速く見えていない）"
    breaking_input: "同期を取らずに測る"
    observed: "見かけの短縮が条件ごとに fp32 5.0% / tf32 1.2% / bf16 12.3% / fp16 7.9% と異なって出た。同期なしの値で比べると bf16 が fp32 比で過大に見える"
  - judgement: "全条件が同じバッチ列を使っている"
    breaking_input: "保持するバッチ数を変える（40 step 用の 90 本と 200 step 用の 410 本）"
    observed: "バッチ列の要約値が 17bef89b… から 7355892c… へ変わり、bf16 の倍率も 1.204 から 1.174 へ動いた"
  - judgement: "B1 の区画を再生成する手順が文書を壊していない"
    breaking_input: "所要時間の表を変えずに再生成する（無変更になるはず）"
    observed: "8 区画すべて内容が変わらず --check-doc は差 0 件。表を変えた後の再生成では 8 区画すべてが変わり、その後も差 0 件"
  - judgement: "TF32 の切り替えが実際に効いている"
    breaking_input: "条件ごとに matmul_allow_tf32 と cudnn_allow_tf32 を記録する"
    observed: "tf32 条件だけ matmul が True で他は False。cudnn 側は全条件 True（torch の既定）"
  - judgement: "preflight の P6 が未回答の判断を実際に止める"
    breaking_input: "decisions_required を 2 件残したまま実行する"
    observed: "P6 が FAIL し 2 件を列挙して exit 2。回答を追補に記録して空にすると PASS へ変わった"
  - judgement: "forbidden-check が禁止領域への書き込みを実際に捕まえる"
    breaking_input: "allow_write を宣言せずに Task C の成果物を置く"
    observed: "status fail / violations 9 件（experiments/ の内側）。宣言後は status pass / violations 0 / permitted 9 / rejected 0"

followups:
  - "数値精度を下げた設定を本番の処方として採用するかは未決である。速さでは tf32 と bf16 に差が無く（1.182 倍と 1.174 倍）、記憶領域では bf16 が優り（W1 で 12.8% 減、W2 で 27.1% 減）、変更の小ささでは tf32 が優る。採用すると既存の単精度 run とは比較の土台が変わり分母の再構成が要る"
  - "前契約 T-2026-09-17-frozen-feature-cache-timing の C1 §7 と RESULT.md が「縮退なし（K=3・装置 2・24h/日）: 153.1〜208.7 日」と書いた括弧の前提が誤りである。153.1〜208.7 は 12h/日・Stage 1 + Tier 1 のみの値で、24h/日・全体なら 84.1〜111.9 日が正しい。PR #181 は未併合のため訂正の commit を足せる"
  - "torch.compile を通すには third_party の set_criterion.py の F.one_hot を動的な形でも通る形へ直す必要がある。利用者の判断で本契約では改変せず除外した。別契約として起票するかは未決"
  - "plan.env.preflight に cuda_ext_loaded と gpu_free を書く契約と書かない契約が混在している。GPU を使う契約では宣言を必須にするか、kind や phases の gpu 欄から自動で導くかの判断が要る"
  - "experiments/ へ書く run を伴う契約は contract.allow_write の宣言が要る。起票の雛形に入れるか、Task C 相当の run がある契約で自動的に補うかの判断が要る"

unknowns:
  - "基準 run の壁時計と GPU 時間。証跡に時間の列が無く、本ホストで fp32 の 6 epoch を回していない。step 実測から積むと 4.23 h になるが導出値であり実測ではない"
  - "基準 run の step 別の損失。記録に存在しないため、並べられるのは epoch 別の val mAP までである"
  - "torch.compile の上乗せ分。実装を改変しないと完走しないため測っていない"
  - "bf16 と fp16 での折り A の主指標。Task C は最良条件の tf32 の 1 本のみ回した"
  - "半精度で減った記憶領域のぶんバッチを増やした場合の倍率。処方の変更にあたるため測っていない"
  - "工程側（TeCNO 系）の run に同じ手当てが効くか。本契約は検出側だけを測った"

commits:
  - "af53fff0"
