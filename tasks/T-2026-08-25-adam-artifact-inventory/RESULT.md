# RESULT — T-2026-08-25-adam-artifact-inventory

**ホスト:** `adam` / **分岐:** `feat/adam-artifact-inventory` / **kind:** `analysis`
**証跡:** `audit.md`（475 行）。本文は行番号で指す。**同じ内容を二度書かない。**

## 判定

**verdict: PASS**（読み取りのみで完了。失ったものは無い）

| Gate | 結果 | 根拠 |
|---|---|---|
| G1（Phase A 後） | **PASS** | `audit.md` L262–271 |
| G2（Phase B 後） | **PASS** | `audit.md` L427–436 |

## 完了判定

| # | 判定 | 結果 | 実測値 |
|---|---|---|---|
| A | 版管理の位置を実測で確定 | PASS | `/home/ubuntu/slocal2/m2`。`~/slocal/m2` は不在 |
| B | 未追跡を件数・大きさ・更新時刻で記録 | PASS | **2 件**（本契約の spec.yaml / SPEC.md のみ） |
| C | 家の直下を全件列挙し 5 台の喪失物を表に | PASS | **29 件**。`~/bin` `~/.local/state/syncthing` `~/claude-sync` `~/.codex` は**すべて不在** |
| D | 実験の成果物を追跡の有無で分けて計数 | PASS | 追跡 8,263 件/796,743 B、無視 1,921 件/**25,372,914,081 B** |
| E | 同期処理と中継の不在を確認（両方向対照つき） | PASS | 実体 **0 件**。陽性対照 `zsh`=5 件、陰性対照=0 件 |
| F | 復元の可否で分類 | PASS | 下表 |
| G | 版管理へ入れられるかを判断し理由を記載 | PASS | `audit.md` L334–350 |
| H | 秘匿の混入を形で確認（陽性対照つき） | PASS | **陽性対照が一度落ち、検査器の欠陥を検出した**（下記） |
| I | 控えを取り原本と要約値が一致 | PASS | **1834 件中 1833 件一致**。不一致 1 件は本対話の記録（後述） |
| K | Task 1 で数えたものが全て残っている | PASS | `audit.md` L440–475。減少ゼロ |

## 失われうるもの（優先順位つき）

**本ホストは同期に参加しておらず、版管理だけが外部へ配る唯一の経路である。**

| 順位 | 対象 | 大きさ | 復元の可否 | 版管理へ | 控え |
|---|---|---|---|---|---|
| **1** | **初期化された 5 台が産んだ run の版管理外実体** | **3,379,519,786 B** / 1,299 件 | **産出元は初期化済み。本ホストが唯一の現存複製である可能性がある（UNKNOWN）** | **不可**（大きすぎる） | **取れていない** |
| 2 | index に載らない `experiments` の版管理外実体 | 16,635,522,640 B | 出所不明。再現には再学習が要る | 不可 | 取れていない |
| 3 | `~/.claude/`（特に `projects/` の対話記録） | 7,406,357 B / 463 件 | **家の下。同期も版管理も届かない。本ホスト固有** | 要編集で可 | **取得済み** |
| 4 | `.remember/logs/` | 6,383,104 B / 84 件 | 60 件は同期の競合複製 | 可 | **取得済み** |
| 5 | `wandb/` `outputs/` `logs/` の版管理外分 | 2,059,317 B / 937 件 | W&B に原本がある可能性 | 可 | **取得済み** |
| 6 | `docs/m2_plan_rewrite/.remember/` | 55,050 B / 97 件 | 本ホスト固有 | 可 | **取得済み** |
| 7 | `~/.ssh/` `env-passphrase` `.env` | 3,060 B / 9 件 | **秘匿。** `.env.gpg` は追跡済みで合言葉があれば復元可 | **不可** | **取らない**（複製を増やすこと自体が危険） |
| — | `data/` `third_party/` `.venv/` | 20.5 GB | 外部から再取得・再構築できる | 不可 | 不要 |

### 最優先で保全すべきもの

**順位 1。** `runindex/index.csv` の `host` 列によれば、**adam が産んだ run は 1177 件中 0 件**である。
一方で**1177 件すべての run 実体が本ホストの disk に在る**。同期で運ばれてきたものである。
このうち **lecun(827) andrew(69) philip(31) bengio(3) の 930 run は、産出元が初期化されている。**
**本ホストの複製が唯一の現存物である可能性がある。** 3.38 GB は版管理へ入れられない。

**ただし他ホストが再構築時に復元した可能性は本ホストからは確かめられない（禁止 4）。UNKNOWN である。**

## 本契約では保全していない

**控えは取ったが、版管理へは一件も入れていない。** 控えは同一 disk 上の
`~/adam-preserve-2026-08-25/`（11,981,805 B / 1,515 件）にあり、
**本ホストが失われれば控えも共に失われる。** 保全としては不完全である。

## 次に要る判断

1. **順位 1 の 3.38 GB を、同期で他ホストへ配るか、外部の保管へ出すか、諦めるか。**
   版管理へは入れられない。**この判断が最も急ぐ。**
2. 順位 3〜6（計 15.9 MB）を版管理へ入れるか。対話記録は既存慣行どおり
   `docs/sessions/digest/` へ機械抽出のみを入れる形が妥当。
3. adam を同期の群れへ戻すか。現在 `~/bin/` も syncthing も無く、**版管理以外の配布経路が無い。**

## 実測（次の判断に使う値）

| 項目 | 値 |
|---|---|
| 版管理の位置 | `/home/ubuntu/slocal2/m2` |
| `conventions_rev`（実測） | `d422b087`（spec 記載 `d422b08` と同一 commit） |
| 無視エントリ | 2,276 件 |
| 未追跡 | 2 件（本契約の契約ファイルのみ） |
| index の run | 1,177 件（adam 産出 **0 件**、disk 上に **1,177 件**） |
| 同期処理の実体 | **0 件**（`/proc/PID/exe` で計数。両方向対照つき） |
| 待ち受けの一覧 | **UNKNOWN**（`ss` `netstat` `lsof` `ip` すべて不在） |
| 常駐の登録 | **UNKNOWN**（`crontab` 不在、systemd user バス不通） |
| 控えの位置 | `~/adam-preserve-2026-08-25/` |
| 控えの要約値 | `MANIFEST.sha256` = `fddeb3d49e01d08c43f9448fcf7eb8ce330f1507d374ff5cfe62ccbfb2b5e903` |

## 起票者の誤り

| # | 型 | 内容 |
|---|---|---|
| 1 | `check_does_not_check` | `inputs.data`/`inputs.code` は schema で必須かつ `minItems: 1`。**データもエントリも持たない `kind: analysis` を表現できない。** 本契約は `inputs.data` を参照しないと SPEC.md が明記しながら `dataset: egosurgery_phase_v1` と `split_files` の記入を強いられている |
| 2 | `asserted_without_measuring` | 「環境の事実」表が `~/.ssh/**` の一覧は拒まれることがあるとするが、**本ホストでは拒まれなかった**（7 件を列挙できた）。SPEC.md の指示どおり実測を正とした |
| 3 | `self_contradiction` | `outputs.must_have` は `RESULT.md` のみだが、SPEC.md Task 3 は `result.yaml` と `tasks/inbox.d/<task_id>.md` の作成も求める。**必須の一覧が指示書本文と食い違う。** 本文に従い 3 つとも作成した |

## 逸脱

| # | 逸脱 | 理由 |
|---|---|---|
| 1 | 契約の取り込みに 3 回失敗した | 台帳の本文が L1 schema に落ち（8 件→4 件）、次に**古い添付 v2 が新しい v3 より先に走査され要約値が食い違った**。起票者が本文と添付を差し替えて解消。**検査は迂回していない** |
| 2 | `.venv/bin/python` が消えた pyenv 3.11.4 を指しており、`make task-*` が全く動かなかった | uv で 3.11.16 を導入し **`bin/python` と `pyvenv.cfg` だけを差し替えた**。既存 227 パッケージは温存。利用者の承認を得た。**再構築はしていない** |
| 3 | `source scripts/load_env.sh` が合言葉不在で失敗した回があり、平文 `.env` から読んだ | 他 34 箇所と同じ経路。**後半は利用者が合言葉を配置し、以降は `load_env.sh` を正規に使った** |
| 4 | 未commit のセッションダイジェスト 1 件を commit した | `task-start` が作業ツリーの清浄を要求するため。同種 15 件が追跡済みで慣例に従った。利用者の許可を得た |
| 5 | git identity をリポジトリ局所に設定した | 本ホストに未設定で commit できなかった。既存 commit と同一の値を用いた |
| 6 | `.sync-pause` は実行者が作成も削除もしていない | 実行前から存在した。`~/bin/` が無く常駐処理も無いため抑止は本ホストで無意味である |

## 想定外

| # | 事象 |
|---|---|
| 1 | **秘匿の検査器が陽性対照に落ちた。** `${(P)var}` という zsh 記法を bash が解釈できず、**2 つの鍵の照合が黙って飛ばされ、囮を見逃したまま exit=0 を返した。** 陽性対照の要求が無ければ「秘匿なし」と誤報告していた。`${!var}` に直して両方向の対照が成立 |
| 2 | 控えの 1 件が原本と一致しない。**本契約を実行している対話そのものの記録**であり、複製後も書き込まれ続ける。**原理的に静止した控えを取れない** |
| 3 | `~/.claude/` の大きさが測定中に増えた（7,347,932 → 7,406,357 B）。同じ理由。**定数として扱わない** |
| 4 | 本ホストに GitHub の鍵が無く `git fetch` が失敗した。利用者が `gh auth login` を実行し `~/.ssh/id_ed25519` が生成されて解消 |

## UNKNOWN

| 項目 | 理由 |
|---|---|
| 版管理外実体が他ホストに現存するか | **禁止 4**（他ホストへ接続しない）。版管理の記録は産出元しか示さない |
| 待ち受けの一覧 | `ss` `netstat` `lsof` `ip` がすべて不在。**迂回しない** |
| 常駐の登録 | `crontab` 不在、systemd user バス不通 |
| adam が過去に同期へ参加していたか | `.stfolder` と 60 件の競合複製（装置識別子 6 種、2026-07-04〜08-05）は参加と整合するが、**本ホスト単独では確証を取れない** |
| `inputs.data` の参照 | **本契約は参照していない。** 雛形の必須項目のため記入されているだけである（SPEC.md 明記） |

## 送出

| 項目 | 値 |
|---|---|
| PR | #149 https://github.com/takuya3h/m2/pull/149 |
| `make task-report` | 未送信。この commit の直後に実行する |
