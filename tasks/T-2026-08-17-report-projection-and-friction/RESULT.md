# RESULT — T-2026-08-17-report-projection-and-friction

**実行ホスト:** `lecun` / **分岐:** `feat/report-projection`（`origin/phase0` = `b8c6de3` から作成）
**状態:** pass（G1 / G2 / G3 / G4 すべて pass）

---

## 1. 解決された参照

### `contract.inject_verbatim: [conventions#prohibitions]` — 原文

```
## prohibitions

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |
```

### `conventions_rev` と検証

起票時 `d422b08` / 実測 `d422b08` で**一致**。置換は不要だった。
`make task-validate` は **WARN 0 件で exit 0**。分母を宣言しないため L2-8 も出ない。
**警告なしで取り込めた最初の契約である**（前 2 契約はいずれも 3 件の WARN を伴い、
続行の可否を利用者へ確認していた）。

プリフライトは `4 PASS / 4 SKIP / 0 FAIL`。SKIP は P2 `cuda_ext_loaded`・
P3 `deterministic_flags`（`plan.env.preflight` に記載なし）、P4 `prereg_committed`・
P5 `frozen_source_hash`（`kind=impl` のため対象外）。**SKIP は合格ではない。**

---

## 2. Phase A — 報告を投影から読めるように

### 様式を広げた（`result.schema.json` / `_templates/result.yaml`）

| 項目 | 版 1 | 版 2 |
|---|---|---|
| `gates[]` | id と verdict | **`note` を必須**（何を実測したか） |
| `deviations` | 件数（整数） | **一覧**（`type` と `note`）。空にできない |
| `issuer_defects[].note` | 長さ自由 | **80 字以上** |
| `followups` `unknowns` | 本文 | 変えない |

**版で分けた。過去の報告に遡って適用しない**（禁止 3）。版 1 の 4 件はいずれも
今も検証を通ることを実測した。

`deviations[].type` は `spec_defect` / `environment` / `judgement` の 3 種に固定。
**列挙にない型は増やしていない。** 本 task の 3 件はこの 3 種で足りた。

### 長さの下限を 80 にした理由（実装と実測から決めた）

既存 15 件の `note` の長さは昇順で
`[76, 93, 94, 96, 105, 119, 121, 124, 128, 135, 151, 153, 161, 180, 183]`。
**最短の 76 字は誤りの内容だけを述べ、指示どおり実行すると何が起きるかを欠く。**
次に短いのが 93 字である。80 はその間にあり、結果を書かない最短の例を拒みつつ、
両方を書いた最短の例を通す。**字数を稼がせるための下限ではない。**

検証は `tools/validate_task.py` が `Draft202012Validator` で行っており（`:176`）、
様式を広げれば実際に効くことを先に確かめてから決めた。

### 投影へ転記した（`context/auto/results_recent.md`）

直近 5 件を新しい順に、ゲート・起票者の誤り・逸脱・申し送り・断定できなかったことを
**そのまま転記する。要約しない。** 並びは `task_id` の降順で、識別子が
`T-YYYY-MM-DD-` で始まるため**壁時計を使わずに新しい順**になる。

上限を 5 とした理由は実装に書いた。1 契約の転記が 20〜40 行になるため、5 件で 200 行
前後に収まる。超えた分の在り処（各契約の `result.yaml` と `tasks_summary.csv`）を
冒頭に書き、**失われていないことを明示した。**

版 1 の記述も落とさない。`gates[].note` が無い行は「記述なし。様式 v1」と出し、
`deviations` が整数の行は「N 件（様式 v1 のため件数のみ）」と出す。
**型を揃えて隠さない。** 揃えると「中身がある」と誤解させる。

### G1 ゲート（`pass`）

| 検査 | 実測 |
|---|---|
| 転記が原文と一致するか（既存 4 件） | **45 件を照合して不一致 0 件** |
| 転記が原文と一致するか（本 task の版 2） | **17 件を照合して不一致 0 件** |
| 冪等 | 2 回生成して md5 一致 / `taskindex-check` exit 0 |
| 手による編集の検出（陽性対照） | 1 行を足すと**非ゼロ**、再生成で 0 へ戻る |

**要約は入っていない。** 一致しなければ `projection_summarizes_prose` に当たるが、
62 件すべてが原文のまま現れた。

---

## 3. Phase B — 検証の摩擦

### 分母を宣言しない契約で母集団の警告を出さない

`_warn_population_drift` は `meta.created_from.counts` と実数の差だけを見ており、
**契約が分母を使うかどうかを見ていなかった。** 契約を起票してから実行されるまでに
母集団は必ず動くため、分母を宣言しない契約でも毎回 3 件の警告が出て、実行のたびに
続行の可否を尋ねることになっていた。

`inputs.denominator` を持つ契約に限った。**`conventions_rev` の警告（L2-6）は残した。**
注入対象が変わっていないかの確認は分母と無関係に意味がある。

### G2 ゲート（`pass`）— 両方向

| 向き | 対象 | 実測 |
|---|---|---|
| 陰性 | 本 task（分母の宣言なし） | **L2-8 が 0 件 / exit 0** |
| 陽性 | 分母を宣言した一時契約 | **L2-8 が 3 件発火** |

**片方だけを試して満足していない。** 分母を持つ契約は 1 件も実在しなかったため、
一時的な契約を作って測り、確認後に削除した（作業領域に痕跡なし）。

その一時契約は `inputs.denominator.ref` の解決（L2-2）では FAIL したままである。
参照は `exp:<group>/<experiment_id>` を要求するが、実在の実験 id は `@val` や
2 段の `/` を含むため、完全に通る一時契約を作れなかった。**警告は L1 を通過した
あとの L2 段で出る**ため、発火の確認には足りている（L1 で落ちた版では警告が
出ないことも実測した）。

### 数の主張の規約

`tasks/README.md` に「数の主張は文書に書かない」を追加した。
**現在の状態を述べる数は書かず、出所を指す。過去を述べる数は残してよい。**
前契約の実測（現在を述べる数の主張 3 件がいずれも古く、いずれも機械検査を通過して
いた）を根拠として引いた。**追加の検査は作っていない**（指示どおり）。
出所の書かれていない数は原理的に照合できないためである。

---

## 4. Phase C — 派生物の食い違い

### 個別記録に列が無かった（`harvest_runindex.py`）

| 群 | 件数 | 外部記録の 2 列 |
|---|---:|---|
| `transfer_legacy` | **29** | **無し**（鍵 55） |
| その他 | 722 | 有り（鍵 57） |

生成経路が 2 系統あり、第 2 経路（`transfer_legacy` を組み立てる箇所）が新しい列を
出力していなかった。**主経路と同じ `harvest_tracking(run_dir)` を呼ぶ**ようにした。
`None` を詰めるのではなく同じ出所から取る。`wandb_run.json` が無い run が空欄になるのは
正しく、**遡っての対応づけは行わない。**

### 既存の値が変わらないことの確認

再生成の前に、`index.csv` の全 751 行の `path` が追跡下であること（**追跡外 0 件**）を
確かめた。正本を生成できるホストの条件である。

| 照合 | 実測 |
|---|---|
| 新旧のヘッダの集合差 | **追加 0 / 削除 0**（`index.csv` には元から 2 列あった） |
| 共有 91 列 × 751 行 | **差分 0 セル** |
| `experiments.csv` / `verdicts.csv` の md5 | **不変** |
| 個別記録の列 | **751 件すべてに有り / 無し 0 件** |
| 差分の範囲 | 個別記録 29 件（各 +2 行）と `backlog.md` の再生成のみ |

**名前の部分一致では探していない。** 新旧のヘッダを集合として比較した。

### 投影の来歴が遅れる問題（`build_context.py`）

来歴に「`runindex/` に触れた最後の commit」を刻んでいた。索引と投影を同じ commit へ
入れると、**その commit 自身が「最後に触れた commit」になり**、再生成した値が記録済みの
値と食い違って検査が落ちる。順序を入れ替えても解消しない。前契約
`T-2026-08-11-canonical-index-refresh` の §5-4b では、実際に停止して利用者の判断を
仰いでいる。

前契約に記録されていた 3 案から **(i) 内容の要約値**を選んだ。

| 案 | 採否と理由 |
|---|---|
| (i) 内容の要約値にする | **採用。** 記録したかどうかに左右されない。G3 の「どちらの時点でも通る」を満たす唯一の案 |
| (ii) 1 commit にまとめて省く | 不採用。来歴そのものを失う |
| (iii) 2 段 commit を規約として書く | 不採用。**失敗する中間状態を許す**ため G3 を満たせない |

`generated_from_commit` と `generated_from_date` を `generated_from_digest`
（正本 3 ファイルの内容の sha256 先頭 12 桁）へ置き換えた。git を呼ばないことを
試験で固定した（呼ぶと作業ツリーの記録状態に依存して遅れが戻る）。

### G3 ゲート（`pass`）— 3 時点すべて

| 時点 | `context-check` |
|---|---|
| 索引を再生成した直後（記録前） | **exit 0** |
| **索引を記録した直後** | **exit 0**（従来はここで必ず落ちていた） |
| 投影も記録した直後 | **exit 0** |

---

## 5. Phase D — 調査のみ

### 失敗する試験 4 件の原因（特定できた）

`tests/test_research_logger.py` の 4 件は、**実装に後からガードが入り、試験が更新されて
いない**ために失敗している。推測ではなく実装から特定した。

- 試験の `_fake_manager` は空のディレクトリを作るだけで `metrics.json` を置かない。
- `log_run` は既定 `status="completed"` で、`23aeca6`「fix(notion): efros で
  auto_notion_sync が黙死する二重バグを修正」で入ったガードに当たる。
  `metrics.json` が空/欠落なら**投稿せず `None` を返す**（`research_logger.py:72-85`）。
- 試験が最後に触れられたのは `c4228ed` で、**ガードより前**である。

したがって `log_experiment_to_notion` は呼ばれず、`call_count` は 0、戻り値は `None`。
試験が期待する `"page-abc"` にならない。**環境には依存しない。**

### 直し方の案 3 つ（選ばない）

| 案 | 内容 | 影響 |
|---|---|---|
| (a) 試験が `metrics.json` を置く | ガードの意図（空 metrics を completed として投稿しない）を保ったまま、冪等性の検証を回復する | 試験のみ。最小 |
| (b) 試験が `status="running"` を使う | ガードは `completed` のみに掛かるため通る | **「completed の冪等性」は測れなくなる** |
| (c) ガードを呼び出し側（スイープ）へ移す | `log_run` は投稿に専念し、何を投稿しないかは呼び出し側が決める | 責務の変更。影響範囲が広い |

**判断が要る点:** `log_run` の戻り値の規約である。現在は「skip」も「失敗」も `None` で
区別できず、呼び出し側は見分けられない。無認証で no-op になる設計もあるため、
**no-op のときの戻り値も併せて決めないと同じ食い違いが再発する。**（`B-40` に起票済み）

### 外部記録への送信実績（読み取りのみ）

| 観点 | 実測 |
|---|---|
| 手元の痕跡 | `wandb/` に **319 の run ディレクトリ**。`offline-run-` は 0 件。最古は 2026-06-29 |
| 索引が読む対応づけ | `wandb_run.json` が **0 件**。`index.csv` の `wandb_run_id` は全 751 行が空 |
| 外部への到達 | **到達できた。** 認証も通り API が応答した |
| 外部の件数 | 現在の資格情報の entity 配下の **project は 0 件**。目的の project も存在しない |
| 一致するか | **突き合わせられない。UNKNOWN** |

**到達性の問題と実績の有無を混同しない。** API へは到達できているので「確かめられ
なかった」のは到達性ではない。しかし資格情報と entity は `eef6687`
「rotate the W&B credential and entity」で更新されており、**過去の送信先は現在の
資格情報からは辿れない。** 現在の entity に project が無いことは、過去に送信されて
いなかったことを意味しない。

**手元に痕跡があることは送信された証拠にならない。** `offline-run-` が 0 件である
ことから実行時は送信する設定だったと読めるが、送信が成功したかは手元からは判定
できない。資格情報の値は出力していない。

### G4 ゲート（`pass`）

`research_logger` と `tracking` への差分は、`origin/phase0` との比較でも作業ツリーでも
**0 件**。調査のみで実装は変更していない。

---

## 6. 完了判定

| # | 判定 | 実測 | |
|---|---|---|---|
| 1 | 様式が広がった | 版 2 で `gates[].note` 必須・`deviations` 一覧・`note` 80 字以上を検証 | ✅ |
| 2 | 古い版を拒まない | 版 1 の既存 4 件がいずれも exit 0 | ✅ |
| 3 | 投影が転記する | 既存 45 件 + 本 task 17 件 = **62 件すべて原文一致** | ✅ |
| 4 | 投影が冪等 | 2 回生成して md5 一致 | ✅ |
| 5 | 手による編集を検出 | 1 行追加で非ゼロ → 再生成で 0 | ✅ |
| 6 | 分母なしで警告が出ない | L2-8 が 0 件 | ✅ |
| 7 | 分母ありで警告が出る | L2-8 が 3 件 | ✅ |
| 8 | 数の主張の規約がある | `tasks/README.md`「数の主張は文書に書かない」 | ✅ |
| 9 | 個別記録の列が揃った | 751 件すべて有り / 無し **0 件** | ✅ |
| 10 | 既存の値が不変 | 91 列 × 751 行で差分 0 セル、他 2 ファイルは md5 不変 | ✅ |
| 11 | 二段階のどちらでも検査が通る | 3 時点すべて exit 0 | ✅ |
| 12 | 失敗の原因が記録 | ガードと試験の時期の食い違いとして特定 | ✅ |
| 13 | 送信実績が記録 | 到達できたが突き合わせ不能。**UNKNOWN** と明記 | ✅ |
| 14 | 調査対象を変更していない | 差分 0 件 | ✅ |
| 15 | 契約検証が通る | exit 0 | ✅ |
| 16 | 実行前検査が通る | exit 0 | ✅ |
| 17 | 試験が不変か減った | 開始前 `5 failed / 314 passed` → 終了後 `5 failed / **342 passed**`。増分 28 は本 task の新規試験で、失敗は同じ 5 件 | ✅ |
| 18 | 禁止領域が無変更 | `experiments` `transfer` `data/splits` `context/conventions.md` すべて出力なし。過去の完了報告の変更 0 件 | ✅ |
| 19 | 抑止の目印を解除した | 起票の後に実施（§9） | ✅ |

---

## 7. 起票者の誤り

| # | 型 | 内容 |
|---|---|---|
| 1 | `self_contradiction` | G1 が Phase A の直後に「本 task 自身の完了報告を拡張した様式で書く」ことを求める一方、報告の作成は Task 8 に置かれている。**指示どおり Phase A の直後に評価しようとすると、まだ測っていないゲートの判定や試験の件数を書くことになり、禁止 10 と衝突する** |
| 2 | `check_does_not_check` | Task 6 Step 2 が案を読むために示した探し方 `grep -rn "スタンプ" ... \| head` は、**表示用の切り詰めによって肝心の 3 案が並ぶ行を落とす**。指示どおり実行すると案を読まずに選ぶことになる。SPEC 自身が注意 3 で「記録を作る流れに表示用の切り詰めを混ぜない」と禁じている型である |

---

## 8. deviations

**空ではない。** 3 件（`result.yaml` に型つきで記録）。

1. **`spec_defect`** — G1 の評価時点。機械的な部分を Phase A 直後に実測し、報告を使う
   照合は報告を書いた後に行った（§7-1）。
2. **`judgement`** — 投影の生成物を列挙している文書 5 箇所を、指示に無いが同時に直した。
   前契約の実測から、投影を足した時点で直さなければ必ず古くなるためである。
3. **`judgement`** — G2 の陽性対照の一時契約が参照解決で FAIL したまま、警告の発火を
   確認した。警告は L1 通過後に出るため確認には足りている（§3）。

---

## 9. 残った課題

`result.yaml` の `followups` 5 件と `unknowns` 3 件に構造化して記録した。
**投影 `context/auto/results_recent.md` から原文のまま読める。**
本 task 以降、この節を人が転記する必要は無い。
