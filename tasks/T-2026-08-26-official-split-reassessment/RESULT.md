# RESULT — T-2026-08-26-official-split-reassessment

**kind:** `analysis` / ホスト `philip` / 分岐 `feat/official-split-reassessment` / **GPU 不使用**

証跡は `audit.md`。解析の結論は `experiments/analysis/official_split_reassessment/REPORT.md`。

---

## 判定

**verdict: pass**（`UNKNOWN` 4 件、逸脱 5 件）

| Gate | 結果 | 根拠 |
|---|---|---|
| G1 | **pass** | 三つの異質な方法（`experiments.csv` の列 / `index.csv` の列 / `experiment_id` の文字列）が一致し、陰性対照 3 種がすべて 0 件（`audit.md:124-166`） |
| G2 | **pass** | 136 行すべての効果量・種の数・揺らぎの出所・決定性の有無を表にした。記録が無い欄は `(記録なし)` で残した（`audit.md:184-200`） |
| G3 | **pass** | 分け方に依存する判定を当てていない（索引に fold を表す列が **0 件**）。対照は **55.1 倍**に分離（`audit.md:202-224`） |
| G4 | **pass** | 二つの枠組みを並べ、食い違いを特定した（`audit.md:226-240`） |

---

## 結論（起票者が判断に使う事実）

🔴 **公式の分割で測られた結果は、六つの結論のうち三つにしか存在しない。**
🔴 **存在する三つはすべて検証側（val: 09, 10）である。評価側（test: 04, 05, 07）は全結論で 0 件。**

| 結論 | val | test |
|---|---:|---:|
| 1 術具の情報を工程認識へ渡す | **8** | 0 |
| 2 全体平均の特徴を足す害 | **0** | 0 |
| 3 正しい術具存在の追加の利得 | **1** | 0 |
| 4 入力側の雑音除去 | **0** | 0 |
| 5 工程を弁別しない術具を落とす | **11** | 0 |
| 6 工程の情報を術具検出へ渡す | **0** | 0 |

**「公式の分割だけを主軸に据える」ことは、いまの記録では三つの結論についてしか行えない。**

**すべての行に共通する制約** — 検証側のみ / 種 3 本 / 揺らぎは**対の差**（`paired_delta` が 136/136 件）/
**決定性の記録が索引に 0 件**（`determin` `cudnn` `benchmark` `nondet` のいずれも列が無い）。
**「記録が無い」は「制御されていた」を意味しない。**

### 撤回の対象は、公式の分割ではどうだったか

**「工程を弁別しない術具を落とす」は公式の分割（検証側）で 11 件。評価側は 0 件。**

- 分類の正しさ Δ: 最小 −0.01799 / 中央 **+0.02403** / 最大 +0.05462。**11 件中 10 件が正**
- 工程平均 Δ: 最小 −0.03741 / 中央 **+0.05898** / 最大 +0.11108。**11 件中 8 件が正**

**食い違いは符号の向きにある。** 公式の分割の検証側では大半が**正**、撤回の根拠は**負**の方向であった。
**どちらが正しいとは書かない。撤回の可否は判断しない。**

---

## 完了判定

| # | 判定 | 実測 | 空振りでないことの確認 |
|---|---|---|---|
| a | 所在を確かめた | 三方法一致（val 200/1142/200、test 0/0/0） | 存在しない語 3 種すべて **0 件** |
| b | 検証側と評価側を区別した | 全行に `split` を併記。**全件 val、test 0 件** | 区別の無い行は 0（`split` 空の 13 行は六つの結論に該当しない） |
| c | 効果量と種と揺らぎの出所を整理した | 136 行。`sigma_source=paired_delta` 136/136、`delta_n_seeds=3` | 記録が無い欄は `(記録なし)` で残っている |
| d | 推定で埋めていない | 推定値 0 | 出所の無い数値は無い。全数値が 2 つの csv へ遡る |
| e | 当てられない判定を当てていない | 分け方に依存する判定を**当てていない** | 索引に `fold`/`cv`/`leave` を名に含む列が **0 件** |
| f | 当てられない理由を記録した | 「公式の分割には分け方が存在しない」（`conventions#split` の原文で裏づけ） | 理由が `controls.txt:6-13` にある |
| g | 陽性対照が検出された | `b2a_det2phase_oracletool`: `significant`、\|Δ\|/σ=**11.42** | SPEC 指定の GAP は 0 件のため代替。選定理由を記録 |
| h | 陰性対照が検出されなかった | `t1a_region_only_mask_top3`: `not_significant`、\|Δ\|/σ=**0.21** | 陽性の **55.1 倍**の差。常に検出する判定ではない |
| i | 陰性対照が経路を通っている | 同じ学習・評価・対の差を経て Δ分類=+0.00049 | **構造上の零ではない**。同じ行の Δ分節=−4.63、\|Δ\|/σ=4.16 と指標により零から離れる |
| j | 決定性の有無を併記した | 全行 **「記録なし」** | 索引の列を機械的に走査して 0 件を確認（推測していない） |
| k | 二つの枠組みを並べた | `REPORT.md` §2.2 の表 | 同じ節に両方がある |
| l | 食い違いを特定した | **符号の向き**（公式の分割は大半が正、撤回の根拠は負） | 零件ではない |
| m | どちらが正しいと書いていない | 優劣の記述なし | 「枠組みが違えば違う数字が出る」と明記 |
| n | 撤回の対象の結果を明示した | 11 実装の Δ を全件表に | 値が入っている |
| o | 撤回の可否を判断していない | 判断なし | 「起票者と利用者の判断事項」と明記 |
| p | 変更範囲の一覧を出した | `forbidden-check` **exit 2・違反 7 件**。**全件が本契約の成果物**（別契約の混入 0） | 内訳を一件ずつ列挙 |
| q | PR が存在し base が正しい | §送出 | base=`phase0`（起点と同じ）。既定の `master` ではない |
| r | 報告が二つに分かれている | `RESULT.md`（事実）と `audit.md`（証跡） | 別ファイルで行番号で指している |
| s | 締切に対する経過が記録されている | A=**541 分** / B=543 / C=545 / D=545 / E=548（上限 600） | 各 Phase の入口で測った。§想定外を参照 |

---

## 次の契約で使う値

| 値 | 実測 |
|---|---|
| 公式の分割の評価側の記録 | **0 件**（三方法で確認）。test での再評価は**未着手の空白** |
| 判定を持つ行 | **136 件**、すべて `arm=injection`・`control_of=分母` |
| 揺らぎの出所 | **`paired_delta` が 136/136** |
| 決定性の列 | 両 csv とも **0 件** |
| 分母自身の `sigma_source` | **空**（SPEC 第 5 節の引き継ぎと整合） |
| 索引の `split` 列の意味 | **未確定**。文書 §3.17 は test 値を載せるが、索引では同じ群が `val` |

---

## 起票者の誤り

1. **`asserted_without_measuring`** — SPEC Task 4 Step 2 が陽性対照に指定した
   「全体平均の特徴を足すことの害」は、**公式の分割で 0 件**である。
   指示どおり設置しようとすると**対照が置けない**。代替を選ぶ判断を実行者に強いた。
2. **`asserted_without_measuring`** — 同 Step 2 が陰性対照に指定した
   「工程の情報を術具検出へ渡すことの全体的な効果」も、**公式の分割で 0 件**である。同上。
3. **`asserted_without_measuring`** — 申し送り 3「公式の分割では術具を落とす手法について、
   分類の正しさは動かず、工程ごとの成績を平均した指標が改善していた」は**実測と食い違う。**
   分類の正しさも動いている（11 件中 10 件が正、\|Δ\|/σ 0.21〜28.32）。
   指示どおり「確かめられるか見る」を実行した結果、**確かめられなかった。**
4. **`asserted_without_measuring`** — SPEC 第 1 節は「これまでの研究はその分割に従って進められてきた」
   と述べるが、**評価側で測られた結論は一つも無い。** 従っていたのは**検証側だけ**である。
   この前提のまま「公式の分割を主軸に据える」と、**存在しない評価側の結果があるかのように読まれる。**
5. **`self_contradiction`** — `outputs.destination` が `experiments/` 配下を指すのに
   `forbidden-check` は `experiments/` を禁止領域として扱う。SPEC 第 3 節 Task 6 Step 2 は
   これを「道具の側の欠陥」と正しく予告しているが、**道具は直っていない。**
   予告があっても毎回 exit 2 になり、内訳の手作業が要る。
6. **`check_does_not_check`** — 完了判定 s「締切に対する経過が記録されている」は
   **経過を測るだけで、超過を防がない。** 本契約では `decisions_required` の回答待ちに
   **9 時間**を要し、Phase A の開始時点で既に **541/600 分**を消費していた。
   **待ち時間が上限に算入される設計であることが本文に書かれていない。**

---

## 逸脱・想定外・UNKNOWN

### 逸脱

1. **`decisions_required` 3 件を利用者へ提示して停止し、回答（三件とも契約の外）を得て空にした。**
   自分では決めていない。
2. **一つ抜き検証側の数字を再集計していない。** 残り時間が足りず、先行契約の報告を引用した。
   **公式の分割側は全件を実測している。**
3. **`make taskindex` / `make inbox` を回していない**（SPEC 禁止 4）。手順書 §6 の要求と両立しない。
4. **前の契約の退避（`stash@{1}`）を戻していない。** 元の分岐へ戻ってから戻す。
5. **索引の `split` 列の意味を実装から確かめていない。** 文書 §3.17 との食い違いを記録するに留めた。

### 想定外

- 🔴 **Phase A の開始時点で既に 541/600 分を消費していた。**
  P6 の回答待ちに **9 時間**を要したためである（開始 UTC 06:27:11 → 回答の記録 UTC 15:27:27。
  `spec.yaml` の更新時刻で裏づけ）。時計は飛んでいない。
  **残り 59 分で読み取りと再集計を完了させた。** 落とした範囲は逸脱 2・5 に記した。
- **出力が途中で切れた。** `python … | tee out.txt | head -60` と書いたため
  `head` が閉じて SIGPIPE で処理が死に、**六つの結論のうち 3 件しか書かれなかった**（107 行）。
  `head` を外して取り直した（214 行）。
- **退避の添字がずれた。** 前契約で作った退避が `stash@{0}` → **`stash@{1}`** へ移った。
  他ホストの契約が退避を積んだためである。**添字ではなく名前で指すこと。**
- **`forbidden-check` が exit 2・違反 7 件を返した。** SPEC 第 3 節が予告したとおり、
  **成果物の置き場所そのものが禁止領域**である。**全 7 件が本契約の成果物**であり、
  別の契約のファイルの混入は **0 件**であった（前契約では 12 件混入していた）。

### UNKNOWN

| # | 測れなかったもの | 理由 |
|---|---|---|
| 1 | 文書 §3.17 の test 側の値を索引で裏づけること | 索引では該当群の `split` が `val`。列の意味を実装から確かめる時間が無かった |
| 2 | 決定性が制御された実行が一つでもあるか | 索引に決定性を表す列が **0 件**。記録から読めない |
| 3 | 公式の分割の**評価側**での六つの結論のいずれか | **0 件**。測られていない |
| 4 | 一つ抜き検証側の数字の再集計 | 残り時間が足りず、先行契約の報告を引用するに留めた |

---

## 送出

（この節は commit・PR の後に埋める）
