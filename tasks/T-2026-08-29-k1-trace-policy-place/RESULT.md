# RESULT — T-2026-08-29-k1-trace-policy-place

**kind:** `impl` / ホスト `philip` / 分岐 `feat/k1-trace-policy-place` / **GPU 不使用**

証跡は `audit.md`、成果物は `docs/stage0/`。**同じ内容を二度書かない。**

---

## 判定

**verdict: partial** — Phase A と Task 2・3 は完了。**Task 1（方針文書の配置）は
SPEC の前提が実測と食い違い、SPEC §7 の指示に従って停止した。**

| Gate | 結果 | 根拠 |
|---|---|---|
| G1 | **pass** | 四系統すべてが {方法・範囲・揺れ・件数} で確定し、結論が二値で一意に定まった（`audit.md:26-150`） |

---

## K1 の結論

**「遡れない（確定）」。** 四系統すべてで、K1 の三値を同時に説明する出所は見つからなかった。

| 系統 | 方法 | 範囲 | 件数 |
|---|---|---|---|
| 1 版管理の全履歴 | `git log --all -S` で初出 commit を特定 | 全 commit・全ファイル | K1 の文脈での対は **1 件**（初出 `909dd193` 2026-08-02） |
| 2 生ログの直接走査 | 索引を経由せず `metrics.json` / `per_class_ap.json` を直接読む | `s4_phase_baseline_01{0,1,2}_..._aligndetr*` と分母 計 6 run | **一致 0 件** |
| 3 外部の実験記録 | 設定済み資格情報で**読み取りのみ**照会 | run 479 件の summary 全数値 | `0.686` 近傍 **3 件**（すべて `grasp_inference` の別実験）。他は **0 件** |
| 4 リポジトリ内の文書 | 現在版で `0.801` と `0.179` の共起を走査 | 版管理下の全文書 | **11 件**。うち**出所を主張する文書は 0 件** |

**検索器の対照**: 陽性 `0.050877` → **4 commit** ／ 陰性（乱数 2 種）→ **0 commit**。

### 系統 1 の初出を一段遡った結果

初出 `experiments/analysis/frozen_source_signature3_R_index/REPORT.md:52` は

> **台帳記載値のみで、per-phase F1 の生数値はこのホストでは再現できない**

と書いている。🔴 **初出の時点で既に出所が無いと述べており、典拠の連鎖はここで止まる。**

### 系統 2 で判明した二つの事実

1. **「metrics.json が空」は philip では成り立たない**（918〜920 バイトで実在）。
   上の文書は別ホスト（andrew）の視点で書かれている
2. 🔴 **config が宣言する凍結条件が名前と食い違う。** aligndetr 群 3 件・分母 3 件とも
   `test_cfg.backbone = relation_detr_resnet50_frozen_seed42`。
   2026-07-03 の「AlignDETR 学習失敗 → 通常学習 ckpt で代替」の実体である

生ログから直接計算した **Δ_phase = −0.051925**（台帳 −0.0201 との差 **0.031825**）。
hemostasis F1 は 0.1667 / 0.0 / 0.0 で **0.801 にも 0.179 にも一致しない**。

**以後 K1 は出所不明として扱い、正規の再測定は送り手掃引で行う。**
矛盾する複数の出所へは遡っていないため escalate に当たらない。

---

## 完了判定

| # | 判定 | 実測 | 空振りでないことの確認 |
|---|---|---|---|
| a | 配置の同一性 | 🔴 **判定不能。** 旧バンドルに本文の区画が存在せず、配置していない | 抽出器の陽性対照は成立（実在する 2 区画は正しく取り出せた）。**不在は抽出の失敗ではない**（`audit.md:163-194`） |
| b | K1 捜索の網羅 | 四系統すべてが表で埋まり、**結論は「遡れない（確定）」** | 陽性 `0.050877` **4 commit** ／ 陰性 乱数 2 種とも **0 commit**（`audit.md:37-43`） |
| c | 取り込みの拡張 | 陽性（宣言済み追加ファイル）**通過** ／ 陰性1（未宣言）**拒否** ／ 陰性2（要約値改竄）**拒否** | 三つの試験バンドルの終了と拒否理由を `audit.md:231-245`。**試験バンドルは版管理に入れていない** |
| d | 後方互換 | 三種のみの形式が**通過**。実在する 3 契約のバンドルとも**検証通過** | `audit.md:247-253` |
| e | 変更範囲と文書検査 | 変更は §2 の対象のみ**対象外 0 件**。`docs-check` `agent-check` `forbidden-check` すべて **exit 0**。リンク 57 件すべて実在 | 実在しない経路を含む一時複製で検出 exit=1（複製は削除）（`audit.md:283-300`） |
| f | 出所の区別 | 三種すべてが実在 | 実測 `A7:87` ／ 提供元回答 `A3:48` ／ 文献確認 `A9:3`（`audit.md:273-281`） |

---

## 実測（次の契約で使う値）

| 値 | 実測 |
|---|---|
| 取り込み拡張後の宣言形式 | `spec.yaml` の `inputs.bundle_extras: [{path, sha256, bytes}]`。仕様は `tasks/README.md` |
| 要約値の数え方 | **末尾の改行を 1 つに正規化した本文**で数える（`parse_bundle` の正規化と揃える） |
| 追加ファイルの置き場 | **契約ディレクトリ配下のみ**。最終配置は契約の Task が行う |
| 旧バンドルの実体 | `T-2026-08-28-policy-v2-doc-sync` の添付は **19,354 バイト・二区画**（`spec.yaml` 2267 / `SPEC.md` 16840）。全体の要約値は宣言と一致 |
| Stage 0 の成果物 | `docs/stage0/` に A0〜A9 と `stage0_summary.md`（相対リンク 10 件すべて実在） |
| 生ログ由来の Δ_phase | **−0.051925**（索引経由の −0.050877 とわずかに違うのは母集団の範囲差） |

---

## 起票者の誤り

1. **`asserted_without_measuring`** — SPEC §1「バンドル内の三つ目の区画
   `research_policy_v2_2026-08-28.md` の要約値と大きさは、**同バンドル内の SPEC.md の §1 に宣言済み**
   （短縮形で 2eb9c882、60490 バイト）」。**実測では三つ目の区画も宣言値も存在しない。**
   旧 SPEC.md の §1 全文を確かめたが `2eb9c882` も `60490` も `research_policy_v2` も 0 件。
2. **`asserted_without_measuring`** — 同 §1「方針文書の正本バンドルは、台帳の行
   T-2026-08-28-policy-v2-doc-sync に添付されている」。添付は実在し要約値も一致するが、
   **中身は二区画（`spec.yaml` と `SPEC.md`）のみ**で本文を含まない。
   指示どおり進めると「三つ目の区画」を取り出す段で必ず止まる。

---

## 逸脱・想定外・UNKNOWN

### 逸脱

1. **開始前から在った汚れ（`.sync-pause.released` 1 件）を退避してから進めた。** `mv` は使っていない。
2. **Task 1 を停止し、Phase A と Task 2・3 を続けた。** SPEC §7 の
   「配置せず停止して報告（Phase A と Task 2・3 は続けてよい。部分完了として報告）」に従った。
   **存在しない本文を書き起こしてはいない**（禁止 1 と整合性の要求による）。
3. **`git checkout` 系の破壊的操作を使っていない。** 前契約で再生成物を失った事故を踏まえ、
   一時複製は `cp` で作り `rm` で消した。

### 想定外

- 🔴 **旧バンドルに方針文書の本文が無かった。** 取得物全体の要約値は宣言と一致しており
  改竄ではない。**最初から入っていない。**
- **試験の途中で自分の誤りを見つけた。** `pack_bundle` の組み立てループが `ALLOWED_FILES` のみを
  回っており、宣言済みの追加ファイルを詰めていなかった。検証側だけ直して組み立て側を
  見落としていた。直してから四試験とも通した（`audit.md:255-264`）。

### UNKNOWN

| 項目 | 理由 |
|---|---|
| K1 の三値の出所 | 四系統すべてで零件。**「遡れない」として確定**した（UNKNOWN ではなく結論） |
| 方針文書 v2 の本文 | **バンドルに存在しない。** 配置できていない |
| 試していない表現の揺れ | 有効数字を増やした形・指数表記・百分率小数 2 桁。**「探した」に数えていない** |

---

## 送出

| # | 実測 |
|---|---|
| commit | `09476214`（**16 ファイル**）。対象外の混入 **0 件**。**試験バンドルは版管理に入れていない** |
| push | `origin/feat/k1-trace-policy-place` **exit 0** |
| PR | **#163**。**`base=phase0`**（分岐の起点と同じ）。接頭辞 `feat/` |
| `make task-validate` / `task-preflight` | **exit 0** / **exit 0**（5 PASS / 4 SKIP / 0 FAIL） |
| `make docs-check` / `agent-check` / `forbidden-check` | すべて **exit 0** |
| 投影の再生成 | `make taskindex` `make inbox` を実行。**索引は再生成していない**。三検査とも exit 0 |
| `make task-report` | （送信後に記す） |
| `.sync-pause` | （解除後に記す） |

### 秘匿の自主検査

| 対照 | 対象 | 結果 |
|---|---|---|
| **陽性** | 実値を埋めた囮（**版管理外**） | `live:NOTION_API_KEY=1, live:WANDB_API_KEY=1, notion_token=1, pem_private_key=1` **exit 1** |
| **陰性** | 送出する 16 ファイル | `wandb_key_shape=1`（`audit.md` のみ）**exit 1** |

🔴 **陰性対照の 1 件を目視した。** バンドルの**区切り文字列**
（`BUNDLE-378eef9428be…`、`audit.md` に構造の記録として引用）であった。
git の対象ではないが、**`parse_bundle` が本文の一行目から読む公開の構造値**であり資格情報ではない。
**環境の実値との照合は 0 件。検査は無効にしていない。**
