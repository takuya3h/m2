# RESULT — 起票者の誤りを、過去の実例を教師にして機械で捕まえる

**task_id:** `T-2026-08-11-issuer-defect-detector`
**実行ホスト:** `lecun`  **分岐:** `feat/issuer-defect-detector`  **status:** pass
**PR:** #82（`phase0` へ Draft で起票。**統合していない**）

機械可読の対は `result.yaml`。教師データは `defects.md`。
**散文と対は別々に書いてある。** 散文から値を抜いて対を作っていない。

---

## 1. 解決された参照

### `contract.inject_verbatim: [conventions#prohibitions]`

`context/conventions.md`（版 `d422b08`）の該当アンカーの**原文**。要約していない。

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |

`spec.yaml` の `contract.prohibitions` が挙げる 5 件はすべてこの表に存在する。

### `inputs.sigma_policy`（省略）

`spec.yaml` が省略しているため `context/conventions.md#sigma` の既定値を継承した。原文。

    series: pstd
    sigma_source: paired_delta
    delta_sigma_source: paired

本 task は `kind: impl` で数値の比較を行わないため、この継承は使っていない。
**使わなかったことを記録する。** 解決を省いたのではない。

### `contract.conventions_rev`

実測して `d422b08`。`spec.yaml` の記載と一致したため置換していない。

    git --no-pager log -1 --format=%h -- context/conventions.md

### 解決しなかった参照

`inputs.denominator.ref` と `inputs.frozen_source.ref` は `spec.yaml` に無い。
`kind: impl` のため `prereg.md` も無い。

### L3 で実行されなかった検査

`make task-preflight` は 5 PASS / 0 WARN / 4 SKIP / 0 FAIL。
**`SKIP` は合格ではない。** 実行されなかった 4 件は次のとおり。

| 検査 | SKIP の理由 |
|---|---|
| P2 `cuda_ext_loaded` | `plan.env.preflight` に記載なし |
| P3 `deterministic_flags` | `plan.env.preflight` に記載なし |
| P4 `prereg_committed` | `kind=impl` のため対象外（exp のみ） |
| P5 `frozen_source_hash` | `kind=impl` のため対象外（exp のみ） |

---

## 2. 何をしたか

**文言による自制は働かなかった。だから機械にした。** これが本 task の全体である。

材料は既にあった。`tasks/*/result.yaml` の `issuer_defects` に、実行者が型つきで書いてきた
記録が **39 件**ある。これを検出器の正解データとして使い、**検出率を測った。**
検出率が出るということは、**検出器そのものが空振りかどうかを測れる**ということである。

4 層を作った。

| 層 | 誰が回すか | 何を守るか | 実体 |
|---|---|---|---|
| 1 | 起票者（起票のたび） | 契約の本文に既知の危険が無いこと | `make spec-check TASK=<task_id>` |
| 2 | 起票者（書く時点） | 完了判定の各行に空振りでないことの確認があること | `make task-validate`（L1-9） |
| 3 | 実行者（報告時） | 対に陽性対照の記録があること | `make task-validate`（`positive_controls`） |
| 4 | 実行者（実行直前） | 層 1 を回し忘れても気付くこと | `make task-preflight`（P9、**警告**） |

---

## 3. 教師データ（Phase A・G1）

### 対象の一覧が正しいことを先に確かめた

一覧を 3 経路で取り、**13 件で完全に一致した。**

| 経路 | 件数 |
|---|---|
| `ls -1 tasks/*/result.yaml` | 13 |
| `find tasks -name result.yaml -type f` | 13 |
| `git ls-files 'tasks/*/result.yaml'` | 13 |

git 経路も一致したため、**追跡外の対が隠れていない**ことも同時に分かる。

**しかし 3 経路すべてが同じものを取りこぼしていた。** 13 件のうち 1 件は雛形
`tasks/_templates/result.yaml` であり契約ではない。その `issuer_defects: []` は placeholder で
あって実測ではないため、教師データに混ぜると分母が汚れる。除外して**実契約の対は 12 件**、
**対を持たない契約は 23 件**（本契約自身を含む）。

一致は正しさを意味しない。**同じ探し方の変種を 3 つ並べても、同じ誤りを 3 回犯すだけである。**

### 型ごとの件数（手で数えていない）

| 型 | 件数 |
|---|---|
| `check_does_not_check` | 12 |
| `asserted_without_measuring` | 13 |
| `self_contradiction` | 11 |
| `shell_assumption` | 3 |
| **合計** | **39** |

`issuer_defects` が空の対は 0 件。12 件すべてが 1 件以上を持つ。

### 検出可能性の 3 分類

| 分類 | 件数 |
|---|---|
| `syntactic` | 3 |
| `structural` | 13 |
| `semantic` | **23** |
| **合計** | **39**（型ごとの合計と一致） |

**`semantic` が 23 件、つまり 59% は構文でも構造でも捕まらない。** 各件について
「何が分からないと判定できないか」を `defects.md` に書いた。多くは
「命令が実際に何件返すか」「実装の比較キーが何か」「外部台帳の行の状態」といった、
**契約の本文の外にしか答えが無いもの**である。

`structural` の定義は「契約の構造どうしの矛盾」だが、`host_mismatch` の 2 件は
**本文の宣言と実行環境の事実**の照合であり、3 分類のどれにも厳密には収まらない。
機械で確実に検出できるため `semantic` には入れず、定義の隙間を申し送りに残した。

---

## 4. 検査（Phase B・G2）

### 検出率は 11 / 16

対象は `syntactic` と `structural` の **16 件**。8 規則で **11 件**を検出した。

| 規則 | 分類 | 裏付けの教師データ |
|---|---|---|
| `truncation_in_measurement` | syntactic | report-projection#2 |
| `unquoted_glob` | syntactic | hts-comparability-audit#3 |
| `separated_source` | syntactic | codex-parity#1 |
| `forbidden_vs_output` | structural | impl-history#1、template-leak#1 と #3、codex-parity#4 |
| `host_mismatch` | structural | bundle#3、docs-reconciliation#3 |
| `integration_prohibited_without_pause` | structural | bundle#5、template-leak#2 |
| `gate_requires_report_before_end` | structural | report-projection#1 |
| `reverify_contradiction` | structural | hts-comparability-audit#2 |

**検出できなかった 5 件と、その理由。**

| 教師データ | 理由 |
|---|---|
| `codex-parity#1` | 誤りは監査対象の**手順書側**にあり、契約の `SPEC.md` 内の行を特定できない（教師データの箇所が UNKNOWN） |
| `codex-parity#2` | `files_vs_check` を実装できていない。検査の走査範囲を解決する道具が無い |
| `implementation-history-index#2` | 同上 |
| `bundle-attachment-transport#3` | `host_mismatch` は**実行ホストに依存する**。同契約は `lecun` を宣言し実行環境も `lecun` のため一致する。この誤りは bengio で実行されたときに起きた |
| `docs-reconciliation#2` | 「判定が空振りかを問わない」型。規則にせず **Phase C の様式で構造的に防いだ** |

`bundle#3` は重要である。**規則は正しいのに、この環境では発火しない。**
検出率は環境の関数であり、単一の数ではない。試験は期待値を `hostname` から計算する。

### 規則を思いつきで足していない

各規則は教師データの実例を裏付けに持つ。**裏付けの無い規則を足すと分母が動き、検出率が
意味を失う。** 試験がこれを強制する（`test_every_rule_is_backed_by_teacher_data`）。

### 誤検出は 1 件

素朴な実装は該当 **301 件**を出した。SPEC の「誤検出が多い → 規則を絞る。検査を無効に
しない」に従い 2 度絞り込んだ。

| 規則 | 絞り込み | 該当の変化 |
|---|---|---|
| `truncation_in_measurement` | 件数を指定しない切り詰めに限る（`head -30` は書き手が上限を選んでいる。素の `head` は既定 10 行で**黙って**落ちる） | 102 → 6 |
| venv 依存の検出 | 既存実装の「分離された `source`」へ委譲し、さらに `source` が `source` へ続く場合を除く（後の読み込みは前に依存しない） | 151 → 18 → 9 |

合計 **301 → 64 件**。

該当 64 件を機械的に「対応の無いものは誤検出」と数えると 60 件になる。**しかし
「誤検出とは、誤りが無いのに該当することである。」** 定義に照らして 1 件ずつ読んだ。

| 内訳 | 件数 |
|---|---|
| 教師データを持つ 12 契約への該当 | 25 |
| うち教師データに対応する規則 | 13 |
| うち対応が無く、**実在する誤りだが報告に書かれていない**（記録漏れ） | 11 |
| うち**誤検出** | **1** |
| 対を持たない 23 契約への該当（対応の有無を判定できない） | 39 |

**誤検出の 1 件**は `host_mismatch` を完了済み契約（`codex-parity`、`efros` を宣言）へ
当てたものである。規則の誤りではなく**適用範囲の誤り**で、完了した契約は別のホストで
実行されたのだから、いま別のホストから走らせれば当然一致しない。

**記録漏れの 11 件**は実物を読んで確認した。`split-and-recipe-audit:320-321` の未引用グロブ
4 件、`implementation-history-index:46` の統合の禁止、`forbidden_vs_output` 4 件、
`make-task-start:20` と `:26` の分離された読み込み 2 件。試験で固定してある。

**対を持たない 23 契約への 39 件は誤検出とも正検出とも数えていない。** 教師データが
存在しないため、対応の有無を判定する材料が無い。**教師データは網羅ではない。**

### 陽性対照は規則の数だけある

8 規則それぞれに意図的に違反する最小の入力を作り、**8 件すべてが検出された。**
検出されない規則は 0 件。あわせて違反しない最小の入力 7 件で該当 0 件を確かめた。
**陽性側だけでは空振りを排除できない。**

### 本契約自身が最初の実運用である

`make spec-check TASK=T-2026-08-11-issuer-defect-detector` は 8 規則すべてに該当せず exit 0。

---

## 5. 様式（Phase C・G3）

### 実装前に落ちる件数を測った

| 候補の必須項目 | 満たさない件数 |
|---|---|
| 完了判定の表に「空振りでないことの確認」欄がある | **34 / 35** |
| 対に陽性対照の記録がある | **12 / 12** |

**先に測ったから、移行の方法が必要だと分かった。** 実装してから測れば、既存が全滅した
状態で「どうするか」を考えることになる。

### 版で分岐する仕組みは既にあった

`tasks/_schema/result.schema.json` が `allOf` の `if/then` で `result_version` 1 と 2 を
分けている。**実装を読んでからこれに合わせた。** 推測で様式を壊していない。

- `spec_version: 2` — 完了判定の表に「空振りでないことの確認」の欄を要求する（L1-9）
- `result_version: 3` — `positive_controls` を要求する。**版 2 の要件をすべて含む**

版 3 が版 2 の要件を継承するため、版 2 の分岐の条件を `const: 2` から `minimum: 2` に
変えた。`const` のままだと版 3 が版 2 の要件を失う。試験で固定してある。

### 3 方向すべてを測った

| 向き | 実測 |
|---|---|
| 新様式で欄が空 | **失敗**（L1-9 該当 1 件） |
| 新様式で欄が埋まっている | **通る**（該当 0 件） |
| **既存の契約** | **落ちない**（35 task 0 failed、exit 0） |

対の側も同様に測った。版 3 で `positive_controls` が無い・空配列・`observed` が空文字の
いずれでも失敗し、版 1 と版 2 の既存の報告は通る。

### 検査が本物の表を見ていることを確かめた

**実装当初、この検査は空振りしていた。** 見出しの正規表現を `^\s*\|` と書いたため、
契約が新しい欄の書き方を**字下げして載せた例示の表**（`SPEC.md:247`）を先に拾い、
その直後は表でないため走査が即座に終わり、**本物の完了判定表（324 行）を一度も検査しない
まま合格していた。** markdown では 4 字下げは code block である。

先頭に空白を許さない形へ直し、本物を見ていることを陽性対照で確かめた。判定 1 の欄を
空にすると `SPEC.md:325` を指して失敗する。**行番号が本物の表を指していることが確認の本体
である。** 例示を先に置いた入力で本物が検査されることを試験で固定した。

**本 task が捕まえようとしている誤りの型を、本 task の実装で犯した。** 検査を書く側も
同じ罠を踏む。だから陽性対照が要る。

---

## 6. 実行直前の警告（Phase D）

`WARN` を第 4 の状態として足した。既存が `SKIP` と `PASS` を区別しているのと同じ理由で、
**警告と合格を区別する。**

終了コードは `FAIL` の数だけで決まる（`return 1 if summarize(checks)["FAIL"] else 0`）。
**実装を読んでから合わせたので、`WARN` は終了コードを変えない。**

| 向き | 実測 |
|---|---|
| 該当する契約（`template-leak`） | P9 **WARN**、該当 2 件を行番号つきに出す、`4 PASS / 1 WARN / 4 SKIP / 0 FAIL`、**exit 0** |
| 該当しない契約（本契約） | P9 **PASS**、`5 PASS / 0 WARN / 4 SKIP / 0 FAIL`、**exit 0** |

契約の誤りは実行者の責任ではなく、実行を止める根拠にもならない。
**気付かせることだけが目的である。**

---

## 7. 原理的な限界

**「この検査が対象を検証しているか」は一般には判定できない。** 意味論の問題である。

本 task は全件検出を目標にしていない。測った結果は次のとおりで、**捕まえられない型が
明示されていること自体が成果である。**

- 教師データ 39 件のうち、検査の対象は 16 件（41%）
- 対象 16 件のうち、検出は 11 件（この環境で）
- **39 件全体に対する検出は 11 件（28%）**

「半分捕まえられれば発生率は半減する」という前提には届いていない。**届いていないことを
数で書く。**「概ね」と書かない。

ただし層 2 と層 3 は検出ではなく**様式による強制**であり、検出率とは別の経路で効く。
`docs-reconciliation#2` の型を規則にせず様式で防いだのはその例である。

---

## 8. 完了判定

| # | 判定 | 期待 | 空振りでないことの確認 |
|---|---|---|---|
| 1 | 誤りを全件抽出した | 件数が出る | 対を持たない契約 23 件も出した。雛形 1 件の除外も記録した |
| 2 | 型ごとの件数が出る | 合計が一致 | 分類ごと 39 と型ごと 39 を機械で突き合わせた。不一致なら生成器が exit 1 |
| 3 | 検出可能性で分類した | 3 分類の件数 | `semantic` 23 件すべてに「何が分からないと判定できないか」を書いた |
| 4 | 検査が教師データを検出する | 11 / 16 | 検出できない 5 件を理由つきで列挙し試験で固定した |
| 5 | 規則ごとに陽性対照がある | 8 規則ぶん | 検出されない規則は 0 件。陰性対照 7 件で該当 0 も測った |
| 6 | 誤検出しない | 該当が教師データに対応 | 対応の無い 12 件を 1 件ずつ読み、誤検出 1 件と記録漏れ 11 件に分けた |
| 7 | 欄が空なら検証が失敗する | 非ゼロ | 埋めたら通ることも測った。行番号が本物の表を指すことも確かめた |
| 8 | 既存の契約が落ちない | exit 0 | 落ちる件数（34/35 と 12/12）を実装前に測ってある |
| 9 | 実行直前に警告が出る | 警告あり | 該当しない契約で出ないことも測った |
| 10 | 警告が終了コードを変えない | 変わらない | 該当あり・なしの双方で exit 0 を実物の `main()` で測った |
| 11 | 契約検証が通る | exit 0 | 本契約は `spec_version: 2` であり L1-9 の対象である（版 1 なら空振りだった） |
| 12 | 実行直前の検査が通る | exit 0 | SKIP 4 件の一覧を報告に含めた |
| 13 | 試験が不変 | 失敗が同数 | 開始前 5 failed / 383 passed を先に測った。終了後 5 failed / 423 passed |
| 14 | 禁止領域が無変更 | `make forbidden-check` が exit 0 | 前 task の道具を使い、生成物を除外した件数も出る |
| 15 | 過去の記録が無変更 | `result.yaml` に差分なし | 教師データが固定されている（禁止 8） |
| 16 | 抑止を解除した | 目印が無い | 別名へ移す方式（前 task の実測）。記録に `auto-merge` が出る |

---

## 9. 起票者の誤り

**4 件。** 型と本文は `result.yaml` の `issuer_defects` にある。要約すると次のとおりで、
**本文は要約していない方を読むこと。**

1. `self_contradiction` — Task 2 Step 4「過去の契約が該当するのは正常」と Task 6 Step 2
   「`make spec-check` を exit 0 に」と Step 2 要件 5「省略時は全契約」が同時に成立しない
2. `check_does_not_check` — Task 1 Step 1 の一覧の取り方が雛形を拾う。3 経路で突き合わせても
   3 経路すべてが拾うため一致し、検査は通る
3. `self_contradiction` — Task 4 と Task 5 の Files 欄が、同 Task の Step が要求する変更対象を
   含まない（対の雛形と手順書）
4. `asserted_without_measuring` — 「20 task 連続」は repo から検証できない。報告を持つ契約は
   12 件しかなく、「12 連続」までしか言えない

**`shell_assumption` は見つからなかった。** SPEC が主張した `core.pager` の設定は実測して
`cat` であり、頁送りの記述は正しかった。zsh に関する申し送り（波括弧・単語分割・配列の添字）
も踏まなかった。**申し送りが効いた。**

---

## 10. 逸脱

`result.yaml` の `deviations` に 8 件。**空にしていない。** 主なものは次のとおり。

- 教師データから雛形を除外した（分母の汚染を避けるため）
- `make spec-check` を `TASK=` 前提の運用にした（契約の矛盾の解消）
- 本契約の `spec_version` を 1 から 2 へ上げた（自己適用のため）
- Files 欄に無いファイルを 2 か所で変更した（Step が要求しているため）
- `check_agent_docs.py` の非公開の名を import した（取り出しの自作を避けるため）
- 自作の分析用正規表現の誤りを、数字が都合よく揃ったことから疑って直した

---

## 11. 未解決

`result.yaml` の `unknowns` に 4 件。要点は 2 つである。

**教師データは網羅ではない。** 報告を持つ契約は 35 件中 12 件で、23 件は空白のままである。
過去の報告は書き換えない（禁止 8）ため、検出器が実在する誤りを見つけても教師データには
反映されない。**分母は固定され、分子だけが実態より小さく見える。**

**4 層が実際に回されるかは測れない。** 本 task は仕組みを作っただけである。層 4 が二重の網
として機能するかは、層 1 を回し忘れた契約が次に来たときにしか分からない。
