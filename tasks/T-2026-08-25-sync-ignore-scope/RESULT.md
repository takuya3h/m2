# RESULT — T-2026-08-25-sync-ignore-scope

**同期の除外範囲を決め直し、禁止領域への書き込みを止める**

**verdict:** `partial`　**host:** `lecun`　**branch:** `feat/sync-ignore-scope`
**手続きの証跡は `audit.md`。本書は事実だけを書き、行で指す。**

## 1. 解決された参照

`contract.inject_verbatim: [conventions#prohibitions]` の**原文**（`context/conventions.md`、
実測 rev `d422b087`。`spec.yaml` の記載は `d422b08` で、短縮形として一致した）。

```
<a id="prohibitions"></a>
## prohibitions

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |
```

`inputs.data`（`egosurgery_phase_v1` / `data/splits/ego_val.txt`）は
**雛形の必須項目であり、本契約では参照しなかった**（SPEC の指示どおり）。
`inputs.denominator` `inputs.sigma_policy` `inputs.frozen_source` は本契約に無い。

## 2. 判定

| Gate | 判定 | 何を実測したか |
|---|---|---|
| G1 | **pass** | 規則 68 行の要約値が正本と反映先で一致。REST の展開形 184 行を取得。禁止領域の一時ファイル 0 件。除外を足しても既存が消えないことを実装 6 点と実測 3 点で確定。反映に再起動が不要であることを確認 |
| G2 | **pass** | 同期 5256 件のうち二重管理 473 件・同期のみ 4783 件を分離。3 案を実在 51,245 件へ当てて評価。陽性 8 分類・陰性 5 分類の両方向で対照 |
| G3 | **partial** | 展開形が 184→156 行。消えるべき 7 展開形が消え、残るべき 5 展開形が残った。禁止領域 3 領域が件数・バイトとも完全同一。接続は**見える 1 台**が維持。**四ノードは測れない** |

## 3. 完了判定

| # | 判定 | 実測値 |
|---|---|---|
| A | 規則の全文と要約値、両者の一致 | `61593e99…9a2a`（`.stglobalignore` = `.stignore`、2223 バイト / 68 行） |
| B | 処理が解釈した除外の形 | `ignore` 68 行 → `expanded` 184 行。**1 行が 4 行へ展開され `**/` が前置される** |
| C | 禁止領域に同期されたもの（触っていない） | 一時ファイル **0 件**。同期対象 **2612 件 / 39,749,977,564 バイト**（全体の 49.7% 件 / **94.6% バイト**） |
| D | 除外を足したとき既存がどうなるか | **消えない。** `UNKNOWN` ではない（`audit.md` §4） |
| E | 反映の手段 | REST（**再起動不要**）／`keeper.sh:48-49` の正規経路。**再起動は使っていない** |
| F | 何を配り何を配らないかの表 | `audit.md` §8。18 分類に実測値つき |
| G | 規則の案と各行が外すもの | `audit.md` §9。案 A / B / C |
| H | 広すぎず狭すぎないことの両方向の対照 | 陽性 8 分類（外れる 2665 件 / 11,260,275,860 バイト）／陰性 5 分類（残る）。**新たに増えるものは 3 案とも 0 件** |
| I | 正本の変更前後の要約値 | `61593e99…9a2a` → `4b71ae4e…fbba1`（7 削除 / 7 追加） |
| J | 反映と、それが効いたこと | `expanded` 184 → **156** 行。消えるべき 7 / 残るべき 5 を照合 |
| K | **既存のものが消えていない** | `experiments/` **10628 件 / 27,183,167,123 バイト（同一）**、`transfer/` **111 件 / 202,614,876 バイト（同一）**、`data/` **217 件 / 12,774,818,641 バイト（同一）** |
| L | 四ノードとの接続 | **UNKNOWN。** 本ホストに見える相手は中心 1 台のみ。**その 1 台は切れていない**（`connected = 1 / 1`） |
| M | 他台への波及の見込み | マージ → keeper 最大 30 分 → 走査（監視 10 秒 / 定期 3600 秒）。**概ね 30 分〜1 時間** |

## 4. 決めたこと

**起票者の判断（案 A・宣言どおり外す・lecun で続行）を受けて適用した。**

### 何を版管理で配り、何を同期で配るか

| 配り方 | 対象 | 根拠 |
|---|---|---|
| **版管理** | `runindex/**`、`experiments/**` の config・metrics・notes 等（追跡 8263 件）、`logs/` の 391 件 | git で配れる。**同期は不要** |
| **同期** | `experiments/**/checkpoints/**` 14,344,344,219 バイト、`experiments/**/*.pth` 11,109,660,437 バイト、`experiments/**/predictions/**` 1,297,617,569 バイト、`data/processed/**` 3,476,982,868 バイト | **`.gitignore` により git で配れない。実験の証跡そのもの** |
| **どちらでも配らない** | `wandb`（1596 件）、`.remember`・`docs` の記録ログ（236 件）、`third_party` 2,149,645,702 バイト、`data/external/weights` 8,797,633,322 バイト | **文面 33〜37 行が「同期しない」と宣言していた対象。宣言に実態を合わせた** |

**原則は「版管理で配れるものは版管理に任せ、同期は版管理で配れないものに限る」。**
**この原則だけでは `experiments/**` の扱いは決まらない**（§5）。

### 変更内容

**無制限行 7 行（`!*.pth` `!*.pt` `!*.ckpt` `!*.onnx` `!*.safetensors` `!outputs` `!logs`）を削除した。**
処理はこれらを `!**/*.pth` `!**/logs/**` などへ展開し、**repo の全階層へ及ばせていた。**
実験成果物は**範囲を持つ 43〜54 行**が受け持つ。同じ位置に理由を 7 行のコメントで残した。

## 5. 禁止と同期の矛盾は解けたか

**解けていない。別の判断が要る。**

**禁止領域の外への漏れは止まった**（2665 件 / 11,260,275,860 バイトが外れた）。
**しかし `experiments/**` への同期は残る**（2591 件 / 30,750,569,270 バイト）。

**全除外（案 C）すれば矛盾は完全に解けるが、同期は 42,010,845,130 バイト → 11,844 バイトになり、
checkpoints・重み・predictions が一切届かなくなる。**
**起票者は案 A を選び、禁止事項の再定義を起票者側へ差し戻す判断をした。**

🔴 **`experiments/**` を「実行者の手による変更の禁止」と読むのか「同期による配布も含む禁止」と
読むのかは、契約側で決める必要がある。** 本契約では決めていない。

## 6. 既存の一時ファイルの扱い

**本契約では触っていない。** そもそも**本ホストには 0 件であった**（変更前後とも）。
SPEC が挙げた `experiments/baselines/s0_002_maskdino_bbox_seed123/.syncthing.epoch_12.pth.tmp` は
**本ホストに存在しない。** 中心（philip）で観測されたものと解される。
**消すかどうかは次の判断である。**

## 7. 他台への波及

| 段階 | 所要 |
|---|---|
| 本分岐 → `origin/phase0`（PR のマージ） | 人の操作 |
| `origin/phase0` → 各台の `.stignore`（`keeper.sh:48-49`） | 最大 30 分 |
| `.stignore` → 各台の処理（走査時に検出） | 監視 10 秒 / 定期 3600 秒 |

🔴 **マージまでは、本ホストの `.stignore` も次の keeper ループで旧版へ戻る。**
戻っても**実体は消えない。** 外していたものが再び同期対象に戻るだけである。

## 8. 実測（次の契約で使う値）

| 値 | 実測 |
|---|---:|
| 群れの規模（変更前・中心と一致） | 5256 件 / 42,010,845,130 バイト |
| 本ホストの同期対象（変更後） | **2591 件 / 30,750,569,270 バイト** |
| 禁止領域が同期に占める割合 | 件数 **49.7%** / バイト **94.6%** |
| 二重管理（版管理と同期の重複） | 変更前 473 件 → **変更後 82 件** |
| 規則の展開比 | **1 実効行 = 4 展開行** |
| 案 C を採った場合の同期 | 188 件 / 11,844 バイト |
| 実在ファイル総数 | 51,245 件 / 48,999,959,399 バイト |

## 9. 起票者の誤り

| 型 | 内容 |
|---|---|
| `shell_assumption` | SPEC 5 行目が repo を `~/slocal2/m2` と断定。**本ホストに `~/slocal2` は無い。** `cd ~/slocal2/m2` は失敗する。`keeper.sh:26` が `[ -d ~/slocal2 ]` で分岐しており、**ホストごとに違う値である** |
| `asserted_without_measuring` | 「環境の事実（再測定は不要）」表が中心を「philip（本ホスト）」「四ノードが接続済み」と断定。**本ホストは lecun で、見える相手は 1 台。** 完了判定 L が測れない |
| `asserted_without_measuring` | Goal が禁止領域の一時ファイルを実在するものとして扱うが、**本ホストには変更前から 0 件。** 触らずに数える指示は空振りした |

## 10. 逸脱・想定外・UNKNOWN

| 種別 | 内容 |
|---|---|
| 逸脱 | **`make task-start` を使っていない。** 契約は手順 1 に従い `make task-notion` で取得済みで、作業ツリーに契約の成果物があるため `task_start.sh` の前提（未 commit 0 件）を満たせない。**同じ規則で分岐を作った**（`feat/sync-ignore-scope`、起点 `origin/phase0`） |
| 逸脱 | 未追跡 **5 件**を退避した（SPEC の許可による）。§11 で戻した |
| 逸脱 | 反映に REST を使った。**正規経路（phase0 → keeper）はマージ待ちのため、本ホストで効果を確かめる手段として併用した** |
| 想定外 | 文面のコメントと実態が **3 件**食い違っていた（`wandb` `third_party` `data/external`）。処理の解釈を正とした |
| 想定外 | `/rest/db/browse` が HTTP 500（`could not find child '.remember' for path '.remember/logs'`）。**索引 DB の直読みで代替した** |
| 想定外 | `logs/` の **391 件が版管理と同期の二重管理**だった。`.stglobalignore:29` が防ぐと掲げた問題が別の場所で起きていた |
| **UNKNOWN** | **完了判定 L の「四ノード」。** 本ホストからは中心 1 台しか見えない。**残る三台は測っていない** |
| **UNKNOWN** | 他台での反映の**実測**。他台に触れていないため見込みのみ（§7） |
| **UNKNOWN** | 「今から足した行で既存が消えない」ことの**中心ホストでの実測**。本ホストでは消えなかった（判定 K） |

## 11. 送出


| 項目 | 値 |
|---|---|
| commit | `2f61013f` |
| 分岐 | `feat/sync-ignore-scope`（起点 `origin/phase0`） |
| PR | **#147** |
| 秘匿検査 | **PASS**（自前・陽性対照 1/1 検出・陰性対照 0 件・値も名前も出力していない） |
| `make task-validate` | exit 0 |
| `make task-preflight` | 4 PASS / 1 WARN / 4 SKIP / 0 FAIL（WARN は `P9 spec_lint`: `separated_source@SPEC.md:48`・`host_mismatch@SPEC.md:5`。SKIP は `P2 cuda_ext_loaded` `P3 deterministic_flags` `P4 prereg_committed` `P5 frozen_source_hash`） |
| `make taskindex-check` | exit 0 |
| `make inbox-check` | exit 0 |
| `make forbidden-check` | `status: pass` / `violations: []`（生成物 4 件を除外し 7 件を検査） |
| 退避したもの | **5 件を戻した**（`.sync-pause.released`、`docs/sessions/digest/` の 4 件） |
| 抑止 | §12 |

## 12. 触っていないものが無変更であること（完了判定 O）

| 項目 | 値 |
|---|---|
| syncthing 処理数（`/proc/PID/exe` で絞った） | **2**（変更前と同じ。`/proc/*/cmdline` の照合は使っていない） |
| keeper 処理数 | 2 |
| `experiments/` `transfer/` `data/` の中身 | **件数・バイトとも変更前と完全同一**（判定 K の表） |
| 禁止領域への手による変更 | **無し**（`make forbidden-check` が `violations: []`） |
