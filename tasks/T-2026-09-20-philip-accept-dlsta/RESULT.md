# RESULT — T-2026-09-20-philip-accept-dlsta

**verdict: pass**　kind: impl　host: philip（`.servername`。`hostname` は `aolab`）
branch: `feat/philip-accept-dlsta`（`origin/phase0` = `de3b2a6a` から作成）
手続きの証跡は `audit.md`。以下は行番号で指す。

## 判定

| Gate | 判定 | 実測 |
|---|---|---|
| **G1** | **pass** | 受け入れ一覧 6 件・権限 600・sha256 `ab3fe1cb…86fb`。相手の実体 **6 件**（ひな型 `defaults/device` 1 件は別階層として除外。階層を見ない数え方なら 20 件）。稼働 **2 件**（`/proc/PID/exe`、自分と祖先・子孫を除外、両方向の対照つき）。控えは repo 外・同一ファイルシステムへ取り要約値が原本と一致（audit.md:44-119, :121-140） |
| **G2** | **pass** | 提出物 2 件は版管理に在る。指紋 `SHA256:5jUsv9rr…KO4` が前契約 `RESULT.md:33,:53` と一致。三検査（先頭 `ssh-` 1 行 / 秘密鍵の書き出し 0 件 / 行数 1）と囮で検査が働くことを確認。未登録を陽性対照つきで確認（audit.md:178-216） |
| **G3** | **pass** | 消えた行 **0 件**、増えた行 **1 件**で指紋一致。相手の実体 6→**7**、共有フォルダ **2 件のまま定義に差分なし**、接続の一覧 5→**6 項目**に dlsta が出現、五ノードは全て `connected=True`（audit.md:218-338） |

## 完了判定

| # | 項目 | 実測 |
|---|---|---|
| A | 受け入れ一覧を測った | 6 件 / sha256 `ab3fe1cb…86fb` / 権限 600 / 指紋 6 件を註釈つきで記録 |
| B | 相手の実体を数えた（ひな型を含めない） | **6 件**。ひな型 `defaults/device id=""` 1 件は除外 |
| C | 稼働しているものを数えた（両方向の対照） | **2 件**。陽性 `python3.12` = 1 / 陰性 `zzz_no_such_binary` = 0 |
| D | 控えを repo の外へ取り原本と一致 | `authorized_keys.orig` `config.xml.orig` とも sha256 一致。権限 600 |
| E | 戻し方を記録した（実行していない） | audit.md:142-173。権限 600 へ戻す形を含む |
| F | 提出物が 2 件ある | `dlsta.pub` 95 bytes / 1 行、`dlsta.txt` 64 bytes / 1 行。ともに追跡済み |
| G | 指紋を前契約の報告と照合 | `SHA256:5jUsv9rrpScleVa1jvO008WDPpg7LzQq0G8qSZjgKO4` **一致**。識別子 63 文字も一致 |
| H | 公開鍵だけである（三検査と陽性対照） | 1 / 0 / 1。囮は (b) 1 件・(c) 3 行で掛かり、(a) を通らなかった |
| I | 既に登録されていないか（陽性対照つき） | 受け入れ一覧 0 件（対照 efros = 1）、相手の実体 0 件（対照 efros = 1、陰性 0）、両フォルダとも 0 件 |
| J | 追記後の件数と権限 | 空行を除いた件数 **7**、権限 **600**（開始時と同じ） |
| K | **既存がすべて残っている** | **消えた行 0 件**。集合差の道具は囮に対し 1、自分自身に対し 0 を返した |
| L | 増えた一件の指紋が期待と一致 | **1 件** `SHA256:5jUsv9rr…KO4 dlstatophilip` |
| M | 全行が解析できる | 空行を除いた件数 7 = `ssh-keygen -lf` の 7 |
| N | 相手として登録した | 識別子は `scripts/sync/device_ids/dlsta.txt` から読んだ。名前 `dlsta`、住所 `dynamic`。`POST /rest/config/devices` → HTTP 200 |
| O | 共有フォルダへ加えた（定義は変えていない） | `claude-sync` `m2` とも `PATCH` で **`devices` キーのみ**送出 → HTTP 200。定義属性の差分**なし** |
| P | 相手 7 件 / フォルダ 2 件 / 権限 | **7** / **2** / `600`（開始時と同じ）。解析は成功 |
| Q | **同期処理が新しい相手を認識した** | `GET /rest/system/connections` の項目数 5→**6**。`BRPEYOX`（dlsta）が出現 |
| R | **五ノードとの接続が切れていない** | `connected=True` **5 件**（andrew / bengio / efros / lecun / ilya）。22000 は LISTEN のまま、稼働プロセスは 2 件のまま |
| S | 報告が指定の構成と分量 | 本ファイル |
| T | 触っていないものが無変更 | 下記「無変更の確認」 |
| U | 禁止語の検査（陽性対照つき） | 下記「送出」 |
| V | 秘匿検査（値を出力していない。陽性対照つき） | 下記「送出」 |
| W | 変更が契約の範囲に限られ PR が存在 | 下記「送出」 |
| X | 台帳へ返り、抑止が外れ、退避が戻った | 下記「送出」 |

## 実測

### 受け入れ一覧

| 項目 | 開始時 | 完了時 |
|---|---|---|
| 空行を除いた件数 | 6 | **7** |
| 解析できた件数 | 6 | **7** |
| 権限 | `600` | **`600`** |
| sha256 | `ab3fe1cb…86fb` | `d1bc4263…e71a` |
| 消えた行 | — | **0 件** |

### 同期処理

| 項目 | 開始時 | 完了時 |
|---|---|---|
| 相手の実体（`configuration` 直下） | 6 | **7** |
| 共有フォルダ | 2（`claude-sync` `m2`、ともに `sendreceive`） | **2、定義属性の差分なし** |
| 各フォルダの相手 | 6 | **7** |
| 接続の一覧の項目数 | 5 | **6** |
| `connected=True` | 5 | **5** |
| 設定の権限 | `600` | **`600`** |
| 稼働プロセス | 2 | **2** |
| TCP 22000 | LISTEN | **LISTEN** |

**認識の状態**: dlsta（`BRPEYOX`）は `connected=False` `paused=False` `type=''` で接続の一覧に現れた。
**項目ができたこと自体が認識の証拠である**（設定を読んだだけでは項目はできない）。
再起動は行っていない。

### 無変更の確認

| 対象 | 実測 |
|---|---|
| `~/.ssh/` の他（`config` `config.d` `id_ed25519_*` `known_hosts*`） | 要約値を記録。mtime は最新でも 2026-09-20 13:52 で、本セッション開始（16:39）より前 |
| 常駐処理 | keeper PID **72428**（Task 1 時点と同じ）。`keeper.sh` の mtime は 2026-08-23 |
| `~/bin/m2-sync.sh` | mtime 2026-09-20 17:33:10。**keeper が毎ループ `origin/phase0` から自己更新した**もので、実行者の操作ではない。`sync-pause` 対応は 2 のまま |
| 共有フォルダの定義 | 控えとの差分**なし**（id / label / path / type） |
| 目印 | `.sync-pause` **1 件** |
| 抑止の効き | 本セッション中の philip のループ 2 回（17:03, 17:33）とも「一時停止中」。`auto-merge` / `auto-push` は **0 件** |

## 疎通の未確認

**dlsta から philip へ実際に ssh で入れるか、同期が実際に繋がるかは、中心からは測れない。**

- 受け入れ一覧に鍵が在ることと、その鍵で認証が通ることは別である。確かめるには dlsta 側から接続する必要があり、他ホストへの接続は禁止 5 に当たる。
- dlsta は `connected=False`。中心は住所 `dynamic` で相手へ繋ぎに行かず、dlsta の側はまだ中心を登録していない。繋がる条件が揃うのは dlsta 側の登録の後である。
- したがって本契約が示せるのは **中心の側の登録が済んだこと**までである。疎通の確認は後続の契約で dlsta 側から行う。

## 起票者の誤り

| 型 | 内容 |
|---|---|
| `asserted_without_measuring` | SPEC が「共有相手の属性名は `id`。`deviceID` ではない（前契約で誤った）」と断定したが、稼働中の REST v2.1.3 の `folder.devices[]` のキーは `deviceID` である。`id` は `config.xml` 側の属性名であり、SPEC は二つの表現を取り違えている。指示どおり `id` を送れば、相手の一覧が識別子を持たない要素に置き換わり、既存 6 台の共有が壊れうる。実在する名前を読んでから `deviceID` を使った |
| `check_does_not_check` | L3 の `P9 spec_lint` の `host_mismatch` は `socket.gethostname()` と本文の宣言を比べる。philip と ilya はどちらも `aolab` を返すため（`m2-sync.sh` の註に明記）、この判定はホストの同一性を判定できていない。指示どおり従えば、正しいホストで実行していても毎回 WARN が出る一方、ilya で誤って実行しても同じ WARN しか出ず区別できない。`.servername` と `GET /rest/system/status` の `myID` で同一性を確かめた |

## 規約の適用判定

| 規約 | 判定 | 根拠 |
|---|---|---|
| `proposal_gate` の禁止語 | **適用する** | 規約が「提案と契約の本文」を対象とする。送出物へ当てた（下記） |
| `folds` | **適用されない** | 動画単位の 5-fold の割り当てを定める規約で、学習と評価の手続きにしか掛からない。本契約は塔も界面も動かさず、`inputs.data` も参照していない |
| `symmetry` | **適用されない** | 規約が「対象: exp 契約のすべて」と定める。本契約は `kind: impl`。L3 の `P13 symmetry_table_complete` が「kind=impl のため対象外」で SKIP したことが裏づけ |

## 逸脱・想定外・UNKNOWN

### 逸脱

| 型 | 内容 |
|---|---|
| `environment` | Task 3 の追記命令を auto mode の分類器が `Unauthorized Persistence` で拒否した。命令は実行されず、受け入れ一覧は sha256 も件数も開始時のまま無傷だった。SPEC の想定外表「実行基盤が拒むことがある」に従い停止して利用者へ諮り、利用者が自分の手で追記した。**実行者が検査や拒否を迂回してはいない。** 追記後の検証は実行者が行った |
| `environment` | Task 4 の直後、設定ファイルを直接読む検証命令と REST の読み取り命令も同じ分類器に拒否された。命令を細かく分け、読み取りだけの形にして測り直した。測れなかった項目は無い |
| `judgement` | 開始時から在った未追跡 `experiments/transfer/pd_refin_empty_seed42_tf32/`（212M）が `checkout` を阻んだため退避した。repo と同一ファイルシステム（`/dev/sda`）の `/home/ubuntu/slocal2/.task-stash/` を選んだ。`/home/ubuntu` は overlay で、跨ぐと実コピーになる |
| `judgement` | 控えを版管理へ置かなかった。`config.xml` は画面の鍵を含み、伏せる工程を挟むより版管理の外に留めるほうが誤りが少ない。置く前の検査として両控えに秘密鍵の書き出しが 0 件であることは確かめた |
| `judgement` | `conventions_rev` は置換しなかった。`origin/phase0` 上の実測が `c801e17c` で、契約の `c801e17` と一致したためである。古い分岐で見たときに出た WARN は、分岐を切り直した後は消えた |
| `judgement` | 稼働プロセスの計数で、1 周目の陽性対照 `python3` が 0 を返して落ちた。`realpath` 後の実体が `/usr/bin/python3.12` で basename が一致しないためで、2 周目に自分の exe の実体から basename を取って測り直した |

### 想定外

- 該当の 2 件（実行基盤による拒否）は上の逸脱に書いた。契約の `escalate_if` に挙がった 5 つの事象はいずれも起きていない。

### UNKNOWN

- **dlsta からの疎通**。上の「疎通の未確認」に理由を書いた。中心からは測れない。
- `~/.ssh/` の他のものの無変更は、開始時の要約値を取っていないため **mtime で示した**。要約値による照合ではない。触れる命令を一つも発していないことと合わせての判断である。

## 送出

（送出後に記録する）
