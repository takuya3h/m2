# RESULT — T-2026-09-21-m2dir-local

**常駐処理が三種目の repo の位置を見つけられるようにする（dlsta）**

**verdict: pass** / kind `impl` / host `dlsta`（`hostname` は容器の識別子）
branch `feat/m2dir-local`（`origin/phase0` = `23333eb6` から）
証跡は `audit.md`（267 行）。以下は行番号で指す。

## 判定

| Gate | 判定 | 実測 |
|---|---|---|
| **G1** | **pass** | 解決部分は `keeper.sh:28` と `m2-sync.sh:10` で**完全に同一の一行**。**一つ目だけが検査され、二つ目は無条件の落とし所**。判定は**親ディレクトリの存在**。**環境変数で上書き不可**。本ホストでは `[ -d ~/slocal2 ]` も `[ -d ~/slocal ]` も偽で `/home/ubuntu/slocal/m2`（不在）に解決していた (:10, :54) |
| **G2** | **pass** | 両正本の直後へ **4 行を追加**（削除 0 / 変更 0）。`bash -n` 両方 exit 0、囮は落ちた。隔離した家 **7 通り × 2 正本 = 14 件すべて期待どおり**。**既存候補と `~/local/m2` が両方在る場合、既存が勝つ**ことを実測 (:111) |
| **G3** | **pass** | 配置後に起こし直し、**自己更新が働き**・**`m2-sync.sh.new` の残骸が消え**・**`.stignore` の更新時刻が 16:58:29 へ**。抑止の記録 `[dlsta] 一時停止中: /home/ubuntu/local/m2/.sync-pause …` が出た。中継 1・同期処理 2・中心との接続は無傷 (:173) |

## 完了判定

| # | 項目 | 実測値 |
|---|---|---|
| A | 解決部分を行番号つきで記録 | `keeper.sh:28` / `m2-sync.sh:10`。**同一の一行**（文字列照合で一致） |
| B | **最初に見つかったもので止まるか** | **一つ目のみ検査される**（`[ -d ~/slocal2 ]`）。**二つ目は検査されない無条件の落とし所**。繰り返しではなく三項一行 |
| C | 何をもって見つかったとするか | **親ディレクトリの存在**。repo 本体 `~/slocal2/m2` でも、版管理であることでもない |
| D | 何が止まっているか | `[dlsta]` の記録 **0 件**（対照 `[philip]` 141 / `[efros]` 192）。抑止の記録 **0 件**（対照 450）。`m2-sync.sh.new` が **0 bytes** で本日 16:44 に残存。`.stignore` の更新時刻が前契約で手で置いた 2026-09-20 21:07:21 のまま |
| E | 配置中の版と正本 | `keeper.sh` `9fe9c423002e…`・`m2-sync.sh` `bcf46ba9031a…` とも**一致**（自己更新が働いていないため前契約のまま） |
| F | 両方の末尾へ足した | 各 **4 行**（註 3 行＋判定 1 行）。`[ -d ~/slocal2 ] \|\| [ -d ~/slocal ] \|\| { [ -d ~/local/m2 ] && M2DIR=~/local/m2; }` |
| G | 差分が追加だけ | `keeper.sh` **+4 / −0**、`m2-sync.sh` **+4 / −0**。差分全体の削除行 **0 件** |
| H | 構文検査 | 両方 **exit 0**。囮（足した行の `}` を落とした写し）は**落ちた** |
| I | **本ホストで新しい候補に解決** | 両正本とも `/home/ubuntu/local/m2` |
| J | **既存の候補が在るときそちら** | 隔離した家で **7 通り × 2 正本すべて期待どおり**。要の 2 件（既存候補＋`local/m2` 併存）で**既存が勝った** |
| K | 配置物と正本の一致 | 両方**一致**、権限 **775** 維持。旧版は `/home/ubuntu/.task-stash/T-2026-09-21-m2dir-local/*.pre`（`~/bin` と同一 fs、repo の外） |
| L | 起こし直し | 実装（`nohup … 9>&- &`、中継の親は既に init）を読んでから実施。**kill 直後 keeper 0 / 中継 1 / 同期処理 2** |
| M | **repo を見つけた** | 自己更新が `~/bin/m2-sync.sh` を `origin/phase0` の版へ置換／`m2-sync.sh.new` の残骸が消えた／`.stignore` が **16:58:29** に再反映 |
| N | 除外規則の自動反映 | 更新時刻 `2026-09-20 21:07:21` → **`2026-09-21 16:58:29`**。正本と要約値一致。`.stignore.new` の残骸なし |
| O | **抑止が効いた** | `2026-09-21 16:59:25 [dlsta] 一時停止中: /home/ubuntu/local/m2/.sync-pause があるため分岐へ書き込まない` |
| P | 既存が無傷 | 中継 **1 件**（`22001`）／同期処理 **2 件**（`22000`）／画面 `8384`／中心 **`connected=True`**／`claude-sync` `m2` とも **completion 100.0000%・needBytes 0** |
| Q | `env-facts.md` へ足した | 「repo の位置」へ三種の順序と判定の性質（**+4 行**）、「keeper と m2-sync」へ自己更新の帰結（**+1 行**）。削除 0 |

## 解決の仕組み（実装から読んだこと）

    keeper.sh:28 / m2-sync.sh:10（同一）
    M2DIR=$([ -d ~/slocal2 ] && echo ~/slocal2/m2 || echo ~/slocal/m2)

| 問い | 答え |
|---|---|
| 候補の並び | **繰り返しではない。** 三項の一行で結果は 2 通り |
| 最初のもので止まるか | **一つ目は検査され、真なら止まる。二つ目は検査されない**（存在しなくても使われる） |
| 何をもって見つかったとするか | **親ディレクトリの存在**（`~/slocal2`）。repo 本体でも版管理でもない |
| **環境変数で上書きできるか** | **できない。** `M2DIR=$(…)` の無条件代入で `${M2DIR:-…}` ではない。**上書きで済ませる道は無い** |
| 二つの正本で書き方が同じか | **同一**。よって同じ形で両方へ足した |
| `set -e` / `set -u` | **どちらにも無い**。足した行の終了コードは実行を止めない |

足した行は**既存と同じ判定の仕方（親ディレクトリの存在）**を使う。基準を変えると
「既存の候補が在る」の意味が二通りになるためである (:113)。

## 他の台への影響

**無い。配置も要らない。**

| 根拠 | 実測 |
|---|---|
| 既存の台は一つ目か二つ目で短絡する | 隔離した家の 7 通りで、既存候補が在る 4 通りは**すべて既存に解決**した (:146) |
| **三種目が併存しても既存が勝つ** | `slocal2`＋`local/m2` → `~/slocal2/m2`／`slocal`＋`local/m2` → `~/slocal/m2` |
| どれも無い台の振る舞いは変わらない | `~/slocal/m2`（従来どおり） |
| `keeper.sh` は自己更新されない | 既存の台の `~/bin/keeper.sh` は変わらない。**既存の候補で動いているため変更が要らない** |
| `m2-sync.sh` は自己更新される | 統合後に各台へ配られるが、**解決の結果は変わらない**（上の 4 通り） |

**他の台に `~/local/m2` が在るかは本ホストから測れない**（禁止 2）。
**在っても既存の候補が先に当たることを隔離で示したため、測る必要が無い。**

## 規約の適用判定

| 規約 | 判定 | 根拠 |
|---|---|---|
| `proposal_gate` の禁止語 | **適用する** | 規約が「提案と契約の本文」を対象とする。送出物へ当てた |
| `folds` | **適用されない** | 学習も評価も行わず、折りにも val/test にも触れない |
| `symmetry` | **適用されない** | 規約が「対象: exp 契約のすべて」。本契約は `kind: impl`。L3 の `P13` も同理由で SKIP |

## 起票者の誤り

| 型 | 内容 |
|---|---|
| `asserted_without_measuring` | 「候補は上から順に試され、最初に見つかったものが使われるはずである」と述べ、足す位置の表もそれを前提にする。**実装は候補列ではなく三項一行で、二つ目は存在を確かめない無条件の落とし所**である。字義どおり「末尾に候補を足す」と読んで `\|\| echo ~/local/m2` を三つ目に並べると、二つ目が常に当たるため到達しない |
| `self_contradiction` | 禁止 1 が「既存の候補の順序を変える・削る」を禁じ、Task 2 Step 1 が「既存の行は一文字も変えない」と求める一方で「`~/local/m2` を既存の候補の後ろへ足す」と指示する。**一行の三項に対し、行を変えずに候補を後ろへ足すことは字義どおりには不可能**で、別の文として足す解釈が要る |
| `check_does_not_check` | 完了判定 O は「配置 → 起こし直し → 抑止の記録が出る」を求めるが、`keeper.sh:49` が `~/bin/m2-sync.sh` を**毎ループ `origin/phase0` から自己更新する**。**統合前に配置した修正は、repo を見つけた最初の周回で戻される。** 指示どおりの順序では O は原理的に達成できない |

## 逸脱・想定外・UNKNOWN

**逸脱**

1. **自己更新で戻された `~/bin/m2-sync.sh` を置き直し、`keeper.sh:54` と同じ呼び方で一度走らせて O を確かめた。** 常駐処理の周回を待っても、周回のたびに戻されるため達成できない (:225)
2. **常駐処理を起こし直した**（周期 1800 秒を待たない）。SPEC が許容し、前契約でも同じ判断を採った
3. **`conventions_rev` は置換しなかった**（実測 `c801e17c` と契約の `c801e17` が一致）
4. **0 節の `task-start` が未追跡 1 件で止まったため stash で退避した**（利用者の指示）。退避したのは前セッションの抽出物で、**報告の commit に含めて戻す**
5. **抽出物 `docs/sessions/digest/` の 1 件を送出に含めた。** SPEC の範囲指定には無いが、`tasks/README.md` が「契約の記録と一緒に含めること」と定める。入れる前に伏せ字を確認（秘密鍵の塊 0 / ssh 鍵の表記 0 / 資格情報 0 / 禁止語 0）

**想定外**

- **`m2` の同期が完了していた。** 前契約の時点で completion 4.16%・残り 98.96 GB だったが、本契約の時点で **100.0000%・needBytes 0**
- `P9 spec_lint` の WARN が **2 件**。`host_mismatch` は既知（`gethostname()` が容器の識別子）。
  **`separated_source@SPEC.md:51` は検査器の偽陽性**で、`source … && source … \` の**行継続を分割**して次行の `&& make …` を別命令と見なしている。実際には一命令であり、本セッションで実行して成功している
- `escalate_if` の 4 事象はいずれも起きていない

**UNKNOWN**

- **他の台に `~/local/m2` が在るか**は測っていない（禁止 2）。**在っても既存が勝つことを隔離で示したため、判断に不要である**
- **統合後に他の台で実際に何が起きるか**は本ホストから測れない。隔離した家での模擬が根拠である
- `inputs.data`（`egosurgery_phase_v1` / `data/splits/ego_val.txt`）は雛形の必須項目であり**参照していない**

## 送出

（Step 3 実行後に記入）
