# RESULT — T-2026-08-14-bundle-attachment-transport

**task_id:** `T-2026-08-14-bundle-attachment-transport`
**kind:** `impl`
**実行ホスト:** `bengio`（SPEC の指定は `lecun`。§5 に記す）
**分岐:** `feat/attachment-transport`（基点 `origin/phase0` = `29f8ca4`）
**実行日:** 2026-08-10

---

## 1. 解決された参照

### 1.1 `contract.inject_verbatim`

`context/conventions.md`（実測 rev `d422b08`）の該当アンカーの**原文**。要約していない。

#### `conventions#prohibitions`

    ## prohibitions

    | id | 禁止事項 |
    |---|---|
    | `no_split_redefine` | split を再定義しない |
    | `no_raw_write` | `data/raw` `data/external` に書き込まない |
    | `no_frozen_change` | 凍結源を変更しない |
    | `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
    | `no_runindex_hand_edit` | `runindex/` を手で編集しない |

### 1.2 継承された `sigma_policy`

`spec.yaml` が `inputs.sigma_policy` を省略しているため `conventions#sigma` の既定値を継承した。
本 task は `kind: impl` で σ 判定を行わないが、記録のため残す。

    series: pstd
    sigma_source: paired_delta
    delta_sigma_source: paired

### 1.3 解決しなかった参照

| 参照 | 状態 |
|---|---|
| `inputs.denominator.ref` | `spec.yaml` に記載なし。解決対象外 |
| `inputs.frozen_source.ref` | 記載なし。`P5` も `kind=impl` で SKIP |

### 1.4 `conventions_rev` の実測と置換

起票時 `1201f4f`、実測 `d422b08`。SPEC Task 4 Step 1 の手順に従い置換した。
`1201f4f..d422b08` は `+10 / -0` の追記のみで、変更箇所は `frozen_source` 節と変更履歴表。
**引用対象 `prohibitions` は新旧バイト一致**であることをアンカー単位で確認した。

---

## 2. 検証の結果

| 検査 | 開始時 | 完了時 |
|---|---|---|
| `make task-validate` | `exit 0`（WARN 3） | `exit 0`（WARN 2。L2-6 は `conventions_rev` 置換で解消） |
| `make task-preflight` | `exit 0` | `exit 0` |
| `make taskindex-check` | — | `exit 0` |
| `make inbox-check` | — | `exit 0` |

プリフライトの内訳。**SKIP は「合格」ではなく「実行されなかった」。**

    P1 venv_active            PASS
    P2 cuda_ext_loaded        SKIP  plan.env.preflight に記載なし
    P3 deterministic_flags    SKIP  plan.env.preflight に記載なし
    P4 prereg_committed       SKIP  kind=impl のため対象外
    P5 frozen_source_hash     SKIP  kind=impl のため対象外
    P6 decisions_answered     PASS  decisions_required は空
    P7 destination_writable   PASS
    P8 contract_valid         PASS
    RESULT: 4 PASS / 4 SKIP / 0 FAIL

---

## 3. 実測

### 3.1 添付の仕組み（実装と台帳の双方から）

**取得の経路は実装に無かった。** `tools/fetch_task.py` は code ブロックのみを集め、
`src/egosurgery/utils/notion_ops.py` に添付を扱う箇所は無い。SPEC の指示に従い、
この事実を記録したうえで、推測ではなく実測に基づいて設計した。

台帳側の実測（2026-08-10）。

| 項目 | 実測 |
|---|---|
| 台帳の列 | status / bytes / target_host / kind / note / sha256 / issued_at / task_id の 8 つ |
| `files` 型の列 | **存在しない** |
| したがって添付の置き場所 | **ページの子の `file` ブロック**のみ |
| 添付投稿の能力 | `GET /v1/file_uploads` が HTTP 200（版 2022-06-28 と 2025-09-03 の双方）。統合は workspace 権限の bot |
| 既存 4 行の `file` ブロック | **0 件**（全頁を頁送りして確認） |

既存行 `T-2026-08-13-implementation-history-index` のブロック内訳は
`code 20 / paragraph 89 / heading_1 1 / heading_2 1 / table 1` で、
**本文が記法として解釈され構造へ変換された状態**がそのまま観測できる。

### 3.2 確認用の本文

壊れやすい要素を意図的に含めた。**表示用の切り詰めは混ぜていない。**

| 要素 | 含めた形 |
|---|---|
| 行末の空白 | 3 個の空白で終わる行、および末尾に空白を置いた最終行（計 2 行） |
| 連続する空行 | あり |
| 引用符とバッククォート | 単引用・二重引用・バッククォート・バックスラッシュ |
| 行頭の記号 | `#` `-` `>` `1.` タブ |
| 長い区切り文字 | `-` 80 個、`=` 80 個 |
| 2000 字を超える連続した塊 | 2100 字 |
| 日本語と英数字の混在 | 全角・半角カナ・絵文字・記号 |
| 表 | markdown 表 |

壊れやすい要素は**すべて `SPEC.md` 側へ寄せた。** `spec.yaml` に混ぜると YAML として
解釈できず、G2 の「取り込めること」を測れなくなるためである。

| 項目 | 値 |
|---|---|
| バイト数 | **4355** |
| sha256 | `a735ac1a6e15845371ada643292a6953f709597bf3b7e25ac1df1752b41dec01` |
| 最長行 | 2100 |

置く前に、この束が自分の解析器と取り込み経路を通ることをローカルで確認した
（`--src` 経由で `exit 0`、設置内容がバイト一致、後始末済み）。

### 3.3 G1 — 添付の往復

添付は `POST /v1/file_uploads` → multipart 送信 → `file_upload` を指す `file` ブロックを
子に持つ行の作成、の 3 手順で置いた。送信後の `content_length` は 4355 で送信バイト数と一致。

| 項目 | 値 |
|---|---|
| 台帳の記載 | `a735ac1a…` |
| 取得した本文 | `a735ac1a…` |
| バイト数 | 4355 → **4355** |
| 要約値の一致 | **True** |
| **バイト完全一致** | **True** |

**G1 PASS。** 行末の空白も連続空行も 2100 字の塊も絵文字も、一文字も変わらなかった。

### 3.4 照合が空振りでないこと（陽性対照）

同じ本文を 3 通りに崩し、いずれも要約値が変わることを確認した。

| 変え方 | バイト数 | 要約値が元と異なるか |
|---|---|---|
| 本文中の 1 文字を置換（長さ不変） | 4355 | **異なる** |
| 行末の空白を 1 つ減らす | 4354 | **異なる** |
| 連続空行を 1 つに潰す | 4354 | **異なる** |

4 通りすべて異なる要約値。**照合は空振りではない。**

### 3.5 参照先の有効期限

| 項目 | 実測 |
|---|---|
| 期限 | **60 分**（取得時刻から） |
| 同じブロックを二度取得したときの URL | **異なる**（毎回新しい署名つき参照先が返る） |
| 期限の値 | 呼ぶたびに更新される |
| 取り直した参照先からの再取得 | 要約値一致 |

**取得のたびに参照先を取り直す必要がある。** URL を保存して使い回す設計は取れない。
実装の再試行は「ブロックから取り直してもう一度取得する」形にした。

### 3.6 優先順を決めた理由

**添付を優先し、添付が無い行だけ本文から集める**を採った。SPEC の推奨と一致するが、
実装と台帳を読んでから判断した。理由は次の 2 つである。

1. 記法の解釈を受けないため確実である。本文の経路は §3.1 のとおり実際に壊れている。
2. **既存の行の挙動を変えない。** 既存 4 行の `file` ブロックは全頁を頁送りして 0 件であり、
   添付を優先しても既存行は必ず本文の経路へ落ちる。これは仮定ではなく実測である。

「両方あれば失敗」は採らなかった。既存行との互換を壊さずに曖昧さを消せるのは
「添付優先」で足り、失敗させる利得が無いためである。

**添付があるのに取得できないときは、黙って本文へ落ちない。** 明確な理由で失敗させる。
落ちてしまうと、壊れた本文を読んで要約値の照合に頼ることになり、
「添付を置いたのに本文が使われた」ことに気づけない。

### 3.7 取得の失敗経路の扱い

| 状況 | 扱い |
|---|---|
| HTTP 401 / 403 | `AttachmentExpired`。**ブロックから参照先を取り直して 1 回だけ再試行する** |
| その他の HTTP エラー | `BundleError`。理由（HTTP コード）を添えて失敗 |
| 到達できない | `BundleError`。理由を添えて失敗 |
| UTF-8 として読めない | `BundleError`。符号化の問題として失敗 |
| 取得できたが要約値が合わない | 既存の照合が拒否（添付・本文で共通） |

**資格情報は送らない。** 参照先は外部の保管先が発行した署名つきで台帳の資格情報を
必要としない。見出しを付けると資格情報が台帳以外のあて先へ渡るため、
`_notion_call` を流用せず認証見出しを持たない別関数に分けた。

### 3.8 G2 — 実地での確認

| 検査 | 終了コード | 判定 |
|---|---|---|
| 正常系（添付から取り込む） | **0** | 成功。設置内容が添付とバイト一致 |
| 存在しない識別子 | **1** | 非ゼロ |
| 資格情報が無い | **1** | 非ゼロ |
| 要約値が合わない | **1** | 非ゼロ |
| 添付の参照先が壊れている | — | `添付を取得できません: HTTP 400`。期限切れ扱いにしない |

痕跡の確認。

| 項目 | 前 | 後 |
|---|---|---|
| 契約の件数 | 24 | **24** |
| 作業領域の変更 | 本 task の変更のみ | **同じ** |
| 確認用の契約 `tasks/T-2026-08-14-probe-attachment` | — | **残っていない** |
| 一時ディレクトリ `/tmp/.task_fetch_*` | — | **残っていない** |

**G2 PASS。**

### 3.9 巻き戻しを複製していないこと

`rollback()` は 1 箇所に定義され、`fetch()` の 2 箇所から呼ばれるのみ。
本 task が触ったのは `read_notion_bundle` とその補助関数だけで、
取得した本文は従来どおり `read_source` を通じて既存の取り込み経路へ渡される。
一時ディレクトリへの展開・設置・検証・巻き戻しには手を入れていない。

### 3.10 鍵の更新後に必要な操作（Phase C）

既存の `docs/secrets_and_tracking.md` を読み、要件の充足状況を確かめた。

| 要件 | 追記前 |
|---|---|
| 1. 平文を編集したら読み込みより先に再暗号化する | **記載あり**（「編集と再暗号化の順序」節） |
| 2. 鍵を更新したら各ホストで平文を消してから読み直す | 警告への対処としてのみ記載。**手順として無し** |
| 3. 読み込みが警告を出したら放置しない | **記載あり** |
| 4. ホスト固有の値を共有の設定へ入れない | **記載なし** |

2 と 4 を追記した。4 については事故の機序を実測で特定した。

`c61a673 chore(secrets): drop host-specific SERVERNAME from the shared env` は
**暗号文 `.env.gpg` だけを直しており、`stat` は 1 ファイルのみ**である。
テンプレート `.env.example` は 17 行目に `SERVERNAME=bengio` を持ったまま残っている。
共有の設定は全台へ同じ内容が配られるため、**このテンプレートを写した各ホストが
揃って `bengio` を名乗る。** これが「論理名が全台で誤った値になった」の機序であり、
**再発の導線が今も生きている。**

テンプレートの修正は本契約の Files 範囲外のため、文書化にとどめて起票した（§5・§6）。

---

## 4. 完了判定

| # | 判定 | 期待 | 実測 | |
|---|---|---|---|---|
| 1 | 添付の往復が忠実 | 要約値が一致 | 一致。4355 バイトがバイト完全一致 | OK |
| 2 | 照合が空振りでない | 陽性対照が検出 | 3 通りすべてで要約値が変化 | OK |
| 3 | 参照先の期限を実測 | 記録あり | **60 分**。取得のたびに URL が変わる | OK |
| 4 | 添付から取り込める | 成功 | `exit 0`。設置内容がバイト一致 | OK |
| 5 | 本文しか無い行も取り込める | 成功 | 試験 `test_body_path_is_unchanged_without_attachment` が pass | OK |
| 6 | 優先順が定まっている | 実装と記録が一致 | 添付優先・無ければ本文。§3.6 に理由 | OK |
| 7 | 存在しない識別子を拒む | 非ゼロ | `exit 1` | OK |
| 8 | 資格情報が無いとき拒む | 非ゼロ | `exit 1` | OK |
| 9 | 要約値が合わないとき拒む | 非ゼロ | `exit 1` | OK |
| 10 | 失敗時に痕跡が残らない | 件数不変・清潔 | 24 → 24。確認用の契約も一時ディレクトリも残らず | OK |
| 11 | 巻き戻しを複製していない | 既存経路を呼ぶ | `rollback()` は 1 定義 2 呼び出しのまま | OK |
| 12 | 資格情報が出力に無い | 検査で 0 件 | 一致 6 件を**目視**。すべて変数名・環境変数名・見出しの組み立てで値は 0 件 | OK |
| 13 | 鍵の更新後の操作が文書化 | 該当あり | `docs/secrets_and_tracking.md` に 2 節を追記 | OK |
| 14 | 契約検証が通る | exit 0 | `exit 0` | OK |
| 15 | 実行前検査が通る | exit 0 | `exit 0`（4 PASS / 4 SKIP） | OK |
| 16 | 試験が不変 | 開始前と同じ | 開始前 5 failed / 292 passed → 5 failed / 298 passed。**新規失敗 0** | OK |
| 17 | 禁止領域が無変更 | 出力なし | 出力なし | OK |

判定 12 で一致した 6 件の内訳（**何に一致したかを目視した**）。

| 箇所 | 一致した内容 |
|---|---|
| `fetch_task.py:229` | `os.environ.get("NOTION_API_KEY", ...)` — 環境変数名 |
| `fetch_task.py:235` | `f"Bearer {api_key}"` — 送信見出しの組み立て（既存コード。出力先は HTTP 見出しのみ） |
| `fetch_task.py:354, 356` | 未設定を報せる分岐と文言 — 環境変数名 |
| `secrets_and_tracking.md:27, 98` | 環境変数名を挙げた説明文 |

**値そのものは 1 件も無い。** 試験ファイルには `SECRET-VALUE-MUST-NOT-LEAK` が 3 件あるが、
これは「この値が漏れないこと」を検査するために試験が置いたカナリアである。

判定 16 の失敗 5 件は本 task 開始前から失敗しているもので、
`test_engines.py::test_mmdet_trainer_eval_recipe_in_metrics` と
`test_research_logger.py` の 4 件である。

---

## 5. deviations

**空にしてはならない項目である。**

### 5.1 起票者の検査・記述の誤り（5 件）

申し送り（12 task 連続で検査が検証対象を検証できていない）に該当する誤りを見つけた。

| # | 箇所 | 誤り | 対処 |
|---|---|---|---|
| 1 | Task 1 Step 3 と Task 2 Step 5 | **両立しない。** 確認用の行を `superseded` にせよと言う一方、G2 正常系はその行から取り込めと言う。`read_notion_bundle` は `superseded` の行を候補から除外する（`live = [r for r in rows if _select_name(r, "status") != "superseded"]`） | 確認用の行の状態を一時的に `distributed` へ戻して測り、直後に `superseded` へ復した。既存の行には触れていない |
| 2 | Task 1 の Files と Step 3 | 担当が定まらない。Files は「台帳へ確認用の行を作り」と本 task の作業として書き、Step 3 は「この段階は起票者が行う」と書く | 確認用の行は存在しなかった。判断を仰いだうえで実行者が作成した |
| 3 | Task 2 Step 4 要件 1 | 「**行に**添付があれば」と書くが、台帳の列に `files` 型は存在せず、行の列としては添付を置けない。置き場所が未測定のまま書かれている | ページの子の `file` ブロックとして実装した。列の追加は禁止 3 に触れるため行っていない |
| 4 | SPEC 冒頭 | 実行ホストを `lecun` と指定しているが、実行環境は `bengio` である | ホスト固有の前提は本 task に無いため `bengio` で実行し、ここに明記した |
| 5 | 完了判定 12 の検査 | 「検査で 0 件」を期待とするが、実際には変数名・環境変数名で必ず一致する。0 件になることはない | SPEC 自身の注記「何に一致したかを目視する」に従い、6 件すべてを目視して値でないことを確認した |

### 5.2 環境が禁止事項に抵触した（1 件）

**禁止 9「統合する」に事実として抵触している。実行者の操作ではない。**

常駐の `keeper.sh` が 30 分毎に呼ぶ `m2-sync.sh` は、作業分岐へ
`git merge --no-edit origin/phase0` を自動実行する設計である
（`~/bin/m2-sync.sh` の「auto-merge」節）。本 task の実行中に発火した。

    2026-08-10 20:06:40 [bengio] auto-merge: feat/attachment-transport <- origin/phase0 (2 commits)

結果として merge commit `9d89a7b` が入った。`git reflog` でも `HEAD@{1}` が
`merge origin/phase0` として実行者の操作なしに現れている。

**契約が環境の既定動作と衝突している。** 禁止を守るには `EGOSURGERY_AUTOSYNC=0` を
export して常駐を止める必要があるが、他の作業へ影響するため実行者の判断では止めなかった。
自動 push と自動 PR は発火しておらず、分岐は未送出・PR も無い状態を確認した。

### 5.3 自分で判断した箇所

| # | 判断 | 理由 |
|---|---|---|
| 1 | 壊れやすい要素を `SPEC.md` 側へ寄せ、`spec.yaml` は妥当な契約にした | 混ぜると YAML として解釈できず、同じ束で G2 の「取り込めること」を測れなくなる。1 つの束で G1 と G2 の両方を兼ねられる |
| 2 | 子ブロックの走査を**一度**にまとめた | 最初の実装は添付探索と本文収集で 2 周しており、添付を持たない既存行で呼び出しが倍になっていた。既存試験 `test_paginated_blocks_are_concatenated` がこの退行を検出した |
| 3 | 添付の取得失敗時に本文へ落ちない設計にした | 落ちると「添付を置いたのに本文が使われた」ことに気づけない。SPEC の要件 1〜5 は落とすともフォールバックするとも書いていない |
| 4 | 期限切れ（401 / 403）だけを再試行対象にした | 実測で期限が 60 分・URL が毎回変わることを確認したため。他のエラーは取り直しても回復しない |
| 5 | `.env.example` の `SERVERNAME` を修正せず文書化にとどめた | Task 3 の Files は `tasks/README.md` または運用手順の文書に限られ、テンプレートは範囲外。共有の資格情報テンプレートの変更は明示の承認に値する |
| 6 | 確認用の行を台帳に残した（`superseded`・印つき） | Task 1 の Files が「確認後に印を付ける」としているため。不要なら削除してよい旨を `result.yaml` に記した |

### 5.4 手順として実施したもの（逸脱ではない）

| # | 内容 |
|---|---|
| 1 | `conventions_rev` を `1201f4f` から実測値 `d422b08` へ置換した。SPEC Task 4 Step 1 が「これは逸脱ではなく手順である」と明記している |

### 5.5 実装・検査の不具合と、見つけた経緯

| # | 内容 |
|---|---|
| 1 | 試験ファイルに書きかけの関数を残して構文エラーにした。同じ内容は別の試験が担っていたため削除した |
| 2 | `/databases/{id}/query` を GET で呼び HTTP 400。POST が要る。既存の `_notion_call` を使って直した |
| 3 | **終了コードの検査が空振りしていた。** `${PIPESTATUS[0]}` は bash の書式で、zsh では `pipestatus`（小文字・1 始まり）である。`exit=` が空のまま「非ゼロを確認した」ことになりかけた。パイプを外して `$?` を直接測り直した（SPEC §0 の「対話シェルは bash ではない」と注意 5 が当たった） |
| 4 | `result.yaml` の `issuer_defects.type` に語彙外の値（`unmeasurable_as_written` / `environment_violates_prohibition`）を書き、`task-validate` が `exit 2` になった。スキーマ `tasks/_schema/result.schema.json` を読まずに書いたのが原因。許される 4 語彙のうち、いずれも本質は「起票者が確かめずに断定した」ため `asserted_without_measuring` へ写した。`shell_assumption` は使っていない。`${PIPESTATUS[0]}` の件は**起票者が §0 で警告していたのに実行者が踏んだ**ものであり、起票者の誤りではないためである |
| 5 | 検査を `for t in "task-validate TASK=..."; do make $t; done` の形で回し、`exit 2` を得た。**zsh は変数展開で単語分割をしない**ため、`make $t` は文字列全体を 1 つの target 名として渡す。bash なら 2 語に分割されて動く。契約や実装ではなく検査ループ側の誤りで、個別に実行し直すと 5 検査すべて `exit 0` だった。再現も確認済み（`make ${=t}` なら通る）。**本 task で shell の差に起因する検査の空振りは 2 件目**である（1 件目は `${PIPESTATUS[0]}`） |

---

## 6. 申し送り

### 6.1 `.env.example` が事故の再発導線を保持している

**最優先。** 17 行目の `SERVERNAME=bengio` を写すと、全ホストが同じ論理名を名乗る。
`c61a673` は暗号文だけを直した。判断が要る点は次の 2 つ。

- テンプレートから `SERVERNAME` の行を落とすか、コメントとして「ここへ書かない」と残すか
- 既に `.env` に `SERVERNAME` を持つホストがあるかを各ホストで確かめる必要がある（未測定）

論理名の正しい注入先は各ホストの起動時設定である
（`scripts/sync/setup_host_servername.sh` が `~/.zshenv` 等へ入れる）。

### 6.2 契約と環境の自動同期が衝突している

禁止 9 を掲げる契約を、`m2-sync.sh` が 30 分毎に自動 merge する環境で実行している。
どちらかを変えないと、以後の契約でも同じ抵触が続く。取りうる案は 3 つある。

- 契約の実行中だけ `EGOSURGERY_AUTOSYNC=0` を export する運用を手順書へ入れる
- `m2-sync.sh` の auto-merge を対象分岐で絞る
- 禁止 9 の意図を「PR を統合しない」に限る旨を契約側の言葉で明確にする

**選んでいない。** 判断は起票者に委ねる。

### 6.3 台帳に識別子が空の行がある

`status` も空のため `read_notion_bundle` は live として扱う。
同じ識別子で 2 件以上になれば失敗する設計だが、空の識別子が候補に残る構造は残っている。
実害は未確認である。

### 6.4 期限切れからの再試行は実地では未確認

参照先の期限が 60 分であることは実測したが、実際に 60 分待って期限切れを起こし、
取り直しで回復することは確かめていない。試験では応答を差し替えて経路のみ検査した。

### 6.5 確認用の行と添付を台帳へ残した

`T-2026-08-14-probe-attachment`（`status=superseded`、note に確認結果を記載）。
本番の契約とは混ざらない。不要なら削除してよい。

---

## 7. 生成物

| 種別 | パス |
|---|---|
| 変更 | `tools/fetch_task.py`（添付からの取り込み） |
| 変更 | `tests/test_fetch_task_notion.py`（試験 5 件を追加） |
| 変更 | `docs/secrets_and_tracking.md`（鍵の更新後の操作・ホスト固有の値） |
| 変更 | `tasks/README.md`（添付優先と鍵の更新の要点） |
| 新規 | `tasks/T-2026-08-14-bundle-attachment-transport/RESULT.md` / `result.yaml` |
| 新規 | `tasks/inbox.d/T-2026-08-14-bundle-attachment-transport.md` |
| **未変更** | `runindex/` `context/auto/` `context/conventions.md` `experiments/` `transfer/` `data/splits/` |
