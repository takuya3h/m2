# 完了報告の構造化された対。散文（RESULT.md）とは別に、実行者が事実だけを書く。
result_version: 1
task_id: "T-2026-08-14-bundle-attachment-transport"
status: pass
host: "bengio"
branch: "feat/attachment-transport"
pr: 70
merged: false

gates:
  - {id: G1, verdict: pass}
  - {id: G2, verdict: pass}

tests:
  before_failed: 5
  after_failed: 5
  after_passed: 298

deviations: 6

issuer_defects:
  - type: self_contradiction
    note: "Task 1 Step 3 は確認用の行を superseded にせよと言うが、G2 正常系はその行から取り込めと言う。read_notion_bundle は設計上 superseded の行を候補から除外するため両立しない。確認用の行の状態を一時的に戻して測り、直後に superseded へ復した"
  - type: self_contradiction
    note: "Task 1 の Files が『台帳へ確認用の行を作り、確認後に印を付ける』としながら、同じ Task の Step 3 が『この段階は起票者が行う』としている。担当が定まらない。確認用の行は存在しなかったため、判断を仰いだうえで実行者が作成した"
  - type: asserted_without_measuring
    note: "実行ホストを lecun と指定しているが、実行環境は bengio である。ホスト固有の前提は本 task には無かったため bengio で実行した"
  - type: asserted_without_measuring
    note: "『添付が同じ忠実性を持つかは未測定である』は正しいが、添付の置き場所（行の列か、ページの子か）も未測定のまま『行に添付があれば』と書かれている。台帳の列に files 型は存在せず、添付はページの子の file ブロックとしてしか置けない"
  - type: asserted_without_measuring
    note: "禁止 9 は統合を禁じるが、常駐の m2-sync.sh が 30 分毎に作業分岐へ origin/phase0 を自動 merge する。20:06:40 に auto-merge が実行され、実行者の操作なしに merge commit 9d89a7b が入った。契約が環境の既定動作と衝突している"

followups:
  - ".env.example は 17 行目に SERVERNAME=bengio を含んだままである。c61a673 は暗号文だけを直しテンプレートを直していないため、写すと『全台が同じ論理名を名乗る』事故が再発する。テンプレートの修正は本契約の Files 範囲外のため文書化にとどめた"
  - "配布台帳に task_id が空の行が 1 件ある（status も空）。read_notion_bundle は superseded 以外を live として扱うため、識別子が空の行が候補に残る構造になっている。実害は未確認"
  - "添付の参照先の期限は 60 分と実測したが、期限切れからの再試行は試験でのみ検査した。実地で 60 分待って期限切れを起こす検査は行っていない"
  - "確認用の行 T-2026-08-14-probe-attachment を台帳へ残した（status=superseded、印つき）。不要なら削除してよい"

unknowns:
  - "他ホストでの添付取り込みは未実測。bengio 上でのみ実測した"
  - "添付が複数ある行の扱いは未実測。実装は最初の file ブロックを採るが、そうした行を作って確かめてはいない"
  - "外部の連携機能（起票者が使う面）が添付をどう置くかは未実測。実行者は API 経由で置いた"

commits:
  - "d342620"
  - "1fee5bf"
