# RESULT — T-2026-08-16-docs-reconciliation

**実行ホスト:** `lecun`（SPEC の指定は `bengio`。§1 に記す）
**分岐:** `feat/docs-reconciliation`（`origin/phase0` = `d56d894` から作成）
**状態:** pass（G1 pass / G2 pass）

---

## 1. 解決された参照

### `contract.inject_verbatim: [conventions#prohibitions]` — 原文

```
## prohibitions

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |
```

### `conventions_rev` の実測

起票時 `1201f4f` / 実測 `d422b08`。手順に従って置換した。注入対象のアンカーは
両版とも 9 行で完全一致（機械比較で `True`）。差分は `frozen_source` 節の +10 行のみ。

### 検証時の WARN 3 件

提示して続行の許可を得た。内容は前契約と同一で、`conventions.md` の変更（注入対象は不変）と
分母の移動（`index.csv` 749→751 / `experiments.csv` 206→207）である。本契約は
`inputs.denominator.ref` を持たず、分母を使う数値主張をしない。

### 実行ホストの相違

**SPEC は `実行ホスト: bengio` と指定するが、契約は `lecun` へ配布され `lecun` で実行された。**
本契約の測定対象は追跡下の文書・Makefile・`tools/` であり、いずれもホストに依存しない。
作業内容は変えていない。`result.yaml` の `host` は実測どおり `lecun` と書く。

---

## 2. Phase A — 棚卸し

### 分類

追跡下の `*.md` は 837 件。`tasks/T-` 配下の契約 60 件を除いた 777 件が母集団である。
記録として自明な領域を除いた 96 件を個別に判定し、**42 件を現行手順**とした。
判定の基準は「いま読んで従うものか」で、**迷うものは記録として扱った。**

一覧は `docs/docs_audit.md` にある。この表は `tools/check_docs.py` が読む
**対象の一覧を兼ねる。**

### 実態の測定

| 対象 | 実測 |
|---|---|
| Makefile のターゲット | 27 件（本 task で `docs-check` を足して 28 件） |
| `tools/*.py` | 18 件 |
| `scripts/sync/*.sh` | 6 件 |
| `context/auto/` | 6 件 |
| `origin/exp/` の分岐 | 22 本 |

### 食い違い

当初 9 件を提示して承認を得た。**うち 2 件は私の誤りだった**（§3）。
無効になった 2 件の代わりに、調査中に見つけた 1 件を再提示して承認を得た。
**直したのは 8 件である。**

| # | 文書 | 内容 |
|---|---|---|
| 1 | CLAUDE.md 54-57 | 道具の一覧が 6/4/2 件。実在は 8/5/4 件。**契約実行の中心である `task` スキルが未記載** |
| 2 | AGENTS.md 54-57 | **実体は CLAUDE.md への symlink。** 別文書ではない。1 の修正で同時に解消 |
| 3 | context/README.md 11 | `context/auto/` を 4 件と記載。実在 6 件 |
| 4 | context/README.md 12 | 人手管理を 3 件と記載。`glossary.md` と `plan_mirror.md` は**履歴に一度も存在しない** |
| 5 | context/README.md 33 | 研究方針は `context/plan_mirror.md` の役目と記載。実態は外部の運用ハブ |
| 6 | .claude/agents/paper-writer.md 15 | 存在しない 2 つの道具を使えと指示。`af1fc58` で空の scaffold として作られ `41e9ac1` で意図的に削除されている |
| 7 | 現行手順 42 件すべて | `result.yaml` の記載が 0 件。schema と雛形が実在し契約 3 件が使用中 |
| 8 | 現行手順 42 件すべて | 投影（`make taskindex` / `followups.md` / `tasks_summary.csv`）の記載が 0 件 |
| 9 | README.md 400-401 | `make delta` を「算出」、`make tables` を「書き出し」と説明。実際は在り処を表示するだけ（追加承認分） |

件数が 0 の 2 件（7 と 8）は、現行手順 42 件に限った探索と、記録領域を除く追跡下の
全文書 96 件に対する探索の**双方で 0 件**であった。

---

## 3. 食い違いと判定したが誤りだったもの

**当初提示した 9 件のうち 2 件は誤りだった。** 隠さずに記す。

| 当初の判定 | 実際 |
|---|---|
| `make s0` `s2` `s4` `s5` `s6` は存在しない | **5 つとも Makefile の 37 40 43 46 49 行に実在する。** `make -n s0` は `bash scripts/run_s0.sh` を返し、1 行目の `.PHONY` にも並んでいる |
| `make s0` が無いので `run_s0.sh` は呼ばれない | 呼ばれる。元の記述は正しい |

一度書き換えたが、**元の記述へ戻した**（`git checkout -- README.md` で差分 0 を確認）。

**原因は SPEC が示した実態の測り方をそのまま信じたことである。**

    grep -E "^[a-z-]+:" Makefile | cut -d: -f1 | sort

この式は `[a-z-]+` であり、**数字を含むターゲットに一致しない。** `s0` `s2` `s4` `s5` `s6`
がちょうど落ちて 22 件と出る。実際は 27 件である。SPEC 自身が注意 1 で
「一致件数が 0 のとき、別の探し方でも 0 になることを確かめる」と書いているのに、
私は一覧に無いことだけを見て結論した。**一覧の作り方を疑わなかった。**

残る 7 件は、それぞれ 2 通り以上の探し方で確かめ直した（`ls` と `find`、
`git log --all` と全 ref の走査、実在検査と近似名の検索）。誤りは 2 件のみだった。

**この誤りは Phase C の検査が見つけた。** 検査は `^[a-z][a-z0-9-]*:` で読むため 27 件を数え、
22 件との差の 5 件がちょうど `s0` `s2` `s4` `s5` `s6` だった。

---

## 4. Phase B — 更新

承認された範囲だけを直した。**同じ文書の中の過去の記述は変えていない。**

| 文書 | 変更 |
|---|---|
| CLAUDE.md | 道具の一覧を実態へ。`task` スキルを筆頭に置き、Codex 側の呼び方も併記 |
| context/README.md | `context/auto/` を生成器ごとに 2 行へ分割。人手管理を `conventions.md` のみに。`make taskindex` と各検査が自分の出力だけを見ることを追記。研究方針の置き場を運用ハブへ |
| .claude/agents/paper-writer.md | 表の材料を `runindex/verdicts.csv` と `runindex/experiments.csv` へ。`paper/tables/` が無いことを明記 |
| tasks/README.md | ディレクトリ図に `result.yaml` を追加。「完了報告」節と「投影」節を新設 |
| .claude/skills/task/SKILL.md | §6 に `result.yaml` と投影の確認手順を追加 |
| README.md | `make delta` / `make tables` の説明を実態へ（追加承認分） |

**触らなかった過去の記述**を挙げる。

| 箇所 | 理由 |
|---|---|
| OPERATION.md 104 の `exp/aolab-wip-20260703` | 過去の分岐についての、現在も正しい説明である |
| context/conventions.md | 禁止 3 |
| tasks/T- 配下の契約 60 件 | 禁止 1。実測でも変更 0 件 |
| 記録として分類した 735 件 | 対象外 |

---

## 5. Phase C — 機械で確かめる

### 作ったもの

| ファイル | 内容 |
|---|---|
| `tools/check_docs.py` | 現行手順の文書に書かれた操作と経路の実在を確かめる |
| `tests/test_check_docs.py` | 16 件。通る例・通らない例・偽陽性の除外・落ちないこと |
| `Makefile` | `docs-check` を `taskindex-check` の直後へ追加 |

対象の一覧は `docs/docs_audit.md` の分類表から読む。**記録は見ない。**
走査するのは行内のバッククォートとコードブロックの中だけで、散文は見ない。
英文の "about to make a non-trivial change" を操作名と誤読するためである（実測）。

### 誤検出を避けた範囲

7 種類の偽陽性を実測で切り分けた。規則で外せないものには
`<!-- docs-check: ignore-line -->` を **7 箇所**置いた。全件を `docs_audit.md` に記す。

| 除外 | 手段 |
|---|---|
| 散文中の英単語 make | コードとして書かれた箇所だけを走査する |
| 変数・グロブ・省略 | `$` `<` `{` `*` `...` を含む記述を対象外 |
| ホスト依存のデータ・実行時生成の証跡 | `data/` `experiments/` `third_party/` を対象外 |
| 分岐名 | `git branch -r` と突き合わせて除外 |
| 候補の優先順位列挙・外部リポジトリ相対・「存在しない」と述べる文 | 除外の印 |

### 検査自身にあった欠陥

対象の一覧を読む正規表現が先頭のドットを許しておらず、**`.claude/` 配下の 17 文書が
静かに対象から落ちていた。** 対象が 42 ではなく 25 と表示されたことで気付いた。
直して試験を足した。**件数を表示していなければ気付けなかった。**

### G2 ゲート — 両方向（`pass`）

| 方向 | 対象 | 結果 |
|---|---|---|
| 実文書 | 現行手順 42 件 | 食い違い 0 件、exit 0 |
| 陽性対照 | 存在しない操作と経路を書いた一時文書 1 件 | **2 件を検出、exit 1** |

陽性対照は反応した。**検査は空振りしていない。** 確認後、一時文書を消し、
作業領域に痕跡が無いこと（`git status` に該当 0 件）と、消したあとも exit 0 に
戻ることを確かめた。

---

## 6. 完了判定

| # | 判定 | 期待 | 実測 | |
|---|---|---|---|---|
| 1 | 文書が分類された | 一覧がある | `docs_audit.md` の分類表。現行手順 42 件と記録の集約 20 行 | ✅ |
| 2 | 食い違いが一覧になっている | 表が埋まっている | 9 行（うち 1 行は symlink により 1 の修正で解消） | ✅ |
| 3 | 承認された範囲だけを直した | 範囲外の変更が無い | 無効になった 2 件は実施せず復元。追加の 1 件は再提示して承認を得た | ✅ |
| 4 | 過去の記録が不変 | 該当 0 件 | `tasks/T-`（自分以外）0 件。`experiments` `transfer` `third_party_snapshot` すべて出力なし | ✅ |
| 5 | 検査が実在を確かめる | 現行手順の文書が通る | 42 文書 / 食い違い 0 / exit 0 | ✅ |
| 6 | 検査が空振りでない | 陽性対照が非ゼロ | 2 件検出 / exit 1 | ✅ |
| 7 | 確かめていない範囲が明示 | 記載あり | `docs_audit.md` 末尾に確かめる 3 点と確かめない 5 点 | ✅ |
| 8 | 一時的な文書が残っていない | 作業領域が清潔 | 残存なし。`git status` に痕跡なし | ✅ |
| 9 | 契約検証が通る | exit 0 | `make task-validate` = 0 | ✅ |
| 10 | 実行前検査が通る | exit 0 | `make task-preflight` = 0 | ✅ |
| 11 | 試験が不変 | 開始前を先に測る | 開始前 5 failed / 298 passed → 終了後 5 failed / **314 passed**。増分 16 は本 task の新規試験。失敗は同じ 5 件 | ✅ |
| 12 | 禁止領域が無変更 | 出力なし | `runindex` `context/auto` `experiments` `transfer` `data/splits` `third_party_snapshot` `context/conventions.md` すべて出力なし | ✅ |
| 13 | 抑止の目印を解除した | ファイルが無い | 起票の後に実施（§10） | ✅ |

### プリフライトの SKIP 一覧（合格ではない）

`RESULT: 4 PASS / 4 SKIP / 0 FAIL`

| 項目 | 理由 |
|---|---|
| P2 `cuda_ext_loaded` | `plan.env.preflight` に記載なし |
| P3 `deterministic_flags` | `plan.env.preflight` に記載なし |
| P4 `prereg_committed` | `kind=impl` のため対象外 |
| P5 `frozen_source_hash` | `kind=impl` のため対象外 |

---

## 7. 契約の実行中に常駐処理が何をしたか

**統合は起きていない。そして今回は、抑止が実際に効いたためである。**

前契約で作った抑止が `phase0` に届き（PR #71 が 04:48 に統合）、keeper が
`~/bin/m2-sync.sh` を自己更新済みであった（`grep -c sync-pause` = 2）。
契約の開始時に `.sync-pause` を置いた。

    2026-08-11 06:01:41 [lecun] 一時停止中: /home/ubuntu/slocal2/m2/.sync-pause があるため分岐へ書き込まない（消せば再開）

**これが稼働中の常駐処理で抑止が働いた最初の記録である。** 前契約では稼働版に判定が
無く、統合が起きなかったのは behind が 0 だったからにすぎなかった。今回は違う。

`git reflog` にも本分岐への統合は無い（`HEAD@{0}` は分岐作成の checkout）。
`MERGE_HEAD` も無い。

---

## 8. 起票者の誤り

| # | 型 | 内容 |
|---|---|---|
| 1 | `check_does_not_check` | Phase A Step 2 が示す実態の測り方 `grep -E "^[a-z-]+:" Makefile` が**数字を含むターゲットに一致しない。** 22 件と出るが実際は 27 件で、落ちた 5 件がそのまま「存在しない操作」の誤検出になった。**SPEC が 15 task 連続で起きていると警告した型が、SPEC 自身の測定コマンドに入っていた** |
| 2 | `check_does_not_check` | 判定 5「検査が実在を確かめる／現行手順の文書が通る」は、**検査の対象一覧が正しいことを確かめない。** 対象が 42 件でなく 25 件に落ちていても「通る」ため合格する。実際に落ちたが、判定ではなく件数の表示で気付いた |
| 3 | `asserted_without_measuring` | `実行ホスト: bengio` と断定しているが、契約は `lecun` へ配布され `lecun` で実行された。起票時に配布先を測っていない |

---

## 9. deviations

**空ではない。** 4 件。

1. **承認済み 9 件のうち 2 件を実施せず、書き換えを元へ戻した。** 私の判定が誤りだった
   （§3）。無効になった分の代わりに、調査中に見つけた 1 件を再提示し、**承認を得てから**
   直した。範囲を勝手に置き換えてはいない。
2. **現行手順の文書 7 箇所へ除外の印を足した。** 承認された食い違いの修正とは別種の変更である。
   SPEC Phase C Step 2 が「除外の印を決めるか、対象の文書を限る」を認めているため実施した。
   全 7 箇所を `docs_audit.md` に記録した。
3. **文書以外に 3 ファイルを触った**（`tools/check_docs.py` `tests/test_check_docs.py`
   `Makefile`）。Phase C が「例外的に実装を作る」と明示している範囲である。既存の実装は
   変更していない。
4. **実行ホストが SPEC 指定の `bengio` ではなく `lecun` である。** 測定対象がホストに
   依存しないため作業内容は変えていないが、SPEC の指定とは異なる。

`spec.yaml` の `conventions_rev` 置換は、SPEC が「逸脱ではなく手順」と明記しているため
逸脱に数えていない。

---

## 10. 報告後の再確認（利用者の指摘による）

報告のあと、利用者から `OPERATION.md` の扱いと、他に見落としが無いかを問われた。
**照合の型を変えて 42 文書を洗い直したところ、3 件の見落としが出た。**

| 文書 | 現在の記述 | 実測 |
|---|---|---|
| OPERATION.md 471 | backlog を「34 件」 | 当時の数のまま。数を書くのをやめ、出所（`STATE.md`）だけを示した |
| docs/reproduce_on_new_machine.md 87 90 | 「99 テスト収集。既知の 1 件が fail。それ以外がパスすれば環境健全」 | **319 テスト / failed 5 件。** 新規ホストの構築者が健全な環境を壊れていると誤判定する状態だった |
| README.md 542 | 「評価指標 4 件 + StageATrainer 2 件」 | 8 件 / 10 件 |

いずれも**追加の承認を得てから**直した。

### 見落としの型

**Phase A の照合を「操作名」と「経路」に寄せていた。** これらは存在するかしないかの
二値なので機械化しやすい。一方「34 件」のような**数の主張**は、対応する実測値がどこに
あるかを人が知らないと照合できない。3 件とも `docs-check` を通過している。

洗い直しでは数の主張 57 件と裸のファイル名 28 件を抽出した。裸のファイル名は
**全件が非該当**（外部パッケージ・`third_party/` 配下・gitignore 済み・ホスト依存
データ・変数）。研究サーバー 11 台の名前は**全て定位置分岐に実在**し、以前 UNKNOWN と
していた「11 台」は裏が取れた。「実験に使用中の 4 台」だけは検証できず UNKNOWN のままである。

### 試験の失敗 4 件を起票した

`docs/reproduce_on_new_machine.md` の「既知の失敗は 1 件」が誤っていた原因は、
`tests/test_research_logger.py` の 4 件が `origin/phase0` の時点から失敗していることである。
`log_run` が `None` を返し `log_experiment_to_notion` が呼ばれない。**環境非依存**である
ことを実測した（MagicMock による純粋な論理の照合で、資格情報も外部接続も要らない）。
SPEC の「文書を実装に合わせ、未解決事項として起票する」に従い `B-40` として起票した。

### 起票の際に自分が起こした事故

`B-40` を追記するとき、**別の文字列定数 `RUNINDEX_README` の中へ入れてしまった。**
錨にした一文がその末尾にもあり、ファイル全体で一意だったため置換は成功してしまった。
投影に `B-40` が現れず BL 行が 38 のままだったことで気付き、正しい位置へ入れ直した。
`runindex/` は再生成していないため汚染は無い（差分 0 件を確認）。

**一意であることは、正しい場所であることを意味しない。**

## 11. 残った課題

| # | 内容 |
|---|---|
| 1 | 検査は**コードとして書かれた箇所しか見ない。** バッククォートを付けずに散文へ書かれた操作名や経路は検出できない。検出漏れの件数は未測定 |
| 2 | 検査が確かめるのは実在だけである。手順の順序・前提条件・説明の内容が実装と合っているかは人が読むしかない |
| 3 | 記録として分類した 735 件の内容は確かめていない。過去の記述が現在と食い違うのは当然であり、対象外とした |
| 4 | `docs/m2_plan_rewrite/` 25 件を記録として扱ったが、現行の計画は外部の運用ハブにある。両者の重複と役割分担は未整理 |
| 5 | 他ホストでは本 task の変更を実行していない。`lecun` 上でのみ実測した |
