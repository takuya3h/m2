# RESULT — T-2026-08-11-leftover-relocation

**実行者:** `lecun` / `feat/leftover-relocation` / `63edc44`（`origin/phase0` と一致）
**実行日時:** 2026-08-09T14:15Z 〜 2026-08-09T16:18Z
**判定:** **PASS** — 34 件を退避し、このホストの索引が正本と完全一致することを実測で確認した。

| 検証 | 結果 |
|---|---|
| 退避対象が追跡下に無い | ✅ 34 件すべて未追跡。配下の追跡ファイルも 0 件 |
| 退避物が失われていない | ✅ 481 ファイル / 2715007414 バイトが前後で完全一致 |
| 退避後の索引が正本と一致 | ✅ CSV 4 種の md5 が完全一致。751 行 |
| 追跡外の経路を持つ行 | ✅ **34 → 0** |
| 追跡下の実験証跡の移動 | ✅ **0 件** |

**このホストは索引の正本を生成できる条件を満たした。**

---

## 1. 解決された参照

| 項目 | spec の記載 | 解決結果 |
|---|---|---|
| `inputs.denominator.ref` | **記載なし** | 対象外（本契約に分母の宣言は無い） |
| `inputs.sigma_policy` | **記載なし** | 対象外（判定を行わない） |
| `inputs.frozen_source.ref` | **記載なし** | 対象外。preflight の `P5` も `kind=impl` のため SKIP |
| `contract.conventions_rev` | `1201f4f` | **`d422b08` へ実測置換**（SPEC Task 4 Step 2 の手順に従う） |
| `contract.inject_verbatim` | `conventions#prohibitions`, `conventions#naming` | 下記に原文を転記 |

### `conventions#prohibitions`（原文）

```
<a id="prohibitions"></a>
## prohibitions

| id | 禁止事項 |
|---|---|
| `no_split_redefine` | split を再定義しない |
| `no_raw_write` | `data/raw` `data/external` に書き込まない |
| `no_frozen_change` | 凍結源を変更しない |
| `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
| `no_runindex_hand_edit` | `runindex/` を手で編集しない |
```

### `conventions#naming`（原文）

```
<a id="naming"></a>
## naming

実験フォルダは手作業で命名せず、`ExperimentManager` が次の規則で自動採番する。

    {step}_{seq:03d}_{description}_seed{seed}

- `step`: `s0`〜`s9`、または `a1`〜`a7`
- `seq`: 同一 category と step 内の3桁ゼロ埋め連番
- `description`: 実験内容の短い説明
- `seed`: 乱数シード。既定42

転記元: `README.md` の「命名規則」。
```

### `conventions_rev` の差分

`1201f4f` → `d422b08` は **+10 / −0**。差分ハンクは `frozen_source` 節（L56 に 9 行）と
変更履歴（L143 に 1 行）の 2 箇所のみ。**原文注入する 2 アンカーはいずれも無変更**
（`prohibitions` L98–108 / `naming` L121–133）。

---

## 2. ゲートの通過状況

| gate | 判定 | 実測 |
|---|---|---|
| **G1**（after A） | **PASS** | 3 通りの探し方が同一の集合 34 件で一致。追跡下 0 件。控え作成済み。索引を md5 一致で復元 |
| **G2**（after B） | **PASS** | 退避後の索引が CSV 4 種の md5 で正本と完全一致。追跡外の経路 0 件 |

---

## 3. 索引の差（実測）

| 測定 | 値 |
|---|---|
| 統合先に記録されている行数 | **751** |
| 退避前にこのホストで再生成した行数 | **785** |
| 差 | **+34** |
| 先行調査の想定 | 34 |

**実測と先行調査は一致した。** 契約の `meta.created_from.counts` は `index: 749` だが、
これは起票時点の値であり、その後 `64576f3`（退避物を持たないホストでの再生成）で
751 へ更新されている。`make task-validate` が L2-8 で警告を出すのはこのためであり、
**契約の値は書き換えていない。**

---

## 4. 2 通りの探し方の照合

**件数の一致だけでは判断していない。集合そのものを突き合わせた。**

| 方法 | 探し方 | 件数 |
|---|---|---|
| A（索引側） | `index.csv` の全 785 行の `path` を `git ls-files --error-unmatch` にかけ、失敗したものを集める | **34** |
| B（ディスク側・SPEC 記載） | `find experiments transfer -maxdepth 3 -type d` のうち `config.yaml` を持ち、かつ未追跡のもの | **34** |
| C（ディスク側・条件を緩めた版） | 同上だが `config.yaml` を必須とせず、`metrics.json` か `command.sh` でも可とする | **34** |

**A と B の集合は完全に一致した**（差分 0 行）。B と C も完全一致。

### 事前の懸念は外れた

着手前に「一覧に `_aborted_codetr_no_config` があり、名前が `config` の不在を示すため、
`config.yaml` を条件とする方法 B は 3 件取りこぼす」と予想した。**実測では外れていた。**
当該 3 ディレクトリはいずれも `config.yaml` を持っている。ディレクトリ名は中断の原因を
指すもので、現在のファイル構成を指していない。**SPEC の検索パターンは不完全ではなかった。**

### 逆方向の食い違いも無い

SPEC の方法 B は `transfer` も走査対象にするが、収穫器は `experiments/` しか走査しない。
`transfer/` に未追跡ディレクトリがあれば「索引に無いものをディスク側だけが拾う」という
逆方向の食い違いが起きうる。実測では `transfer/` 直下の 29 ディレクトリは
**全 29 件が追跡下**であり、未追跡は 0 件だった。

---

## 5. 追跡下のものが混ざっていないことの確認

| 検査 | 結果 |
|---|---|
| 34 経路を一件ずつ `git ls-files --error-unmatch` にかけた | **追跡下 0 件 / 未追跡 34 件** |
| 各ディレクトリ配下に追跡下ファイルが 1 件でもあるか | **合計 0 件** |

2 番目はディレクトリ単位の判定より強い検査である。ディレクトリが未追跡でも、
その配下に追跡下のファイルが混ざっていれば移動で失われるため、配下まで数えた。

**`tracked_artifact_moved` のリスクは 0 であることを移動前に確定させた。**

---

## 6. 退避した経路の一覧と分類

全 34 件の対応表は `relocation_log.md` にある。内訳は次のとおり。

| 分類 | 索引の除外理由 | 件数 | 親ディレクトリ |
|---|---|---|---|
| 動作確認 | `smoke_test` | 19 | `_smoke_prior_simplehead` 6 / `_smoke_v2_part3` 6 / `_pre_redo_s0_smoke` 6 / `_smoke_e3` 1 |
| 置き換え済み | `superseded` | 6 | `_prior_no_eval_recipe` 6 |
| 失敗した run | `failed_run` | 5 | `_failed_num_workers_zero` 5 |
| 中断した run | `aborted_run` | 4 | `_aborted_codetr_no_config` 3 / `_aborted_s0_cuda_visible_misconfig` 1 |
| 計 | | **34** | 8 ディレクトリ |

先行調査の分類（動作確認 19 / 置き換え済み 6 / 失敗 5 / 中断 4）と**一致した**。

---

## 7. 移動前後の照合

退避先は `~/m2-archive/20260811/`。repo と**同一ファイルシステム**
（デバイス番号 1048697）のため、移動は rename でありデータの複製は発生していない。

| 項目 | 移動前 | 移動後 |
|---|---|---|
| ディレクトリ数 | 34 | 34 |
| 合計ファイル数 | **481** | **481** |
| 合計容量 | **2653644 KB** | **2653644 KB** |
| 合計バイト数 | **2715007414** | **2715007414** |

照合は 2 通りで行った。

1. ディレクトリ単位の `path` `ファイル数` `容量` の突き合わせ → **差分なし**
2. 全 481 ファイルの `サイズ` と `相対経路` の突き合わせ → **差分なし**

移動は 34 件すべて成功し、失敗 0 件。元の場所に残存するものは 0 件。
`file_count_mismatch` は発生していない。

### 追跡下のものが動いていないこと

| 検査 | 結果 |
|---|---|
| `git status --porcelain` の削除（D）扱い | **0 件** |
| `git ls-files --deleted` | **0 件** |
| `git ls-files --modified` | **0 件** |
| `git diff --stat HEAD` | **0 行** |

**追跡下の実験証跡は一件も移動していない。**

### 元の場所に残した空ディレクトリ

移動元の親にあたる 8 ディレクトリは**空のまま残している**。
禁止事項「退避物を削除する（移動のみ）」に従い削除しなかった。
空ディレクトリは版管理が追跡せず収穫器も走査しないため、索引には現れない
（退避後の再生成で追跡外の経路が 0 件であることを実測で確認済み）。

---

## 8. 退避後の索引

| 項目 | 退避前 | 退避後 |
|---|---|---|
| このホストで再生成した行数 | 785 | **751** |
| 統合先に記録されている行数 | 751 | 751 |
| `git diff --stat runindex/index.csv` | 差分あり | **0 行** |
| CSV 4 種の md5 が統合先と一致 | 不一致 | **完全一致** |
| 追跡外の経路を持つ行 | **34** | **0**（751 行すべて追跡下） |
| `runindex/` `context/auto/` の作業ツリー差分 | — | **0 件** |

**このホストは索引の正本を生成できる条件を満たした。**
`tasks/README.md` の「索引の正本」が定める 2 条件、すなわち
「追跡外の run を持たないホストで行う」「全行の `path` が Git 追跡下であること」を
いずれも満たしている。

### 正本側に 1 行の残留物があった

退避後の再生成で `runindex/anomalies.md` に **1 行の差**が残った。調べた結果、
**原因はこのホストではなく正本側にある。**

| 実測 | 結果 |
|---|---|
| 差分の内容 | `_smoke_proptest_20260804_223211` の行が**消える**（0 ファイルの群として正本に載っている） |
| 当該ディレクトリが lecun のディスクに | **存在しない** |
| 版管理の追跡下に | **0 ファイル**（履歴も無し） |
| 正本 `anomalies.md` を作った commit | `64576f3` chore(runindex): regenerate the index on a host without leftovers |

これは 2026-08-04 の伝播テストで作った一時ディレクトリの**空の殻**が、
正本を生成したホストに残っていたものと読める。**空ディレクトリは版管理が追跡しないため、
そのホストの「追跡外 0 件」検査でも検出できない。**

結果として、**退避後の lecun が生成する索引のほうが正本より 1 行清潔**である。
`index.csv` は完全一致しているため解析には影響しない。
利用者の判断により、この差分は破棄して作業ツリーを清潔に戻した（§10 D-6）。
SPEC Task 4 Step 7 の「索引は記録しない。差分が無いことを確認するのが目的」と整合する。

---

## 9. 完了判定

| # | 判定 | 期待 | 実測 |
|---|---|---|---|
| 1 | 2 通りの探し方で件数が一致 | 一致 | ✅ **3 通りとも 34 件、集合も完全一致** |
| 2 | 追跡下のものを含まない | 0 件 | ✅ **0 件**（配下の追跡ファイルも 0） |
| 3 | 控えが作られた | 存在 | ✅ `manifest_before.txt` 34 行ほか計 5 種 |
| 4 | 移動前後でファイル数が一致 | 一致 | ✅ **481 = 481**、バイト数も一致 |
| 5 | 元の場所が空 | 何も出ない | ✅ **残存 0 件** |
| 6 | 追跡下が削除されていない | 削除なし | ✅ **0 件**（4 通りの検査） |
| 7 | 索引に差分が無い | 差分なし | ✅ `index.csv` **0 行** / CSV 4 種の md5 完全一致 |
| 8 | 追跡外の経路が 0 件 | 0 | ✅ **0**（751 行すべて追跡下） |
| 9 | 退避物が残っている | 容量あり | ✅ **2.6G / 486 ファイル** |
| 10 | 戻す手順が書かれている | 記載あり | ✅ `relocation_log.md`「戻す方法」 |
| 11 | 契約検証が通る | exit 0 | ✅ exit 0（WARN 2 件は L2-8 の分母変動） |
| 12 | 実行前検査が通る | exit 0 | ✅ 4 PASS / 4 SKIP / 0 FAIL |
| 13 | 試験が不変 | 開始前と比較 | ✅ **前 5 failed, 252 passed → 後 5 failed, 252 passed**。失敗テスト名も同一 |
| 14 | 禁止領域が無変更 | 出力なし | ✅ **出力なし**（並行作業との衝突なし） |

**判定13 の基準点（本 task 開始前・2026-08-09 14:16 実測）**

```
FAILED tests/test_engines.py::test_mmdet_trainer_eval_recipe_in_metrics
FAILED tests/test_research_logger.py::test_log_run_idempotent
FAILED tests/test_research_logger.py::test_run_logging_invokes_log_run_on_finally
FAILED tests/test_research_logger.py::test_run_logging_no_double_post_on_normal_exit
FAILED tests/test_research_logger.py::test_run_logging_swallows_exception_in_user_block
5 failed, 252 passed, 22 warnings in 27.03s
```

開始前に本ホストで測ってから比較した。**5 件は本 task 着手前から赤であり、増えていない。**

### preflight で SKIP された項目（合格ではない）

| 項目 | 理由 |
|---|---|
| `P2 cuda_ext_loaded` | `plan.env.preflight` に記載なし → **未実施** |
| `P3 deterministic_flags` | `plan.env.preflight` に記載なし → 未実施 |
| `P4 prereg_committed` | `kind=impl` のため対象外 |
| `P5 frozen_source_hash` | `kind=impl` のため対象外 |

本 task は演算装置を使わないため、`P2` `P3` の未実施に実害は無い。

---

## 10. deviations（指示書どおりにしなかった箇所）

### D-1. 一時ファイルの置き場を変えた

- **指示:** `/tmp/idx_before.txt` `/tmp/leftover_list.txt` 等
- **実際:** 本セッションの作業領域へ置いた。機能は同一。
  退避先 `~/m2-archive/20260811/` には `leftover_list.txt` の写しを残してある。
- **分類:** **環境差**

### D-2. 控えの作成が私のコマンドの欠陥で 6 行に切れた

- **指示:** Phase A Step 5 の `... | tee ~/m2-archive/20260811/manifest_before.txt`
- **実際:** 表示のために `| head -6` を足した結果、`head` が 6 行読んだ時点でパイプを閉じ、
  上流の `tee` と `while` ループに SIGPIPE が飛んで **`manifest_before.txt` が 6 行で打ち切られた**。
  作り直して 34 行にした。
- **重大さ:** **控えが 6 行のまま移動していたら、残り 28 件の照合基準が失われていた。**
  SPEC の元コマンドに `head` は無く、欠陥は私が持ち込んだものである。
- **教訓:** 記録と表示を同じパイプラインに混ぜない。
- **分類:** **判断が必要だった**（自分で気付いて正した）

### D-3. `rm` の別名で削除が実行されていなかった

- **指示:** Phase A Step 6 の `xargs -r rm -f`
- **実際:** `-f` を落とした `rm --` をシェルの `while` ループ内で使ったため、
  別名 `rm='rm -i'` が効いて対話プロンプトになった。さらに**回答をリストと同じ標準入力から
  読んだ**ため 1 行おきに食い違い、**1 件も削除されなかった。**
  `xargs -0 -r rm -f --` へ切り替えて解決した（`xargs` は別名を経由せず直接 exec する）。
- **重大さ:** 「消えたつもりで消えていない」状態は後続の照合を狂わせる。
- **分類:** **判断が必要だった**（自分で気付いて正した）

### D-4. `command rm` が `xargs` から起動できなかった

- **実際:** 別名の回避に `xargs -0 -r command rm -f` を試したところ
  `xargs: command: No such file or directory`（exit 127）。`command` はシェル組み込みであり
  `xargs` からは exec できない。`xargs -0 -r rm -f` へ修正した。
- **分類:** **判断が必要だった**

### D-5. 追跡欠損の検査が zsh の MULTIOS で壊れた

- **実際:** `git ls-files -z | xargs -0 -n500 ls -d 2>&1 >/dev/null | wc -l` が
  **7289**（追跡ファイル総数と同値）を返した。zsh の MULTIOS により
  `2>&1 >/dev/null` が「上書き」ではなく「両方へ複製」として働き、標準出力まで数えていた。
- **修正:** `git ls-files --deleted` / `--modified` / `git diff --stat HEAD` で測り直した
  （いずれも 0）。`git status --porcelain` が `??` 2 件のみである時点で結論は既に出ていた。
- **分類:** **環境差**（bash では再現しない）

### D-6. `anomalies.md` の 1 行差分を破棄した

- **指示:** SPEC の想定外規定「退避後も索引に差分がある → G2 停止」
- **実際:** `index.csv` と CSV 4 種は完全一致したが `anomalies.md` に 1 行差が残った。
  原因を調べたところ**正本側にあるホスト固有の空ディレクトリ**であり、
  このホストの退避不足ではなかった（§8）。利用者へ提示し、
  **差分を破棄して先へ進む**判断を得た。
- **分類:** **判断が必要だった**

### D-7. 空の親ディレクトリ 8 件を削除しなかった

- **指示:** SPEC は移動対象として 34 件の run ディレクトリのみを挙げる
- **実際:** 移動後に親 8 ディレクトリが空で残ったが、
  禁止事項「退避物を削除する（移動のみ）」に従い**削除していない**。
  索引には現れないため acceptance には影響しない。
- **分類:** **判断が必要だった**

### D-8. `${VAR}` の波括弧を補った

- **実際:** `git show "$H:runindex/index.csv"` が zsh の変数展開修飾子 `:r`（拡張子除去）に
  食われ、`e13fc4f...unindex/index.csv` という存在しない引数になった。
  `"${H}:runindex/..."` へ修正した。
- **注記:** 前 task でも同型の事故が `:t`（basename）で起きている。
  **`$VAR:` の直後の 1 文字が偶然修飾子名だと無言で書き換わる。**
- **分類:** **環境差**（bash では再現しない）

### D-9. `conventions_rev` を実測値へ置換した

- **指示:** SPEC Task 4 Step 2 が「実行者が実測して置換する。**これは逸脱ではなく手順である**」と明記
- **実際:** `spec.yaml` の `conventions_rev` を `1201f4f` → `d422b08` に更新した
- **分類:** 手順どおり（記録のため列挙）

---

## 11. 既存の起票の扱いについての所見

### PR #51 は、いま退避した 34 件を正本へ戻してしまう

実測で確認した。

| 項目 | 実測 |
|---|---|
| PR #51 | Draft / `exp/lecun-wip-20260703` → `phase0` / 現 phase0 比 76 ファイル |
| #51 が持つ `index.csv` | **784 行** |
| うち今回退避した run に由来する行 | **34 行** |
| 現在の phase0 の `index.csv` | **751 行**（同種の行 **0 件**） |

**PR #51 をそのまま統合すると、本 task で退避した 34 行が正本へ戻る。**
`tasks/README.md`「索引の正本」が定める「汚れた索引を記録しない」に正面から反する。

さらに #51 の head 分岐 `exp/lecun-wip-20260703` は、
新しい命名規約 `exp/<logical-host>`（日付と `wip` を含めない）にも合致していない。

**したがって #51 は、少なくとも `runindex/` と `context/auto/` を除いた形へ絞り込むか、
新しい分岐で起こし直す必要がある。** どちらを採るかは利用者の判断であり、
本 task では**触れていない**（禁止事項「統合する」「他ホストへ書き込む」に従う）。

#51 に含まれる価値ある変更（`make setup`、資格情報の失効検出、ホスト環境の既知差、
配線検証の run の証跡と記述）は、索引を除けば現在の phase0 と衝突しない見込みだが、
**実測していないため断定しない。**

### 本 task の起票

本 task は `feat/leftover-relocation` から `phase0` へ起票する。
**索引は記録していない。** 差分が無いことを確認するのが目的であり、
記録すると「lecun で生成した索引」を正本へ持ち込むことになるためである。

---

## 12. 未解決・申し送り

### 12-1. 正本にホスト固有の空ディレクトリの行が残っている

§8 のとおり。`runindex/anomalies.md` の `_smoke_proptest_20260804_223211` の行は、
正本を生成したホストにのみ存在する空ディレクトリに由来する。**空ディレクトリは
版管理が追跡しないため、「全 path が追跡下」の検査では検出できない。**
正本の清潔さを機械的に保証するには、群の一覧に載る 0 ファイルのディレクトリも
検査対象に含める必要がある。本 task では起票していない（`tools/` は範囲外）。

### 12-2. 空の親ディレクトリ 8 件の扱いが未決

索引には影響しないが、将来の監査で「なぜ空の `_smoke_prior_simplehead` があるのか」と
読まれる余地がある。削除するか、退避先へ移すかは未決。

### 12-3. 退避物の保管方針が未決

`~/m2-archive/20260811/` に 2.6 GB を保持している。版管理の外にあり、
共有ファイル同期の対象でもないため、**このホストが失われれば失われる。**
保管期限や別ホストへの複製の要否は決まっていない。

### 12-4. 他ホストの退避状況が未確認

本 task が扱ったのは lecun のみである。他ホストにも同種の退避物があるかは未測定。
`tasks/README.md` の「索引の正本」は「追跡外の run を持たないホストで行う」と定めるが、
**どのホストがその条件を満たすかの一覧は存在しない。**

---

## 13. 数値の出所

**すべての数値は本ホスト（lecun）での実測である。** 未測定の項目は無い。
先行調査の数値（34 件・分類の内訳）は参照したが、**すべて本 task で測り直して一致を確認した。**
断定できない事項（PR #51 の索引以外の部分が現 phase0 と衝突するか、他ホストの退避状況）は
**未実測と明記**しており、推測で補っていない。
