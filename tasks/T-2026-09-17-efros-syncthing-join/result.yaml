result_version: 3
task_id: "T-2026-09-17-efros-syncthing-join"
status: pass
host: "efros"
branch: "feat/efros-syncthing-join"
pr: 180
merged: false

gates:
  - id: G1
    verdict: pass
    note: "実行権 600 / 目印 0 件 / syncthing 0 / 中継 0 を両方向の対照つきで実測（正 zsh=5、負 zzz=0）。控えは ~/.syncthing-config-backup-20260917-155900（stat -c %d が双方 233 で同一ファイルシステム）。中心へ ssh -N で入り Authenticated to 192.168.196.150:50072 を得た。負の対照（口 50073）は Connection refused"
  - id: G2
    verdict: pass
    note: "autoUpgradeIntervalH 12→0、globalAnnounceEnabled true→false、relaysEnabled true→false、localAnnounceEnabled は true のまま。自分の登録名は実測で既に efros のため置換 0 件。中心を tcp://127.0.0.1:22001 で登録。count(/configuration/folder)=2、count(/configuration/defaults/folder)=1、count(//folder)=3。xmllint --noout exit 0、権限 600"
  - id: G3
    verdict: pass
    note: "keeper.sh sha256 9fe9c423…dd90、m2-sync.sh sha256 bcf46ba9…e25f が正本と一致。bash -n は両方 0（負の対照は 2）。~/.zshrc の起動行は既存 1 件のため追記せず（.zshrc の sha256 は開始時から不変）。.sync-pause を置き grep -c sync-pause ~/bin/m2-sync.sh = 2 を確認"
  - id: G4
    verdict: pass
    note: "目印 ~/.tunnel_to_philip（2 行、権限 600）。keeper 1 件（PID 53967、錠 ~/.keeper.lock）。中継 PID 53974 は keeper の子で引数に ubuntu@192.168.196.150 を含み 22001 の待ち受けは 2（負の対照 65533 は 0）。中継が立ってから chmod 700、sha256 は前後同一。syncthing 2 件（73191 → 73210 の親子）、版は自分 v2.1.3 / 中心 clientVersion v2.1.3。最上位フォルダ 2 のまま、autoUpgradeIntervalH 0 のまま、grep -ci upgrade = 0"

tests:
  before_failed: 0
  after_failed: 0
  after_passed: 0

deviations:
  - type: judgement
    note: "~/.zshrc へ起動行を追記しなかった。grep -c 'keeper.sh' ~/.zshrc が 1 で既存が在ったため（SPEC Task 3 Step 3 の指示どおり）。本ホストは起動行だけ先に配られ、指す先の脚本が無い状態であった"
  - type: judgement
    note: "conventions_rev を置換しなかった。実測 e7a51005 に対し契約の記載 e7a5100 は同じコミットの短縮形であり、validate_task.py の照合（git diff <rev>..HEAD -- context/conventions.md）に差分が出ないため置換が無意味である"
  - type: judgement
    note: "開始前からの未追跡 2 件を退避しなかった。分岐 feat/efros-syncthing-join が既に origin/phase0 と同じ位置に在り、git checkout -b が不要であったため。2 件には触れていない"
  - type: environment
    note: "ss / netstat / lsof / ip がいずれも本ホストに存在しない。待ち受けは /proc/net/tcp と /proc/net/tcp6 の st=0A を数えて判定した"
  - type: judgement
    note: "~/claude-sync/.stfolder を先回りして作らず、起動後の実挙動を測った。syncthing が起動時に自分で作り、フォルダは両方とも正常に動いた。新しいホストを入れるとき人が作る必要は無い"

issuer_defects:
  - type: asserted_without_measuring
    note: "SPEC は「起動時に設定は書き戻される。要約値は変わる。定義が消えていないことで確かめる」と断定する。指示どおり定義で確かめたが、実測では config.xml の sha256 も大きさも変わらなかった（c4e6c320…d63b5 / 11179 bytes のまま）。要約値の変化を起動の証拠に使う手順を書くと、この版では常に「起動していない」と誤判定する"
  - type: asserted_without_measuring
    note: "SPEC は中継について「周期は千八百秒。実測は四百十三〜千五百六十九秒」と書く。これは既に回っている keeper の次の周回を待つ場合の値である。本契約のように自分で keeper を起こす場合は最初の周回の先頭で張られ、実測は約 4 秒であった。指示どおりに数百秒待つ設計にすると、立っているものを待ち続ける"

positive_controls:
  - judgement: "プロセスの計数が実在するものだけを数える"
    breaking_input: "存在しない語 zzz_no_such_token で数える"
    observed: "最初の実装は 1 を返した。実体は自分の命令文を命令行に持つ自分の子シェルであった。shell-snapshots を除外して修正し 0 になった。正の対照 node は 12"
  - judgement: "待ち受けの判定が開いている口だけを拾う"
    breaking_input: "使われていない口 65533 を問う"
    observed: "0 を返した。正の対照である口 22（sshd）は 2 を返した"
  - judgement: "中心への到達の判定が届かない場合に落ちる"
    breaking_input: "開いていない口 50073 へ同じ鍵で入る"
    observed: "Connection refused となり Authenticated to の行は出なかった。正の口 50072 では出た"
  - judgement: "脚本の構文検査が誤りを捕まえる"
    breaking_input: "if true; then だけを書いた脚本を bash -n に掛ける"
    observed: "exit 2。配置した keeper.sh と m2-sync.sh は両方 exit 0"
  - judgement: "禁止語の検査が禁止語を検出する"
    breaking_input: "禁止語 2 語（画期的・大幅）を含む囮の markdown を検査する"
    observed: "2 件検出して exit 1。送出物 SPEC.md / audit.md / RESULT.md は 0 件で exit 0。囮は版管理の外に置き commit していない"
  - judgement: "中心がファイルを持っているかの判定が、持っていない場合を区別する"
    breaking_input: "同じ経路で存在しないファイル名 probe-zzz-nonexistent.txt を問う"
    observed: "HTTP 404。存在する probe-efros.txt は HTTP 200 で availability に中心の識別子が現れた。置いた直後の一度目は needItems=1 で「中心は持っていない」と正しく返した"
  - judgement: "秘匿の検査が資格情報の混入を検出する"
    breaking_input: "画面の合言葉をそのまま書いた囮のファイルを検査する"
    observed: "一致 1 件で exit 1。検査は値を出力せず長さ（32）と件数だけを出した。送出物 4 件では合計一致 0 件。囮は版管理の外に置き commit していない"

followups:
  - "repo フォルダ m2 の受け取りは起動から約 29 分で完了した（17:10:06 に state=idle / needBytes 0、du -sb は 68314468570 → 73028668690 で 4714200120 bytes が届いた）。中心への送り出しは同時点で完了率 75.66% / needBytes=11983979383 であり完了していない。完了を待たない指示のため打ち切った"
  - "~/claude-sync/ に記録の衝突ファイルが生まれた（sync-alerts.sync-conflict-20260917-074045-LW4CO4U.log 他）。正常な挙動であり両方残してある。整理するかどうかは起票者の判断である"
  - "本ホストの ~/.zshrc には keeper の起動行だけが先に配られていて、指す先の脚本が無い状態であった。他に同じ状態のホストが残っていないか確かめる価値がある"

unknowns:
  - "中心が本ホストの分を取り込み終える時刻。完了を待たない指示のため測っていない。17:10 時点で完了率 75.66% / needBytes=11983979383。本ホストの受け取り側は完了している"
  - "中心側から見た本ホストの登録状態。中心で命令を実行しない禁止事項のため、自ホストの経路からしか確かめていない"

commits: ["7ae0a73d"]
