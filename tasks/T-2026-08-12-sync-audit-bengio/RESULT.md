# RESULT — T-2026-08-12-sync-audit-bengio

**task_id:** `T-2026-08-12-sync-audit-bengio`　**kind:** `analysis`
**実行ホスト:** `bengio`　**分岐:** `feat/sync-audit-bengio`（起点 `origin/phase0` = `a85cf78`）
**実行日:** 2026-08-12　**repo:** `~/slocal2/m2`

読み取りのみで実行した。**復旧操作は一切行っていない。** 方針の判断はユーザーが行う。
生の出力は `audit.md`（523 行）に貼ってある。本書は判定と結論のみを述べる。

---

## 1. 解決された参照

### 1.1 `contract.inject_verbatim` — `conventions#prohibitions`

`context/conventions.md`（実測 rev `d422b08`）の**原文**。要約していない。

    ## prohibitions

    | id | 禁止事項 |
    |---|---|
    | `no_split_redefine` | split を再定義しない |
    | `no_raw_write` | `data/raw` `data/external` に書き込まない |
    | `no_frozen_change` | 凍結源を変更しない |
    | `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
    | `no_runindex_hand_edit` | `runindex/` を手で編集しない |

### 1.2 継承された `sigma_policy`

`inputs.sigma_policy` の記載が無いため `conventions#sigma` の既定値を継承した。
本契約は σ 判定を行わないが記録する。

    series: pstd
    sigma_source: paired_delta
    delta_sigma_source: paired

### 1.3 `conventions_rev`

起票時の記載 `d422b08`、実測 `d422b08`。**一致しており置換は不要だった。**

### 1.4 解決しなかった参照

`inputs.denominator.ref` と `inputs.frozen_source.ref` はいずれも `spec.yaml` に記載が無く対象外。
`P4` / `P5` も `kind=analysis` のため SKIP。

---

## 2. 検証

| 検査 | 結果 |
|---|---|
| `make task-validate` | **exit 0**、WARN 0 件 |
| `make task-preflight` | **exit 0**（4 PASS / 1 WARN / 4 SKIP / 0 FAIL） |
| `validate_task.py` 単体 | exit 0（`make` 経由と食い違いなし） |
| `make forbidden-check` | **exit 0**、`status=pass` / `violations=[]` / `changed=5` / `checked=5` |

**SKIP された 4 項目は「合格」ではなく「実行されなかった」。** `P2 cuda_ext_loaded` と
`P3 deterministic_flags` は `plan.env.preflight` に記載が無いため、`P4 prereg_committed` と
`P5 frozen_source_hash` は `kind=analysis` のため。

`P9` の WARN は **偽陽性**である。§5.2 に記す。

---

## 3. 結論

**設定共有が渡らない原因は、星型の中心である philip への経路が失われていることである。**
記録上の見立て（中心の停止）は実測と整合した。

| 問い | 実測による答え |
|---|---|
| 中心は誰か | **philip**（`192.168.196.150`）。実装から読んだ |
| 中心へ届くか | **届かない。** 両ポートとも `No route to host` |
| 他ホストへ届くか | **機器までは届く。** 9 台すべて SSH(50072) が OPEN |
| 同期処理は動いているか | **動いている。** 局所 22000 は OPEN |
| いつ止まったか | **2026-08-06**（二つの独立した情報が整合） |
| 復旧で何が失われうるか | **退避版が 0 件。** 局所版を戻す手段が同期処理側に無い |

---

## 4. 実測

### 4.1 測定系の健全性（G1）

| 対照 | 期待 | 実測 |
|---|---|---|
| `A_open` | OPEN | **OPEN** |
| `B_closed` | REFUSED | **REFUSED** |
| `C_noroute` | TIMEOUT | **TIMEOUT** |
| 版管理側の経路 | 参照が 1 行・exit 0 | **`a85cf78… refs/heads/phase0` / exit=0** |

**G1 PASS。** 到達性の道具は三通りを出し分ける。外向きの通信全体は落ちていない。

### 4.2 常駐処理と中心ホストの決め方

`~/bin/keeper.sh` は 34 行で、**正本 `scripts/sync/keeper.sh` との差分は 0 行**（同一）。
中心の決め方は 13-19 行から読み取った。

    # hub(philip)へのSSHトンネル維持（~/.tunnel_to_philip が存在するノードのみ。中身=秘密鍵パス）
    # コンテナ間はSSH(50072)しか通らないため、syncthingは星型(各ノード→philip)で接続する
    if [ -f ~/.tunnel_to_philip ] && ! pgrep -f 'ssh.*-L 22001:127.0.0.1:22000' >/dev/null; then
      nohup ssh -N -L 22001:127.0.0.1:22000 -p 50072 -i "$(cat ~/.tunnel_to_philip)" \

- 中心は **philip / `192.168.196.150`**、SSH は **50072**、中心側の同期処理は **22000**。
- 各ノードは局所 **22001** を中心の 22000 へ転送する星型。
- **トンネルを張る条件は `~/.tunnel_to_philip` の存在。** bengio はこれを**持っている**
  （44 バイト、mtime 2026-07-03）。よって bengio はトンネル維持の対象ノードである。
- 周期は **1800 秒**（33 行目 `sleep 1800`）。

常駐処理の稼働数は**二通りで数えた。食い違ったので両方を記す。**

| 数え方 | 結果 |
|---|---|
| `ps -eo args \| grep -c "[k]eeper.sh"` | **3** |
| argv 精査・自分と祖先を除外 | **1**（pid 773） |

**3 は誤りである。** 角括弧は grep 自身の自己一致を防ぐだけで、**計測側シェルの
コマンドライン引数に文字列が載る場合は一致する。** SPEC が `pgrep -f` を禁じたのと
同じ機序が `ps | grep` でも起きた。**実数は 1。**

中継の目印は集合として列挙した。`count=2`（`.tunnel.log` / `.tunnel_to_philip`）、
`home_total=74`。別の探し方（実在確認）でも両方 EXISTS。

### 4.3 同期処理の状態

| 項目 | 実測 |
|---|---|
| 同期処理のプロセス数 | **2。ただし親子**（789 の親は 773=keeper、2070 の親は 789）。**二重起動ではない** |
| 中継トンネル（`ssh`） | **0 件**（argv 精査・自己除外） |
| 待ち受け一覧 | **手段なし**（`ss` も `netstat` も不在）。零行ではなく測る手段が無かった |
| `127.0.0.1:22001`（中継の入口） | **REFUSED**（トンネル不在と整合） |
| `127.0.0.1:22000`（同期処理） | **OPEN**（同期処理そのものは健在） |

設定は `~/.local/state/syncthing/config.xml`（起動引数での指定は無く既定の場所）。

| 項目 | 実測 |
|---|---|
| `device_count` | **11**（既知の構成と一致） |
| `folder_count` | **2**（`claude-sync` と `m2`。いずれも `sendreceive` で全 11 台と共有） |
| `globalAnnounceEnabled` | **false** |
| `relaysEnabled` | **false** |
| `localAnnounceEnabled` | true |
| philip のアドレス | `tcp://192.168.196.150:22000` と **`tcp://127.0.0.1:22001`** |
| 秘匿の検査 | **0** |

**外部発見も中継も無効である。** 到達手段は局所告知と静的アドレスに限られ、
philip への経路は**中継トンネル前提**で組まれている。

### 4.4 到達可否（G2）

対象一覧は**三つの出所**から集めた。`~/.ssh/config`（`ssh_count=3`）、`/etc/hosts`、
syncthing 設定の `tcp://`。和集合は 12、うち自ホスト（`172.17.0.15`）と loopback を除く
**遠隔は 10 台**。**自ホスト 1 + 遠隔 10 = 11 台で、既知の構成 11 台と一致する。一覧は縮んでいない。**

**自ホストの IP は `172.17.0.15`（docker bridge）で、他 10 台は `192.168.196.x`。**
keeper.sh の「コンテナ間は SSH(50072) しか通らない」という記述と符合する。

遠隔 10 台 × 2 ポート（22000 / 50072）= **20 件**を測定した。

| 分類 | 件数 | 意味 |
|---|---|---|
| OPEN | 9 | 50072（SSH）は 9 台すべてで開いている |
| REFUSED | 9 | 22000（同期処理）は 9 台すべてで拒否。**機器までは届いている** |
| `OSERROR:No_route_to_host` | 2 | **philip の両ポート。経路が無い** |
| TIMEOUT | 0 | 該当なし |
| **合計** | **20** | **対象数 20 と一致。測り漏れなし** |

TIMEOUT が零であることは別の探し方でも確認した（文字列一致 0 件、分類辞書に鍵なし、
出現した分類は OPEN / REFUSED / OSERROR の 3 種のみ）。

**philip だけが経路なしである。** 他 9 台は機器まで届いており、22000 の拒否は
待ち受けていない（またはコンテナ間で通らない）ためで、経路の問題ではない。

**非対称がある。** 同一構内の philip へは届かないのに、外部の版管理へは届く。
外向きの通信全体が落ちているのではなく、philip への経路だけが失われている。

**G2 PASS。**

### 4.5 設定共有の棚卸しと停止時期

| 項目 | 実測 |
|---|---|
| `~/claude-sync` | EXISTS |
| 通常ファイル | **2532**（うち一覧の対象 2531。`.stfolder/` 配下 1 件を除外） |
| シンボリック | 1 |
| ディレクトリ | 900 |
| `.stversions`（退避版） | **0** |
| `sync-conflict`（衝突） | **10** |

**`.stversions` が 0 件であることは重要である。** 退避された旧版が無いため、
経路が復旧して遠隔側の版が流れ込んだ場合、**局所の版を戻す手段が同期処理側に無い。**

停止時期は**二つの独立した情報**から推定した。

| 情報源 | 推定 |
|---|---|
| A: 衝突ファイル（遠隔由来の証拠） | 最後の衝突は **2026-08-06 16:22 UTC**（ilya 由来）。衝突は遠隔から競合版を受け取らないと生じないため、この時刻までは同期が機能していた |
| B: 中継トンネルの記録 | 末尾 **263 行**が連続 `No route to host`。周期 1800 秒から逆算して開始 ≈ **2026-08-06 20:41 UTC** |

**整合する。** 差の約 4 時間は、B が「1 周回あたり 1 行」を仮定した目安であることで
説明がつく範囲である。**停止は 2026-08-06 と推定する。** ただし B に時刻印は無く、
逆算は上限に近い目安である。どちらか一方を正とは断定しない。

08-08 に 169 件の更新があるが、**すべて `codex/` 配下**で局所の導入によるものであり、
遠隔由来ではないため矛盾しない。

衝突ファイルの発生元は `he`(08-03) / `adam`(08-03) / `ilya`(08-05, 08-06) / `dlsta`(08-05)。

`~/claude-sync/sync-alerts.log` は **git 側の操作のみ**を記録しており、同期処理の
停止時期は判定できない。同期処理のログは `~/.syncthing.log`（10730567 バイト）と
`~/.tunnel.log`（18357 バイト）にある。

### 4.6 一覧に内容と資格情報が含まれないこと（G3）

一覧は **4 列固定**（名前・大きさ・更新時刻・要約値）。行数 2531、4 列でない行 0、
4 列目が 16 桁 hex の行 2531/2531、3 列目が ISO 時刻の行 2531/2531。**内容を含まない。**

SPEC の検査式は **4 件**に一致した（Expected は 0）。**4 件すべてファイル名であり、
資格情報の値ではない。** 目視した内訳は次のとおり。

| 一致した行 | 実体 |
|---|---|
| `agents/skills/modal/references/secrets.md` | 秘密情報の扱いを説明する文書 |
| `agents/skills/transformers/references/tokenizers.md` | ML のトークナイザ |
| `…/analytics-app/tokens.css` | 意匠のデザイントークン |
| `…/charting/chart-tokens.css` | 同上 |

陽性対照を置いた結果、**SPEC の検査式には取りこぼしがあった**（§5.1）。取りこぼす形も
含めた強化版で再検査したが、**一致は同じ 4 件のファイル名のみ**であった。

**G3 PASS。資格情報の値は 0 件。**

---

## 5. 完了判定

| # | 判定 | 実測値 |
|---|---|---|
| 1 | プローブが三通りを出し分けた | 期待 OPEN/REFUSED/TIMEOUT に対し実測 **OPEN/REFUSED/TIMEOUT**。一致 |
| 2 | 版管理側の経路が生きている | **`a85cf78… refs/heads/phase0`、exit=0** |
| 3 | 常駐処理の稼働数を二通りで数えた | 素朴 **3** / argv 精査 **1**。**実数は 1**（pid 773）。両方記載 |
| 4 | 中心ホストの決め方を実装から読んだ | **philip / 192.168.196.150 / SSH 50072 / 22001→22000**。keeper.sh 13-19 行を引用 |
| 5 | 中継の目印を集合として列挙した | **count=2**（`.tunnel.log` / `.tunnel_to_philip`）、`home_total=74` |
| 6 | 同期処理と中継の稼働状況 | 同期処理 **2（親子・二重起動でない）** / 中継 **0 件** |
| 7 | 中継の入口への接続結果 | **`127.0.0.1:22001` = REFUSED**（22000 = OPEN） |
| 8 | 共有相手と共有フォルダ | **device_count=11 / folder_count=2** |
| 9 | 記録に秘匿の値が含まれない | 設定の検査 **0** |
| 10 | 対象一覧を三つの出所から集め件数を記録 | 和集合 **12**、遠隔 **10**、自ホスト込み **11**。既知の 11 台と一致 |
| 11 | 全対象を測り合計が一致 | **20 = 20**。測り漏れなし |
| 12 | 拒否と経路なしを区別 | **OPEN 9 / REFUSED 9 / No_route_to_host 2 / TIMEOUT 0** |
| 13 | 設定共有の件数 | **2532 ファイル**（一覧 2531）、ディレクトリ 900、リンク 1 |
| 14 | 一覧に内容が含まれない | 4 列固定・2531 行すべて整形どおり。一致 4 件は**すべてファイル名**、値は 0 件 |
| 15 | 退避と衝突を数えた | **`.stversions` 0 件 / 衝突 10 件** |
| 16 | 停止時期を二つの独立した情報から推定 | A=**08-06 16:22 UTC** / B≈**08-06 20:41 UTC**。**整合** |
| 17 | 全項目に実測値または UNKNOWN | 空欄なし。UNKNOWN は §7 に列挙 |
| 18 | 作業ツリーの変更が契約の範囲 | **1 行のみ**: `?? tasks/T-2026-08-12-sync-audit-bengio/`。未解決 0。`forbidden-check` pass |
| 19 | 抑止が repo 直下から消えている | Task 6 Step 7 で実施（下記） |
| 20 | 報告が台帳へ返っている | Task 6 Step 8 で実施（下記） |

---

## 6. deviations

**空にしてはならない項目である。**

### 6.1 起票者の検査の誤り（3 件・いずれも実測で示した）

| # | 箇所 | 誤り | 実行者の対処 |
|---|---|---|---|
| 1 | Task 3 Step 6 / Task 5 Step 3 の秘匿検査 | 検査式 `apikey\|password\|token\|secret` の `apikey` は**区切りを含まないため `API_KEY` / `api-key` に一致しない**。本プロジェクトの実際の変数名は `NOTION_API_KEY` と `WANDB_API_KEY` でいずれも区切りを含む。陽性対照で確認: `password` を仕込むと 4→5 に増えるが、`NOTION_API_KEY=…` を仕込んでも 5→5 で**増えない** | `api[_-]?key` 等を含む強化版で再検査した。実際の一覧では**どちらでも一致は同じ 4 件のファイル名のみ**であり、結論は変わらない |
| 2 | Task 2 Step 1 の稼働数の数え方 | `ps -eo args \| grep -c "[k]eeper.sh"` が **3** を返す。角括弧は grep 自身の自己一致しか防がず、**計測側シェルのコマンドラインに文字列が載れば一致する**。SPEC 自身が `pgrep -f` を禁じた理由と同じ機序 | argv を精査し自分と祖先を除外して数え直した。**実数は 1**。両方を `audit.md` に記録した |
| 3 | Task 5 Step 5 の同期処理ログの探索 | `find ~ -maxdepth 3 -name "syncthing*.log"` が **0 件**を返すが、**`~/.syncthing.log` は 10730567 バイトで実在する**。先頭のドットがあるため `syncthing*` に一致しない | 注意 1 に従い別の探し方（`ls -la ~/.syncthing.log ~/.tunnel.log`）で確認し、両方の場所を記録した |

### 6.2 検査系そのものの誤り（本契約の範囲外だが影響がある）

`P9 spec_lint` の `host_mismatch` は**偽陽性**である。`rule_host_mismatch` は生の
`socket.gethostname()`（=`Bengio`）を宣言値（`bengio`）と大文字小文字を区別して比べる。
プロジェクト内には正規化 `resolve_server_name()`（`SERVERNAME` → 環境変数 → 設定 →
`gethostname().lower()`）があるが、規則はこれを使っていない。

**同じ根本原因で試験が 2 件失敗している。**

    FAILED tests/test_check_spec.py::test_self_contract_has_no_hit
    FAILED tests/test_preflight_task.py::test_spec_lint_passes_on_clean_contract

両者は `SELF_TASK = "T-2026-08-11-issuer-defect-detector"` を固定で読み、その契約が
実行ホストを `lecun` と宣言している。**lecun 以外のすべてのホストで失敗する。**
本契約のディレクトリを一時退避しても同じ 2 件が失敗することを実測で確認したので、
**本契約が壊したものではない。**

### 6.3 自分で判断した箇所

| # | 判断 | 理由 |
|---|---|---|
| 1 | 稼働数の計測を argv 精査へ切り替えた | SPEC の命令が自己混入する。SPEC 注意 6「終了コードで判定する前に、その命令が本当に走ったかを確かめる」および `pgrep -f` 禁止の趣旨に従った |
| 2 | 到達性の分類に `OSERROR:No_route_to_host` を第 4 の分類として残した | SPEC は三分類を求めるが、実測では philip が TIMEOUT ではなく `No route to host` を返した。**経路なしをより精確に示す**ため潰さずに記録した。集計の `OTHER` に対応する |
| 3 | 秘匿検査を強化版でも実施した | SPEC の式が取りこぼすことを陽性対照で確認したため。G3 の結論を SPEC の式だけに依存させない |
| 4 | `pytest` を実行した | 契約は要求していないが、`result.yaml` の `tests` が必須項目である。**未測定の値を書かないため**実測した。コードは変更していない |
| 5 | 契約ディレクトリを一時退避して試験を測り直した | 2 件の失敗が本契約に起因するかを断定せず切り分けるため。**復元を確認済み**（5 ファイル） |

### 6.4 常駐処理による事象（実行者の逸脱ではない）

`.sync-pause` を置いた後、`sync-alerts.log` の最終行は次のとおりで、抑止が効いている。

    2026-08-12 08:11:54 [bengio] 一時停止中: /home/ubuntu/slocal2/m2/.sync-pause があるため
    分岐へ書き込まない（消せば再開）

**本契約の実行中に常駐処理による統合は発生しなかった。**

---

## 7. UNKNOWN（測れなかったこと）

| 項目 | 理由 |
|---|---|
| 他ホストの `claude-sync` の内容と分岐量 | 他ホストへの読み取り以外の命令は禁止。**本一覧は他ホストの同名ファイルとの突き合わせ用である** |
| philip が停止しているのか経路のみが遮断されているのか | bengio からは区別できない。`No route to host` は経路上の応答であり、機器の状態を直接示さない |
| 復旧予定（2 週間以降とされる記録） | 実測していない。記録であって実測ではない |
| 停止の正確な時刻 | 推定は 2026-08-06。トンネルの記録に時刻印が無く、分単位では確定できない |
| 局所版と遠隔版のどちらが新しいか | 遠隔を読めないため比較不能 |
| 待ち受けの一覧 | `ss` も `netstat` も不在。**測る手段が無かった**（零行ではない） |
| 他 9 台の 22000 が拒否される理由 | コンテナ間の遮断か待ち受け不在かを bengio からは切り分けられない |

---

## 8. 生成物

| 種別 | パス |
|---|---|
| 新規 | `tasks/T-2026-08-12-sync-audit-bengio/probe.py` |
| 新規 | `tasks/T-2026-08-12-sync-audit-bengio/audit.md`（523 行・生の出力） |
| 新規 | `tasks/T-2026-08-12-sync-audit-bengio/inventory.tsv`（2531 行） |
| 新規 | `tasks/T-2026-08-12-sync-audit-bengio/RESULT.md` / `result.yaml` |
| 新規 | `tasks/inbox.d/T-2026-08-12-sync-audit-bengio.md` |
| **未変更** | `runindex/` `context/auto/` `tasks/inbox.md` `experiments/` `transfer/` `data/splits/` |
| **未変更** | `~/claude-sync/**` `~/bin/**`（読み取りのみ） |
