# RESULT — T-2026-08-12-sync-audit-andrew

**kind:** analysis  **status:** pass  **host:** `Andrew`  **branch:** `exp/andrew`
実測の生ログは `audit.md`、設定共有の棚卸しは `inventory.tsv`、到達性の道具は `probe.py`。

---

## 1. 解決された参照

| spec の記載 | 解決先 | 値 |
|---|---|---|
| `contract.conventions_rev` | `git log -1 --format=%h -- context/conventions.md` | **`d422b08`**（spec.yaml の記載と一致。置換不要） |
| `contract.inject_verbatim: [conventions#prohibitions]` | `context/conventions.md:98-107` | 下記に原文をそのまま置く |

`inputs.denominator.ref` / `inputs.sigma_policy` / `inputs.frozen_source.ref` は本契約に無い。

### conventions#prohibitions（原文・要約していない）

    <a id="prohibitions"></a>
    ## prohibitions

    | id | 禁止事項 |
    |---|---|
    | `no_split_redefine` | split を再定義しない |
    | `no_raw_write` | `data/raw` `data/external` に書き込まない |
    | `no_frozen_change` | 凍結源を変更しない |
    | `no_estimated_values` | 未測定の値を書かない。未測定は UNKNOWN |
    | `no_runindex_hand_edit` | `runindex/` を手で編集しない |

`spec.yaml` の `contract.prohibitions` に挙がる 5 件はいずれもこの表に存在する。

---

## 2. 結論

**設定共有が渡らない原因は、中心に据えた `philip` への経路が失われたことである。**
本ホストからの実測でその見立ては崩れなかった。ただし**「構内が落ちている」のではない。**

1. **中心 `philip`（`192.168.196.150`）だけが `No route to host`。** 他の 9 台へは SSH の口
   （`50072`）が **OPEN** で、機器は生きていて届く。外向き（`github.com`）も生きている。
   落ちているのは**単一ホストへの経路だけ**である。
2. **星型は設定だけでなく接続履歴でもそうだった。** `~/.syncthing.log` の
   「接続確立」21 件は **21 件すべてが `philip`**。他の 9 台へ直接つながった記録は 1 件も無い。
   したがって中心が落ちれば、他が生きていても**全対向が同時に切れる。**
3. **停止時刻は 2026-08-06 20:24:02（UTC）。** 二つの独立した情報が整合した（§4）。
4. **本ホストの分岐は 1 ファイルだけ。** 停止後に変わったのは `sync-alerts.log`
   （`m2-sync.sh` が 30 分ごとに追記する自ホスト由来の記録）のみ。
   **復旧によって失われる固有の成果は無い。**
5. ただし **`.stversions` が 0 件**＝版の退避が働いていない。衝突として残らない形で
   上書きされた場合、同期処理からは復元できない。既存の衝突ファイル 10 件がすべて
   `sync-alerts.*.log` であることから、復旧時にもこの 1 ファイルが衝突する見込みが高い。

**判断材料としての含み**（方針の提案ではない）: 他の 9 台が SSH で相互に届く以上、
中心を別ホストへ移す案も、各ノードが直接つなぐ案も、**経路としては成立しうる**。
同期処理の待受（`22000`）は外側の住所では全台 `REFUSED` であり、
現行の「SSH 転送を通す」方式が前提になる。**どれを採るかはユーザーが決める。**

---

## 3. 完了判定 16 項目（実測値。「実施した」ではなく「何が出たか」）

| # | 判定 | 実測値 |
|:--:|---|---|
| 1 | プローブが三通りを出し分けた | 期待 `OPEN`/`REFUSED`/`TIMEOUT` に対し実測 `OPEN`/`REFUSED`/`TIMEOUT`。**3/3 一致** |
| 2 | 版管理側の経路が生きている | `ls-remote` が `refs/heads/phase0` = `a85cf78` を 1 行返し `exit=0` |
| 3 | 常駐処理の稼働数を二通りで数えた | 方法1 `grep -c "[k]eeper.sh"` = **1**／方法2 `ps -eo pid,etime,args` = keeper **1**・syncthing **2**・`ssh -N` **0**。両者一致 |
| 4 | 中心ホストの決め方を実装から読んだ | `keeper.sh:13-19`。中心 = `philip`／`192.168.196.150`／SSH `50072`／`-L 22001:127.0.0.1:22000`。**定数で直書き**、動的解決ではない。条件は `~/.tunnel_to_philip` の存在のみ。稼働中の実体は repo 正本と**差分 0 行** |
| 5 | 中継の目印を集合として列挙した | `.tunnel.log` / `.tunnel_to_philip` の **2 件**（`home_total=60`） |
| 6 | 同期処理と中継の稼働状況を記録した | syncthing **2** プロセス（881 と 522602。ppid により **親子**と確定、二重起動ではない）／中継 `ssh -L` は **0 件** |
| 7 | 中継の入口への接続結果を記録した | `127.0.0.1:22001` = **REFUSED**（`22000` = `OPEN`、`8384` = `OPEN`） |
| 8 | 共有相手と共有フォルダを記録した | device **11**（既知の構成と一致）／folder **2**（`claude-sync`・`m2`）。両方とも 11 台全部と共有、`paused` は全件なし |
| 9 | 記録に秘匿の値が含まれない | `/tmp/stcfg.txt` の検査 = **0**。陽性対照 = 1、元 `config.xml` には該当 **25 行**が実在 |
| 10 | 対象一覧を三つの出所から集め件数を記録した | `~/.ssh/config` **2**／`/etc/hosts` **7 行**／同期設定 **11 アドレス** → 構内 **10 台** + 自ホスト = **11 台**。既知の構成 11 台と一致し、**縮んでいない** |
| 11 | 全対象を測り合計が一致した | 20 対象。`OPEN 9 + REFUSED 9 + TIMEOUT 0 + OTHER 2 = 20 = total`。**一致** |
| 12 | 拒否と経路なしを区別した | `REFUSED` **9**（届くが閉＝9 台の `:22000`）／`No route to host` **2**（経路なし＝`philip` の両ポート）／`TIMEOUT` **0** |
| 13 | 設定共有の件数を記録した | `EXISTS`。ファイル **2497**・symlink **1**・ディレクトリ **896**。一覧 **2496 行**（`.stfolder` 除く）。零ではない |
| 14 | 一覧に内容が含まれない | 列数の種類 = **1**（4 列のみ）。秘匿検査 **4 件**はすべて**ファイル名**（`tokenizers.md`/`secrets.md`/`tokens.css`/`chart-tokens.css`）で値ではない。ハッシュ失敗 **0** |
| 15 | 退避と衝突の痕跡を数えた | `.stversions` **0**（4 通りで一致）／衝突ファイル **10**（全て `sync-alerts.*.log`、08-03〜08-06、由来は he・adam・dlsta・ilya の 4 台） |
| 16 | 停止時期を二つの独立した情報から推定した | A 更新時刻分布 = `2026-08-06T17:05:14`／B 同期処理の記録 = `2026-08-06 20:24:02` に接続喪失。**整合**。第三の裏取りとして中継の連続失敗 **261 行** ≒ **261.6** 個の 30 分周期 |
| 17 | 16 項目すべてに実測値または UNKNOWN | 空欄なし（本表） |
| 18 | 作業ツリーの変更が契約の範囲に限られる | `git status --porcelain` = **1 行**（`?? tasks/T-2026-08-12-sync-audit-andrew/`）。`forbidden-check` は `violations: []` で `exit=0` |
| 19 | 抑止が repo 直下から消えている | Task 6 Step 7 で移動（末尾に結果を記す） |
| 20 | 報告が台帳へ返っている | Task 6 Step 8（末尾に終了コードを記す） |

### 測れなかったもの（UNKNOWN）

| 項目 | 理由 |
|---|---|
| 待ち受けポートの一覧 | `ss` も `netstat` も存在しない（`手段なし`）。**零行ではなく手段の不在**。代替として `22000`/`22001`/`8384` へ直接接続して確認した |
| 他ホスト側の分岐量 | 読み取り専用かつ他ホストで命令を実行しない契約のため測れない。**推測で埋めない** |
| `philip` が停止した理由・復旧見込み | 本ホストからは測れない |
| 9 台の `:22000` が `REFUSED` な理由 | 待受が外側の住所に出ていないことは確かだが、各機の内部設定は本ホストからは読めない |

---

## 4. 停止時期の推定（二つの独立した情報）

| 情報 | 出所 | 値 | 何を測っているか |
|---|---|---|---|
| A | `inventory.tsv` の更新時刻分布 | `2026-08-06T17:05:14` | 内容が最後に変わった時刻 |
| B | `~/.syncthing.log` | `2026-08-06 20:24:02` | 接続が最後に失われた時刻 |

**整合する。** A（17:05）が B（20:24）の 3.3 時間前にあり、その間は単に更新が無かったと
読める。順序が逆であれば矛盾するが、そうなっていない。

第三の裏取り: `~/.tunnel.log` の末尾は 261 行連続で `No route to host`。keeper は
`sleep 1800`（30 分）周期であり、B から本日の最終ループ（`2026-08-12 07:11:00`）までは
130.8 時間 = **261.6 周期**。実測の 261 行とほぼ一致する。
ただし `~/.tunnel.log` に時刻は無く、これは周期からの**導出**であって直接の測定ではない。

**接続の推移**（`~/.tunnel.log` の並び順）: 先頭 9 行が移行期
（`channel open failed: Connection refused` → `Timeout, server not responding` → `No route`）で、
10 行目以降は `No route` のみ。**相手の同期処理が落ちてから、機器そのものへ届かなくなった**
という順序が読める。

---

## 5. 陽性対照（判定が空振りでないことの確認）

| 判定 | 何を入れれば失敗するはずか | 実測 |
|---|---|---|
| 到達性プローブの三分類 | 開いた口・閉じた口・経路の無い先を与える | `OPEN`／`REFUSED`／`TIMEOUT` が期待どおり出し分けられた（3/3） |
| 設定抽出の秘匿検査 | 該当語を含む行を検査に通す | ダミー 1 行で **1** を返した。かつ元 `config.xml` には **25 行**該当が実在し、抽出物は **0**。「元から無いので 0」ではない |
| 一覧の秘匿検査 | 該当語を含む行を検査に通す | ダミー行 `x/token.md` で **1** を返した |
| プロセス走査（`ssh -L` = 0） | 実在するプロセス名を同じ走査にかける | 同じ走査で `keeper.sh` を **1** 件検出。走査は空振りしていない |
| 更新時刻の閾値走査（分岐 = 1 件） | 十分古い閾値を与える | 閾値 `2000-01-01` で **2496** 件＝総件数と一致 |
| 版管理側の経路 | 参照が返らなければ外向き全体の障害 | `refs/heads/phase0` が 1 行返り `exit=0` |

---

## 6. 起票者の誤り

**4 件あった。** 型は `check_does_not_check` / `asserted_without_measuring` /
`self_contradiction` / `shell_assumption` の 4 語に限る。

### 1. `check_does_not_check` — 中継の計数が検索命令自身に一致する

Task 3 Step 1 の `ps -eo args | grep -c "[s]sh .*-L"` を指示どおり実行すると **`1`** を返す。
該当行は実行中の対話シェルの包み（`zsh -c ... grep -c "[s]sh .*-L" ...`）そのものであり、
中継のプロセスではない。**指示どおり進めれば「中継は 1 本張られている」と結論し、
本契約の核心（中継が張られていない）を取り違える。** 申し送りは `pgrep -f` について
同型の誤りを警告しているが、Step 1 の命令自身が同じ欠陥を持つ。
是正: `ps` の出力を先にファイルへ保存し、別命令で走査した（実測 **0**、陽性対照 keeper=1）。

### 2. `check_does_not_check` — `find` のパターンが主たる証拠に一致しない

Task 5 Step 5 の `find ~ -maxdepth 3 -name "syncthing*.log"` は **0 件**を返す。
同期処理の記録は `~/.syncthing.log` で**先頭にドットが付く**ため、このパターンには一致しない。
`*syncthing*.log` にすると 1 件返る。**指示どおりなら「同期処理のログは別の場所に無い」と
結論し、停止時刻を直接示す唯一の時刻付き記録（39955 行）を見落とす。**
本契約の完了判定 16（停止時期を二つの独立した情報から推定する）は、この見落としのもとでは
達成できない。是正: パターンを広げて探索し、`~/.syncthing.log` と `~/.tunnel.log` を読んだ。

### 3. `self_contradiction` — 同一 Step 内で期待値と但し書きが両立しない

Task 5 Step 3 は `grep -c -i -E "apikey|password|token|secret|PRIVATE KEY"` に
**`Expected: 0`** を課しながら、同じ Step で「ファイル名に該当語がある場合はその行を残しつつ、
一覧が名前と要約値のみで内容を含まないことを明記する」と述べる。**該当があってよいなら
期待値 0 は成り立たない。** 実測は **4 件**で、すべてファイル名
（`tokenizers.md` / `secrets.md` / `tokens.css` / `chart-tokens.css`）である。
期待値だけを見て停止すると、秘匿の混入が無いにもかかわらず G3 で止まる。
是正: 但し書きに従い行を残し、列数の種類が 1（4 列のみ）であることを別途測って
内容が含まれないことを示した。

### 4. `check_does_not_check` — 到達不能な分岐に意味を与えている

Task 3 Step 2 は `(ss -ltn 2>/dev/null || netstat -ltn 2>/dev/null || echo "手段なし")` の
結果について「**零行なら**手段が無かったということ」と述べる。しかし末尾の `echo` があるため
**行数は必ず 1 以上になり、零行は原理的に発生しない。** 実測は 1 行 `手段なし` であった。
「零行」という判定条件は決して真にならないため、この記述に従って状態を切り分けることは
できない。是正: 行数ではなく内容（`手段なし`）で判定し、待受一覧を UNKNOWN として
代替の直接接続で補った。

---

## 7. 逸脱（deviations）

1. **judgement** — Task 3 Step 1 の中継計数が偽陽性（前項 1）だったため、指示された 1 命令を
   「`ps` の保存」と「保存物の走査」の 2 命令に分けて測り直した。値は `1` から `0` へ変わった。
2. **judgement** — Task 3 Step 2 で待受一覧が取得できなかった（`ss`・`netstat` とも不在）。
   UNKNOWN とし、代替として `127.0.0.1` の `22001`/`22000`/`8384` へ直接接続して補った。
   **起動もインストールもしていない。**
3. **environment** — 自分が書いた `awk` 命令で zsh のクォートが壊れ、awk が起動しないまま
   `件数=0` という**偽の零**を得た。申し送り 1・6 に従い別の書き方で測り直し、陽性対照
   （閾値 `2000-01-01` で総件数 2496）を付けて確認した。**偽の零を採用していない。**
4. **judgement** — Task 5 Step 5 の `find` が主たる記録に一致しない（前項 2）ため、
   パターンを広げて `~/.syncthing.log` を読み、停止時刻の主たる根拠とした。
5. **judgement** — 停止時期の第三の裏取りとして、SPEC が求めていない `~/.tunnel.log` の
   連続失敗行数と keeper の周期（`sleep 1800`）の突き合わせを行った。時刻を持たない記録
   からの**導出**であり、直接の測定ではないことを明記している。

### 常駐処理による統合（実行者の逸脱ではない・事実として記録）

`~/claude-sync/sync-alerts.log` の末尾より、本契約の開始前に自動統合が動いている。

    2026-08-12 07:11:00 [andrew] auto-merge: exp/andrew <- origin/phase0 (7 commits)
    2026-08-12 07:11:02 [andrew] auto-push: exp/andrew (7 commits)

抑止の目印 `.sync-pause` を置いたのは `07:28` であり、上記はそれ以前である。
置いた後の統合は記録に現れていない。稼働中の `~/bin/m2-sync.sh` が抑止に対応していることは
`grep -c sync-pause ~/bin/m2-sync.sh` = **2**（0 なら未対応）で確認した。

### 測定が対象に痕跡を残したこと

`~/.syncthing.log` の最終行は `2026-08-12 07:30:37 WRN Failed TLS handshake
(address=127.0.0.1:50504 error=EOF)` である。これは**本契約の到達性測定
（`127.0.0.1:22000` への接続）そのもの**であり、同期処理の異常ではない。
読み取りのみの契約だが、接続を試みる以上、相手側の記録には残る。

---

## 8. `P9 spec_lint` の該当（終了コードは変わらない）

    P9 spec_lint  WARN  host_mismatch@tasks/T-2026-08-12-sync-audit-andrew/SPEC.md:4

`tools/check_spec.py:296` の `rule_host_mismatch` が `socket.gethostname()` と本文の宣言を
比較する。実測 `Andrew`、宣言 `andrew`。**大文字小文字の差**であり、同一の機体を指す。
実行ホストの取り違えではない。

`SKIP` された項目（合格ではなく実行されなかった）: `P2 cuda_ext_loaded`、
`P3 deterministic_flags`（いずれも `plan.env.preflight` に記載なし）、
`P4 prereg_committed`、`P5 frozen_source_hash`（いずれも `kind=analysis` のため対象外）。

---

## 9. 生成物

| ファイル | 内容 |
|---|---|
| `probe.py` | 到達性プローブ（`OPEN`/`REFUSED`/`TIMEOUT`/`OSERROR:*` を出し分ける） |
| `audit.md` | Task 1〜6 の実測出力（要約せず貼付） |
| `inventory.tsv` | 設定共有 2496 件の一覧（名前・大きさ・更新時刻・要約値の 4 列のみ。**内容を含まない**） |
| `RESULT.md` | 本ファイル |
| `result.yaml` | 機械可読の対 |

`inventory.tsv` は**他ホストの同名ファイルと突き合わせるためのもの**である。
要約値は sha256 の先頭 16 文字。

---

## 10. 抑止の設置と解除（実測）

    07:28  touch .sync-pause                       設置
    07:41  常駐処理が記録: 「一時停止中: /home/ubuntu/slocal2/m2/.sync-pause があるため
                            分岐へ書き込まない（消せば再開）」
    07:28 以降の auto-merge / auto-push            0 件
    07:11 （設置前）                                auto-merge と auto-push が各 1 件

**抑止は実際に効いた。** 目印を置いただけで止まったことを、記録の側から確認している
（置いたのに止まっていなければ `auto-merge` が周期ごとに出るはずだが、出ていない）。
稼働中の `~/bin/m2-sync.sh` が抑止に対応していることは `grep -c sync-pause` = **2** で
事前に確認した（0 なら未対応）。

解除は削除ではなく移動で行った。

    mv .sync-pause /tmp/.sync-pause.released.T-2026-08-12-sync-audit-andrew   → released
    ls -la .sync-pause                                                        → repo 直下から消えた

**退避先: `/tmp/.sync-pause.released.T-2026-08-12-sync-audit-andrew`。**
repo 直下に未追跡ファイルを残していない。
