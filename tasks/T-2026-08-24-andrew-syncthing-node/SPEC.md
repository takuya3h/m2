# 二台目のノードを中心へ繋ぐ（andrew）

**task_id:** `T-2026-08-24-andrew-syncthing-node`  **kind:** `impl`
**depends_on:** `T-2026-08-24-bengio-syncthing-node`
**実行ホスト:** `andrew`  **repo:** `~/slocal2/m2`

## Goal

**一台目（bengio）が中心へ繋がり、双方向でファイルが届くことを実証した。**
本契約は同じ手順を二台目へ適用する。**残る二台（ilya / lecun）はこの後に続く。**

### 前契約で確定した事実

| 事実 | 実測 |
|---|---|
| 中心 | `philip`。識別子は `scripts/sync/device_ids/philip.txt`。**本文の転記を信用しない** |
| 中心の住所 | 中継の出口 **`tcp://127.0.0.1:22001`** |
| 版 | **`v2.1.3`**。実行ファイルの要約値 `e8a08fdd…` |
| 受け入れ一覧 | **登録済み**（`philip-accept-node-keys` で四台分を追記、五件・全行解析可） |
| 共有フォルダ | `claude-sync` と `m2`。ともに `sendreceive`。**識別子は中心と同じでなければならない** |
| **repo の大きさ** | **五十一ギガ**（同期対象は四十・七四ギガ）。初回は時間がかかる |
| 記録の衝突 | **上書きではなく衝突ファイルが生まれる。消えない。慌てない** |
| 起動の引き金 | 常駐処理が実行権だけを見て起こす。**周期は最大六十分** |

### 前契約のつまずき（本ホストでも起きる）

| # | 内容 |
|---|---|
| 1 | **既定値のひな型の節に `id=""` の folder がある。同期対象ではない。触らない** |
| 2 | **起動と同時に自動更新が走る。** 実行権を戻す前に `autoUpgradeIntervalH` を零にする |
| 3 | **設定に画面の鍵が実値で入っている。** 版管理へ置くなら伏せる |
| 4 | **正常時もプロセスは二件**（監視役と作業役）。親子関係で切り分ける |
| 5 | 起動の記録は **`~/.syncthing.log`** |
| 6 | 起動時に設定は書き戻される。**要約値は変わる。定義が消えていないことで確かめる** |

## 0. 前提と禁止事項

    cd ~/slocal2/m2
    touch .sync-pause
    grep -c "sync-pause" ~/bin/m2-sync.sh
    source .venv/bin/activate && source scripts/load_env.sh \
      && make task-start TASK=T-2026-08-24-andrew-syncthing-node

**二つ目が `2` なら効く。** 分岐が `feat/` で始まることを確かめる。**解除は最後の Task。**

| # | 禁止 |
|---|---|
| 1 | **中心の状態を変更する。中心で命令を実行する**（中継を張る接続は許す） |
| 2 | 他ホストへ接続する（中心を除く） |
| 3 | 鍵を生成・変更・削除する。受け入れ一覧を変更する |
| 4 | **`~/claude-sync/` の中身を消す・移動する** |
| 5 | **既定値のひな型の節を編集する** |
| 6 | **生成物を再生成する** |
| 7 | 未追跡の成果物を削除する |
| 8 | **秘匿の値を出力・記録する。** 公開鍵と指紋と識別子は秘匿ではない |
| 9 | 装置を使う |
| 10 | 未測定の値を書く（未測定は `UNKNOWN`） |
| 11 | `experiments/**` `transfer/**` `data/**` を変更・削除する |
| 12 | 常駐処理を停止・再起動する |

**禁止 1 の注記。** 中継は中心へ SSH で入る操作だが**状態は変えない。**
**到達の確認は `ssh -N`（命令を伴わない形）で行う**（前契約の指摘）。

### 環境の事実（再測定は不要）

対話シェルは zsh。**配列添字で終了コードを取れない。単語分割が起きない。**
`pgrep -f` も `ps` と `grep` の組も自己一致する。**`/proc/*/cmdline` を実行ファイル名で照合する。**
`ss` `netstat` `lsof` `ip` は無い。`/proc/net/tcp` から復号する。頁送りは無い。
`.venv/bin/python` は uv 管理の実体を指す。**`uv venv --clear` は六ギガを捨てる。使わない。**

### 起票者からの申し送り

| # | 注意 |
|---|---|
| 1 | **起票者が「確定」と書いた値も、実測と食い違えば実測を正とする** |
| 2 | **丸めた表示を実数として扱わない**（前契約で `8.0K` を実数と誤った。実体は四千三十一バイト） |
| 3 | **無い**ことと**読めない**ことを区別する |
| 4 | **終了コードを件数と呼ばない。** 数えるなら `grep -c` |
| 5 | 対照は両方向で取る。**片方向では「常に零を返す壊れ方」と区別できない** |
| 6 | **秘匿を守る検査が秘匿を出力してはならない。** 長さと有無で測る |
| 7 | **要素の階層を見ずに検索しない。** ひな型と実体を取り違える |
| 8 | 無変更は要約値で確かめる |

**前契約の `RESULT.md` と `audit.md` に実測がある。** 本 SPEC と食い違えば**そちらを正とする。**

`conventions_rev` は**実行者が実測して置換する。逸脱ではなく手順である。**

---

## Task 1 (Phase A): 開始状態を封印し、中心への到達を確かめる

**Files:** Create: `tasks/T-2026-08-24-andrew-syncthing-node/audit.md`

- [ ] **Step 1: 現状を要約値で記録する**

測るのは**設定・実行ファイル・常駐処理・除外規則の要約値と権限**、**中継の目印の件数**、
**共有領域の大きさと件数**（丸めない実数）、**稼働しているものの数**である。

**実行権が `644`、目印が零件、同期処理と中継が零件**であること。
**稼働の計数は両方向の対照を取る**（実在する語で一以上、存在しない語で零）。

- [ ] **Step 2: 控えを repo の外へ取る**

**設定一式を退避する。** 版管理内へも置く場合は**画面の鍵を伏せてから**置く。
**置く前に、秘密鍵の書き出しが混ざっていないかを確かめる。**

- [ ] **Step 3: 戻し方を記録する**

`audit.md` に**具体的な手順として**書く。**実行はしない。**
**実行権を落とせば起動が止まる**ことを含める。

- [ ] **Step 4: 中心の値を版管理から読み、到達を確かめる**

`scripts/sync/device_ids/philip.txt` と自分の識別子を読む。
**自分の値が現在の設定と一致すること。**

**中心へ鍵で入れることを確かめる。** 使う鍵は前契約で作った自分専用のものである。
**`ssh -N` を使い、中心で命令を実行しない。**
**受け入れの控えを隔離し、`~/.ssh/known_hosts` を汚さない。**

**入れなければ停止して報告する。** 中継が張れないので先へ進めない。

| # | 完了判定 |
|---|---|
| A | 開始状態を要約値で記録した（実行権 `644`、目印零件、同期処理と中継が零件） |
| B | 共有領域の大きさと件数を**実数で**記録した |
| C | 控えを repo の外へ取り、秘匿の混入が無いことを確かめた |
| D | 戻し方を記録した（実行していない） |
| E | 自分の識別子が設定と一致した |
| F | **中心へ入れた**（`ssh -N` で確認） |

---

## Task 2 (Phase B): 版を揃え、設定を組み立てる

**Files:** Modify: `~/bin/syncthing`, `~/.local/state/syncthing/config.xml`

**停止中に直接編集する。** 命令列は常駐を要するため使えない。

- [ ] **Step 1: 版を中心に揃える**

**中心は `v2.1.3`、要約値は `e8a08fdd…` である。** 本ホストが古ければ入れ替える。

**実行権は `644` のままにする。まだ起動させない。**
**置き換えた実行ファイルの要約値が中心と一致すること。**

- [ ] **Step 2: 自動更新を止める**

**これを最初に行う。** 要素名は版によって異なるので**実在する名前を確かめてから**変える。

- [ ] **Step 3: 告知と外部の中継を無効にする**

**公開の探索網と公開中継を無効にし、局所の告知は有効のまま**にする。
**既定はいずれも有効である。** そのまま起動すると外部へ出る。

- [ ] **Step 4: 中心を相手として登録する**

| 項目 | 値 |
|---|---|
| 識別子 | `scripts/sync/device_ids/philip.txt` の内容 |
| 名前 | `philip` |
| 住所 | **`tcp://127.0.0.1:22001`** |

**他のノードは登録しない。** 星型であり、ノード同士は直接繋がらない。

- [ ] **Step 5: 共有フォルダを二つ定義する**

| 識別子 | 位置 | 型 | 共有相手 |
|---|---|---|---|
| `claude-sync` | 本ホストの共有領域 | `sendreceive` | 自分と中心 |
| `m2` | 本ホストの repo | `sendreceive` | 自分と中心 |

**識別子は中心と同じでなければならない。** 違うと別のフォルダとして扱われる。

**既定値のひな型の節は触らない**（禁止 5）。同期対象ではない。

- [ ] **Step 6: 書式と定義を確かめる**

**解析できること。** **最上位の共有フォルダが二件**であること（**階層を見て数える**。
単純な検索はひな型の節も拾う）。相手の実体が二件、権限が変わっていないこと。

| # | 完了判定 |
|---|---|
| G | 実行ファイルの要約値が中心と一致し、権限が `644` |
| H | 自動更新を止めた（要素名と値） |
| I | 告知と外部の中継を無効にした |
| J | 中心を相手として登録した（識別子の出所、名前、住所） |
| K | **最上位の folder が二件**で、識別子が中心と同じ |
| L | 書式が解析でき、相手の実体が二件、権限が保たれている |

---

## Task 3 (Phase C): 中継を張り、起動する

**Files:** Create: `~/.tunnel_to_philip`。Modify: `~/bin/syncthing`（権限のみ）

**順序が要である。** 目印 → 中継 → 実行権 → 起動。

- [ ] **Step 1: 目印を置く**

**一行目に鍵の経路、二行目に中心の住所。** 権限は所有者だけが読める形にする。

- [ ] **Step 2: 中継が立つのを待ち、確かめる**

**常駐処理が張る。最大六十分かかる。**

**`22001` が待ち受けていること。** 中継の処理の引数に中心の住所が含まれること。
**立たなければ記録して報告する。実行権を戻さない。**

- [ ] **Step 3: 実行権を戻す**

**中継が立ってから行う。** 要約値が変わっていないこと。

- [ ] **Step 4: 起動を待ち、確かめる**

**常駐処理が起こす。** 起動したら次を確かめる。

| 確認 | 期待 |
|---|---|
| プロセス | **二件**（監視役と作業役）。親子関係で切り分ける |
| `22000` | 待ち受け（自分の同期処理） |
| `22001` | 待ち受けのまま（中継） |
| 版 | 中心と同じ |
| 定義 | **最上位の folder が二件のまま。自動更新が零のまま** |

**起動時に設定は書き戻される。要約値は変わる。** 定義で確かめる。

**記録から中心と繋がったことを示す行を探す。**

| # | 完了判定 |
|---|---|
| M | 目印を置いた（内容と権限） |
| N | **中継が立った**（`22001` と、引数に中心の住所） |
| O | 実行権を戻し、要約値が変わっていない |
| P | 同期処理が起動した（プロセスの数と親子関係、版） |
| Q | **定義が保たれ、自動更新が零のまま** |
| R | 中心と繋がった記録がある |

---

## Task 4 (Phase D): 実際に届くことを確かめる

**Files:** Modify: `tasks/T-2026-08-24-andrew-syncthing-node/audit.md`

**「繋がった」ではなく「届いた」を確かめる。**

- [ ] **Step 1: 自分から中心へ送る**

**共有領域に試験用のファイルを置く。** 内容に時刻と乱数を含め、要約値と大きさを控える。

- [ ] **Step 2: 中心が持っていることを確かめる**

**中心で命令を実行しない**（禁止 1）。**自ホストの画面の REST へ問い合わせる。**

**前契約はこの方法で確かめている。** 合言葉は変数へ読み込み、**画面へ出さない。**

| 問い | 見るもの |
|---|---|
| 中心は共有フォルダを全部持っているか | 完了率と不足のバイト数 |
| **自分の試験ファイルを中心が持っているか** | そのファイルを**どの相手から取れるか** |

**中心の識別子が現れれば、往復している。**

- [ ] **Step 3: 中心から届いたものを測る**

**共有領域の大きさと件数が開始時から増えているか。** 実数で測る。

**記録が衝突していれば、衝突ファイルが生まれる。** 上書きではない。**両方残る。**
**どちらが起きたかを記録する。**

- [ ] **Step 4: repo の同期の様子を測る**

**四十・七四ギガある。完了を待たない。** 進み方を記録して次へ渡す。

| # | 完了判定 |
|---|---|
| S | 試験ファイルを置き、要約値と大きさを記録した |
| T | **中心が持っていることを確かめた**（方法と実測値） |
| U | 共有領域が開始時から増えたかを実数で記録した |
| V | repo の同期の進み方を記録した |

---

## Task 5 (Phase E): 報告し、送出する

**Files:** Create: `tasks/T-2026-08-24-andrew-syncthing-node/RESULT.md`,
`result.yaml`, `tasks/inbox.d/T-2026-08-24-andrew-syncthing-node.md`

### 報告の書き方（**分量を抑える**）

**`RESULT.md` は起票者が判断に使う事実だけを書く。** 手続きの証跡は `audit.md` へ置く。

**次の構成にする。目安は百五十行以内。**

| 節 | 内容 | 入れないもの |
|---|---|---|
| 判定 | `verdict` と各 Gate の結果。一行ずつ | — |
| 完了判定 | 表。各項目に**実測値**（「実施した」ではなく「何が出たか」） | 命令の全文 |
| 実測 | **次の契約で使う値**の表 | 経過の説明 |
| 起票者の誤り | 型と内容。**一件三行以内** | 引用の全文 |
| 逸脱 | 何をなぜ | 手順の再掲 |
| 想定外・UNKNOWN | 測れなかったものと理由 | — |
| 送出 | PR の番号、検証の終了コード**だけ** | 出力の全文 |

**`audit.md` へ置くもの**: 命令とその出力、参照の解決の全文、検証の出力、
秘匿検査の出力、変更範囲の一覧、台帳の応答。

**`RESULT.md` から `audit.md` の該当箇所を行番号で指すこと。**
**同じ内容を二度書かない。**

- [ ] **Step 1: 報告を書く**

- [ ] **Step 2: 触っていないものが無変更であることを確かめる**

**常駐処理と受け入れ一覧と `~/.ssh/` の他が無変更であること。**
**目印が一件であること。**

- [ ] **Step 3: 検証を通す**

`conventions_rev` を実測して置換し、`task-validate` `task-preflight` `forbidden-check` を通す。
**`make` を含む命令には読み込みを同じ命令に含める。**
**生成物の検査が差分を報告しても再生成しない**（禁止 6）。

- [ ] **Step 4: 送信前に自分で秘匿を検査する**

**判定するのは件数ではなく形である。** **検査そのものが値を出力してはならない。**
**陽性対照を取り、囮は commit しない。**

- [ ] **Step 5: 送出し、報告し、抑止を外す**

**変更が契約のディレクトリと受け皿に限られること。**
commit・push・PR を作る。**既存の PR があれば新規作成せず本文を更新する。**

**台帳へ返す。** 抑止を外す（削除ではなく移動）。

| # | 完了判定 |
|---|---|
| W | 報告が指定の構成で、目安の分量に収まっている |
| X | 常駐処理・受け入れ一覧・目印が期待どおり |
| Y | 秘匿検査を自分で行った（**検査が値を出力していない**。陽性対照つき） |
| Z | 分岐が送出され PR が存在し、報告が台帳へ返り、抑止が外れている |

---

## 想定外が起きたときの扱い

| 事象 | 対応 |
|---|---|
| **中心へ入れない** | **停止して報告。** 目印を作らない |
| **中継が立たない** | **記録して報告。** `~/.tunnel.log` を読む。**実行権を戻さない** |
| **同期処理が起動しない** | 実行権と記録を確かめる。**六十分待っても起きなければ報告** |
| プロセスが三件以上 | **記録して報告。** 二件は正常 |
| **自動更新が走った** | **記録して報告。** Task 2 Step 2 が効いていない |
| **共有領域の中身が消えた** | **停止して報告** |
| 記録が衝突した | **正常である。両方残る。記録する** |
| repo の同期が終わらない | **待たない。** 進み方を記録して次へ渡す |
| 中心の状態が変わった | **停止して報告。** 本契約は中心を触らない |
| 前契約の実測と本 SPEC が食い違う | **前契約を正とし、食い違いを報告する** |

**言い訳をしない。事実と、測れなかったことを書く。**
