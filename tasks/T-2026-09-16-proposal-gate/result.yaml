result_version: 3
task_id: "T-2026-09-16-proposal-gate"
status: pass
host: "lecun"
branch: "feat/proposal-gate"
pr: null
merged: false

gates:
  - id: G1
    verdict: pass
    note: "開始前から在る未追跡 2 件を git stash（移動）で退避し作業ツリーを清浄にした。context/conventions.md のアンカーを実装の正規表現で数えて 8 件、tools/check_spec.py の RULES タプルの要素数を読んで 8 件と記録。P9 の自己申告『規則 8 件』と独立に一致した"
  - id: G2
    verdict: pass
    note: "禁止語は 0 語で 0 件・1 語で 1 件・2 語で 2 件。カードは見出し 14 件で 0 件・13 件で missing_heading 1 件・#5 の数字を消して missing_number 1 件。境界（未踏＋空白である理由の仮説）は 0 件、条件が無ければ 1 件。六通りすべて期待どおり"

tests:
  before_failed: 6
  after_failed: 6
  after_passed: 535

deviations:
  - type: judgement
    note: "contract.allow_write に context/conventions.md を足した。契約の Task B が同ファイルへの追記を命じており、allow_write はスキーマ上の正規キーで許可の上限（data/ のみ）にも触れないため、停止せず宣言を補った。結果 forbidden-check は permitted 1 件 / violations 0 件で通る"
  - type: judgement
    note: "開始前から在った未追跡 2 件（.sync-pause.released と前セッションの digest）を git stash push -u で退避した。禁止事項 6 のとおり消していない。戻すのは git stash pop"
  - type: judgement
    note: "逐語注入の原文 2 件を RESULT.md へ四字下げで貼った。原文の見出しが本書の節構造と混ざるのを避けるためで、下げ幅を除いた文字列は規約の当該節と一致する。要約はしていない"

issuer_defects:
  - type: self_contradiction
    note: "Task B は context/conventions.md への追記を命じるが、同ファイルは tools/check_forbidden.py の FORBIDDEN_FILES にあり、契約は contract.allow_write を宣言していない。指示どおり実行すると Task E-1 の make forbidden-check が status: fail / violations 1 件（禁止されたファイル context/conventions.md）で落ち、完了判定に到達できない"
  - type: check_does_not_check
    note: "SPEC 2 節の確定事実 2 は『変更履歴に行を足すと naming の解決結果が変わり、naming を注入する既存契約で L2-6 が WARN になる』と述べるが、_warn_conventions_rev（tools/validate_task.py:439）は inject_verbatim を読まず git diff の有無だけを見る。実測では変更履歴に行を足す前、proposal_gate 節を足しただけの時点で、naming を注入しない T-2026-08-11-issuer-defect-detector にも同じ WARN が出た"

positive_controls:
  - judgement: "完了判定 a: conventions#proposal_gate が L2 の解決で引ける"
    breaking_input: "存在しないアンカー名 conventions#proposal_gate_zz を inject_verbatim に置いて validate_l2 を呼ぶ"
    observed: "[L2-5] contract.inject_verbatim: アンカー proposal_gate_zz が存在しません が出て解決に失敗した。実在する proposal_gate と issuer_cautions では該当 0 件"
  - judgement: "完了判定 b: 禁止語の検査器が検出する"
    breaking_input: "禁止語を含まない本文を渡す。さらに二語含む本文を渡して 1 件で頭打ちにならないか見る"
    observed: "含まない本文は検出 0 件 exit 0。一語で 1 件 exit 1。二語で 2 件（画期的・決定的）。境界では未踏のみ 1 件、空白である理由の仮説の節を足すと 0 件"
  - judgement: "完了判定 c: カード充足の検査器が見出しを見る"
    breaking_input: "見出しを 14 件そろえた本文を渡す。さらに #5 の本文から数字だけを消す"
    observed: "14 件そろえば 0 件 exit 0。#7 を落とすと missing_heading 1 件。#5 の数字を消すと missing_number 1 件。#5 #6 #10 のそれぞれで同じ結果（parametrize で 3 通り）"
  - judgement: "完了判定 f: 追加した試験が働いている"
    breaking_input: "陽性対照 test_forbidden_one_word_gives_one の本文から禁止語「画期的」を消して「堅実」に替える"
    observed: "assert 0 == 1（1 failed / 25 passed）で落ちた。復元して 26 passed に戻ることも確かめた"
  - judgement: "完了判定 e: L2-6 が規約の変更に感応する、という起票者の説明"
    breaking_input: "naming を注入しない契約（prohibitions のみ）を検査する。起票者の説明が正しければ WARN は出ないはず"
    observed: "T-2026-08-11-issuer-defect-detector でも WARN [L2-6] が出た。説明と実装が食い違う。起票者の誤り 2 として記録した"
  - judgement: "検査器が規約から一覧を読めなかったときに全件合格しない"
    breaking_input: "section_text() が空文字を返すよう差し替えて、禁止語を含む本文を検査する"
    observed: "status: fail、errors に『context/conventions.md の proposal_gate 節を読めません』。禁止語 0 語で合格にはならなかった"

followups:
  - "docs/proposal-gate.md は docs/docs_audit.md に載っていないため make docs-check の対象外である。対象数は前後とも 42 で変わらない。登録するなら、文書が言及する docs/evidence/ が並行契約 T-2026-09-16-evidence-map-ab で実在するようになってからにする（いま登録すると不在の経路で落ちる）"
  - "context/conventions.md の変更履歴の表に a8c07e81（2026-08-25、issuer_cautions 節の追加）の行が欠けている。本契約は既存節の本文を変えない禁止を守るため足していない。補うなら別契約で"
  - "tools/check_proposal.py は引用の中の禁止語を区別しない。docs/proposal-gate.md 自身へ当てると過去の欠陥を引用した 13 行目を 1 件検出する。提案文書だけに使う前提であり、引用の除外が要るなら規則を足す判断が要る"
  - "context/auto/* と tasks/inbox.md は SPEC の禁止事項 3 により再生成していない。並行契約との統合後に一台で一度だけ make taskindex && make inbox を回すこと"

unknowns:
  - "make docs-check の通過は docs/proposal-gate.md について空振りである（対象が docs_audit.md 依存）。代わりに文書内の経路を手で確かめ、context/conventions.md と tools/check_spec.py は実在、docs/evidence/ は不在と実測した"

commits:
  - "6c95e4f1"
