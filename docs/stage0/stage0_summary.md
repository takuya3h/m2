# Stage 0 総括

契約 A（`T-2026-08-29-stage0-contract-a`）と契約 A の続き（`T-2026-08-29-k1-trace-policy-place`）
による実測の一覧。**数値はすべて実測か、理由つき UNKNOWN である。**

## 項目別の一行要約

| 項目 | 要約 | 出所の種別 | 成果物 |
|---|---|---|---|
| **A0** 入力の所在 | 15 動画の工程・術具・HTS、追加 6 動画、保存済み特徴、予測出力 1191 件がすべて実在 | リポジトリ内の実測 | [A0](A0_data_inventory.md) |
| **A1** 折りの材料 | 15 動画 / 23 クリップ / 工程 9 種 / 術具 15 クラス。総フレーム 17,233。**HTS 3 系統が全動画を被覆** | リポジトリ内の実測 | [A1](A1_fold_material.md) |
| **A2** 動画ID重複 | 追加 6 動画（17-22）と公式 test の重複 **0 件**。ID 空間が互いに素 | リポジトリ内の実測 | [A2](A2_id_overlap.md) |
| **A3** 術者・症例重複 | リポジトリ内には**属性情報が存在しない**（測れない）。**提供元回答により重複は無い**と判明し R4 の前提は解消 | 実測（不在）＋**利用者経由の提供元回答** | [A3](A3_attribute_overlap.md) |
| **A4** クリップID識別 | region-token **0.9712** / 在否 **0.6319**（偶然水準 0.0769）。**判定に変換していない** | リポジトリ内の実測 | [A4](A4_clipid_probe.md) |
| **A5** H(tool\|phase) 上界 | **6.3425 ビット**（底 2・平滑化なし）。工程で減る分 2.3145 ビット | リポジトリ内の実測 | [A5](A5_entropy_bound.md) |
| **A6** 検出誤りの分解 | 誤り 424 件を **分類 58.5% / 局在 29.7% / 見落とし 11.8%** へ。基準は既存実装の IoU 0.5 | リポジトリ内の実測 | [A6](A6_error_decomposition.md) |
| **A7** K1 出所照合 | **遡れない（確定）。** 索引四種＋版履歴・生ログ・外部記録・文書の四系統すべてで零件 | リポジトリ内の実測＋外部記録の読み取り照会 | [A7](A7_k1_provenance.md) |
| **A8** Cholec80 | **philip に存在しない。** 取得しない判断。L0 代理は未実施 | リポジトリ内の実測 | [A8](A8_cholec80.md) |
| **A9** 推論プロトコル | 文献値は**推論様式・境界の緩和・来歴**の三属性で割れる。比較表は三列を必須とする | **起票者の文献確認** | [A9](A9_inference_protocol.md) |

## 設計に効く三点

1. **K1 は出所不明として扱う。** 正規の再測定は送り手掃引で行う（A7）
2. **文献値の比較表は三属性の列を必須とする。** offline 系の数値を online の上限に使えない（A9）
3. **折りの設計に術者・症例は使えない**（属性情報がリポジトリに無い）。
   ただし追加動画との重複は提供元回答により無い（A3）

## 未了

| 事項 | 状態 |
|---|---|
| Cholec80 の取得 | **取得しない**と判断済み（2026-08-29）。L0 代理は未実施 |
| 契約 B | **未実施** |
| A9 の文献値の独立検証 | 起票者の文献確認であり、リポジトリ内で再現していない |
| 方針文書 v2 の配置 | **未配置。** 旧バンドルに本文の区画が無く、配置できなかった（下記） |

## 方針文書 v2 について

`T-2026-08-29-k1-trace-policy-place` の Task 1 で配置を試みたが、
**旧バンドルには二区画（`spec.yaml` と `SPEC.md`）しかなく、本文の区画が存在しなかった。**
取得物全体の要約値は宣言と一致しており改竄ではない。配置していない。詳細は同契約の報告。

**取り込みの拡張は完了した**ため、以後は `spec.yaml` の `inputs.bundle_extras` に宣言すれば
本文を機械配布できる（`tasks/README.md` の「追加ファイルを運ぶ」）。

## 追記（2026-08-29）— A7 K1 の行の更新

`T-2026-08-29-k1-verify-policy-place` で lecun の六 run を現物確認した。
**上の表の A7 の行を次のとおり読み替える**（既存の行は履歴として残す）。

| | 前契約時点（k1-trace） | 本契約後（k1-verify） |
|---|---|---|
| A7 の結論 | **遡れない（確定）** | **run が無い・読めない。** 六 run は 6/6 実在するが metrics を一件も持たない |
| 理由 | 四系統すべてで零件 | 「どこにも無い」のではなく**「run は在るが metrics が残っていない」** |
| 出所の種別 | リポジトリ内の実測＋外部記録の読み取り照会 | 左に **lecun 上の現物の実測**が加わった |

**変わった点。**

1. **六 run の所在が確定した。** `experiments/_orphan_no_metrics/transfer/` に 6/6 実在する。
   隔離前の md5 記録と 6/6 一致。遡れなかった理由は run 名ではなく **metrics の欠落**である。
2. **`0.686` は版管理された一次ログへ遡れた。** bbox AP 68.5960
   （`evidence/aligndetr_s0frozen_incident_20260703/aligndetr_s0frozen_v2_train_log.txt:1670`）。
3. **分母 0.8986 は索引の 3 run の 3-seed 平均として再現した。** ただし同条件の 3 件組
   26,235 通りのうち 277 通りが同じ 4 桁表示になるため、**一意には特定できない。**
4. **記録の三値は内部で整合している**（paired 差の平均・ばらつき・平均どうしの引き算）。
   **整合は記録の中だけで、run の実測とは結び付いていない。**

**変わらない点。** K1 は依然として実測扱いにできない。正規の再測定は送り手掃引で行う。

**新たに分かった回復経路。** 六つの checkpoint は健在である（各 2,599,650 バイト、md5 照合済み）。
**再評価すれば数値は回復しうる。** 本契約は GPU 使用が禁止のため実施していない。判断待ち。

## 追記（2026-08-29 その 2）— A7 K1 の行を再度更新（実測復帰）

`T-2026-08-29-k1-reeval-and-harvest` で六 checkpoint を再評価した。
**上の表と直前の追記の A7 の行を、次のとおり読み替える**（既存の行は履歴として残す）。

| | k1-trace | k1-verify | **k1-reeval（本契約）** |
|---|---|---|---|
| A7 の結論 | 遡れない（確定） | run が無い・読めない | 🔴 **一致（実測復帰）** |
| 根拠 | 四系統すべてで零件 | 六 run に metrics.json が無い | **checkpoint の `val` に当時の値が残っており、再評価とビット一致し、記録値とも一致した** |

**直前の追記の「変わらない点: K1 は依然として実測扱いにできない」は撤回する。**

### 何が誤っていたか

**「metrics が失われた」は誤りだった。** 失われていたのは `metrics.json` という成果物
ファイルであって、数値そのものは `checkpoints/best_tecno.pth` の `val` キーに入っていた。
前契約は `metrics.json` の不在だけを見て結論し、**checkpoint を開いていなかった。**

**教訓: 成果物ファイルの不在を、数値の不在と同一視しない。**

### 確定した数値

| 量 | 実測 | 記録 |
|---|---|---|
| relationdetr 平均 acc（3-seed） | 0.9476347635 | 0.9476 |
| aligndetr 平均 acc（3-seed） | 0.8785478548 | 0.8785 |
| relationdetr hemostasis F1 平均 | 0.8009595544 | 0.8010 |
| aligndetr hemostasis F1 平均 | 0.1788488701 | 0.1788 |
| paired 差の平均 | −0.0690869087 | −0.06909 |

九量すべて記録の丸め幅の内側。seed 別 paired 差も三件とも一致した。

### 索引の状態

本契約で追跡外 61 run を収穫し、索引は **1177 → 1238 行**になった。
`b2a_lovo` 30・`b2a_seglovo` 30・`b2a_det2phase_oracletool_009` 1 件である。
**六 run 自身は索引に入っていない**（`metrics.json` を持たないため収穫器の走査対象外）。

### 残る判断待ち

1. 六 run を索引へ載せるか。載せるには `metrics.json` の生成が要るが、それは
   当時の生成物ではないため本契約では禁止されている。**別途の規約が要る。**
2. 六 run の学習時刻・学習ホスト・ハイパーパラメータの記録は依然として無い。
