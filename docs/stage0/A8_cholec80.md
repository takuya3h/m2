# A8 Cholec80 の実体確認

## 結論

**philip に Cholec80 は存在しない。** L0 代理は実施していない（データが無いため）。

## 実測

| 方法 | 結果 |
|---|---|
| `find / -maxdepth 6 -iname '*cholec80*'`（`/proc` 除く） | **0 件** |
| `data/external/` の中身 | `cholect45/`（`.gitkeep` のみ **1 ファイル**）、`egoexor/`（1）、`phakir/`（1）、`weights/`（35） |
| `data/external/cholect45/` の実体 | `.gitkeep` のみ。**空の scaffold であり Cholec80 でもない**（CholecT45 は別データセット） |

### 空振り確認（陰性対照）

同じ探索を存在しない語 `*zzznosuchdataset*` で行うと **0 件**。
一方 `data/external/weights/` は 35 ファイルを返す。→ **探索は働いている。**

## 取得について

**外部からの取得は行っていない。** SPEC 禁止 3「外部データの取得・外部サービスへの問い合わせ」による。
**他ホストの実測も行っていない**（SPEC の指示による）。

利用者の判断（2026-08-29）: **取得しない。A8 は実体確認のみで閉じる。**

## L0 代理

**未実施。** 入力データが無いため。SPEC の「無い場合: 実体確認の結果だけを報告し、
取得の可否を判断へ回す」に従った。
