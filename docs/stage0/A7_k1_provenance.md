# A7 K1 の出所照合

## 結論（三値のうち）

**「実在の run に遡れない」。**

台帳の K1 の数値（Δ_phase = **-0.0201**、hemostasis F1 が **0.801 → 0.179**）は、
**索引の三つの csv のいずれにも、丸め幅の許容差では存在しない。**

## 照合の規約

| 項目 | 値 |
|---|---|
| Δ_phase の目標 | -0.0201（表示 4 桁） |
| 許容差 | **±5e-5**（表示 4 桁の丸め幅。丸め表示を実数として扱わないため） |
| F1 の目標 | 0.801 / 0.179（表示 3 桁） |
| 許容差 | **±5e-4**（同じ理由） |
| 走査対象 | `runindex/experiments.csv`（213 行）・`runindex/index.csv`（1177 行）・`runindex/verdicts.csv`（1038 行）・`runindex/per_class.csv`（8370 行） |

## 照合器の対照（空振りでないことの確認）

| 対照 | 入力 | 結果 |
|---|---|---|
| **陽性** | 索引に確実に存在する既知の値 `0.8973014948553679`（分母の accuracy_mean） | **1 件一致** — `phase1/s4_phase_baseline/frozen_tecno_phase_baseline@val~relation_detr_seed42` の `accuracy_mean` |
| **陰性** | 最終桁を一つ変えた `-0.0202` | **0 件** |
| **陰性** | 最終桁を一つ変えた `-0.0200` | **0 件** |

**照合器は働いている。** 下の 0 件は「検出できていない」ではない。

## 候補系統ごとの結果

### 系統1: 無効判定された 010-012 の aligndetr seed 群

| 項目 | 実測 |
|---|---|
| 行 | `phase1/s4_phase_baseline/frozen_tecno_phase_baseline_aligndetr@val~aligndetr...` |
| n_runs / n_runs_excluded | **3 / 3（全件が除外扱い）** |
| accuracy_mean | 0.8464246424642464 |
| 分母との差 | **Δ = -0.050877** |
| 台帳の -0.0201 との差 | **0.030777**（許容差 ±5e-5 の 600 倍以上） |
| hemostasis F1（per_class・3 seed） | 0.0 / **0.16666…** / 0.0（いずれも `excluded=True`） |

**0.801 にも 0.179 にも一致しない。Δ も一致しない。**

### 系統2: 2026-07-10 以降の作り直し版

索引に `aligndetr` を含む experiments 行は **2 件のみ**（`baselines/s0/aligndetr_bbox@val` と
上記の phase 行）。**作り直し版として区別できる別行は索引に存在しない。**
`baselines/s0/aligndetr_bbox@val` は `accuracy_mean` が空である。

### 系統3: それ以外の一致

`per_class.csv` を全走査したところ、**`hemostasis` の F1 で 0.179 に一致する行が 1 件**あった。

| 項目 | 実測 |
|---|---|
| ledger_key | `transfer__b2a_ro_oracle_noise000_009_b2a_ro_oracle_noise000_seed456` |
| group / step | `transfer` / `b2a_ro_oracle_noise000` |
| seed / split / host | 456 / val / lecun |
| excluded | **False** |
| value | 0.1794871794871795 |

**これは AlignDETR 凍結源とは無関係の、ノイズ注入実験（`b2a_ro_oracle_noise000`）の 1 run である。**
同じ step の hemostasis F1 は seed ごとに 0.827 / 0.637 / 0.179 / 0.0 とばらついており、
**`0.801 → 0.179` という対を構成しない。** 偶然の一致である。

`0.801` に一致した 9 件はいずれも別クラス（Suction Cannula / incision / Mouth Gag / Raspatory）で、
**hemostasis ではない。**

## AlignDETR の検出性能 0.686 に対応する値

`baselines/s0/aligndetr_bbox@val` の `accuracy_mean` は**空**であり、
検出性能を示す値がこの行に入っていない。**0.686 の所在は索引からは特定できない。**

## K1 を実測扱いにできるか

**できない。** 台帳の数値は、許容差を明示した照合では**索引のどの run にも遡れない**。
唯一の数値一致（hemostasis 0.179）は別系統の実験の偶然の一致であり、
Δ_phase も対になる 0.801 も伴わない。

**複数系統が同じ数値を持つ状況ではないため、escalate の条件には当たらない。**

---

## 追記（2026-08-29）— 索引以外の四系統による再捜索

**出所の種別: リポジトリ内の実測（系統 1・2・4）と外部の実験記録の読み取り照会（系統 3）。**

上の本文は**索引だけ**を対象にした照合である。「一致零件は探し方を疑う」の規約に照らし、
索引に載らない場所を四系統で捜索した。

### 検索器の対照

| 対照 | 入力 | 結果 |
|---|---|---|
| 陽性 | `0.050877`（前契約で実在が確定した値） | **4 commit** |
| 陰性 | `0.3971428571`（乱数） | **0 commit** |
| 陰性 | `-0.8264193`（乱数） | **0 commit** |

### 試した表現の揺れ

`-0.0201` `0.0201` `2.01` `2.01pt` `-2.01` `0.801` `0.179` `0.686` `68.6` `80.1` `17.9`、
複合形 `0.801→0.179`（矢印なし）と `0.801 → 0.179`（空白つき）。

**試していない揺れ**: 有効数字を増やした形（`-0.02010` 等）、指数表記、
百分率での小数点以下 2 桁（`2.010%` 等）。これらは「探した」に数えない。

### 四系統の結果

| 系統 | 方法 | 範囲 | 件数 |
|---|---|---|---|
| 1 版管理の全履歴 | `git log --all -S` で内容変化を追い、初出 commit を特定 | 全 commit・全ファイル | 単独値は多数出るが**K1 の文脈での対は 1 件**（初出 `909dd193` 2026-08-02） |
| 2 生ログの直接走査 | 索引を経由せず `metrics.json` / `per_class_ap.json` を直接読む | `s4_phase_baseline_01{0,1,2}_frozen_tecno_phase_baseline*`（aligndetr 3 + 分母 3） | **一致 0 件** |
| 3 外部の実験記録 | 設定済み資格情報で**読み取りのみ**照会 | run 479 件の summary 全数値 | `0.686` 近傍 **3 件**（すべて `grasp_inference` の別実験）。`-0.0201` `0.801` `0.179` は **0 件** |
| 4 リポジトリ内の文書 | 版管理下の現在版で `0.801` と `0.179` の共起を走査 | 全文書 | **11 件**。うち**出所を主張する文書は 0 件** |

### 系統 1 の初出を一段遡った結果

初出は `experiments/analysis/frozen_source_signature3_R_index/REPORT.md:52`（commit `909dd193`）。
その本文は次のとおり。

> **hemostasis F1「0.801→0.179」の downstream 数値の直接検証**: … は
> checkpoints/logs/predictions/visualizations のみで metrics.json が空（efros で実行された空 scaffold）。
> **台帳記載値のみで、per-phase F1 の生数値はこのホストでは再現できない**

🔴 **初出の時点で既に「台帳記載値のみ」と書かれている。**
この文書は数値の出所ではなく、**出所が無いことを報告している文書**である。典拠の連鎖はここで止まる。

### 系統 2 で判明した二つの事実

**(1) 「metrics.json が空」は philip では成り立たない。** 実体がある（918〜920 バイト）。
上の文書は別ホスト（andrew）の視点で書かれている。

**(2) config が宣言する凍結条件が、名前と食い違う。**

    aligndetr 群 3 件とも  test_cfg.backbone = relation_detr_resnet50_frozen_seed42
    分母側     3 件とも  test_cfg.backbone = relation_detr_resnet50_frozen_seed42

**名前は `aligndetr` だが、中身は Relation-DETR の凍結源である。**
2026-07-03 の「AlignDETR 学習失敗 → 通常学習の ckpt で代替」の実体であり、
索引が `n_runs_excluded=3` として無効判定した根拠と整合する。

生ログから直接計算した値（**索引を経由しない**）:

| 指標 | 分母（relation_detr） | aligndetr | 差 |
|---|---|---|---|
| accuracy 平均 | 0.898350 | 0.846425 | **Δ_phase = −0.051925** |
| hemostasis F1 | 0.3478 / 0.3333 / 0.1250 | 0.1667 / 0.0 / 0.0 | — |

**台帳の −0.0201 との差は 0.031825。0.801 にも 0.179 にも一致しない。**

## 結論（二値）

**遡れない（確定）。**

四系統すべてで、K1 の三値を同時に説明する出所は見つからなかった。
**以後 K1 は出所不明として扱い、正規の再測定は送り手掃引で行う。**

互いに矛盾する複数の出所へは遡っていないため、escalate の条件には当たらない。

---

## 追記（2026-08-29 その 2）— lecun での六 run の現物確認

**出所の種別を先に分ける。** 本節の記述は三種類の出所から成る。

| 印 | 出所 | 意味 |
|---|---|---|
| **［実測］** | lecun 上の現物 | 本契約で実際に測った値・ファイル・出力 |
| **［記録］** | 記録側の意思決定記録（2026-07-10 付） | 起票者が読んだ記載。lecun では検証できない |
| **［前契約］** | `T-2026-08-29-k1-trace-policy-place` の実測 | 上の「追記（2026-08-29）」の結果 |

起票者は記録側から出所を特定した。［記録］によれば K1 の三値は lecun 上の六 run
（`t1a_frozen_src_{relationdetr,aligndetr}` の各三 seed）の 3-seed 集計値である。
本節はこの推定を lecun で現物確認した結果である。

### 結論（三値のうち）

🔴 **「run が無い・読めない」。**

**六 run はすべて実在する。しかし metrics を一件も持たないため、
記録値を再計算で照合することができない。** 一致とも不一致とも判定できない。

### 何がどこまで在ったか［実測］

| 事項 | 実測 |
|---|---|
| 六 run の実在 | **6/6 実在。** ただし置き場は `experiments/transfer/` ではなく `experiments/_orphan_no_metrics/transfer/` |
| 各 run が持つファイル | **`checkpoints/best_tecno.pth` 一つだけ**（六 run 合計 6 ファイル） |
| `metrics.json` | **0/6** |
| `config.yaml` `command.sh` `git_commit.txt` `notes.md` | **0/6** |
| `predictions/` `visualizations/` | 存在するが**すべて空** |
| 学習ログ | 生成器は `/tmp/gpu_waiter_logs/` へ出力。**当該ディレクトリは存在しない** |
| 索引への登録 | `index/experiments/verdicts/per_class` の四種すべてで **0 件** |

**六 run が当の六 run であることは確かめた。** 版管理された
`experiments/analysis/t1a_diag_2026-07-29/csv/t2_ckpt_inventory.csv` が隔離前の経路での
md5 を持っており、現物と **6/6 一致**した（六つの md5 は互いに異なるため複製ではない）。

### なぜ前契約で遡れなかったのか［実測］

`.gitignore:157-162` が理由を記録していた。

> 孤児 run 退避 (#05-lecun): metrics.json/config.yaml/command.sh/git_commit.txt/
> notes.md がすべて無く checkpoints のみ・失敗ログも無い 9 run を退避。
> (transfer/t1a_frozen_src ×6, phase1/s4_phase_baseline_*_aligndetr_010-012 ×3)

**metrics の欠落は本契約で生じたのではなく、隔離の時点で既に確認されていた。**
`experiments/_orphan_no_metrics/` は版管理から除外されており、収穫器も
`metrics.json` を持たないため走査しない（`tools/harvest_runindex.py` の `EXCLUSION_ALLOWLIST`）。
**「run 名が t1a_frozen_src 系だから遡れなかった」のではない。metrics が存在しないためである。**

### 出所の連鎖

| 段 | 出所 | 内容 | 状態 |
|---|---|---|---|
| 1 | リポジトリ内の初出（2026-08-02 の分析報告、`909dd193`） | hemostasis F1 0.801 → 0.179 | ［前契約］**数値の出所ではない。**「台帳記載値のみで再現できない」と自ら書いている |
| 2 | 記録側の意思決定記録（2026-07-10 付） | 三値と seed 別 paired 差 | ［記録］起票者が読んだ。**seed 別で残る唯一の量はここにある** |
| 3 | lecun の六 run | 3-seed 集計の元データ | ［実測］**実在するが metrics が無い。連鎖はここで切れる** |

**段 2 から段 3 への接続が取れない。** 段 3 に数値が無いためである。

### 再計算の結果［実測］

SPEC が求めた四つの再計算のうち、run の metrics を要する三つは実行できない。
実行できたのは、記録側に seed 別で残る paired 差 3 件と、平均どうしの引き算だけである。
**これは記録の内部整合の検査であって、run への遡及ではない。**

許容差は入力（丸めた表示）の丸め幅を出力へ伝播させて取った。伝播は端点の総当たりで求めた。

| 量 | 再計算 | 伝播区間 | 記録 | 判定 |
|---|---|---|---|---|
| mean(paired 3 件) | −0.069067 | [−0.069117, −0.069017] | −0.06909 | 一致 |
| pstdev(paired) ddof=0 | +0.009480 | [+0.009434, +0.009526] | 0.00949 | 一致 |
| stdev(paired) ddof=1 | +0.011610 | [+0.011555, +0.011666] | 0.00949 | **不一致** |
| acc_rel − 分母 | +0.049000 | [+0.048900, +0.049100] | +0.0490 | 一致 |
| acc_ali − 分母 | −0.020100 | [−0.020200, −0.020000] | −0.0201 | 一致 |
| acc_ali − acc_rel | −0.069100 | [−0.069200, −0.069000] | −0.06909 | 一致 |

**記録の三値は互いに整合している。** ばらつきの系統は `ddof=0`（pstd）で、
`context/conventions.md#sigma` の既定 `series: pstd` と一致する。
**ただし整合しているのは記録の中だけであり、run の実測とは結び付いていない。**

陰性対照（seed を一つ除く三通り、および値を一つ差し替える一通り）は**四通りとも落ちた**。
陽性が通り陰性が落ちるため、検算器は働いている。

### 分母 0.8986 について［実測］

**記録値のままにしない。lecun 上で再現した。**

    s4_phase_baseline_001_frozen_tecno_phase_baseline_seed42   0.9023102310231023
    s4_phase_baseline_002_frozen_tecno_phase_baseline_seed456  0.8957095709570957
    s4_phase_baseline_003_frozen_tecno_phase_baseline_seed123  0.8976897689768977
    3-seed 平均 = 0.8985698569856986 -> 4 桁表示 0.8986

🔴 **ただし一意ではない。** 同じ description の 55 行から作れる 3 件組 26,235 通りのうち
**277 通り（1.06%）が 4 桁表示で 0.8986 になる。**
001/002/003 を採る根拠は「連番の最初の 3-seed 組で基準点として自然」という点にあり、
**数値の一致はそれを排除しないという以上の意味を持たない。**

### AlignDETR の検出性能 0.686 について［実測］

上の本文は「0.686 の所在は索引からは特定できない」としていた。**索引の外に在った。**

`evidence/aligndetr_s0frozen_incident_20260703/aligndetr_s0frozen_v2_train_log.txt:1670`
（版管理された一次ログ）:

    [07/06 03:25:53] d2.evaluation.testing INFO: copypaste: 68.5960,81.1582,73.8712,0.4680,23.9375,69.5419

**bbox AP 68.5960。［記録］の「作り直し版の検出性能 68.60」と一致する。**
K1 の三値のうち `0.686` だけは版管理された一次証拠へ遡れる。

### aligndetr 側の特徴の由来［実測 + 推定］

**［実測］** 生成器は `RELDETR_FROZEN_TAG=aligndetr_s0frozen_seed42` を渡し
（`scripts/run_when_gpu_free.sh`）、受け側はそれを経路に使う（`scripts/train_t1a.py:57-61`）。
採用されている `data/processed/t1a_regiontoken/aligndetr_s0frozen_seed42/` の生成日は
**2026-07-10 17:39〜17:47** で、07-06 版は `.discarded_20260706` として退避されている。
**07-06 の作り直し以降の生成物であり、07-03 の失敗版ではない。**

**🔴 直接の連結は取れない。** 六 run に `config.yaml` が無いため、
「config が指す特徴ファイル」という形での確認は**できない（UNKNOWN）**。
また `evidence/discarded_caches/t1a_regiontoken/aligndetr_s0frozen_seed42.discarded_20260706.md`
は「v3 の ckpt は存在しない」「07-10 の再抽出はログを残していない」と記録しており、
07-10 版が v2 ckpt から出たことは**消去法による推定である。**

### checkpoint の日付は学習時刻ではない［実測］

六 checkpoint の mtime は 2026-07-10 17:47:49〜17:48:38 の **49 秒**に収まる。
生成器は 6 run を `--epochs 50` で逐次回す実装であり、49 秒で終わることはない。
順序も逐次の順（relationdetr 42→123→456、aligndetr 42→123→456）と食い違う。

**この mtime は複製の時刻である。六 run がいつ・どのホストで学習されたかは
現物からは判別できない（UNKNOWN）。**

### K1 を実測扱いへ復帰させられるか

**できない。** 六 run は実在するが数値を持たない。

**［前契約］の「遡れない（確定）」は覆らない。** ただし理由は具体化した。
「どこにも無い」のではなく、**「run は在るが metrics が残っていない」**である。
checkpoint は六つとも健在（各 2,599,650 バイト、md5 照合済み）であるため、
**再評価による回復は原理的に可能である**（本契約では GPU 使用が禁止のため行っていない）。

互いに矛盾する複数の出所へは遡っていないため、escalate の条件には当たらない。
