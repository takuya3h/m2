# Claude Code 起動プロンプト —— M2研究計画 枠組み章リライト

> 以下をそのまま Claude Code セッションの最初の指示として貼り付ける。リポジトリ直下（`egosurgery_multitask`）で実行する想定。

---

## ミッション

Notion「M2研究計画」本体を、**H1〜H4「相互改善」フレーム**から **タスク結合の原理提案 × 分析ファースト**フレームへ改訂する。**枠組み章のみ刷新し、知識章は保全する（外科的再構成）**。あなた（Claude Code）の仕事は、改訂後の §1〜§16 を **ローカル Markdown として `docs/m2_plan_rewrite/` に高品質ドラフト＋検証**することまで。**Notion への書き込みは行わない**（Notion 反映はチャット側が章単位で実施する＝方式B）。

## 入力（必読）

1. `docs/m2_plan_rewrite/blueprint.md` —— 改訂仕様の正本。章別分類・旧→新対応辞書・参照張り替えマップ・各刷新章の目標仕様が全て入っている。**まずこれを通読**すること。
2. **改訂対象の現行コンテンツ**（変換元）—— Notion ページ「M2研究計画」（ID `361ee4d4-7777-804f-b7e6-c023cf50267d`）。取得方法は次のいずれか:
   - Notion MCP が接続済みなら `fetch` で取得して `docs/m2_plan_rewrite/source_current.md` に保存。
   - 未接続なら、ユーザーが Notion UI から Markdown エクスポートした本文を `source_current.md` として配置済み。
   - ※ フリーズ済みアーカイブ（`384ee4d4-7777-818c-9363-cea8a1170603`）は**ロールバック用の不変スナップショット**。通常は触らない。
3. `docs/m2_plan_rewrite/reference_check.py` —— 参照残存を検出する検証スクリプト。

## 確定済みの意思決定（baked-in 済み・変更不可）

- **D1**: §2 を「研究の問いと仮説プール」へ全面改稿（旧 H1/H2 を「結合効果①/②」に内包、H-C/H-A/H-H を仮説プール、H3/H4 は Phase-2 延期）。
- **D2**: 新仮説ラベル **H-C/H-A/H-H は維持**。
- **D3**: §12/§13 は **STEP 0–D を主構造**に。**S0 / S0-frozen / S4 のみ名前を残して STEP に対応づけ**、S5〜S9 は STEP B/C/D・Phase-2 に畳む。
- **D4**: **§1〜§16 の章番号スケルトンは維持**（中身だけ刷新。番号を振り直さない）。
- **D5**: 冒頭「研究方針の現在地」バナーは改訂後に短いポインタへ縮約。
- **D6**: 手検出は**検出タスク内の追加**（第3の結合軸にしない）。
- **D7**: Notion 反映は**方式B（チャット側で章単位）**。あなたは Notion に書かない。
- **スコープ確定**: 本論文は **検出×工程の2タスク結合**に絞る。関係(H3)・Exo(H4) は Phase-2。
- **source of truth**: 本体 §13 を実行手順書の**正本**とし、別ページ §2.5(b)・§13ピボットは**詳細付録・経緯**の位置づけ（本体に集約。重複は本体優先）。

## ワークフロー

1. **ブランチ作成**: `git switch -c docs/plan-rewrite-2026-06`
2. **作業ディレクトリ**: `docs/m2_plan_rewrite/sections/` を作る。
3. **章ごとに執筆**（`sections/NN_title.md`）:
   - **保全章**（§8.-1, §8.0, §10, §11, §14, §15, §16 ＝ blueprint §2 の「保全」「保全＋参照修正」）は `source_current.md` から**該当章を一字一句コピー**してから、blueprint §5 に従い参照だけ最小修正。**事実・数値・コードパス・表は変えない**。
   - **刷新章**（§1, §2, §4, §5, §6, §7一部, §8.1, §9, §12, §13）は blueprint §4 の目標仕様で執筆。
   - **§11 の子ページ参照** `<page url="...">…</page>` は **22 本すべて**を新しい本文に**そのまま含める**（1本でも落とすと Notion 反映時に子ページ削除エラーになる）。`source_current.md` から URL を正確にコピー。
4. **各章 commit**。区切りごとに `python docs/m2_plan_rewrite/reference_check.py docs/m2_plan_rewrite/sections/` を回し、blueprint §6 の参照マップに沿って未処理の H1〜H4 / S5〜S9 / 「相互改善」/ Stage B,C を解消。
5. **結合**: 全章を章番号順に `docs/m2_plan_rewrite/m2_plan_v2_full.md` へ連結。通し読みで章間参照と用語を統一。
6. **最終検証**: `reference_check.py` を full ファイルに対して実行し、レポートのチェックリストが全て緑になることを確認。
7. **出力**: `m2_plan_v2_full.md`（＝チャットに渡す最終成果）と、`sections/*.md`、`reference_check` の最終レポートを提示。

## 厳守する制約（CRITICAL）

- **章番号を変えない**（§1〜§16）。修正対象は H/S/Stage の**概念参照だけ**（blueprint §6）。
- **知識を壊さない**: §8.-1/§8.0/§10/§11/§14/§15/§16 の事実・数値・per-class AP・eval recipe 規則・DDP 規則・コードパスを保持。
- **テーブルは Notion 様式で記述**: `<table fit-page-width="true" header-row="true">` … `<tr><td>…</td></tr>` …（後段の Notion 反映で崩れないため。Markdown のパイプ表は使わない）。
- **数値は実測値のみ**（捏造禁止）。検出ベースラインは実験 Run 台帳の実測を使う: **Relation-DETR が DETR系10モデル中1位、mAP 0.730（3-seed σ0.004）、AP_rare 0.758**。README 旧値 0.327 は無効。VFNet 0.618（§16）は旧 recipe の途中値として保持しつつ「現行主基準は DETR 系」と注記。
- **Notion に書き込まない**。あなたの成果物はローカル Markdown のみ。
- **口調・様式**は現行本文に合わせる（だ・である調、節番号、`{toggle="true"}` トグル、表様式）。

## 旧→新の翻訳（最重要部分の要約・詳細は blueprint §3/§6）

- H1 → 結合効果①「工程文脈→検出」（内包・Δ mAP で測定） / H2 → 結合効果②「object-centric→工程」（内包・Δ Phase F1）
- H3（関係・mask）/ H4（Exo）→ **Phase-2 延期**（節は残す）
- 「相互改善 / mutual improvement」→「タスク結合の効果」
- S0 → 検出ベースライン（完了・Relation-DETR 0.730） / S0-frozen → STEP A 分母①（維持） / S4 → STEP A 分母②（維持）
- S5/S6 → STEP B（結合手法群） / S7・S8 → Phase-2 / S9 → 最終評価
- Δ mAP / Δ Phase F1 / D-A / D-B → **維持**
- 新土台: STEP 0（eval recipe 一本化＋凍結源確定）→ A（単一タスク基準）→ B（既存結合6手法・4層）→ C（要因分析）→ D（観察から仮説）。Phase-2＝関係/Exo。

## 完了の定義

`m2_plan_v2_full.md` が、(1) §1〜§16 の番号を保ったまま枠組み章が新フレームで書き直され、(2) 知識章が非破壊で保全され、(3) `reference_check.py` のチェックリストが全緑（H1〜H4・S5〜S9・相互改善が意味的に解消、§11 子ページ22本が存在、テーブルが Notion 様式）になった状態。これをチャットに戻すと、チャット側が章単位で Notion 本体（361ee4d4）へ反映する。
