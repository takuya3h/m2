# G-2 事前登録（2026-07-29, prereg commit base = 7e07ad4）

> 本ファイルは指示書 Task P に従い、**学習を 1 本も回す前に**作成・コミットする。
> 実測結果は一切参照していない。
> 12 run を実行する commit は `../commit.txt` に別途記録する（本 prereg コミットより後）。

## 系統

1. base      : region-token 3840-d のみ
2. +bboxROI  : 3840-d ＋ 四角内の画素平均
3. +maskROI  : 3840-d ＋ マスク内の画素平均
4. +randROI  : 3840-d ＋ 同面積・同重心のランダム形状内の画素平均

## 予測 1（主予測）

背景除去が効くなら 3 > 2。術具の充填率 median は 0.204 で、
四角内の約 80% は背景画素である。マスクはそれを除く。

## 予測 2（効果の局在）

signature 3 術具の充填率は 0.12–0.20 と特に低い。
→ それらが特徴づける工程（incision / hemostasis / 閉創）の per-phase F1 が動く。
残り 6 工程は動かない。overall は 3/9 に希釈される。
→ **主指標は per-phase F1。**

## 予測 3（大きさの序列）

2 − 1（チャネル追加そのものの効果）＞ 3 − 2（背景除去の追加効果）。
根拠: region-token は既に画像全体を見ており、ROI チャネルは補助情報である。

## 予測 4（対照）

4 ≈ 2 であるべき。4 が 2 を大きく上回るなら、効いているのは
「形」ではなく「画素数の減少」または「正則化効果」である。

## 判定規約（結果を見る前に固定）

- PASS       : 予測 2 の 3 工程で 3 > 2 が判定閾値を超える
- 部分 PASS  : 3 工程のうち一部のみ → どれが動きどれが動かないかを機構と突き合わせる
- FAIL       : 3 工程とも動かない → 「背景は region-token のボトルネックでない」（負の結果）
- 予測外     : 予測 2 以外の工程が動いた → 被覆率仮説の反証。最も情報量が大きい結果として扱う
- 無効       : 4 が 2 を大きく上回った場合、3 の解釈を保留する

## 判定閾値（結果を見る前に固定）

系統間の差が有意と見なす条件を、以下のとおり**事前に**固定する。

- 3 seed の系統間差について **2 標本の標準誤差** `SE = sqrt(sd_A²/n_A + sd_B²/n_B)`（n_A = n_B = 3）を用い、
  `|差| > t(0.975, df) × SE` を「判定閾値を超える」とする（Welch の df を使用）
- あわせて**動画単位クラスタ・ブートストラップ（B=2,000）**の 95%CI が 0 を跨がないことを確認する
- **両方を満たしたときのみ「超えた」と判定する**（片方だけの場合は「不一致」として記録し、結論を保留する）
- 主指標は per-phase F1。overall accuracy / overall macro-F1 は併記するが判定には使わない
- val と test の両方で評価し、**val だけの結果を結論と呼ばない**

## 事前に想定される無効化条件

- 12 run のいずれかで MSDeformAttn CUDA 拡張のロードが失敗した場合、その run は無効として除外する
- 4 系統が同一 commit・同一ホストで実行されていない場合、比較を無効とする
