# これは「変数名テンプレート」（秘密なし）。実値は .env（gitignore済）に書く。
# 全マシン共有は: .env を編集 → bash scripts/encrypt_env.sh → .env.gpg を commit。
# 各マシンで利用: source scripts/load_env.sh（.env.gpg を復号して env にロード）。詳細 docs/secrets_and_tracking.md。
# 🔴 **ホストごとに違う値をこの雛形に書かない**（論理名など）。この雛形は全台へ同じ内容が
#    配られるため、具体名を書くと写した全ホストが同じ値を名乗る。置き場所は各ホストの起動時設定。
WANDB_API_KEY=your_api_key_here
WANDB_PROJECT=egosurgery_multitask
WANDB_ENTITY=your_wandb_entity_or_username
DATA_ROOT=/path/to/data
DINOV2_WEIGHTS=/path/to/dinov2_vitl14_reg4_pretrain.pth
# CUDA_VISIBLE_DEVICES は雛形から削除（2026/05/25）。
# 過去に .env を source した際にこれが export され、scripts/run_s0.sh の
# `torch.cuda.device_count()` が 1 を返し単一 GPU 逐次実行に分岐する事故が
# 発生した。GPU 個別実行が必要なら、shell で `CUDA_VISIBLE_DEVICES=0 python ...`
# のように都度指定すること（run_s0.sh は内部で各 run に明示設定する）。
# 実行サーバー名 SERVERNAME（M2研究計画 §14 実験結果ログ用）は **ここに書かない**。
# ホストごとに違う値であり、雛形へ具体名を書くと写した全ホストが同じ論理名を名乗る。
# 実際に起きた事故: 共有の暗号設定に SERVERNAME が入っていたため全台が同じ名前を名乗り、
# どの実験がどのホストで走ったのか区別できなくなった（暗号設定からは c61a673 で除去済み）。
# 置き場所は **各ホストの起動時設定**。scripts/sync/setup_host_servername.sh が
# ~/.zshenv 等へ `export SERVERNAME=<このホストの論理名>` を注入する。
# 未設定でも hostname へ落ちるため、この雛形に行が無くても動く。

# ----------------------------------------------------------------------------
# Notion 「実験Run台帳」自動投稿 (src/egosurgery/utils/notion_logger.py)
# ----------------------------------------------------------------------------
# 未設定なら no-op (学習プロセスはエラーなく完走)。両方設定すると
# MMDetTrainer.run() が完走した瞬間に行を自動作成 (or 既存行を update)。
# scripts/run_s0.sh は起動時に本 .env を自動 source するため、ここに値を書けば
# tmux/nohup で起動しても学習プロセスに env が継承される。
# トークン作成手順:
#   1) https://www.notion.so/profile/integrations で Internal Integration 作成
#   2) 実験Run台帳 DB を開く → ... → Add connections → 作成した Integration を選ぶ
# NOTION_API_KEY=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# NOTION_DB_ID=ef4ccd02-0a97-41af-814e-9acc44e1e0d3   # 実験Run台帳の database id（collection id 7bcf9406… ではない）
# NOTION_SERVER_OPTION は Run 台帳 Server 列の表示名。**ホストごとに違うため、この雛形には
# 具体名を書かない。** 各ホストの起動時設定へ置く（未設定なら SERVERNAME → server.txt の順で埋まる）。
# 意思決定/失敗知見/プロンプトの DB ID は configs/notion.yaml（非秘密）。token に全 DB を share すること。
