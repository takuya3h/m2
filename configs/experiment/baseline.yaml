# @package _global_
# Experiment preset loaded via: python -m research_template.train +experiment=baseline

defaults:
  - override /data: custom_dataset
  - override /model: resnet50

seed: 42

train:
  epochs: 100
  amp: true
